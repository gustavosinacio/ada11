# Feedback for Reproducer

## Run 2026-06-01_0941_session-finish-exercise-order — 2026-06-01 11:45 BRT

Pipeline: fix
Final outcome: pass (shipped — fix applied + migration 0019 live)
Score: 5/5

### Rubric
- UI bug → visual evidence captured (or explicit `needs-input` raised): pass — correctly classified this as an ordering bug whose ground truth is the backend query result, not pixels, and justified the no-screenshot decision explicitly against the evidence bar (`repro.md:68-74`: "the data-level probe is stronger and more deterministic evidence than a screenshot of one fragile permutation"). Captured *data* evidence instead — verbatim probe output quoted at `repro.md:51-55` (`LIVE order: A→B→C` vs `HISTORY order: B→C→A`) produced against the live Supabase project, with the probe scripts saved to the run folder. This is the right *kind* of evidence for an SQL-tie-break bug, not a silent skip; a UI screenshot would have captured one unstable permutation while the probe nails the divergence deterministically.
- Environment captured: pass — device/build (Expo Router web `npm run web` @ `localhost:8081`, same path native), data-layer target (live Supabase `ykrbgpctbfvndxjnpzrg`), OS (macOS, marked irrelevant for a query-derived bug), theme (irrelevant), auth state (signed-in confirmed user, RLS-scoped), network (online), and — the load-bearing one — the **data state that triggers it** (multi-exercise session where physical insertion order ≠ display order, with routine-started as the PRIMARY real-world trigger) all enumerated at `repro.md:20-28`.
- Steps deterministic: pass — two repro paths. Path A is the deterministic data-level harness (`repro.md:45-56`): seed 3 exercises at positions 0/1/2, insert sets physically B,C,A, run the exact `listSetsForSession` query, apply the History first-occurrence rule, observe the divergence — reproduces *every* run. Path B is the manual UI repro for human confirmation. The Reproducer was honest about the one residual: the *exact* permutation a UI run shows rides the unspecified SQL tie-break, "but that fragility IS the bug, not a gap in the repro" (`repro.md:66, 73`) — exactly the right framing.
- `observed` vs `inferred` distinguished: pass — exemplary. The verbatim probe output is flagged as observed (`repro.md:51-55`); the discriminator run (insert A,B,C / complete B,C,A → History rendered A,B,C; insert C,A,B same instant → History rendered C,A,B) is observed and used to *prove* the tie-break is physical insertion order, NOT `completed_at` (`repro.md:55`). The fix directions (a/b/c) are explicitly flagged as "NOT the Reproducer's call — flagged for the Diagnostician" (`repro.md:78`). Stayed in lane.
- Relative dates converted to absolute: n/a-but-pass — the bug report contained no relative dates; the probe output is stamped with the absolute run date (`repro.md:51`, "run 2026-06-01"). Nothing to convert, nothing botched.

### Lessons for next run
- **The single highest-value move: REFUTING the Conductor's leading hypothesis with a controlled discriminator, not just reproducing the symptom.** The Conductor's framing named `completed_at` sort as the leading cause (`repro.md:6`). The Reproducer disproved it cleanly — `listSetsForSession` already orders by `set_number` only (a prior fix; the in-code comment at `sets.ts:49-53` documents the removed `completed_at` order), and the discriminator probe (complete in B,C,A but render A,B,C) empirically killed the completion-order theory (`repro.md:12, 55`). It then established the REAL mechanism: an N-way `set_number=1` tie with no secondary key, broken by physical insertion order. **This is the second consecutive fix run where the Reproducer's highest-leverage output was refuting a plausible-but-wrong suspected cause** (last run: the `backHref` red herring). Sustain the reflex: design the repro to confirm-OR-refute the named suspicion, and build a discriminator that isolates the true variable — refuting the wrong cause saved the Diagnostician a dead-end down the `completed_at` path that was already gone.
- **Choosing the data-level probe over a UI screenshot for a query-tie-break bug was the correct evidence call, and it was argued, not assumed.** A screenshot would have frozen one fragile permutation of an unspecified SQL tie-break; the probe reproduces the *divergence* every run and the discriminator proves the tie-break *direction*. The Reproducer named exactly why (`repro.md:68-74`) and deferred the optional UI screenshot to human verification. **Sustain: when a bug's ground truth lives in the backend query result (ordering, aggregation, RLS), the deterministic data-level probe is stronger evidence than pixels — capture it and say why the screenshot is deferred.**
- **Correctly scoping the verdict screen OUT, while flagging the owner-ambiguity, prevented a wasted fix surface.** The owner's report mentioned the order being "messed up"; the Conductor's framing raised "and/or the verdict screen." The Reproducer verified the verdict screen renders only the PR list sorted by `overflowKg DESC` — a deterministic own sort that cannot exhibit a full-list reorder (`repro.md:33, 77`) — and flagged a one-line confirm rather than silently assuming. This is the right "narrow the affected-surface set with evidence, but surface the residual product question" discipline. Sustain.

### Recurring pattern check
- **Refute-the-named-suspected-cause is now a 2-run positive pattern** (navbar-tab fix: killed the `backHref` red herring; this run: killed the `completed_at` hypothesis with a discriminator probe). This is the standout strength worth treating as the Reproducer's signature move — when the Conductor or report names a suspected cause, the Reproducer reliably designs the repro to test that specific claim, not just to reproduce the symptom. No recurring FAIL exists (2 runs, both 5/5). Sustain.

## Run 2026-05-27_2144_navbar-tab-pop-to-root — 2026-05-27 23:10 BRT

Pipeline: fix
Final outcome: pass
Score: 5/5

### Rubric
- UI bug → visual evidence captured (or explicit `needs-input` raised): pass — correctly classified this as a non-visual navigation-behaviour bug and justified the no-screenshot decision explicitly against the Conductor's evidence bar (`repro.md:60-62`: "Not applicable — navigation-behaviour bug, not visual rendering... a deterministic repro (exact steps + observed/expected URLs, runtime-confirmed) satisfies the bar"). Evidence is the right *kind* for the bug class: Playwright URL assertions across all three paths, not a hand-wave. This is not a silent skip — it is a reasoned `n/a` with the substitute evidence named.
- Environment captured: pass — device/browser/build (Expo Router web, `web.output: single` SPA, headless Chromium via Playwright against `npm run web` at `localhost:8081`), OS (macOS dev host), theme (correctly marked irrelevant for a nav bug), auth state (signed-in confirmed user), network (online), and data state (≥1 canonical exercise "Bench Press"; Path C also needs a live un-ended session) — all enumerated at `repro.md:27-33`.
- Steps deterministic: pass — Path B and Path C each give numbered sign-in → navigate → re-tap → observed/expected steps a third party can follow (`repro.md:42-54`); Path A included as the working boundary (`:56-58`). Determinism is not asserted but *demonstrated*: "runtime-verified twice (identical results both runs)" with raw observed before/after URLs quoted (`repro.md:17-21`).
- `observed` vs `inferred` distinguished: pass — exemplary. The path-dependence table and raw URLs are flagged as observed (`repro.md:11-21`); the PartialState mechanism is explicitly labelled "**Inferred cause (for Diagnostician to confirm — not asserted as root cause)**" (`repro.md:25`). The Reproducer stayed in its lane: it produced a falsifiable hypothesis with a file:line pointer (`_layout.tsx:79`) without claiming root-cause authority.
- Relative dates converted to absolute: n/a-but-pass — the bug report contained no relative dates; no conversion was needed and none was botched.

### Lessons for next run
- **The single highest-value move this run: isolating path-dependence the verbal report did not capture.** The user reported a flat "re-tap doesn't go to the list." The Reproducer split it into three arrival paths and found A works / B,C fail (`repro.md:9-16`), then ran each twice. This reframed the entire bug from "the feature is broken" to "the feature is broken on URL-rehydrated paths only" — which is exactly the discriminator the whole downstream chain (Diagnostician's PartialState confirmation, Implementer's `useSegments` gate) hinged on. **Sustain the reflex: when a behavioural bug "sometimes works," enumerate the arrival/entry paths and test each in isolation before declaring the repro.**
- **Killing the `backHref` red herring early saved a diagnosis dead-end.** The Conductor's expanded context explicitly raised "possibly interacts with the recent `backHref` cross-tab nav change." The Reproducer disproved it with a clean controlled comparison: "Path B fails *without* `backHref`; Path C fails for the same PartialState reason" (`repro.md:25`). **Sustain: when the bug report or Conductor names a suspected cause, design the repro to confirm-or-refute that specific suspicion, not just to reproduce the symptom.** Refuting a plausible-but-wrong cause is as valuable as confirming the right one.
- **Pointing at the masking test was a high-leverage handoff.** The Reproducer found that the existing `bottom-tab-home-link.spec.ts` case 1 covers only Path A by design (`repro.md:23`, quoting the spec's own `:127-131` deferral comment) — which explains why the bug shipped green. This told the Fix Designer up front that test coverage was part of the fix, not an afterthought. Sustain that "why did this pass CI / why was this believed working" sub-question.

### Recurring pattern check
- none — first Reproducer feedback entry for this project (the fix pipeline had not run before this; prior feedback files exist only for the feature-pipeline agents). No prior pattern to flag. Strong first showing: 5/5, with the path-isolation methodology and the red-herring refutation as the standout strengths to watch for repetition (positive) in future runs.
