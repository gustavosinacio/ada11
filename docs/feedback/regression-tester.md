# Feedback for Regression Tester

## Run 2026-05-27_2144_navbar-tab-pop-to-root — 2026-05-27 23:10 BRT

Pipeline: fix
Final outcome: pass
Score: 5/5

### Rubric
- Static gates ran (typecheck, lint, unit, build smoke): pass — all four ran with exit codes and counts in the "## Automated checks" table (`regression-report.md:9-14`): typecheck exit 0 / 0 errors, lint exit 0 (0 errors, 1 pre-existing auto-generated `router.d.ts` warning correctly identified as baseline-unchanged), unit 384/384, and `npx expo export --platform web` exit 0 (3143 modules bundled). The build-smoke note (`:16`) is sharp: it explains that `expo export` "catches bundler/compiler issues the typechecker misses" and confirms the new `router`/`useSegments`/`type Href` imports and the `TAB_ROOTS` literal map all bundle cleanly.
- Original repro replayed (or manual checklist for un-reproducible-locally cases): pass — "## Replay of original reproduction" (`regression-report.md:18-37`) replays all three `repro.md` paths (B deep-link, C cross-tab live workout, A working boundary) and confirms "**bug no longer reproduces** ... Path B and Path C now pop to `/exercises` (was: silent no-op). Path A unregressed." Crucially it notes cases 4/5 were confirmed RED pre-fix (`:37`, citing `implementation.md:43`), so the lock-in tests "genuinely exercise the bug rather than passing vacuously." For the un-reproducible-locally surface (native), a 5-step manual checklist is delivered (`:64-70`) — not silently skipped.
- 3-5 adjacent regression checks performed: pass — five adjacent checks (`regression-report.md:39-60`), each chosen for a real shared code path: case 2 (cross-tab + `backBehavior="history"` browser-back invariant — the load-bearing one the `navigate`-not-`replace` choice protects), case 3 (Profile leaf no-op via the `canDismiss`/segments-length gate), `exercise-progress-back-nav.spec.ts` (the adjacent `backHref` flow the diagnosis flagged as red herring), `exercise-progress-ia.spec.ts` 5/5 (shares the `progress.tsx` screen + cross-tab surface), and an ad-hoc History deep-link generalization spot-check (tab-agnostic claim).
- Evidence per check: pass — every check has command output, not "looks fine." Parsed JSON-reporter stats are quoted per suite (`/tmp/pw-bothome.json` 5/5 with per-case PASS lines and `expected=5 unexpected=0 flaky=0`, `:29-36`; `/tmp/pw-adjacent.json` `:45-47`; `exercise-progress-ia` 5/5 `:49-55`; `/tmp/pw-history.json` 1/1 `:57-60`). The "run twice, identical, deterministic" note (`:37`) is the right rigor for a flaky-prone e2e surface.
- Security re-check handled correctly: pass — `security_relevant: no` was carried from `diagnosis.md:71-75` / `fix-plan.md:3`, so the security checklist is correctly skipped with the `n/a` note PRESENT and substantiated (`regression-report.md:90`): "the security checklist is skipped per contract. Confirmatory sanity scan of the production diff: zero auth / secret / credential / network / raw-query / eval tokens introduced." That confirmatory diff-scan even on a `no` is above-bar diligence — it verifies the `no` still holds against the *shipped* code, not just the plan.

### Lessons for next run
- **The single highest-value move: running the `expo export` build gate the Implementer explicitly skipped, and saying so.** The Implementer flagged `[~] npx expo export — NOT run` (`implementation.md:44`) because the dev server crashed under verification load. The Regression Tester closed that gap (`regression-report.md:16`: "the Implementer did NOT run `expo export`... I ran it") and confirmed the new imports + `TAB_ROOTS` map bundle cleanly. This is the regression tester functioning as the *backstop* for an Implementer-acknowledged gap rather than trusting the upstream report — exactly the role's job. **Sustain: when the Implementer marks any static gate as skipped/deferred, run it yourself and report the outcome explicitly; never inherit a green status you did not observe.**
- **Strong positive: honest "zero environmental noise" reporting with the methodology that earned it.** The report does not just say "no flakes" — it explains *why* the Supabase rate-limit / dev-server-crash failure modes the Implementer warned about did NOT recur this run: "paced runs, one spec file at a time, `workers: 1`... no `createConfirmedUser: Internal server error`, no setup-phase `waitForURL` timeouts" (`regression-report.md:62`). This is the right shape — distinguishing "I saw no noise" from "I ran in a way that avoided the noise." **Sustain: when a prior phase warns of an environmental failure mode, state both whether it recurred AND the run conditions, so a green result is interpretable.**
- **Strong positive: the ad-hoc generalization spot-check, created-run-deleted cleanly.** To verify the "single button serves all tabs" fix generalizes beyond exercises, the Tester wrote a throwaway History deep-link spec, ran it (pass), then deleted it and re-verified the working tree was clean (`regression-report.md:56`). This confirmed the tab-agnostic `segments[1] === route.name` claim without leaving residue in the tree (verified independently: no `_adhoc-history-deeplink-popcheck.spec.ts` remains). **Sustain: ad-hoc verification specs are fine as long as they are deleted and the tree is re-verified clean — exactly as done here.**
- **Minor watch-item: the stale `dismissAll` test-comment nit was correctly flagged as non-blocking and handed to the Conductor** (`regression-report.md:82`), who then corrected it out-of-band (`state.md:46`). The Tester drew the right line — comments don't execute, tests pass, so it is not a regression — and surfaced it for cleanup rather than failing the run or fixing it itself (which would breach write boundaries). Sustain that "flag, don't fix, when it's outside my boundary" discipline.

### Recurring pattern check
- none — first Regression Tester feedback entry for this project. No prior pattern to flag. Standout strengths to watch for repetition (positive): backstopping a skipped upstream gate, methodology-grounded noise reporting, and clean ad-hoc spec hygiene.
