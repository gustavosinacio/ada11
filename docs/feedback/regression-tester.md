# Feedback for Regression Tester

## Run 2026-06-01_0941_session-finish-exercise-order — 2026-06-01 11:45 BRT

Pipeline: fix
Final outcome: pass (shipped — fix applied + migration 0019 live)
Score: 5/5

### Rubric
- Static gates ran (typecheck, lint, unit, build smoke): pass — all four ran with exit codes and counts in the "## Automated checks" table (`regression-report.md:13-19`): typecheck exit 0 / 0 errors, lint 0 errors / 1 pre-existing `router.d.ts` warning (baseline-unchanged), unit 455/455 (28 files, incl. the new `session-exercise-order.test.ts` 11/11), and `npx expo export --platform web` exit 0 (3150 modules). The build-smoke note (`:21`) is sharp: it explicitly backstops the gate the Implementer deferred — `expo export` needs the column to exist, so the Implementer couldn't run it; the Tester ran it post-migration and confirmed the new `select("*")` read, the `finishSession`/`updateSessionExerciseOrder` writes, the new hook, the `uuid[]` Drizzle column, and the new helper all bundle cleanly through the bundler the typechecker doesn't fully exercise.
- Original repro replayed (or manual checklist for un-reproducible-locally cases): pass — "## Replay of original reproduction" (`regression-report.md:27-46`) replays the symptom across ALL THREE session shapes the fix-plan calls out, in a table with observed/verdict per shape (routine/persisted, ad-hoc, manually-reordered), via the Implementer's e2e RUN by the Tester post-migration. The seed inserts sets physically C,A,B in every case (so a tie-break-derived render would show C,A,B = the bug), and test (1) shows History renders A,B,C (the persisted order) — "passed every run." The verdict is explicit: "the original symptom no longer reproduces." The owner's UI/native confirmation is delivered as a 5-step manual checklist (`:96-104`), not silently skipped.
- 3-5 adjacent regression checks performed: pass — SIX adjacent checks (`regression-report.md:61-68`), each chosen for a real shared code path: `read-only-history.spec.ts` 5/5 (the sibling History detail screen — the most directly-shared code path, the screen the fix modified most), `end-of-session-verdict.spec.ts` 2/2 (exercises the modified `finishSession({id, exerciseOrder})` snapshot write AND the zero-set Finish path that must OMIT the column key), `progress-page.spec.ts` 8/8, `e1rm-strength.spec.ts` 3/3, `weekly-muscle-volume.spec.ts` 4/4 (all order-independent aggregates), and `remove-exercise.spec.ts` 2/2 (the SHARED `<ExerciseBlock>` component the fix now wires reorder props into). Plus the new-write-path RLS probe. That is the right surface set — it maps almost 1:1 onto the Diagnostician's "ruled out" table, confirming those surfaces stayed unregressed.
- Evidence per check: pass — every check has counts/output, not "looks fine." The replay quotes the e2e PASS line (`regression-report.md:42-46`); each adjacent suite cites its pass count; the RLS probe gives the concrete observed result (attacker READ returned 0 rows = BLOCKED; attacker UPDATE affected 0 rows and the owner's column verifiably unchanged, `:68`); the `uuid[]`↔`string[]` round-trip is confirmed end-to-end ("`readSessionOrder()` reads it back as a JS `string[]` equal to `[A,C,B]`", `:25`); and — the model move — the pre-existing chart-scroll failures are PROVEN pre-existing by `git stash`-ing the fix to baseline `5985c57`, replaying the spec (same 2 fails, same error), then `git stash pop` and re-verifying the tree (`:71`). The one flaky e2e assertion was investigated to root cause (the `PersistQueryClientProvider` cache-rehydration race on `page.goto` reopen) and proven a test-harness artifact via a `toPass`-wrapped throwaway (3/3), with the throwaways deleted and tree re-verified clean (`:55-59`) — attributed correctly as NOT a fix defect.
- Security re-check handled correctly: pass — `security_relevant: no` carried from `diagnosis.md` → `fix-plan-v2.md:3, 286-287`, where it was re-assessed for the v2 NEW write path. The Tester did NOT just stamp `n/a` — because the fix added a new write path (`updateSessionExerciseOrder`), it RAN the cross-user RLS read+write probe the Fix Designer designated as the gate (`regression-report.md:91-94`): two-user probe (owner + attacker via anon key), attacker READ blocked (0 rows) AND attacker UPDATE blocked (0 rows affected, owner's column unchanged). Plus a confirmatory diff-scan of the shipped code (zero `service_role`/`eval`/`.raw`/`child_process` tokens; the write uses the parameterized supabase-js query builder, no string concatenation). This is exactly the contract: when `security_relevant: no` BUT the fix adds a write path, run the RLS gate with evidence rather than skipping — and the `n/a` note on the rest of the checklist is present and substantiated.

### Lessons for next run
- **The single highest-value move (and a 2-run sustained strength): backstopping the static gate the Implementer explicitly deferred, and saying so.** The Implementer marked `[~] expo export — NOT run` (`implementation.md:44`) because it needs the column to exist; the Tester ran it post-migration and reported the outcome explicitly (`regression-report.md:21`), confirming the new column read/write code bundles. This is the second consecutive fix run where the Tester closed exactly this kind of Implementer-acknowledged gap (last run: the same `expo export` backstop). **Sustain: when the Implementer marks ANY gate skipped/deferred, run it yourself and report the result — never inherit a green status you did not observe.**
- **Proving the chart-scroll failures pre-existing via stash-to-baseline is the model "is this fail mine?" discipline.** Rather than asserting the 2 chart-scroll failures are out-of-scope, the Tester reverted the fix files to baseline `5985c57`, replayed the spec, observed the SAME 2 failures with the SAME error, then restored the fix and re-verified the tree (`regression-report.md:71`). This is causation proof, not correlation. **Sustain: when a failing test is in the suite but the fix doesn't touch its surface, prove it pre-existing by replaying against the stashed baseline before declaring it out-of-scope — and re-verify the tree is clean after.**
- **The flaky-assertion investigation was textbook: root-caused, attributed, and proven NOT a fix defect — without breaching write boundaries.** The Tester traced the intermittent reopen-render failure to the app's `PersistQueryClientProvider` localStorage rehydration racing the background refetch on `page.goto`, proved the feature is correct (the persist-write assertion passes EVERY run; a `toPass`-wrapped throwaway copy of the assertion passed 3/3), then FLAGGED it for the Conductor to wrap rather than editing the spec itself (which would breach the Tester's write boundary). The Conductor then did the test-only de-flake. **Sustain that "root-cause the flake, prove it's harness-not-fix, flag don't fix when it's outside my boundary" discipline — and distinguish a write-assertion that passes every run from a render-assertion that catches a paint race.**
- **Running the cross-user RLS write probe (not just read) BECAUSE the fix added a write path is the correct security calibration on a `no`.** A lazier reading of `security_relevant: no` would have skipped the whole checklist; this Tester recognized the new `updateSessionExerciseOrder` UPDATE path is the one thing worth probing and confirmed cross-user write is blocked (inherits `sessions`' `auth.uid() = user_id` for UPDATE). **Sustain: when the fix introduces a new write path even under `security_relevant: no`, run the RLS probe for that path specifically — the new write surface is where a missing policy would bite.**

### Recurring pattern check
- **Backstopping a deferred upstream static gate is now a 2-run positive pattern** (navbar-tab fix: ran the `expo export` the Implementer's crashed dev server prevented; this run: ran the `expo export` the Implementer deferred pending the migration). Both times the Tester reported the gate it ran and the outcome explicitly. This is the Tester reliably functioning as the static-gate backstop — the standout strength worth treating as the agent's signature move. No recurring FAIL (2 runs, 5/5 both). Also sustained: methodology-grounded noise reporting (the "paced runs, workers:1, no createConfirmedUser 5xx across ~20 runs" note at `regression-report.md:11`) and clean ad-hoc-throwaway hygiene (deleted + tree re-verified). Sustain.

## Run 2026-05-27_2144_navbar-tab-pop-to-root — 2026-05-27 23:10 BRT

Pipeline: fix
Final outcome: pass
Score: 5/5

### Rubric
- Static gates ran (typecheck, lint, unit, build smoke): pass — all four ran with exit codes and counts in the "## Automated checks" table (`regression-report.md:9-14`): typecheck exit 0 / 0 errors, lint exit 0 (0 errors, 1 pre-existing auto-generated `router.d.ts` warning correctly identified as baseline-unchanged), unit 384/384, and `npx expo export --platform web` exit 0 (3143 modules bundled). The build-smoke note (`:16`) is sharp: it explains that `expo export` "catches bundler/compiler issues the typechecker misses" and confirms the new `router`/`useSegments`/`type Href` imports and the `TAB_ROOTS` literal map all bundle cleanly.
- Original repro replayed (or manual checklist for un-reproducible-locally cases): pass — "## Replay of original reproduction" (`regression-report.md:18-37`) replays all three `repro.md` paths (B deep-link, C cross-tab live workout, A working boundary) and confirms "**bug no longer reproduces** ... Path B and Path C now pop to `/exercises` (was: silent no-op). Path A unregressed." Crucially it notes cases 4/5 were confirmed RED pre-fix (`:37`, citing `implementation.md:43`), so the lock-in tests "genuinely exercise the bug rather than passing vacuously." For the un-reproducible-locally surface (native), a 5-step manual checklist is delivered (`:64-70`) — not silently skipped.
- 3-5 adjacent regression checks performed: pass — five adjacent checks (`regression-report.md:39-60`), each chosen for a real shared code path: case 2 (cross-tab + `backBehavior="history"` browser-back invariant — the load-bearing one the `navigate`-not-`replace` choice protects), case 3 (Profile leaf no-op via the `canDismiss`/segments-length gate), `exercise-progress-back-nav.spec.ts` (the adjacent `backHref` flow the diagnosis flagged as red herring), `exercise-progress-ia.spec.ts` 5/5 (shares the `progress.tsx` screen + cross-tab surface), and an ad-hoc History deep-link generalization spot-check (tab-agnostic claim).
- Evidence per check: pass — every check has command output, not "looks fine." Parsed JSON-reporter stats are quoted per suite (`/tmp/pw-bothome.json` 5/5 with per-case PASS lines and `expected=5 unexpected=0 flaky=0`, `:29-36`; `/tmp/pw-adjacent.json` `:45-47`; `exercise-progress-ia` 5/5 `:49-55`; `/tmp/pw-history.json` 1/1 `:57-60`). The "run twice, identical, deterministic" note (`:37`) is the right rigor for a flaky-prone e2e surface.
- Security re-check handled correctly: pass — `security_relevant: no` was carried from `diagnosis.md:71-75` / `fix-plan.md:3`, so the security checklist is correctly skipped with the `n/a` note PRESENT and substantiated (`regression-report.md:90`): "the security checklist is skipped per contract. Confirmatory sanity scan of the production diff: zero auth / secret / credential / network / raw-query / eval tokens introduced." That confirmatory diff-scan even on a `no` is above-bar diligence — it verifies the `no` still holds against the *shipped* code, not just the plan.

### Lessons for next run
- **The single highest-value move: running the `expo export` build gate the Implementer explicitly skipped, and saying so.** The Implementer flagged `[~] npx expo export — NOT run` (`implementation.md:44`) because the dev server crashed under verification load. The Regression Tester closed that gap (`regression-report.md:16`: "the Implementer did NOT run `expo export`... I ran it") and confirmed the new imports + `TAB_ROOTS` map bundle cleanly. This is the regression tester functioning as the *backstop* for an Implementer-acknowledged gap rather than trusting the upstream report — exactly the role's job. **Sustain: when the Implementer marks any static gate as skipped/deferred, run it yourself and report the outcome explicitly; never inherit a green status you did not observe.**
- **Strong positive: honest "zero environmental noise" reporting with the methodology that earned it.** The report does not just say "no flakes" — it explains *why* the Supabase rate-limit / dev-server-crash failure modes the Implementer warned about did NOT recur this run: "paced runs, one spec file at a time, `workers: 1`... no `createConfirmedUser: Internal server error`, no setup-phase `waitForURL` timeouts" (`regression-report.md:62`). This is the right shape — distinguishing "I saw no noise" from "I ran in a way that avoided the noise." **Sustain: when a prior phase warns of an environmental failure mode, state both whether it recurred AND the run conditions, so a green result is interpretable.**
- **Strong positive: the ad-hoc generalization spot-check, created-run-deleted cleanly.** To verify the "single button serves all tabs" fix generalizes beyond exercises, the Tester wrote a throwaway History deep-link spec, ran it (pass), then deleted it and re-verified the working tree was clean (`regression-report.md:56`). This confirmed the tab-agnostic `segments[1] === route.name` claim without leaving residue in the tree (verified independently: no `_adhoc-history-deeplink-popcheck.spec.ts` remains). **Sustain: ad-hoc verification specs are fine as long as they are deleted and the tree is re-verified clean — exactly as done here.**
- **Minor watch-item: the stale `dismissAll` test-comment nit was correctly flagged as non-blocking and handed to the Conductor** (`regression-report.md:82`), who then corrected it out-of-band (`state.md:46`). The Tester drew the right line — comments don't execute, tests pass, so it is not a regression — and surfaced it for cleanup rather than failing the run or fixing it itself (which would breach write boundaries). Sustain that "flag, don't fix, when it's outside my boundary" discipline.

### Recurring pattern check
- none — first Regression Tester feedback entry for this project. No prior pattern to flag. Standout strengths to watch for repetition (positive): backstopping a skipped upstream gate, methodology-grounded noise reporting, and clean ad-hoc spec hygiene.
