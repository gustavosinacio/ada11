# Feedback for Reviewer

## Run 2026-06-03_1402_bodyweight-leverage-factors — 2026-06-03 15:40 BRT

Pipeline: feature
Final outcome: pass (Reviewer PASS first round [0 blockers / 0 majors / 2 minors]; Tester PASS first round — the feature the Reviewer cleared shipped correct live)
Score: 5/5

### Rubric
- Severity classes consistent with Validator's: pass — 0 blockers / 0 majors / 2 minors (`review-v1.md:107-116`): MIN-1 (the e1RM-unchanged unit cases assert TAUTOLOGIES, not the production reduce — a test-quality gap, not a regression) and MIN-2 (a 3-part Tester runtime hand-off: R-1 retroactive appearance, live string-shape close-loop, 7-name backfill). Both correctly classified test-quality/runtime-handoff, matching the Validator's severity boundary (invariant-break → major; coverage/runtime → minor). The two v1-rejection causes (MAJ-1/MAJ-2) are verified CLOSED, correctly NOT re-raised.
- Security checklist applied: pass — full section (`:92-96`): RLS/authorization (NO new query surface — the only change is widening the existing `stats.ts` SELECT by one projected column on the SAME `!inner` join, plus the column riding the existing `select("*")`; no new table/policy, reads only); no service-role/signing key in client code (grep-clean, 0 `SERVICE_ROLE` in `src/`/`app/`); input handling (the migration uses fixed string literals, no user input, no string concat, no `rpc`/raw SQL in shipped TS; `parseFloat` on a DB-controlled `numeric`, not user-typed-into-a-query); no new `EXPO_PUBLIC_*`. Correctly scoped to a no-new-surface feature.
- Style checklist applied: pass — `:99-103`: no new `any`/`as any`/`as unknown` (grep-clean across the diff); no new `@ts-ignore`/`@ts-expect-error`/`eslint-disable`; comments-narrate-WHY (the seam docstring explains the string-`numeric` rationale + coalesce-never-0 + Invariant L; build-site comments explain "parseFloat once here so the map only ever holds finite numbers"; the migration comment calls out the retroactive shift); imports `~/`-rooted package-first; new files in conventional folders.
- Decision rule applied correctly: pass — 0 blockers + 0 majors → `pass` (`:3-12`), reasoning walked per finding (both minors are test-quality/runtime-handoff, not regressions). The MAJ-1/MAJ-2 closures are verified with file:line teeth (the string seam re-traced; the 6 builders re-grepped).
- Did not run the app (static-only): pass — the only commands attributed are RE-RUNNING the offline gates (`tsc --noEmit` 0, ESLint 0+1, spot `vitest` on `bodyweight.test.ts` + `volume-target-factor.test.ts` 41/41, `:20-23`) explicitly "I re-ran ... not trusted from implementation.md". The runtime items (R-1 retroactive appearance, live string-shape, 7-name backfill) were correctly handed to the Tester as MIN-2 + the 5-item hand-off summary (`:116-124`); no `npm run web`/playwright attributed to the Reviewer.

- **The standout: both v1-rejection causes RE-VERIFIED RESOLVED against the SHIPPED DIFF (not implementation.md prose), each with file:line teeth, and the string-seam trace proved the test has real teeth.** For MAJ-2, the Reviewer traced EVERY path that puts a factor into a `Map<string,number>` or passes it to the seam and confirmed "no raw numeric string reaches `Number.isFinite` un-parsed" (`review-v1.md:41-50`), citing the seam parse block + all 8 ROW-fed sites + all 6 map build sites with line numbers, and crucially observed that the cross-surface test threads the literal STRING `"0.64"` end-to-end "**if the seam coalesced `"0.64"` to 1.0 these return 800, not 512** ... NOT a false-green" (`:50`). For MAJ-1, it independently re-grepped the 6 builders into a verification table (`:56-67`), confirmed no 7th, and correctly named the 2 deliberate no-bw exclusions. This is the model "trace the shipped code, cite the test that pins it, prove the assertion can actually fail."
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` blocks; the Reviewer routing entry `transcript.md:58-62` shows a self-contained static review + "No peer invocations".)
- Write boundaries: pass — only `review-v1.md` attributed (baseline `3c85c23` resolved; the `review.md` template stub is Conductor scaffolding; the other-run PNGs + prior-run `state.md` are cross-run noise the Reviewer correctly excluded at `:18`). Static-only — no source/test files attributed.

### Lessons for next run
- **Catching the tautological e1RM regression case (MIN-1) is the sharpest test-quality scrutiny this run — sustain it, and note it is the SAME class as the Designer's recurring toothless-negative-assertion.** The Reviewer recognized that `expect(0 > 0).toBe(false)` and `expect(epley1RM(20,5)).toBe(epley1RM(20,5))` (`:114`) DOCUMENT Invariant D but never drive the production reduce, so they "would NOT fail if a future change leaked the factor into the e1RM branch," and prescribed the teeth-bearing fix (run the reduce with `factor="0.64"` vs `null`, assert identical). It correctly kept it MINOR (the invariant is statically verified + dynamically proven by `e1rm-strength.spec.ts` 3/3). Sustain: when a test asserts a NEGATIVE invariant ("X must NOT touch Y"), check whether it actually DRIVES the Y path with-vs-without X, or merely hand-computes the expected value — the latter is a tautology that cannot fail on regression.
- **Re-tracing both rejection causes against the REAL diff + proving the cross-surface test's 512-vs-800 teeth is the model resolution-verification — sustain it.** The Reviewer didn't trust "implementation.md says the string seam is fixed"; it read the shipped seam, traced every map/path, and reasoned about what number a regression would produce (800 vs 512). Sustain "trace the shipped code, then prove what a failure would look like."
- **Sharpest improvement lever (a partition note, not a miss): the live string-vs-number shape was correctly handed to the Tester (MIN-2b) — and the Tester found the two clients disagree.** The Reviewer asked the Tester to "confirm a LIVE push-up session actually reads `bodyweight_factor` as `"0.64"` from PostgREST" (`:116`). The Tester found this client returns a `number`, while the Conductor's MCP read returned a `string` (`test-report-v1.md:85`, `transcript.md:66`) — the seam handles both, no defect. The Reviewer's hand-off framing assumed the design's "it's a string" premise; the strictly-sharper framing would be "confirm the seam produces the leveraged number REGARDLESS of the JSON type, since the design's own seam handles both." The hand-off still closed the loop correctly (the Tester's 512 kg is the empirical proof); the micro-lesson is to frame a data-shape runtime hand-off as "prove the OUTPUT is right" rather than "prove the INPUT is the assumed shape," because the shape can vary by client. Minor; the partition was correct and the loop closed.

### Recurring pattern check
- **Test-quality scrutiny inside static review — RESOLVED and SUSTAINED (8-run arc).** canonical (candidate) → routine (actual fail) → bottom-tab (applied) → bodyweight-volume (seed-row-realness) → e1rm (T-1/T-2 hand-offs) → favorites (verified union cases assert the right thing) → progress-window (verified MAJ-1 closed with teeth + the W-0 deepEqual pins Invariant W) → THIS run (caught the tautological e1RM cases + proved the 512-vs-800 cross-surface teeth). The Reviewer reliably audits the test diff for assertion-CORRECTNESS, not just presence, AND partitions static-vs-runtime. Pattern remains resolved. Sustain.
- **Strong positive: re-run-the-gates-don't-trust + file:line evidence dense, sustained across 8 consecutive runs.** The 10-row claim table re-run against the diff, the security + style checklists, the MAJ-1/MAJ-2 re-traces, the 5-item Tester hand-off summary, the 6 "found SOUND" items. Reviewer output shape is stable and reliable. No recurring fail to flag.
- **The "model-derived PROXY vs rendered ARTEFACT" hand-off (the prior run's standout) did not have a direct surface this run** — this feature's golden-path is a numeric volume total, not a rendered SVG line, so the Reviewer's runtime hand-offs were data-shape + retroactive-appearance + backfill rather than geometry. The discipline (name what static review cannot confirm, hand it on with "what a failure means") held in the new shape. Sustain.

## Run 2026-06-03_1124_progress-chart-window — 2026-06-03 12:30 BRT

Pipeline: feature
Final outcome: pass (Reviewer PASS first round; Tester PASS first round — the feature the Reviewer cleared shipped correct)
Score: 5/5

### Rubric
- Severity classes consistent with Validator's: pass — 0 blockers / 0 majors / 2 minors (`review-v1.md:102-105`): MIN-1 (W-4 rank-FLIP coverage-completeness, two-survivors-swap not exercised) and MIN-2 (e2e seed-name is a DB-state property the static review can't confirm → Tester T-2). Both correctly classified coverage/runtime-handoff, not regressions — matching the Validator's severity boundary (invariant-break → major; coverage/runtime → minor). The Validator's MAJ-1 must-fix is verified CLOSED, correctly NOT re-raised (claim 12).
- Security checklist applied: pass — full section (`:78-82`): RLS/authorization (NO new query surface — both presenters are PURE client-side row filters over already-fetched caches; no new SELECT/INSERT/DELETE; no new table so no policy needed; the e2e seeds via service-role admin, reads via the signed-in UI — the standard pattern); no service-role/signing key in client-bundled code (grep-clean; `SERVICE_ROLE` only in the test); input handling (no raw SQL/`rpc`/shell; typed `MaxVolumeWindowWeeks` from a fixed option list; `computeWindowStart` is pure date math); public env vars (no new `EXPO_PUBLIC_*`). Correctly scoped to a no-new-surface feature.
- Style checklist applied: pass — `:84-89`: no new `any`/`as any`/`<any>` (grep-clean across 8 source files); no new `@ts-ignore`/`@ts-expect-error`/`eslint-disable`; comments-narrate-why (presenter dual-anchor + Invariant W rationale, page seed-vs-bind + why-`new Date()`-inside-memo, selector stateless/no-write contract); imports `~/`-rooted package-first; new files in conventional folders.
- Decision rule applied correctly: pass — 0 blockers + 0 majors + 2 minors → `pass` (`:5-9`), reasoning walked per finding (both minors are documentary/coverage-completeness, not regressions). The MAJ-1 closure is verified with teeth (claim 12: chip `toHaveCount(1)→0→1`, NOT x-axis label count).
- Did not run the app (static-only): pass — review is static; the only commands attributed are RE-RUNNING the 3 offline gates (`tsc --noEmit` 0, ESLint 0+1, `vitest run` on the 3 touched files 52/0, `:34`) explicitly "I re-ran". The runtime items (chart actually redraws, seed-name live resolution, `bg-black` rn-web emission) were correctly handed to the Tester as T-1/T-2/T-3 (`:107-111`); no `npm run web`/playwright attributed to the Reviewer.

- **The standout: the 13-claim re-trace against the REAL diff (not implementation.md prose) + the 3 sharply-partitioned Tester hand-offs.** `review-v1.md:20-36` re-reads the shipped diff: both-loops guard at all 4 heads (`weekly-muscle-volume.ts:72-75,107-110` + `e1rm-strength.ts:111-114,136-139`), the byte-identical guard shape anchored on `started_at` strict-`<`, Invariant W pinned to the W-0 deepEqual at exact test lines, and crucially claim 5 (no THIRD leak path) independently traced that ranking/selection/LOCF read `byExercise`/`byMuscle` populated ONLY in the guarded loops. It then partitioned what static review CANNOT confirm into T-1 (chart actually redraws — chip is a model-derived proxy, not the rendered line), T-2 (seed names resolve in the LIVE catalog), T-3 (`bg-black` actually emitted on web) — each with a sharp "what a failure means." The Tester closed all three with runtime evidence. This is the model "trace the shipped code, cite the test that pins it, hand the runtime-only items onward with teeth."
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks; the Reviewer routing entry `transcript.md:46-50` shows a self-contained static review + "No peer invocations".)
- Write boundaries: pass — only `review-v1.md` attributed (baseline `25db98b` resolved; the `review.md` template stub is Conductor scaffolding; the 5 cache-buster files + `dist/` correctly excluded as out-of-run noise per `review-v1.md:14`).

### Lessons for next run
- **Distinguishing the model-derived PROXY from the rendered ARTEFACT (T-1) is the sharpest hand-off this run — sustain it.** The Reviewer recognized that the e2e's legend-chip assertion is a proxy for "the chart changed" (the chip derives from `model.series`, the same source the chart plots) but is NOT the rendered SVG line itself, and asked the Tester to confirm the actual chart geometry redraws. The Tester did exactly that (polyline 53→9 points). Sustain: when an e2e asserts on a proxy (a legend chip, a count, a label) for a rendered output (an SVG line, a layout), name the gap explicitly and hand the rendered-artefact confirmation to the Tester.
- **Re-tracing the central change against the REAL diff + binding each invariant to its exact unit-case line is the model static review — sustain it.** The Reviewer didn't trust "implementation.md says Invariant W holds"; it read the shipped guard, confirmed it's the only new code, and pointed at the W-0 deepEqual at `weekly-muscle-volume.test.ts:307-352` + `e1rm-strength.test.ts:460-498`. Sustain "trace the shipped code, then cite the test that pins it."
- **Sharpest improvement lever (a coverage nit, not a miss): MIN-1 (W-4 rank-FLIP two-survivors-swap) was correctly flagged optional, but consider whether to upgrade such a gap when the feature's CORE behavior is rank recompute.** The Reviewer flagged that W-4 proves exclusion + rank-0 recompute but never exercises a window where two surviving exercises swap RANK without either dropping — and correctly called it coverage-completeness (the mechanism is verified-correct). On a feature whose whole point is "recompute the windowed top-N," a rank-FLIP case is closer to a core-behavior gap than a peripheral one. It remained correctly a minor (the mechanism is structurally verified), but worth a one-line "is this coverage gap on the feature's CORE behavior?" prompt next time — sometimes that nudges an optional-minor to a should-fix-minor.

### Recurring pattern check
- **Test-quality scrutiny inside static review — RESOLVED and SUSTAINED (7-run arc).** canonical (candidate) → routine (actual fail) → bottom-tab (applied) → bodyweight-volume (applied + seed-row-realness) → e1rm-strength-chart (T-1 seed + T-2 SVG-line hand-offs) → favorite-exercises (verified the 11 union unit cases assert the right thing + settle-gate) → THIS run (verified MAJ-1 closed with teeth, the W-0 deepEqual pins Invariant W, the settle-gate leads every `toHaveCount(0)`, and partitioned T-1/T-2/T-3). The Reviewer reliably audits the test diff for assertion-correctness AND partitions static-vs-runtime. Pattern remains resolved. Sustain.
- **Strong positive: file:line evidence dense + "re-run the gates, don't trust" sustained across 7 consecutive runs.** The 13-row claim table, the no-THIRD-leak-path trace, the security + style checklists, the T-1/T-2/T-3 hand-offs, the 5 "found SOUND" items including the `Toggle Squat (Barbell)` no-double-match check (`:94`). Reviewer output shape is stable and reliable. No recurring fail to flag.

## Run 2026-06-01_1301_favorite-exercises-e1rm — 2026-06-01 15:10 BRT

Pipeline: feature
Final outcome: pass (Reviewer PASS first round; Tester round-1 FAIL was test-only — the feature the Reviewer cleared shipped correct)
Score: 5/5

### Rubric
- Severity classes consistent with Validator's: pass — 0 blockers / 0 majors / 1 minor (`review-v1.md:88-97`): MIN-1 is the `progress.tsx:126-127` unguarded `id` in `mutate` while `isFavorite` guards `!!id` — correctly classified as an effectively-unreachable robustness nit (the screen is unreachable without an `id`). Severity boundary matches the Validator's (invariant-break → major; unreachable/cosmetic → minor). No inflation, no deflation.
- Security checklist applied: pass — full section at `review-v1.md:99-103`: RLS (the only new query surface — `listMyFavoriteExerciseIds` SELECT / `addFavorite` INSERT / `removeFavorite` DELETE — lands on `user_exercise_favorites`, which HAS RLS enabled + 3 policies all gated `auth.uid()=user_id` in migration 0020; the cross-user RLS arm pins B-blocked; canonical-favoritable is safe because the favorites row always carries `user_id=the favoriting user`); `SUPABASE_SERVICE_ROLE_KEY` only in the e2e (never bundled, grep-clean in `src/`+`app/`); no raw SQL/`rpc` (parameterized PostgREST builders); no new `EXPO_PUBLIC_*` vars. Correctly scoped to a per-user write feature — verified the new table HAS a policy (not just assumed).
- Style checklist applied: pass — `review-v1.md:105-110` walks no-new-`any` (grep-clean; the two casts are typed narrowings matching the notes precedent), no new `@ts-ignore`/`@ts-expect-error`/`eslint-disable`, comments-narrate-why (the union dedup-by-construction invariant, the migration's 0010-divergence header, the `favoriteSet` memo re-render gating), imports `~/`-rooted, new files in conventional folders (no new component file — design Alt 14).
- Decision rule applied correctly: pass — 0 blockers + 0 majors + 1 minor → `pass` (`review-v1.md:114-118`), reasoning walked per finding. Both Validator non-blocking notes addressed: MIN-NEW-1 (rose hue) kept as-is per the Validator's "leave-as-is acceptable"; MIN-NEW-2 (regression-surface callout) honored by naming the 2 adjacent specs as the Tester re-run surface.
- Did not run the app: pass — review is static-only; the only commands attributed are re-running the 3 offline gates (`npm run typecheck`/`lint`/`test:unit`, `:58-61`), explicitly "re-run by the Reviewer (not trusted from implementation.md)". The migration-apply + RLS arm + e2e were correctly handed to the Tester as non-blocking notes T-1..T-6 (`:120-126`); no `npm run web`/playwright attributed to the Reviewer.

- **The standout: re-traced the union algorithm byte-for-byte against the REAL diff (not the design) and pinned all 4 invariants to their exact unit-case assertions.** `review-v1.md:14-25` walks `sorted`/`autoTopOverall`/`autoIds`/`extraFavorites`/`selected`/cap against `e1rm-strength.ts:157-212` and ties each property to its test: (a) top-N-favorite no-op → unit case 2 `:535-558` deep-equal; (b) dense ranks → case 6 `:648-675`; (c) Invariant F = literal `sorted.slice(0,topN)` → case 8 `:700-718` (no-arg == empty-Set == pre-change top-5); (d) cap drops non-favorites first → cases 5/10/11. It re-ran the gates itself (477/477), verified the header rewrite preserves the Pencil `canEdit` gate + `"Edit exercise"` label so `canonical-exercise-gating.spec.ts` + `exercise-progress-ia.spec.ts` don't regress (`:47-50`), and verified Invariant D survives (eligibility gate upstream of the union, `:26-27`). This is "verify the claim against source, with teeth, don't trust implementation.md."
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks; the Reviewer routing entry `transcript.md:48-51` shows a self-contained static review with no peer reference.)
- Write boundaries: pass — only `review-v1.md` attributed (baseline `5a0b86e` resolved; the `review.md` template stub is Conductor scaffolding).

### Lessons for next run
- **Re-tracing the central algorithm against the REAL diff (not the design doc) and binding each invariant to its exact unit-case line is the model static review for an algorithm feature — sustain it.** The Reviewer didn't trust "design-v2 says no-op"; it read the shipped `e1rm-strength.ts:157-212`, traced the no-op, and pointed at the deep-equal assertion at `:535-558` that proves it. Sustain "trace the shipped code, then cite the test that pins it."
- **Naming the exact a11y-label/line the adjacent specs assert on (`getByLabel("Edit exercise")` at `canonical-exercise-gating.spec.ts:178,187` + `exercise-progress-ia.spec.ts:119,127,146,223`) and verifying the count is unchanged in every branch is the right way to clear a regression-surface callout statically.** The Reviewer proved the Pencil gate/label is byte-for-byte preserved, then routed the runtime confirmation to the Tester. Sustain "verify the assertion the other spec depends on is unchanged, then hand the runtime re-run to the Tester."
- **The Tester-handoff notes (T-1..T-6) correctly partitioned static-confirmable from runtime-confirmable — sustain.** The Reviewer pre-verified what it could (migration shape, RLS policy presence, the union code, the Pencil gate) and handed the live-only items (migration-apply, RLS arm, the e2e, the seed-name resolution, the bodyweight NEGATIVE e2e) to the Tester with sharp "what a failure means" notes — including the prescient T-3 flag that `getByLabel("Toggle Lat Pulldown")` is only correct if the live catalog name is exactly `"Lat Pulldown"` not `"Lat Pulldown (Cable)"` (`:123`), which the Tester confirmed. Sustain.

### Recurring pattern check
- **Test-quality scrutiny inside static review — pattern RESOLVED and SUSTAINED (6-run arc).** canonical (candidate) → routine (actual fail) → bottom-tab (applied) → bodyweight-volume (applied + seed-row-realness refinement) → e1rm-strength-chart (applied: T-1 seed-name + T-2 SVG-line hand-offs) → THIS run (applied: verified the 11 union unit cases assert the RIGHT thing — case 2 deep-equal, case 8 Invariant F, cases 5/10/11 cap — and the e2e settle-gate leads every `toHaveCount(0)`, `:53,56`). The Reviewer reliably audits the test diff for assertion-correctness AND partitions static-vs-runtime. Pattern remains resolved. Sustain.
- **Strong positive: file:line evidence dense + "re-run the gates, don't trust" sustained across 6 consecutive runs.** The item-by-item union trace, the 11-row implementation-claim verification table (`:66-80`), the security + style checklists, the T-1..T-6 hand-offs. Reviewer output shape is stable and reliable. No recurring fail to flag.

## Run 2026-05-30_2006_e1rm-strength-chart — 2026-05-30 21:30 BRT

Pipeline: feature
Final outcome: pass (Reviewer PASS first round; Tester later found an adjacent test-locator regression Reviewer flagged the risk class of, via T-2)
Score: 5/5

### Rubric
- Severity classes consistent with Validator's: pass — 0 blockers / 0 majors / 2 minors (`review-v1.md:72-74`), both correctly robustness/style: MIN-1 (`last as number` transient-null type-honesty smell, traced to have no runtime effect — unit #6 proves it), MIN-2 (three O(weeks) passes vs one — R-6 LOW micro-inefficiency). Severity boundary matches the Validator's (invariant-break → major; cosmetic/no-runtime-effect → minor). No inflation, no deflation.
- Security checklist applied: pass — full section at `review-v1.md:76-80`: RLS (no new query surface — the presenter is pure over the already-fetched `["stats"]`/`["exercises"]` caches; the e2e seeds via service-role admin, reads via signed-in UI), `SUPABASE_SERVICE_ROLE_KEY` only in the e2e spec (never bundled; grep-clean in `src/`+`app/`), no raw SQL/`rpc`, no new `EXPO_PUBLIC_*` vars. Correctly scoped to a read-only feature — does not invent issues.
- Style checklist applied: pass — `review-v1.md:82-88` walks no-new-`any` (grep-clean across all 5 files), no new `@ts-ignore`/`@ts-expect-error`/`eslint-disable`, comments-narrate-why (Invariant D/E1 rationale, LOCF no-gap-support, R-7 residual, MAJ-1 settle-gate rationale — all cited), imports follow `~/`-rooted project style, new files in conventional folders.
- Decision rule applied correctly: pass — 0 blockers + 0 majors + 2 minors → `pass` (`review-v1.md:91-96`), reasoning walked per finding category. Correctly noted the MAJ-1 carry-in is CLOSED with teeth (the StreakCard "Streak" anchor renders only post-hydration) rather than treating it as still-open.
- Did not run the app: pass — review is static-only; the only commands attributed are re-running the gates (`npm run typecheck` / `npm run lint` / `npm run test:unit`, `review-v1.md:16-20`), explicitly "re-run by Reviewer, NOT trusted from implementation.md". No `npm run web` / playwright execution attributed to the Reviewer — the SVG-line-render check + live-catalog probe were correctly handed to the Tester as T-1/T-2.

- **The standout: the Reviewer independently re-traced the subtlest code (the two-pass LOCF backfill) including the index-0 edge, AND verified the MAJ-1 anchor "has teeth" by source — both load-bearing.** On LOCF (`review-v1.md:28-31`): it walked pass 1 (carry-forward), the `firstReal` lookup, and pass 2 (leading backfill), proving the first-real index is not overwritten, every index `< first` is backfilled, every index `> first` keeps the carry-forward, and the index-0 edge produces no leading null/0 — the SAME trace the Validator produced independently, which is exactly why the GO held with no algorithm rework. On MAJ-1 (`:35`): rather than trust the Implementer's "settle-gate added" claim, it verified `streak-card.tsx` renders the literal "Streak" text ONLY in the loaded branch (the `isLoading` branch is a text-free skeleton), so awaiting it genuinely gates on hydration — and confirmed "Streak" is unique on the Progress page. This is "verify the claim has teeth, don't trust the claim."
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks; the Reviewer routing entry `transcript.md:41-45` shows a self-contained static review with no peer reference.)
- Write boundaries: pass — only `review-v1.md` attributed (baseline `3c00d8e` resolved; the `review.md` template stub is Conductor scaffolding).

### Lessons for next run
- **The prior entry's recommended refinement — "extend test-data scrutiny to 'is the named seed row real in the live catalog?'" — was APPLIED this run as hand-off note T-1.** Last run's lesson noted the Reviewer can't statically confirm a DB-state property (whether a seed name exists in the live catalog) but CAN flag it for the Tester. This run the Reviewer did exactly that: it pre-verified the three seed names against currently-green specs (`Chin-up` at `weekly-muscle-volume.spec.ts:307`, etc.), declared the risk LOW, AND handed it to the Tester as T-1 ("confirm each name resolves (no `pickCanonicalExercise` throw at seed-time); a throw = the catalog drifted further", `review-v1.md:99`). The Tester then ran a live-catalog probe and confirmed all three resolve (`test-report-v1.md:30-41`). **This is the Reviewer's own carry-in refinement resolving cleanly — sustain the "pre-verify what you can statically, then hand the DB-state confirmation to the Tester with a sharp 'what a failure means' note."**
- **T-2 ("positive case asserts the legend, not the SVG line") was the sharpest static catch this run.** The Reviewer noticed the positive e2e tests assert the section header + `Toggle <name>` chip + `opacity-40` toggle but do NOT assert a visible SVG `<polyline>`/`<line>` — so "the line actually renders" was uncovered (`review-v1.md:100`). It handed this to the Tester, who closed it with a runtime polyline probe (`64,34→1232,20`, lastY<firstY = trends up). **This "the test asserts the legend exists but not that the data-bearing element renders" gap is a generalizable static-review catch — when a chart/visualization e2e asserts only the controls, flag 'does the rendered output itself appear?' to the Tester.** Sustain.
- **Re-running the gates yourself (444/444, typecheck 0, lint 0/1) and stating they match the Implementer's claims is the right rigor — sustain "re-run, don't trust."** The Reviewer produced its own numbers and explicitly noted the match (`review-v1.md:16-20`). This is the discipline that catches a fabricated-green or a stale count.

### Recurring pattern check
- **Test-quality scrutiny inside static review — pattern RESOLVED and SUSTAINED (5-run arc), now with the test-data-realness refinement applied.** canonical (candidate addition) → routine-strong-builder (actual fail: missed 2 diff-content e2e bugs) → bottom-tab (applied: caught MIN-1 route-regex + MIN-2 sleep-magnitude) → bodyweight-volume (applied: flagged the runtime-only `opacity-40` assertion as a Tester hand-off + the prior entry RECOMMENDED extending to seed-row-realness) → **this run (applied: T-1 seed-name-realness hand-off + T-2 "asserts legend not line" hand-off, both pre-verified statically where possible and routed to the Tester for the runtime close-loop).** The Reviewer now reliably audits the test diff AND correctly partitions "what static review can confirm" from "what needs a runtime probe," handing the latter to the Tester with actionable failure-meaning notes. Pattern remains resolved and the prior refinement landed. Sustain.
- **Strong positive: file:line evidence dense across all sections** — the 10-item review (Invariant D/E1, LOCF index-0 trace, eligibility+comparator, MAJ-1 teeth, 2 deviations, reuse, mount, MIN-2/3, gates), the implementation-claim verification table (`review-v1.md:50-62`), the T-1..T-4 hand-off notes, security + style checklists. Reviewer output shape is stable and reliable across 5 consecutive runs.

## Run 2026-05-30_0126_bodyweight-volume-per-muscle — 2026-05-30 BRT

Pipeline: feature
Final outcome: pass (Reviewer PASS first round; Tester later found a test-data seed defect Reviewer couldn't statically catch)
Score: 5/5

### Rubric
- Severity classes consistent with Validator's: pass — 0 blockers / 0 majors / 3 minors (`review-v1.md:101-110`), all correctly documentary/robustness/cosmetic: MIN-1 (`features.md` Done not yet written — a finalize concern, correctly handed to the Conductor), MIN-2 (runtime-only `opacity-40` e2e assertion — handed to Tester), MIN-3 (effectively-dead `.every` all-zero guard — cosmetic). Severity boundary matches the Validator's (invariant-break → major; cosmetic/runtime-unverifiable → minor).
- Security checklist applied: pass — full section at `review-v1.md:112-116` covers RLS (the only query change is the widened `stats.ts` SELECT on already-RLS-protected tables; `!inner` provably row-preserving via FK restrict + deleted_at-agnostic RLS), service-role key (grep-clean in shipped code; `SERVICE_ROLE` only in the e2e admin-seed, test-only), raw SQL / `rpc` (none; parameterized PostgREST), public env vars (no new `EXPO_PUBLIC_*`). Correctly scoped — does not invent issues where the change is read-only.
- Style checklist applied: pass — `review-v1.md:118-123` walks no-new-`any` (grep-clean across 14 changed source files; the two `as unknown as` casts in `stats.ts` flagged as PRE-EXISTING, not in the diff), no new `@ts-ignore`/`eslint-disable`, comments-narrate-why (the JSDoc on `effectiveWeightKg`/`bodyweightKgAsOf`/`makeSessionBwResolver` + the MAJ/Invariant inline comments explain rationale + decision IDs), imports follow `~/`-rooted project style, new files in conventional folders.
- Decision rule applied correctly: pass — 0 blockers + 0 majors + 3 minors → `pass` (`review-v1.md:125-134`), reasoning walked per finding category. Correctly noted the carry-in MAJ-3-NEW is CLOSED (both `groupSessionVolumes` callers wired) rather than treating it as still-open.
- Did not run the app: pass — review is static-only; the only commands attributed are re-running the gates (`npm run typecheck` / `npm run lint` / `npm run test:unit`, `review-v1.md:10-13`), explicitly "re-run by Reviewer, not trusted from implementation.md". No `npm run web` / playwright execution attributed to the Reviewer.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks; transcript routing entries declare no peer invocations.)
- Write boundaries: pass — only `review-v1.md` attributed (the review.md template stub is Conductor scaffolding).

### Lessons for next run
- **The 14-site source-trace table is the single most valuable artifact in the review — sustain it.** `review-v1.md:20-37` walks every kernel site, names the exact `w`/`eff`/`effW` source variable per site, and confirms "no 15th `w*r` site exists." This is the THIRD independent pass over the kernel set (after the Designer's re-grep and the Validator's re-grep), and it is precisely why the "same number everywhere" invariant credibly holds. For any fan-out feature, the Reviewer's job is to re-prove the closed set against the actual diff, not just trust the upstream re-grep. Sustain.
- **Re-running the gates yourself (not trusting implementation.md) is the right rigor — sustain it.** The Reviewer independently produced typecheck 0 / lint 0 / 431/431 and stated explicitly they match the Implementer's claims. This is the discipline that catches a fabricated-green or a stale-number; keep "re-run, don't trust" as the contract.
- **Test-quality scrutiny is now correctly applied — MIN-2 was the right hand-off.** The Reviewer flagged the `opacity-40` runtime-only e2e assertion as statically-unverifiable and handed it to the Tester (`review-v1.md:109, 138`); the Tester then empirically resolved it (RN-Web 0.21 DOES emit the class, `test-report-v1.md:56`). This is exactly the "audit the test diff, flag what only a runtime check can confirm" discipline the prior-run lesson asked for. **However:** the Reviewer could NOT have caught the round-1 "Pull-up" seed defect statically — whether the live canonical catalog contains a "Pull-up" row is a DB-STATE property, not inferable from the diff. The Implementer's note even cited the (wrong) `0004` seed as proof it exists; the Reviewer reasonably trusted that. **No penalty (static review cannot catch DB-state), but a sharper hand-off note is worth adding: when a spec seeds against a NAMED canonical row via `pickCanonicalExercise(admin, NAME)`, flag to the Tester "verify NAME exists in the LIVE canonical catalog (0011), not just the seed migration" — that one-line flag would have pre-warned the Tester before the round-1 FAIL.**
- **MIN-3 (the effectively-dead `.every` all-zero guard) is a sharp read.** The Reviewer noticed the JSDoc claims "drops all-zero series" but the actual mechanism is "absent series never allocated" — so the `.every` check is harmless dead defensive code, not the contract-realizing path. That kind of "the comment claims X but the realizing mechanism is Y" catch is the right depth for static review. Sustain.

### Recurring pattern check
- **Test-quality scrutiny inside static review — pattern RESOLVED and SUSTAINED (4-run arc).** canonical (candidate addition) → routine-strong-builder (actual fail: missed 2 diff-content e2e bugs) → bottom-tab (applied: caught MIN-1 route-regex + MIN-2 sleep-magnitude) → **this run (applied: flagged the runtime-only `opacity-40` assertion as a Tester hand-off, correctly NOT treating it as statically confirmable)**. The Reviewer now reliably audits the test diff AND correctly distinguishes what static review can confirm from what needs a runtime probe. Pattern remains resolved. The only refinement (above): extend the test-data scrutiny to "is the named seed row real in the live catalog?" — a DB-state question the Reviewer can't answer but CAN flag for the Tester.
- **Strong positive: file:line evidence dense across all sections** — the 14-site trace table, the implementation-claim verification table (`review-v1.md:84-97`), the 5-deviation audit (`:64-69`), security + style checklists. Reviewer output shape is stable and reliable across 4 consecutive runs.

## Run 2026-05-26_0307_bottom-tab-home-link — 2026-05-26 04:42 BRT

Pipeline: feature
Final outcome: pass (Reviewer passed first try; Tester later found runtime gap Reviewer couldn't statically catch)
Score: 5/5

### Rubric
- Severity classes consistent with Validator's: pass — 0 blockers / 0 majors / 3 minors (`review-v1.md:42-56`), all correctly cosmetic/advisory (route-regex tolerance for `?id=` suffix, magic-number sleep magnitude, brittle `node_modules/` path in a code comment).
- Security checklist applied: pass — full security section at `review-v1.md:58-64` covers RLS (N/A — pure client navigation, no DB queries), service-role key (used only in spec for setup, never in shipped code), input handling (no raw SQL/`rpc`/shell exec), public env vars (no new `EXPO_PUBLIC_*`), auth surface (no change). Explicit "N/A in substance" framing where appropriate — does not invent issues.
- Style checklist applied: pass — `review-v1.md:66-74` walks "no new `any` / no new `@ts-ignore` / no new `as` casts" with diff grep, comments-narrate-why (caught MIN-3: the `node_modules/...` path in a code comment is brittle on version pin drift), imports follow project style. The MIN-3 catch on the brittle absolute-path-in-comment is exactly the right kind of "you put a fragile reference in source" find.
- Decision rule applied correctly: pass — 0 blockers + 0 majors + 3 minors → `pass` (`review-v1.md:111-127`). Reasoning explicit walking each finding category.
- Did not run the app: pass — review is static-only (`review-v1.md:5-9` cites the diff command + line counts; one `npm run typecheck` re-run is the only command attributed; no `npm run web` or playwright execution attributed to Reviewer).
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — only `review-v1.md` attributed.

### Lessons for next run
- **Test-quality scrutiny is now applied at the right depth, with diff-content sibling-precedent cross-references.** The `Test-quality scrutiny` section (`review-v1.md:76-93`) walks each new e2e assertion against sibling precedents and flags MIN-1 (route regex `/exercises/<id>/progress$` ends with `$` but sibling `exercise-progress-ia.spec.ts:259` uses `(\?.*)?$` for `?id=` suffix tolerance) AND MIN-2 (300ms sleep below the 500ms sibling-precedent floor). This is exactly the gap that was flagged as the canonical-exercises run's "candidate addition to the static checklist" and the routine-strong-builder run's actual fail. **This run applied the lesson correctly — the routine-strong-builder string-vs-number sibling-precedent and `.first()` discipline are now both encoded in the checklist and applied.** Sustain.
- **Behavioural-correctness-of-the-listener table** (`review-v1.md:95-109`) is a new format that worked well — walks each guard conjunct against the design's Behaviour matrix, the type contract source, AND the framework's own use of the same dispatch shape (`createNativeStackNavigator.js:62-65`). For framework-event-handler code, this kind of "guard-by-guard against the framework's primitives" cross-check is the right discipline. Sustain.
- **The cross-check against `expo-router`'s own internal use of `{...StackActions.popToTop(), target: state.key}`** at `:107` is high-leverage — by anchoring the dispatch shape to the framework's own pattern, the Reviewer made the upstream-divergence risk verifiable. Sustain this "find the framework's canonical use and cite it" reflex for any new framework-boundary code.
- **The Reviewer correctly identified the limit of static review for this kind of feature.** The runtime gap (round-1 `tabPress` not firing on focused re-tap, round-2 `PartialState` shape from deep-link rehydration) is empirically observable but not statically inferable from the diff. Tester surfaced both. The Reviewer's role here was to verify everything verifiable from the diff (action shape, type contract, deviations, security, style) and pass it to Tester for the runtime close-loop — which is exactly what happened. **No score penalty for missing the runtime gap; static review can't catch what only a probe reveals.** But: Reviewer could have flagged "this design's correctness depends on a runtime ordering claim that hasn't been runtime-verified" as an explicit hand-off note to Tester, sharper than the current "Tester can decide whether MIN-1's regex tolerance is needed based on observed runtime behaviour." Consider adding a checklist item: "if the design depends on event ordering or async dispatch chains, explicitly note this in the hand-off to Tester."
- **All 5 declared deviations verified individually** with file:line evidence (`review-v1.md:30-37`). The 7-row deviation-audit format is exactly the cross-check Validator can't do (no diff yet) and Tester won't do (executes, doesn't read). Sustain.

### Recurring pattern check
- **Test-quality scrutiny inside static review** is now showing maturation. Canonical-exercises run: "candidate addition to the static checklist". Routine-strong-builder run: missed two diff-content e2e bugs (PostgREST string-vs-number, strict-mode locator). **This run: applied the discipline correctly** — caught MIN-1 (route regex `?id=` suffix) and MIN-2 (sleep magnitude) via sibling-precedent cross-reference, both before Tester ran. **Pattern resolved: Reviewer's test-quality methodology is now reliable.** Sustain and watch for new variants.
- **Strong positive: file:line evidence dense across all 5 sections** — verification table (16 claims), deviation audit (5 rows), test-quality scrutiny (10 rows), behavioural-correctness table (5 guards), checklists (security + style). Reviewer output shape is stable and reliable across 3 consecutive runs.

## Run 2026-05-26_0101_routine-strong-builder — 2026-05-26 02:58 BRT

Pipeline: feature
Final outcome: pass (Reviewer passed first try; Tester later found 2 e2e defects Reviewer missed)
Score: 4/5

### Rubric
- Severity classes consistent with Validator's: pass — 0 blockers / 0 majors / 5 minors (`review-v1.md:64-81`), aligned with Validator's classification rubric. All 5 minors are correctly cosmetic / robustness-future / doc-only.
- Security checklist applied: pass — full security section at `review-v1.md:53-60` covers RLS posture (new `routine_exercise_sets` table has 4 explicit policies all gated on `auth.uid() = user_id`), no service-role creds in client code (grep verified — `SERVICE_ROLE` clean in `src/`/`app/`), input handling (parameterized PostgREST only, no raw SQL/`rpc`), public env vars (no new `EXPO_PUBLIC_*`), bulk-insert RLS (`seedSetsForSession` reads `auth.user.id` first and every row carries `user_id = userId`). The new partial-unique `(routine_id, exercise_id)` is correctly flagged as a constraint not a query surface.
- Style checklist applied: pass — `review-v1.md:36` walks "no new `any` / no new `@ts-ignore`" with diff grep; 2 `as unknown as` casts at `routine-exercise-sets.ts:66-67, :259` documented as matching `sets.ts:200-203` / `stats.ts:63,87` precedents. MIN-5 (import-ordering informational) and MIN-3 (`useEffect` reset robustness) catch the right kind of stylistic/foot-gun concerns.
- Decision rule applied correctly: pass — 0 blockers + 0 majors + 5 minors → `pass` (`review-v1.md:83-96`). Reasoning explicit at `:87-94` walking each finding category.
- Did not run the app: pass — review is static-only against the diff (`review-v1.md:5-12` cites the diff command + line counts + new/edited file list; one `npm run typecheck` re-run is the only command — no `npm run web` or playwright execution attributed to Reviewer).
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — only `review-v1.md` attributed.

### Lessons for next run
- **Static review of new e2e specs MUST trace at least one assertion mentally against the runtime data shape.** Reviewer's deviation-audit table (`review-v1.md:39-50`) walked each Implementer-declared deviation, AND the verification table (`:15-37`) walked every implementation claim — but neither cross-checked the test-suite's assertions against the PostgREST data shapes. The line-246 string-vs-number bug (`tests/e2e/routine-strong-builder.spec.ts:246` expected `["60.00", "70.00", "80.00"]` strings, PostgREST returns `numeric` as JS number) is the kind of bug Reviewer SHOULD catch in static review because (a) the assertion is literally in the diff, and (b) there is an established precedent in the same e2e dir at `auto-fill-placeholder-on-check.spec.ts:340` (`parseFloat(row.weight as string)`) that proves the runtime shape. **Add to the static review checklist: for each new e2e assertion against a PostgREST-fetched value, find ONE sibling spec asserting against the same column type and confirm the runtime-shape coercion is consistent.**
- **The line-376 strict-mode locator collision is harder to catch statically** (you'd need to think "this page also renders a tab bar with the same label"), but the sibling-precedent reflex would have caught it: 8 of 9 sibling specs that match `"Exercises"` use `.first()`. **Add to the static review checklist: for any `getByText` / `getByRole` on a label that could plausibly appear in the bottom tab bar (Workout, Exercises, History, Progress, Profile), default-flag and require `.first()` or a `within()` scope.**
- **MIN-3 robustness catch on `routine-exercise-card.tsx:326-329`** (the `useEffect` reset clobbering an in-flight user edit if cache invalidates while typing) is the exact shape of catch the Reviewer SHOULD do — reading the source for "what's the unhappy path?" Sustain that. Note Reviewer correctly classified it as future-robustness, not a regression — the v1 single-user / single-tab context absorbs it.
- **MIN-4 wording catch on the defensive `throw` in `seedSetsForSession`** (`review-v1.md:78-79`) is also a sharp read — Reviewer noticed the comment "DB CHECK invariant should make this unreachable" could mislead a future reader into thinking the throw is dead code. Recommending the rewording is the right shape of catch.
- **Sustain the deviation-audit table** (`review-v1.md:39-51`) — walking each Implementer deviation against the design's flex points is exactly the cross-check the Validator can't do (no diff yet) and the Tester won't do (executes, doesn't read). The 7-row table here is high-value.
- **Sustain the 30+ claim verification table** (`review-v1.md:15-37`) — dense, file:line-cited, makes Evaluator audit trivial.

### Recurring pattern check
- **Test-quality scrutiny inside static review** is a candidate recurring miss: in canonical-exercises run, Reviewer "did NOT catch the soft-deleted-canonical leak" (DB-state, not diff-content, so excused — `docs/feedback/reviewer.md:22` notes the gap as a "candidate addition to the static checklist"). **In this run**, Reviewer missed two diff-content e2e bugs (line-246 PostgREST type mismatch, line-376 strict-mode locator) — both ARE diff-content, both ARE caught by sibling-precedent grep. Pattern: **Reviewer's static methodology audits source code well but does not yet audit test code with the same rigor.** Next-run lesson: extend the verification table to include the test-suite diff explicitly, with sibling-precedent cross-references for each new assertion / locator.

## Run 2026-05-25_1921_canonical-exercises — 2026-05-26 00:35 BRT

Pipeline: feature
Final outcome: pass
Score: 5/5

### Rubric
- Severity classes consistent with Validator's: pass — 0 blockers / 0 majors / 6 minors (`review-v1.md:49-69`), aligned with Validator's classification rubric. None of the minors mis-classified.
- Security checklist applied: pass — full security section at `review-v1.md:71-78` covers RLS posture (new SELECT widened, mutating policies tightened), no service-role creds in client code (grep verified), input handling (parameterized PostgREST only), public env vars (no new `EXPO_PUBLIC_*`), spoof-NULL-INSERT defense (double-locked at API + RLS layers).
- Style checklist applied: pass — `review-v1.md:80-87` walks "no new any" / "no new @ts-ignore" / `as` cast inventory (4 new, all consistent with existing test convention) / comments-narrate-why / imports-follow-project-style (caught 2 specs with helper import inside package block — MIN-2) / new files in conventional folders.
- Decision rule applied correctly: pass — 0 blockers + 0 majors + 6 minors → `pass` (`review-v1.md:91-101`). Reasoning explicit at `:93-100` ("0 blockers + 0 majors + 6 minors — none are functional regressions; all stylistic, robustness-improvement, or documentary").
- Did not run the app: pass — review is static-only against the diff (`review-v1.md:6-10` cites the diff command + line counts + new files; no `npm run web` or e2e execution attributed to Reviewer).
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — only `review-v1.md` attributed.

### Lessons for next run
- The **deviation-audit table** (`review-v1.md:37-44`) — walking each Implementer-declared deviation against the design's flex points — was high-value defense. That's exactly the cross-check the Validator can't do (because it doesn't see the diff yet) and the Tester won't do (because it executes, not reads). Sustain it.
- The 31-claim verification table (`review-v1.md:14-33`) is dense but readable; keep the format. Spot-citing line numbers (e.g. `progress.tsx:50-83`, `created-by-you-chip.tsx:22-33`) makes it trivial for a future Evaluator to audit.
- **MIN-3 (gating spec 5 missing pre-flight SELECT auth proof)** was a sharp robustness catch — proved that you read the test code, not just the source under test. Test-quality scrutiny inside review is rare and valuable.
- The Reviewer did NOT catch the soft-deleted-canonical leak (root cause was DB state, not diff content) — but it's worth noting: a future Reviewer cross-check ("read the migration's intent + check pre-existing DB state where the migration applies") would have surfaced it. Not a fail this run (review is static-on-diff, not DB-state inspection), but a candidate addition to the static checklist for migrations that touch existing rows.

### Recurring pattern check
- none — first feedback entry for Reviewer on this project.
