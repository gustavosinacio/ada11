# Feedback for Discovery

## Run 2026-06-04_1700_routine-preview-start — 2026-06-08 14:30 BRT

Pipeline: feature
Final outcome: pass (shipped — UI/nav feature, code pending commit+deploy+push; NO migration / NO new query [data already exists]; D↔V 2 rounds [v1 NO-GO → v2 GO]; I↔R 1 PASS; I↔T 1 PASS after a Tester socket-drop + Conductor recovery; 1 human decision batch [U1/U3/U4]; feature works live: tap→preview→Start→`/workout/{id}`, Guard-A routes to existing session)
Score: 6/6

### Rubric
- Every section of `discovery.md` filled: pass — Feature prompt (`:3-4`), Premise check (`:6-9`), Scope summary (`:11-12`), Affected files split into in-scope-CHANGE / likely-NEW / read-only-VERIFIED-must-NOT-change with file:line per entry (`:14-32`), §1 current nav+start flow pinned (`:34-39`), §2 the central screen-location fork with 3 options a/b/c each with tradeoff+confidence (`:41-63`), §3 data/hook feeding (`:65-70`), §4 the Start-button-shares-the-existing-flow with the U2 fork (`:72-81`), §5 nav/affordance changes (`:83-90`), §6 active-session/edge cases (`:92-95`), Relevant conventions (5 FACT-marked, `:97-102`), Constraints (Data/UI/Platform/Auth/Performance, `:104-109`), Existing precedents (`:111-115`), §7 the tests-as-load-bearing-blast-radius (`:129-140`), 9 Unknowns each (a)/(b)/(c)+confidence (`:142-152`), Out-of-scope flags (`:154-161`). None missing.
- Concrete code references cite file:line: pass — every claim cites file:line: the start chain `workout/index.tsx:145 → startFromRoutine :60-83 → useStartSessionFromRoutine → router.replace :72`; the data hooks `useRoutineExercises :16-22 → listRoutineExercises routine-exercises.ts:50-61` + `useRoutineExerciseSets :17-23 → routine-exercise-sets.ts:53-69`; the editor grouping `routines/[id]/index.tsx:152-160`; the read-only precedent `set-display.ts:41-60` (`displayWeight`/`displayReps`) + `<ReadOnlySetRow>`/`<ReadOnlyExerciseBlock>`; the 3 guards `:61-63`/`:65-66,80-81`/`:73-79`; and the load-bearing e2e selector inventory `routine-strong-builder.spec.ts:212,217,272,319,420,486`. Dense and accurate.
- `fact` vs `assumption` clearly distinguished: pass — "FACT, verified by reading" on the premise (`:7`); "FACT, HIGH confidence" on the no-migration finding (`:8`); "(FACT — grep)" on the close-the-set verdict (`:127`); "(FACT — read all 5 tests)" on the blast-radius (`:136`); every Option (a)/(b)/(c) carries an explicit Confidence (a MEDIUM-HIGH, b MEDIUM, c HIGH-it's-wrong) and every Unknown a confidence label. The recommendation is explicitly hedged ("Confidence MEDIUM-HIGH — (b) is a legitimate fallback ... a product call U1"). The fact-vs-recommendation boundary is clean throughout.
- `Unknowns` section populated: pass — 9 unknowns ranked by design impact (`:142-152`), each (a)/(b)/(c)+confidence; U1 (screen approach), U3 (preview-only vs keep direct-start), U4 (Edit jump) correctly flagged as HUMAN/product calls — and these are EXACTLY the three the Conductor escalated and the human locked (`state.md:42-46`, `transcript.md:23-24`). U2/U5/U6/U7/U8/U9 correctly framed as Designer-adoptable defaults with recommendations. The decision surface Discovery built is precisely the one the human used.
- Searched for precedent / sibling patterns: pass — the History read-only triad (`<ReadOnlyExerciseBlock>`/`<ReadOnlySetRow>`/`set-display.ts`) named as the 1:1 structural template for the preview's read-only card, with the honest caveat that it operates on `SetRow` (logged) not `RoutineExerciseSetRow` (targets) so it is "a structural PRECEDENT, not a drop-in" (`:112`); the History screen-level edit toggle (`history/[id].tsx:78-81`) as the precedent for Option (b); the editor itself as the hooks+grouping source; the seed-on-start flow as the existing flow the Start button is "a NEW caller of, not new logic" (`:115`). The close-the-set grep (`Start workout|startSession\(|useStartSession|startFromRoutine|seedSetsForSession`) proved exactly ONE start-from-routine affordance + one ad-hoc start, no N+1th (`:117-127`).
- Relative dates converted to absolute: pass — no relative dates in the prompt; n/a-equivalent, handled (the run-id timestamp is already absolute BRT).

- **The standout: Discovery correctly identified the e2e a11y-LABEL coupling as the single highest-impact downstream surface, AND pinned the exact 5-test break with the seed-fail URL re-pin subtlety — and flagged the label-is-both-SR-label-AND-selector caution at the row component.** §7 (`:129-140`) is the load-bearing section: it observed that `routine-list-item.tsx:43`'s `"Start workout: {name}"` label is BOTH the screen-reader label AND the e2e selector for 5 tests, walked each of tests 1/2/3/5/6 with its specific break (the idempotency double-tap moves to the Start button; test 6's bare-`/workout` URL assertion must be re-pinned to the preview route), and named this "the single highest-impact downstream change." This is exactly the right depth for a UI/nav feature whose real risk is the test blast radius, not the production seam. The Conductor flagged this same coupling forward (`transcript.md:20`), the Designer carried it as Flag-for-the-Validator, and it became the spine of the whole D↔V interaction.
- **The premise verification was load-bearing and correct: "UI + navigation feature, not a data feature" reframed the whole effort and proved no-migration by reading the editor's existing data load.** Discovery verified (`:8`) that the editor ALREADY loads the exact shape a preview wants (`useRoutineExercises` + `useRoutineExerciseSets` + the grouping), so "the prompt's 'read-only' constraint is satisfiable with zero data work." This is the 7th consecutive run of the "verify the premise against the live source" discipline, here applied to confirm-and-narrow (it's pure UI/nav) rather than to falsify — and it correctly set the no-migration/no-query invariants that held all the way to ship (`final-summary.md:5`).
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` blocks in the transcript; the Discovery routing entry `transcript.md:17-21` describes a self-contained read + explicit "No peer invocations"; a phrasing scan for unlogged invocations across all run `*.md` returned 0 hits.)
- Write boundaries: pass — only `discovery.md` attributed to Discovery. Baseline `baseline_commit: 592dd51a2baceaf38389efd10a609fe752e490dd` (`state.md:10`, also `Commit:` at `:9`) resolved via `git rev-parse --verify 592dd51…^{commit}` (printed the full hash, exit 0). Ran `{ git diff --name-only <baseline>; git ls-files --others --exclude-standard; } | sort -u`: the only Discovery-attributed run artifact is `discovery.md`; the `design.md`/`validation.md`/`review.md`/`test-report.md` template STUBS + `state.md`/`transcript.md`/`final-summary.md` are Conductor/shared; the prior-run `2026-06-01_1301.../state.md` + the other-run PNGs are the codified cross-run/working-tree noise. ZERO source/test/migration changes attributable to Discovery. Ran this check BEFORE writing the feedback entries so the `docs/feedback/` writes did not pollute the diff.

### Lessons for next run
- **Sustain the "for a UI/nav feature, treat the e2e selector/label coupling as the FIRST-class blast radius" instinct — it was the correct call this run.** Discovery correctly judged that the only real engineering surface here was (1) where the preview lives, (2) re-pointing the tap, (3) sharing the start flow, and (4) the e2e blast radius — and it gave the e2e coupling a dedicated load-bearing section (§7). That framing was right: the v1 NO-GO and the entire Tester round were both about selectors/URLs, not production logic. The generalizable rule, reinforced: when a feature relabels or removes an a11y affordance, the test blast radius IS the feature's real risk — enumerate every spec that queries the affected label/route up front.
- **Sharpest improvement lever (the one residual that propagated into the v1 NO-GO — minor, NOT a Discovery fail this run): §7's a11y-label inventory enumerated the `Start workout:` selectors WITHIN `routine-strong-builder.spec.ts` but did NOT grep the WHOLE suite for the row's labels, so the `crud.spec.ts:113` (`Edit routine:`) + `probe-strong-unify.spec.ts:217,232` (`Start workout:` / `Edit routine:`) consumers were not surfaced.** `:140` claims "No other e2e taps a routine to start" — true for the START affordance, but the row ALSO exposes an `Edit routine:` label that two other specs query, and removing the Edit pill (a LOCKED-U4 consequence) breaks them. The grep at `:118` searched `Start workout|startSession|useStartSession|startFromRoutine|seedSetsForSession` — it did NOT include `Edit routine:` (the OTHER label on the same row). The Designer inherited this scoping and shipped the v1 NO-GO on it; the Validator's whole-suite re-grep caught all 9 sites. **The rule for the next relabel/remove-affordance feature: when a row component exposes MULTIPLE a11y labels (here `Start workout:` AND `Edit routine:`), the close-the-set grep must enumerate EVERY label the component produces across the WHOLE `tests/` tree, not just the one tied to the headline behavior — a removed-affordance label is as load-bearing as a relabeled one.** This is the same close-the-set-completeness axis the Designer's recurring pattern tracks; surfacing it at Discovery would have pre-empted the D↔V round.

### Recurring pattern check
- **"Verify/reframe the prompt's premise against the live source" — now a 7-run sustained positive pattern.** e1rm catalog → favorites file/mechanism → progress-window UI-vocabulary → bodyweight-leverage catalog-gating → hard-sets `set_type`-containment → secondary-muscle premise-FALSIFICATION → THIS run (verified the editor already loads the preview's data ⇒ pure UI/nav, no migration/query). The discipline reliably sets the load-bearing invariants at the cheapest gate. Sustain.
- **"Search thorough-looking but not exhaustive-by-construction" — a NEAR-MISS resurfaced in a NEW sub-variant (multi-label component), but it is LOW-severity and did NOT dock the score.** The close-the-set on START affordances (`:117-127`) was exhaustive and correct (one start-from-routine site, one ad-hoc, no N+1th). The gap was a DIFFERENT axis: the row component exposes a SECOND label (`Edit routine:`) that the grep did not enumerate suite-wide, so two specs querying it were missed and the Designer inherited the under-scope. This is adjacent to (not a recurrence of) the historical kernel-call-sites-vs-input-builders miss — there the missed set was input constructors; here it is a second a11y label on the same component. Severity LOW: it cost the Designer one D↔V round (the Validator caught it cheaply via whole-suite re-grep), and Discovery's PRODUCTION analysis was flawless. Name it for the next relabel feature: grep EVERY label a touched component produces, suite-wide.
- **The "enumerate the cheap MIDDLE option on a human-gated unknown" discipline (the hard-sets 5/6 fail, discharged last run) STAYED discharged this run.** U1's option set is complete (a new-route / b reuse-editor-with-mode / c modal), with the cheap-middle (b) given a fair feasibility verdict ("does NOT actually save the new-read-only-card cost") rather than a strawman dismissal, and U2's start-logic fork enumerates (i) move-handler vs (ii) extract-hook with the move recommended for the single-caller reality. No option-set-completeness fail this run. Sustain.

## Run 2026-06-04_0905_secondary-muscle-attribution — 2026-06-04 09:30 BRT

Pipeline: feature
Final outcome: deferred (owner DEFERRED at the Discovery escalation, before Design — all 3 decision questions → defer; no code/design/migration/deploy; 0 round-trips; the run was KILLED EARLY because Discovery falsified the feature premise against live data)
Score: 6/6

### Rubric
- Every section of `discovery.md` filled: pass — Feature prompt (`:3-4`), Scope summary (`:6-7`), Affected files split into in-scope-CHANGE / likely-NEW / read-only-VERIFIED-must-NOT-change with file:line per entry (`:9-25`), The single attribution seam (FACT, verified by reading, with the today-code + fractional-generalization side-by-side, `:27-44`), WHERE the fractional weights come from — all 5 options a/b/c/c-middle/d with the data-feasibility verdict per option (`:46-62`), Sum-to-1 invariant + back-compat (`:64-67`), Fractional-hard-sets semantic flag (`:69-72`), Close-the-set attribution-site table (7 rows + verdict, `:74-85`), "Other"/MuscleGroup-with-fractions edge (`:87-88`), Tests & conventions (`:90-99`), Coupling note (`:101-102`), Relevant conventions (5 FACT-marked, `:104-109`), Constraints (Data/UI/Platform/Auth/Performance, `:111-116`), Existing precedents (`:118-122`), 9 Unknowns each (a)/(b)/(c)+confidence (`:123-133`), Out-of-scope flags (`:135-141`). None missing.
- Concrete code references cite file:line: pass — the seam at `weekly-muscle-volume.ts:149-170` (read `:149`, map `:150-151`, accumulate `:170`) — VERIFIED by me against source: `:149` is `const primary = (ex.muscles ?? [])[0]`, `:150-151` maps to `MuscleGroup | "Other"`, `:170` is `values[idx]! += metric.contribute(row, w, r)`, and the loop body `:139-171` is metric-agnostic (only `needsLoad`/`include`/`contribute` differ) — byte-accurate. Also cites `progress-page-math.ts:522-544` (`groupExercisesByPrimaryMuscle`, VERIFIED to exist at `:522`), `use-progress-page.ts:329-334`, `set-display.ts:159-162`, `weekly-muscle-volume-section.tsx:34-43,61-75`, `stats.ts`, `schema.ts:59`, `db/types.ts:143-160,172`, `0014:35-141`, `0021_bodyweight_factor.sql` (VERIFIED latest — next is 0022). Dense and accurate.
- `fact` vs `assumption` clearly distinguished: pass — "FACT — verified by reading" on the seam (`:27`), "Load-bearing data finding (FACT — live catalog query, project `ykrbgpctbfvndxjnpzrg`… 95 rows)" on the muscle distribution (`:47`), "FACT — grep" on the close-the-set verdict (`:85`); every option/Unknown carries an explicit confidence label (b=HIGH, c=HIGH, c-middle=MEDIUM-HIGH, a/d=LOW; U1 MEDIUM, U2 LOW-on-values); the recommendation is explicitly "MEDIUM confidence — fundamentally a product/data decision" (`:62`). The fact-vs-recommendation boundary is clean throughout.
- `Unknowns` section populated: pass — 9 unknowns ranked by design impact (`:123-133`), each (a)/(b)/(c)+confidence; U1 (storage a/b/c/c-middle/d), U2 (fraction VALUES), U3 (fractional-hard-sets semantics) correctly flagged as HUMAN/owner decisions; U5/U6 correctly recommended deferrable/out-of-scope. The owner deferred on exactly U1+U2+U3 (`transcript.md:21`, `state.md:40`) — the three human-gated unknowns Discovery surfaced were the precise decision surface.
- Searched for precedent / sibling patterns: pass — Phase-0/bodyweight-leverage (`0021`) named as the EXACT "retroactive redistribution + NULL/identity⇒byte-for-byte + owner-locked numeric values + canonical-only idempotent backfill" template (`:119`); the hard-sets run's `metric`-parameterized scaffold as the precedent for a per-metric `attribution` mode (`:120`); `0014` as the by-name canonical-UPDATE idiom for populating secondaries (`:121`). The close-the-set grep (`\.muscles` + `muscles[` across `app/`+`src/`) proved exactly ONE volume-attribution site and that the two other `muscles[0]` sites group EXERCISES not volume (`:74-85`).
- Relative dates converted to absolute: pass — no relative dates in the prompt; the date-discipline surface (the migration `WHERE`-clause backfill framing, the ISO-week bucketing axis) is handled as absolute. n/a-equivalent, handled.

- **The standout — and the single highest-value move in the entire run: Discovery FALSIFIED the feature's headline premise against the LIVE catalog BEFORE any design, and that one verification killed the run at zero round-cost.** The prompt asserts "bench → partial Arms/Shoulders." Discovery queried the live catalog (`:47-52`) and proved the premise is data-false: `muscles[]` length distribution is `{1: 81, 2: 12, 6: 2}` (85% single-muscle), `Bench Press → ['Chest']` only (no Arms/Shoulders row exists), AND `muscles[0]` is not reliably primary (`Squat → ['Lower back','Legs']`, Legs being a squat's actual primary). The conclusion — "this is a data-authoring feature, not a math feature" (`:7`) — reframed the entire feature from a ~3-line code change into a ~14-compound subjective muscle-fraction authoring task with no literature consensus on the split values. **This pre-empted an entire wasted Design→Validate→Implement→Review→Test cycle on a premise that cannot be satisfied without owner data-authoring first.** It is the exact "verify the prompt's premise against the live source" critical-posture the project most wants, applied at the cheapest possible gate (Discovery, before a single line of design). This is the 6th consecutive run of that positive pattern and the most consequential instance of it — a clean early-kill.
- **The standing "enumerate the cheap MIDDLE option" lesson (from the hard-sets 5/6 run) was DISCHARGED this run — explicitly and by name.** The hard-sets fail was that U3 collapsed to "my lean vs an expensive strawman" and missed the cheap working-only middle. This run Discovery enumerated the FULL option set for U1 — (a) derive-from-order, (b) new structured column, (c) hardcoded code table, **(c-middle) migration-populates-secondaries + code-fractions**, (d) global heuristic — each with the data-feasibility verdict and a cost/confidence label, and it labelled (c-middle) verbatim as "the 'middle option' my standing feedback says to surface — neither pure-code (c) nor pure-new-column (b)" (`:59`). It did NOT lean on extremes: it KILLED (a) and (d) on DATA grounds (81/95 single-muscle rows make order-derivation inert for the headline movements, `:56,:60`), surfaced (b)/(c)/(c-middle) as the realistic feasible set (`:62`), and gave the owner a ranked lean within them. **This is the carry-in lesson read, named, and correctly applied — the loop closed the prior fail class. Credit the feedback loop working: the 5/6 dock from last run is the direct cause of this run's complete, middle-inclusive option set.**
- **Escalation hygiene was exemplary: Discovery separated the THREE genuinely human-gated decisions (U1 storage, U2 fraction VALUES, U3 fractional-hard-sets semantics) from the engineering it could settle itself, and flagged the semantic oddity it could not resolve.** It explicitly refused to invent the fraction values (`:62`, "Discovery cannot invent them; the hypertrophy literature has no single consensus split" — mirroring the owner-locked bodyweight-factor run) and surfaced the fractional-hard-sets oddity (U3, `:69-72`: "0.4 of a set has no clear meaning" in a metric built on integer set-counting, with 3 ranked resolutions and a tonnage-only lean). These were exactly the three questions the Conductor escalated and the owner deferred on. Knowing what NOT to decide — and presenting it as a clean decision surface — is what made the early-kill possible.
- Audit trail: n/a — no peer invocations attributable to this agent. (`grep -c "peer_invocation:" transcript.md` → 0; the full 25-line transcript is 1 Discovery routing entry [`transcript.md:17-21`, describing a self-contained read + live-catalog query, explicitly "No peer invocations"] + 1 Conductor↔Human defer entry [`:23-25`]; the live-DB catalog query in `discovery.md:47` is a direct anon-client read, NOT a peer-agent invocation; no agent-to-agent invocation occurred, playbook hard rule held.)
- Write boundaries: pass — only `discovery.md` attributed to Discovery. Baseline `e098314cc30eb5e4d809fb48ebb18dfb117c09ff` (`state.md:9-10`) resolved via `git rev-parse --verify ...^{commit}` (printed full hash, exit 0). Ran `{ git diff --name-only <baseline>; git ls-files --others --exclude-standard; } | sort -u`: the only run-folder artifacts are `discovery.md`→discovery + the `design.md`/`implementation.md`/`review.md`/`test-report.md`/`validation.md` template STUBS (no `-v*` versions — Conductor scaffolding, deferred before Design) + `state.md`/`transcript.md`/`final-summary.md` (Conductor/shared). **ZERO source-code/migration changes in the diff** — exactly correct for a deferred-before-Design run (no code/migration/deploy). The prior-run `2026-06-01_1301.../state.md` is the codified cross-run finalize noise (excluded); the other-run PNGs are committed-vs-working-tree noise (excluded). Ran this check BEFORE writing this entry so the `docs/feedback/` write did not pollute the diff.

### Lessons for next run
- **Sustain the live-catalog premise-falsification as the standing FIRST move — it just delivered its highest-value payoff yet: an early-kill of a data-false feature at zero round-cost.** Across 6 runs (e1rm catalog → favorites file/mechanism → progress-window UI-vocabulary → bodyweight-leverage catalog-gating → hard-sets `set_type`-containment → THIS run's `muscles[]` premise-falsification) this discipline has caught a wrong premise at the cheapest gate every time. This run it did MORE than reframe — it justified DEFERRING the whole feature, because the falsified premise revealed the work is owner data-authoring, not engineering. The generalizable rule, now battle-tested: when a prompt names specific exercises/muscles/rows, query the live `user_id IS NULL` catalog and reconcile the prompt's claim against it BEFORE design — and if the data falsifies the premise, say so plainly and surface the reframe (here: "data-authoring feature, not a math feature") rather than designing around a hole.
- **Sharpest improvement lever (minor, on an otherwise-clean 6/6): the data-falsification was load-bearing enough that a one-line "premise vs live data" verdict at the TOP of `discovery.md` would make the kill-decision even harder to miss.** The falsification is currently distributed across the Scope summary (`:7`) and the WHERE-the-weights-come-from section (`:47-52`); both are excellent, but a reader scanning top-down hits the seam analysis (`:27-44`) before the premise-falsification. For a run whose ENTIRE outcome turns on "the premise is false," a leading one-liner ("PREMISE CHECK: prompt assumes bench→Arms/Shoulders; live catalog has `Bench Press → ['Chest']` only, 81/95 single-muscle — feature is data-authoring, not code") would put the decision-driving fact first. This is a presentation refinement, not a content gap — the content is complete and correct.

### Recurring pattern check
- **"Verify/reframe the prompt's premise against the live source" — now a 6-run sustained positive pattern, and this run is its strongest instance: a premise-FALSIFICATION that justified an early-kill, not just a reframe.** The prior 5 instances reframed scope (catalog claim, wrong file/mechanism, UI vocabulary, catalog-gating, containment proof); this run the verification PROVED the feature's headline cannot be satisfied with existing data, which is the difference between "design it differently" and "don't design it yet." Reliable, load-bearing, and now demonstrated as a run-killing safeguard. Sustain — this is Discovery's single most valuable repeated behavior.
- **The hard-sets run's NEW fail-axis — "option-set completeness on a human-gated unknown (collapse to lean-plus-strawman, miss the cheap middle)" — did NOT recur; it was RESOLVED this run, explicitly.** Last run's 5/6 dock was U3 collapsing to count-all-vs-fold and missing working-only. This run U1's option set is complete (a/b/c/c-middle/d), the cheap middle (c-middle) is named and credited verbatim to the standing feedback (`:59`), and the extremes (a/d) are killed on data grounds rather than ignored. One-run resolution of a one-run fail — the fastest loop-closure on record for a Discovery fail-axis. No recurring fail to flag.
- **"Search thorough-looking but not exhaustive-by-construction" — stays BROKEN (resolved).** The close-the-set attribution-site table (`:74-85`) is exhaustive-by-construction: a grep for `\.muscles`+`muscles[` across `app/`+`src/`, a 7-row table with each site's attribution-target + in-scope verdict, and an explicit "no N+1th volume-attribution site exists" verdict (`:85`). It correctly distinguished VOLUME attribution (1 site, in-scope) from EXERCISE-list grouping (2 sites, out-of-scope — you can't fractionally section a list). No missed-site class. Sustain.


## Run 2026-06-03_2217_hard-sets-per-muscle — 2026-06-03 23:30 BRT

Pipeline: feature
Final outcome: pass (shipped — code pending commit+deploy; no migration; D↔V 1 round GO, I↔R 1 PASS, I↔T 1 PASS; 0 re-loops; 1 human decision batch; feature works live: kg↔sets toggle, observed sets = working COUNT 3, not tonnage 1,500, not naive row count)
Score: 5/6

### Rubric
- Every section of `discovery.md` filled: pass — Feature prompt + settled-context (`:3-6`), Scope summary (`:8-9`), Affected files split into in-scope-change/add + read-only-verified-NOT-to-change with file:line per entry (`:11-22`), The accumulation seam (FACT, verified by reading, `:24-39`), Presenter-shape A-vs-B with a recommendation (`:41-46`), Where-users-SEE-it UI A-vs-B (`:48-55`), Dropset handling FACT-grounded (`:57-64`), Close-the-set/blast-radius (`:66-73`), Tests & conventions (`:75-83`), Relevant conventions (8 FACT-marked, `:85-92`), Constraints (Data/UI/Platform/Auth/Performance, `:94-99`), Existing precedents (`:101-105`), 9 Unknowns each (a)/(b)/(c)+confidence (`:107-125`), Out-of-scope flags (`:127-132`). None missing.
- Concrete code references cite file:line: pass — every claim cites file:line: the seam at `weekly-muscle-volume.ts:120-130` (guard `:123`, accumulation `:130`), the section at `:39-163`, `stats.ts:18-105` SELECT `:34-36` + server `.neq` `:66,:90`, `db/types.ts:48` for `SetType`, `bodyweight.ts:43-66`/`:51-54,63`, `multi-series-chart.tsx:1-177`/`:81,:84-86`, the 5 dropset-included sites (`exercise-session-row-format.ts:65`, `volume-target.ts:124,167`, `progress-page-math.ts:323`, `history/[id].tsx:206`) + the 2 working-only sites (`workout/[sessionId].tsx:201`, `auto-fill-set.ts:8`), `<ProgressWindowSelector>` `:33-67`. Dense and accurate.
- `fact` vs `assumption` clearly distinguished: pass — "FACT — verified by reading" on the seam (`:24`), "Key correctness divergence (FACT, HIGH confidence)" on the no-w>0-guard (`:39`), "LOAD-BEARING FACT: `WeeklyVolumeRow` does NOT carry `parent_set_id`" (`:62`), "(FACT, HIGH confidence)" / "(FACT — grep)" on the close-the-set findings (`:69-71`); recommendations labelled MEDIUM/LOW inside each Unknown's (c). The fact-vs-recommendation boundary is clean throughout.
- `Unknowns` section populated: pass — 9 unknowns ranked by design impact (`:107-125`), each (a)/(b)/(c)+confidence; U2 (UI) + U3 (dropset, "TOP unknown") correctly flagged for human; U1 marked resolved-by-definition (a correctness assertion). The human resolved U2+U3 (`state.md:42-44`); the Designer adopted Discovery's defaults for U4-U9 (`state.md:46`).
- Searched for precedent / sibling patterns: pass — the tonnage presenter+section+tests triad named as the 1:1 template (`:102`); `e1rm-strength.ts` as the sibling-presenter precedent for UI option B (`:103`); `<ProgressWindowSelector>` as the segmented-control precedent for UI option A (`:104`); `exercise-session-row-format.ts:35-65` as the closest sibling to the hard-sets definition (`:105`). The close-the-set grep proved no N+1th per-muscle/per-week set-count display exists (`:69`) and exactly one production consumer of the presenter (`:70`).
- Relative dates converted to absolute: pass — no relative dates in the prompt; the date-discipline surface is the ISO-week bucketing axis (deterministic `now?` injection, `weekly-muscle-volume.ts:63`), correctly handled as absolute. n/a-equivalent, handled.

- **The standout: Discovery nailed the one-line seam AND the KEY correctness divergence — and proved containment so the change touched ZERO shared infrastructure.** Two load-bearing facts defined the whole feature: (1) the accumulation seam is exactly `weekly-muscle-volume.ts:130` (`+= w*r` -> `+= 1`), with everything above and below shareable verbatim (`:37`); (2) the sets reduce must NOT inherit the tonnage `w>0 && r>0` guard, else "every bodyweight-only set for a user with no logged bodyweight would be uncounted — a systematic undercount of exactly the population the literature-aligned metric most wants to capture (calisthenics-only trainees)" (`:39`). And it proved containment by verifying `set_type` is ALREADY on `WeeklyVolumeRow` and ALREADY in the SELECT -> no `stats.ts`/kernel/migration change, the 6 shared consumers untouched (`:62,:73`). This is the close-the-set discipline applied at exactly the right depth, and the no-w>0-guard finding is the single most important correctness insight in the run — it propagated cleanly into the Designer's Invariant S and the Tester's S-2 teeth case.
- **Demonstrated APPLICATION of the prior-run carry-in lesson: Discovery explicitly reasoned about whether the "enumerate INPUT BUILDERS, not just kernel call sites" lesson applies, and correctly concluded it does NOT here (`:66-67`).** Last run's MAJ-1 root was Discovery enumerating kernel call sites but missing input-struct builders; this run Discovery wrote "It does NOT apply here as a fan-out risk — this feature does NOT thread a new field through `effectiveWeightKg`; the sets reduce ABANDONS that kernel (no new `SetBodyweightInput`/`equipmentByExerciseId` builder to enumerate)." That is the carry-in lesson read and correctly scoped — not mechanically re-applied where it doesn't fit. Credit the loop working.
- Audit trail: n/a — no peer invocations attributable to this agent. (`grep -c "peer_invocation:" transcript.md` -> 0; the Discovery routing entry `transcript.md:17-21` describes a self-contained read + live-`set_type`-verified inventory and explicitly "No peer invocations"; a phrasing scan for unlogged invocations across all run `*.md` returned 0 hits.)
- Write boundaries: pass — only `discovery.md` attributed. Baseline `80621ba9c5b2925d1c7d4b1e3915e5c4dfe3c394` resolved via `git rev-parse --verify ...^{commit}`; `{ git diff --name-only <baseline>; git ls-files --others --exclude-standard; } | sort -u` bucketed by the path map; the `design.md`/`validation.md`/`review.md`/`test-report.md` template stubs + `state.md`/`transcript.md`/`final-summary.md` are Conductor scaffolding; the other-run PNGs + the prior-run `2026-06-01_1301.../state.md` are cross-run noise (codified exclusion).

### Lessons for next run
- **The one fail (Score 5/6): U3 framed the dropset choice as count-all-rows vs expensive-fold-into-parent, MISSING the cheap working-only middle that the Conductor then had to surface and the human ultimately chose.** Discovery's U3 recommendation (`:113`) was "count each non-warmup row as 1 (dropsets included)" and framed the only alternative as fold-into-parent (which needs `parent_set_id` + a SELECT widen — `:62,:64`). It named `set_type === "working"` (dropsets EXCLUDED) as a documented existing convention at `:61` (citing `workout/[sessionId].tsx:201` + `auto-fill-set.ts:8`), so the data was in front of it — but it did NOT enumerate working-only as a candidate ANSWER to U3. The Conductor caught the gap and added it as "a cheap third option Discovery underweighted: count only `set_type==='working'` rows (excludes dropsets, no SELECT change) — dose-accurate + honors the memo's 'don't double-count drops' without parent_set_id" (`transcript.md:21`), and the human locked exactly that (`state.md:43`). **The lesson: when you have already documented BOTH counting conventions in the codebase (`:59-61`), the U3 option set must ENUMERATE both as candidate answers — not collapse to your single lean plus the expensive strawman. The working-only option was the cheapest, most dose-accurate, containment-preserving answer, and it was sitting in Discovery's own evidence; framing U3 as count-all-vs-fold under-weighted it. Surface every convention you found as a ranked option, especially when the memo's lean (fold) conflicts with your containment recommendation (count-all) — that tension is the signal a middle option exists.**
- **Sharpest improvement lever (generalizable): when your recommendation CONTRADICTS the spec memo, that contradiction is a flag to widen the option set, not to pick a side.** Discovery correctly flagged that its count-all rec contradicts the memo's fold-into-parent lean (`:64`, "this contradicts the memo on containment + data-availability grounds — surface explicitly"). Good instinct to surface the conflict. But the conflict resolution it offered was binary (its rec vs the memo's). The actual resolution was a THIRD option (working-only) that honored the memo's INTENT (don't double-count dropsets) AND Discovery's containment constraint (no SELECT change) simultaneously. **Rule: when your containment-driven recommendation conflicts with the spec's stated intent, look for the option that satisfies both before defaulting to "my rec vs theirs" — the conflict usually means the binary framing is too narrow.**

### Recurring pattern check
- **"Verify/reframe the prompt's premise against the live source" — now a 5-run sustained positive pattern, applied here as the containment proof.** e1rm catalog -> favorites file/mechanism -> progress-window UI-vocabulary -> bodyweight-leverage catalog-gating -> THIS run (verified `set_type` is already on the row + in the SELECT, proving zero shared-infra change). A reliable, load-bearing Discovery strength. Sustain.
- **"Search thorough-looking but not exhaustive-by-construction" — did NOT recur as a MISS; the close-the-set was exhaustive (no N+1th display, one consumer, 6 shared consumers enumerated). The 5/6 dock is a DIFFERENT, NEW fail-axis: option-set completeness on a human-gated unknown.** Distinct from the historical search-depth fails: Discovery's SEARCH was complete (it found both conventions), but its U3 OPTION SET was not — it under-enumerated a candidate answer that its own evidence supported. Severity: LOW-MEDIUM — it cost zero rounds (the Conductor caught it pre-Designer and the human chose the right option), but a more complete U3 would have presented the working-only option directly to the human rather than relying on the Conductor to surface it. Name it for the next human-gated unknown: enumerate every documented convention as a ranked candidate answer, not just your lean plus a strawman.

## Run 2026-06-03_1402_bodyweight-leverage-factors — 2026-06-03 15:40 BRT

Pipeline: feature
Final outcome: pass (shipped — migration 0021 applied to live DB human-approved; code pending commit+deploy; D↔V 2 rounds [v1 NO-GO→v2 GO], I↔R 1 PASS, I↔T 1 PASS; feature works live: Push-up 80×10 = 512 kg leveraged)
Score: 6/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope summary (`:6-7`), Affected files (verified, file:line per entry, split by threading shape, `:9-24`), The arithmetic seam (FACT, `:26-30`), the Close-the-set kernel inventory table (14 rows + 3-threading-shapes breakdown, `:32-57`), Where-the-factor-comes-from options a/b/c/d with tradeoffs + live-catalog inventory (`:59-77`), Identity/back-compat (`:79-83`), e1RM exclusion (`:85-89`), Where users SEE/EDIT (`:91-97`), Data-source-for-values (`:99-102`), Relevant conventions (8 FACT-marked, `:104-111`), Constraints (Data/UI/Platform/Auth/Performance, `:113-118`), Existing precedents (`:120-124`), 9 Unknowns each (a)/(b)/(c)+confidence (`:126-135`), Out-of-scope flags (`:137-142`). None missing.
- Concrete code references cite file:line: pass — every claim cites file:line (`bodyweight.ts:26-40`, `volume-target.ts:84-91,115,157,248`, `progress-page-math.ts:88,190,233,310,393`, `stats.ts:18-34`, `db/schema.ts:46-71`, `db/types.ts:162-179`, `api/exercises.ts:11,22,37,50`, the e1RM exclusion sites `progress.tsx:195-202` + `e1rm-strength.ts:14-17,146-150`). The 14-row close-the-set table cites file:line per row.
- `fact` vs `assumption` clearly distinguished: pass — "FACT — verified by reading" on the seam (`:26`), "FACT — confirmed" on e1RM exclusion (`:85`), the **LOAD-BEARING FINDING (FACT)** that "Pull Up" is `equipment=null` not bodyweight (`:69`); recommendations labelled HIGH/MEDIUM/LOW inside each Unknown's (c) (U1 column MEDIUM, U4-seam HIGH, U5 coalesce HIGH).
- `Unknowns` section populated: pass — 9 unknowns ranked by design impact (`:126-135`), each (a)/(b)/(c)+confidence; U1 (storage), U2 (UI scope), U3 (Hanging-Leg-Raise value) correctly flagged for human decision (resolved in `state.md:43-49`).
- Searched for precedent / sibling patterns: pass — Phase-0 named as the exact plumbing template (`:121`); `0014` as the by-name idempotent canonical-backfill precedent (`:122`); `0009`/`0015` `max_volume_window` as the numeric-tuning-column precedent (`:123`); `select("*")`-free-column precedent (`:124`). The live-catalog query (anon client, project `ykrbgpctbfvndxjnpzrg`, `deleted_at IS NULL`) verified the 95-exercise distribution + the exactly-4-bodyweight set (`:60`).
- Relative dates converted to absolute: pass — no relative dates in the prompt; the date-discipline surface (the migration's `WHERE` clauses, the retroactive-shift framing) is correctly absolute. n/a-equivalent, handled.

- **The standout: Discovery verified the LIVE catalog and surfaced the scope-reshaping fact that "Pull Up" is `equipment=null`, not "bodyweight" (`:69`).** The prompt's headline ("pull-up ≈ 1.0 BW") does not apply to the catalog as-gated — Pull Up, Chest Dip, and Hanging Knee Raise all count ZERO bodyweight today. Discovery refused to silently re-classify them (`:141`, out-of-scope flag: "do NOT silently re-classify unless the human asks"), and flagged the reclassification as a human/product decision. The human then locked the reclassify (U3, `state.md:46`). This is the same "verify the prompt's premise against the live source" discipline the prior 3 runs praised, applied to a catalog-gating mismatch — and it was the move that defined the whole feature's true scope (7 rows, not 4; a retroactive shift to call out).
- Audit trail: n/a — no peer invocations attributable to this agent. (`grep -c "peer_invocation:" transcript.md` → 0; the Discovery routing entry `transcript.md:17-21` describes a self-contained live-DB-verified inventory and explicitly "No peer invocations"; a phrasing scan for unlogged invocations across all run `*.md` returned 0 hits.)
- Write boundaries: pass — only `discovery.md` attributed. Baseline `3c85c23e0c486fe601e5b21a2840db2950786806` resolved via `git rev-parse --verify`; `{ git diff --name-only <baseline>; git ls-files --others --exclude-standard; } | sort -u` bucketed by the path map; the `design.md`/`validation.md`/`review.md`/`test-report.md` template stubs + `state.md`/`transcript.md`/`final-summary.md` are Conductor scaffolding; the other-run PNGs + the prior-run `2026-06-01_1301.../state.md` are cross-run noise (codified exclusion).

### Lessons for next run
- **The "verify the prompt's premise against the LIVE catalog, not the seed" check earned its keep again — sustain it as the standing first move for any catalog-touching feature.** This run it caught that 3 prompt-named bodyweight movements are `equipment=null` (count zero today), reshaping scope from a 4-row factor backfill to a 7-row backfill-plus-reclassify with a retroactive-shift call-out. Same discipline as the e1rm-strength catalog claim and the favorites wrong-file/mechanism catch. Discovery correctly escalated the reclassify as a human decision rather than acting on it. Sustain: when a prompt names exercises/rows, query the live `user_id IS NULL` catalog and reconcile the prompt's claim against it BEFORE design.
- **Sharpest improvement lever: the close-the-set inventory counted KERNEL CALL SITES (14) but under-counted INPUT BUILDERS — this is the MAJ-1 root and a generalizable lesson.** Discovery's exhaustive-by-construction table (`:32-57`) enumerated all 14 `effectiveWeightKg` call sites and the three threading shapes correctly, AND at `:54` flagged the MAP-fed build site as "not located in this pass" (handing it to the Designer). But the inventory's mental model was "where the kernel is CALLED," not "where the `SetBodyweightInput`/`equipmentByExerciseId` is CONSTRUCTED." The Designer copied that model and asserted only 2 builders; the Validator's MAJ-1 proved there are 6 (the live workout header, verdict+PR detection, and history-detail builders feed `sumLiveVolume`, a kernel already IN Discovery's 14-row table, but via input-construction sites Discovery never enumerated). **The lesson for the next "thread a new field through a shared kernel" feature: the close-the-set must enumerate the INPUT BUILDERS (every site that constructs the kernel's input struct), not just the kernel CALL SITES — a builder that populates field A but not new-field B is the desync class, and it is invisible to a call-site grep.** Discovery had the right exhaustiveness instinct; it applied it one layer too shallow. A `grep -rn "SetBodyweightInput\|equipmentByExerciseId" app/ src/` at Discovery would have surfaced all 6 builders and pre-empted the no-go round.

### Recurring pattern check
- **"Search thorough-looking but not exhaustive-by-construction" — REGRESSED in a new sub-variant after a 3-run resolution streak.** The arc: bottom-tab (framework grep miss) → bodyweight-volume (close-the-set kernel miss) → e1rm/favorites/progress-window (RESOLVED ×3) → THIS run (the close-the-set ran for CALL SITES but missed INPUT BUILDERS, MAJ-1 root). This is the SAME structural fail as the original Phase-0 bodyweight-volume miss (Discovery enumerated kernel sites confidently but missed a construction layer), recurring on the very same kernel a phase later. Severity: MEDIUM — it cost the D↔V no-go round, but Discovery DID flag the MAP build site as unlocated (`:54`), so it was a partial-miss not a blind one. The resolved variant (close-the-set for end-CONSUMERS like charts) held; the call-site-vs-builder distinction is a NEW layer the close-the-set discipline must now cover. Name it for the next kernel-threading feature.
- **"Verify/reframe the prompt's premise against the live source" — now a 4-run sustained positive pattern** (e1rm catalog → favorites file/mechanism → progress-window UI-vocabulary → THIS run catalog-gating). A reliable Discovery strength; the cheapest place to catch a wrong premise. Sustain.

## Run 2026-06-03_1124_progress-chart-window — 2026-06-03 12:30 BRT

Pipeline: feature
Final outcome: pass (shipped — working tree, uncommitted; D↔V 1 round GO, I↔R 1 round PASS, I↔T 1 round PASS — every gate green round 1)
Score: 6/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope summary (`:6-9`), Affected files split into in-scope-presenters / window-infrastructure / prior-art-selector / tests (`:11-36`), Relevant conventions (6 FACT-marked, `:38-45`), Constraints (Data/UI/Platform/Auth/Performance, `:47-52`), Existing precedents (`:54-59`), the Close-the-set chart inventory table (`:61-73`), 9 Unknowns each with (a)/(b)/(c) + confidence (`:75-121`), Out-of-scope flags (`:123-128`). None missing.
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `weekly-muscle-volume.ts:50-128,59-69`, `e1rm-strength.ts:80-251,214-245`, `window-utils.ts:41-49`, `progress-page-math.ts:74-95,82-84`, `use-progress-page.ts:64-88`, `profile.tsx:147-198,27-34`, `db/types.ts:68,74-76`, `multi-series-chart.tsx:99-102`). The close-the-set table cites file:line per row.
- `fact` vs `assumption` clearly distinguished: pass — "FACT, verified by reading code" markers on the dual-anchor rule (`:40`), the `0`→`undefined` semantics (`:41`), the local-non-persisted-state precedent (`:42`), the no-calendar-dep platform fact (`:50`); recommendations explicitly labelled with confidence inside each Unknown's (c) (HIGH/MEDIUM/LOW), e.g. U4 ephemeral HIGH, U1 discrete-selector MEDIUM, U5 axis MEDIUM.
- `Unknowns` section populated: pass — 9 unknowns ranked most→least design-impacting (`:75-121`), each (a) what / (b) why it matters / (c) recommended default + confidence; U1/U2/U9 correctly flagged for human decision (resolved in `state.md:46-51`); U5 (axis-derivation + LOCF lead-in shift on shrink) is the highest-leverage technical unknown and pre-named the exact unit cases the design/tests later pinned.
- Searched for precedent / sibling patterns: pass — the EXACT wiring template named (`useLifetimeBestWeek` `use-progress-page.ts:64-88` → `useMaxVolumeWindowWeeks()`→`computeWindowStart`→kernel-inside-`useMemo`), the kernel param to copy (`bucketLifetimeWeeklyVolumes`'s `windowStartMs?` filter `progress-page-math.ts:82-84`), the segmented-control idiom (`profile.tsx:147-198`), the bottom-sheet picker precedent (`week-selector.tsx:105-258`) — all with file:line. The close-the-set grep (`:73`) proved no N+1th chart exists.
- Relative dates converted to absolute: pass — no relative dates in the prompt; the date-discipline surface here is the windowing math (device-local ISO weeks via `computeWindowStart`, inclusive `>=` lower bound) which Discovery pinned correctly at `:24,41`.

- **The standout: Discovery correctly re-read "date picker" against the platform and reframed the whole feature before any design.** U1 (`:78-81`) is the load-bearing move: the prompt says "add a date picker" but Discovery verified `package.json` has NO calendar/date-picker dependency (only `date-fns`), traced that a true calendar picker would change the math seam from "weeks→`windowStartMs`" to "arbitrary start/end → bucket boundaries" AND break dual-anchor consistency with the windowed "Max" numbers, and recommended reading "date picker" as "discrete weeks-window selector" reusing `MAX_VOLUME_WINDOW_OPTIONS`. The human confirmed exactly this (`state.md:48`). A literal reading would have triggered a new cross-platform dependency and a materially larger feature. This is the "is the prompt's named mechanism the right one?" check — the same highest-leverage Discovery move the prior 2 entries praised, applied to a UI-vocabulary mismatch this time.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks in `transcript.md` [`grep -c` → 0]; the Discovery routing entry `transcript.md:17-21` describes a self-contained inventory + explicitly "No peer invocations"; a phrasing scan for unlogged invocations across all run `*.md` returned 0 hits.)
- Write boundaries: pass — only `discovery.md` attributed. Baseline `25db98beb5bb219b3405b7d2fa75a13849665d92` resolved via `git rev-parse --verify`; diff vs baseline + `git ls-files --others --exclude-standard` bucketed by the path map; the `design.md`/`validation.md`/`review.md`/`test-report.md` template stubs + `state.md`/`transcript.md`/`final-summary.md` are Conductor scaffolding; the 5 cache-buster files + other-run PNGs + the prior-run `2026-06-01_1301.../state.md` are pre-existing/cross-run noise (verified: the 5 cache-buster files carry ZERO of this run's feature symbols).

### Lessons for next run
- **The "is the prompt's named control/mechanism actually the right one?" check earned its keep again — sustain it as the standing first move.** This run it caught a UI-vocabulary mismatch ("date picker" → discrete weeks selector) that, taken literally, would have added a cross-platform dependency and a much larger math seam. Last run it caught a wrong file + wrong DB mechanism; the run before, a wrong catalog claim. The discipline transfers across surface types (file/mechanism → UI vocabulary). Sustain: when the prompt names a control, library, or mechanism, verify against `package.json`/source that it is the right primitive BEFORE design, and recommend the cheapest correct reframe.
- **The close-the-set chart inventory was exhaustive-by-construction and held end-to-end — sustain the table + "no N+1th" grep.** Discovery built the 6-row chart table (`:61-73`) with each chart's current windowing status + in-scope verdict + the closing grep proving no other progress chart exists. The Designer copied the 2-in-scope verdict, the Validator confirmed (claims 9a/9b), the Reviewer re-verified scope — no missed chart class. The discipline is now a 3-consecutive-run sustained strength on "apply X everywhere" features.
- **U5 (axis-derivation + LOCF lead-in on shrink) was the highest-leverage technical unknown and it pre-specified the design's hardest correctness case.** Discovery flagged at `:98-101` that filtering rows before `earliestMs` auto-shrinks the axis AND that the e1RM LOCF lead-in recomputes over the windowed set — and named the exact unit cases (axis left-edge, all-pre-window exercise drops out, LOCF over windowed first week). The design's W-1/W-2/W-4/W-5 cases map 1:1 to these. Sustain: when windowing/filtering changes a DERIVED axis or a carry-forward, name the recompute behavior AND the unit cases up front.

### Recurring pattern check
- **"Search thorough-looking but not exhaustive-by-construction" — the fail pattern stays BROKEN (RESOLVED 3rd consecutive run).** The arc: bottom-tab (missed framework grep) → bodyweight-volume (missed close-the-set grep) → e1rm-strength-chart (RESOLVED) → favorite-exercises (RESOLVED) → THIS run (RESOLVED again, close-the-set chart inventory + "no N+1th progress chart" grep at `:73`). The discipline has now held across three consecutive runs and transferred across feature types. No recurring fail to flag.
- **"Correct/reframe the prompt's wrong-or-ambiguous premise" — now a 3-run sustained positive pattern.** e1rm (catalog claim) → favorites (wrong file + wrong mechanism) → THIS run (UI-vocabulary "date picker" reframed to discrete weeks selector). A reliable Discovery strength; worth continuing to surface as the cheapest place to catch a wrong premise.

## Run 2026-06-01_1301_favorite-exercises-e1rm — 2026-06-01 15:10 BRT

Pipeline: feature
Final outcome: pass (shipped; D↔V 2 rounds [v1 NO-GO], I↔R 1 round PASS, I↔T 2 rounds — round 1 FAIL test-only; migration 0020 live)
Score: 6/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope summary, Affected files (new + modified, file:line per entry, `:11-28`), §1 the `exercise_notes` precedent studied fully (`:30-52`), §2 the detail-page prompt CORRECTION (`:54-68`), §3 the e1RM close-the-set integration map (12-row table, `:70-102`), §4 favorites→e1RM data join (`:104-111`), §5 migration+RLS (`:113-129`), §6 hooks/cache (`:131-137`), §7 tests (`:139-152`), Relevant conventions, Constraints, Existing precedents, 9 Unknowns each (a)/(b)/(c), Out-of-scope flags — all populated, none missing.
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `0010_exercise_notes.sql:27-67`, `exercise-notes.ts:33-51,52-102`, `use-exercise-note.ts:8-37`, `e1rm-strength.ts:106-148`, `e1rm-strength-section.tsx:29-41,60-70`, `progress.tsx:88-122,224-237`, `rls.test.ts:133-192,308-398`, `canonical-exercise.ts:34-60`, `0011_canonical_exercises.sql:28-30`). The §3 integration table cites file:line per row + per-impact.
- `fact` vs `assumption` clearly distinguished: pass — "FACT (HIGH confidence)" / "verified by reading it" / "verified with `dangerouslyDisableSandbox`" markers throughout; the §1 42P10 analysis is explicitly "FACT-based answer: NO" (`:43`); recommendations labelled "Recommended default" inside each Unknown's (c). The §3 close-the-set verdict is "HIGH confidence … no N+1th site" (`:78`).
- `Unknowns` section populated: pass — 9 unknowns each with (a) what / (b) why / (c) recommended default (`:178-206`); the Designer adopted the defaults wholesale (presenter seam, single list key, header-right star outside `canEdit`, plain INSERT/DELETE, CASCADE, 12-line ceiling). Unknown #2 (palette wrap) was correctly identified as "the headline risk."
- Searched for precedent / sibling patterns: pass — the entire `exercise_notes` triad (migration+API+hook+slot) studied fully as the table template AND its 3 simplifications enumerated (`:52`); the Phase-2a e1RM triad as the integration target; the Pencil header-right pattern; the `measurements.ts:121-159` INSERT+SQLSTATE-discriminator style; the multi-arm RLS script; `pickCanonicalExercise` live-catalog helper — all with file:line.
- Relative dates converted to absolute: pass — no relative dates in the prompt; the date-discipline surface here is the live "today 2026-06-01" used to scope the migration number (0020, verified latest is 0019, `:115`).

- **Two load-bearing prompt corrections, both verified and both upheld by the Conductor.** (1) The detail page is `progress.tsx` (the `useAllExercise`+Pencil+`ExerciseNoteSlot` screen), NOT `index.tsx` (the edit form) — the prompt conflated the two, and Discovery read BOTH files to prove `index.tsx` = `EditExerciseScreen` (react-hook-form, `useExercise`, no header-right slot) vs `progress.tsx` = the actual detail page (`discovery.md:54-68`). A wrong mount point would have wasted a full design+implement cycle. (2) The notes 42P10 partial-unique-upsert trap does NOT apply to favorites — a favorite is presence/absence against a NON-partial composite PK, so plain INSERT (swallow 23505) + DELETE is correct, no read-then-write loop (`:40-43`). The Conductor independently re-verified both (`transcript.md:21`). This "is the prompt itself describing the right file / the right mechanism?" check is the highest-leverage Discovery move this run.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks in `transcript.md` [`grep -c` → 0]; the Discovery routing entry `transcript.md:17-21` describes a self-contained inventory with no peer reference; a phrasing scan for unlogged invocations across all run `*.md` returned 0 hits.)
- Write boundaries: pass — only `discovery.md` attributed. Baseline `5a0b86e6993f39f0721ab6efee2d36bad0c15537` resolved via `git rev-parse --verify`; diff vs baseline + `git ls-files --others --exclude-standard` bucketed by the path map; the `design.md`/`validation.md`/`review.md`/`test-report.md` template stubs + `state.md`/`transcript.md`/`final-summary.md` are Conductor scaffolding; the other-run screenshot PNGs are pre-existing noise.

### Lessons for next run
- **The "read the prompt's named file/mechanism against source and correct it BEFORE design" move is now the standout Discovery value-add — sustain it explicitly.** This run it caught two prompt errors (wrong file + wrong DB mechanism) that each would have cost a full cycle. The discipline: when the prompt names a specific file, mechanism, or precedent, READ it and verify the claim holds (the right screen has the right hook/slot; the cited trap actually applies). Discovery is the cheapest place to catch a wrong premise. Sustain.
- **The close-the-set discipline transferred cleanly from the Phase-2a run.** Last run proved `epley1RM` is centralized in 2 sites; this run proved the e1RM PLOTTED-SET decision is centralized in 2 files via 4 orthogonal greps (symbol / cap+palette constants / model-type consumers / importers) + a 12-row integration table + an explicit "no N+1th site" verdict (`:70-102`). The Designer copied the verdict, the Validator confirmed the closed set, the Reviewer re-verified byte-for-byte — and the "missed site" class did not recur. Sustain the grep-A/B/C/D + hit→row-table + "no N+1th site" shape for any integration/"apply X everywhere" feature.
- **Naming the ONE downstream break (the palette `% length` wrap >8) up front made it the design's headline decision.** Discovery flagged that `colorForRank(i)=PALETTE[i % 8]` with a comment assuming N≤8 is the only hard assumption a union >8 breaks (`:89,98`). The Designer turned that into the 12-color-palette + 12-line-ceiling decision, the Validator confirmed the first-8 byte-for-byte, the Reviewer verified it. **Sustain: when integrating into an existing presenter, isolate the ONE assumption the new path violates and name it as the headline risk — it focuses the whole pipeline.**

### Recurring pattern check
- **"Search thorough-looking but not exhaustive-by-construction" — the 2-run fail pattern stays BROKEN (RESOLVED 2nd consecutive run).** The prior arc was bottom-tab (missed framework-source grep) → bodyweight-volume (missed close-the-set grep) → e1rm-strength-chart (RESOLVED, close-the-set ran at Discovery) → THIS run (RESOLVED again, close-the-set ran on the e1RM selection sites + the integration table). The discipline has now held across two consecutive runs and transferred across feature types (kernel inventory → integration surface). The feedback loop closed this fail class; watch only that it persists on the next "find all sites" feature. No recurring fail to flag this run.
- **A NEW positive pattern is emerging: "correct the prompt's wrong premise."** Last run Discovery corrected the prompt's catalog claim (with a residual about the seed source); this run it corrected TWO premises (wrong file + wrong mechanism) cleanly. 2-run positive arc. Worth watching as a sustained Discovery strength.


## Run 2026-05-30_2006_e1rm-strength-chart — 2026-05-30 21:30 BRT

Pipeline: feature
Final outcome: pass (shipped; D↔V 1 round GO, I↔R 1 round PASS, I↔T 2 rounds — round 1 FAIL test-only)
Score: 6/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope summary, Affected files (verified, file:line per entry), §1 kernel inventory (close-the-set), §2 `<MultiSeriesChart>` reusability, §3 most-performed derivation, §3b per-session vs per-week shape, §4 mount point, §5 test conventions, §6 migration check, Relevant conventions, Constraints, Existing precedents, 9 Unknowns each (a)/(b)/(c), Out-of-scope — all populated, none missing.
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `formulas.ts:1-5`, `progress.tsx:155-159,169-173`, `multi-series-chart.tsx:134-160,81`, `weekly-muscle-volume.ts:112`, `weekly-muscle-volume-section.tsx:60,67-71`, `dates.ts:40-126`, `stats.ts:18-30`, the seed `0001_rls_and_seed.sql:60-91`). The kernel-inventory hit→row table (`:48-52`) cites file:line per row.
- `fact` vs `assumption` clearly distinguished: pass — "verified by reading the current file" / "Verified by grep" / "Verified against source" markers throughout; recommendations explicitly labelled "Recommended" inside each Unknown's (c); HIGH-confidence verdicts stated (e.g. `:97` reuse, `:188` no-migration).
- `Unknowns` section populated: pass — 9 unknowns each with (a) what / (b) why / (c) recommended default (`:216-232`); Unknown #6 proactively pre-empted over-refactoring `progress.tsx` (the carry-in "wire all call sites" lesson, correctly scoped to "only when you CHANGE a kernel"). The Designer adopted the defaults wholesale.
- Searched for precedent / sibling patterns: pass — the entire Phase-1 muscle chart triad (presenter + section + test pair) named as the 1:1 template; the per-exercise e1RM reduce as the semantic reference; `TOP_N=5` in `<ProgressHero>` as the cap precedent; the dangling-skip join idiom; `measurements-chart.ts` null-skip — all enumerated with file:line.
- Relative dates converted to absolute: pass — no relative dates in the prompt; the date discipline here is timezone-of-bucketing, handled correctly (device-local ISO weeks per `dates.ts:12-19`, distinguished from the e2e's UTC seeding).

- **Kernel-inventory exhaustiveness — the load-bearing carry-in lesson APPLIED and PAID OFF.** Last run's fail (search thorough-looking but not exhaustive-by-construction) was the explicit carry-in. This run Discovery ran the close-the-set discipline BY CONSTRUCTION: three orthogonal greps (A = `epley1RM` symbol; B = inline Epley arithmetic `* (1 + reps/30)` and variants; C = alternate 1RM-formula names brzycki/lombardi/wathan/oconner/mayhew), a hit→inventory-row table (`:48-52`), and an explicit "no N+1th site exists" verdict (`:53-54`). Grep B's false positives (NativeWind `/30` opacity tokens, a schema comment) were each named and excluded — the closed-set proof the prior run's Validator had to produce in round 2 was produced HERE, at Discovery, first. The result was correct and held: e1RM is centralized into `epley1RM` from day one (exactly 2 sites — 1 helper + 1 caller), so the "missed inline copy" class that drove last run's no-go round did NOT recur. The Conductor independently re-verified the 2-site claim by grep (`transcript.md:21`) and agreed. **This is the cleanest demonstration so far of a carry-in lesson being read, applied, and verified to close the prior failure class.**
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks in `transcript.md`; the Discovery routing entry `transcript.md:17-21` describes a self-contained inventory with no peer reference.)
- Write boundaries: pass — only `discovery.md` attributed (baseline `3c00d8e` resolved via `git rev-parse --verify`; diff bucketed by the path map; the `design.md`/`validation.md`/`review.md`/`test-report.md` template stubs are Conductor scaffolding).

### Lessons for next run
- **The close-the-set grep + hit→row table + "no N+1th site" verdict is now the proven exhaustiveness pattern — make it the standing first move for any "apply/find X everywhere" feature.** This run it took ~6 lines (grep A/B/C + table + verdict) and pre-empted an entire D↔V no-go round. The crucial sub-move was running greps for the NEGATIVE space too (alternate formula names brzycki/lombardi/… — zero hits = "Epley is the only model"), which is what turns "I found 2 sites" into "there are exactly 2 sites." Sustain.
- **The "is the prompt itself wrong?" check earned its keep — with one residual to tighten.** The Conductor's prompt asserted the catalog "has Chin-up/Dip/Push-up but NOT Pull-up"; Discovery verified against `0001_rls_and_seed.sql:70` and CORRECTED it (Pull-up IS seeded; the load-bearing fact is that abundant WEIGHTED exercises exist for a visible line, and bodyweight ones are the negative case). NOTE the residual: Discovery's correction was anchored on the `0001` per-user TRIGGER seed, but `pickCanonicalExercise` queries the `user_id IS NULL` CANONICAL catalog (migrations `0011`/`0014`) — a distinction the Implementer had to surface as Deviation #1 and the Tester confirmed live (Pull-up MISSING from the live catalog; "Pull Up" no-hyphen in `0014`). Discovery correctly named the WEIGHTED-vs-bodyweight axis (which is what mattered for the design), but the seed-source nuance (`0001` trigger seed ≠ live canonical catalog) is the one thing a future Discovery should pin when citing a migration as "the catalog `pickCanonicalExercise` queries." Minor — it did not cost a round (the Implementer caught it), but Discovery is the right place to name the canonical-catalog source-of-truth so the Designer's e2e plan cites the right migration.
- **The reuse-vs-extend analysis (§2) was decision-grade — sustain it.** Discovery read `<MultiSeriesChart>` in full, extracted the props contract + every behavior (y-min pinned to 0, no gap support, single-dot first week), and concluded "reusable AS-IS, no extension" with HIGH confidence — then flagged the ONE caller-side decision (dynamic palette-by-index for a top-N set vs the muscle chart's fixed keyed record). That single flag is exactly what the Designer turned into Decision #4. Sustain the "read the reuse target in full, extract its contract, name the one decision the caller must make" discipline.

### Recurring pattern check
- **"Search thorough-looking but not exhaustive-by-construction" — the 2-run fail pattern is now BROKEN (RESOLVED this run).** Prior two runs: bottom-tab (missed grepping `node_modules/expo-router` for the framework's auto-pop → Validator MAJ-1) and bodyweight-volume (missed the close-the-set grep → Validator MAJ-1 + MAJ-3-NEW). This run, the carry-in lesson was applied verbatim: the close-the-set grep ran AT Discovery, the closed-set proof was produced first, and the Validator/Conductor independently confirmed it rather than discovering a missed site. The fix transferred across domains (framework-source last time, in-project kernel this time). **This is the second resolution arc in the project (after the Implementer's probe-vs-spec arc) where a named multi-run fail was closed because the carry-in lesson was read and applied — the feedback loop is working.** Watch that the discipline persists on the next "find all sites" feature and does not regress once the lesson scrolls down the file.

## Run 2026-05-30_0126_bodyweight-volume-per-muscle — 2026-05-30 BRT

Pipeline: feature
Final outcome: pass (shipped; 2 D↔V rounds, 1 I↔R, 2 I↔T)
Score: 5/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope summary, Affected files (kernel + every call site), Conventions, Constraints, the 14-row surface-by-surface data-availability table (`discovery.md:85-100`), measurement-query story, exercise→muscle join story, `<ProgressChart>` gap analysis, Migrations (NO migration — verified), Precedents, 9 Unknowns each (a)/(b)/(c), Out-of-scope — all populated.
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `volume-target.ts:74-85,100-114,135-201`, `stats.ts:18-29`, `measurements.ts:100-108`, `measurements-chart.ts:29-31`, `use-progress-page.ts:270-283`, `schema.ts:197-200`, FK restrict `0000_schema.sql:88`). The surface table cites hook + pipeline + kernel + equipment/measurements availability per row.
- `fact` vs `assumption` clearly distinguished: pass — "verified fact" / "verified by grep" / "Verified" markers throughout (`:59, 64-66, 109-110, 132-135`); recommendations explicitly labelled "Recommended default" inside each Unknown.
- `Unknowns` section populated: pass — 9 unknowns each with (a) what / (b) why / (c) recommended default (`:146-164`); Designer adopted the defaults wholesale (plumbing seam, fallback prior→later→null, no window pref, zero-fill, new component, "Other" line). Unknown #9 specifically pre-warned the Designer about the easy-to-miss `computeStripModel` + per-exercise inline reduce.
- Searched for precedent / sibling patterns: pass — windowed-kernel optional-param plumbing (`window-utils.ts`), the `useExercisesThisWeek` client-side join template (`use-progress-page.ts:270-283`), null-weight skip (`measurements-chart.ts:29-31`), ISO-week zero-fill (`computeStripModel`), the removed chart's selector idiom, and the cross-surface volume e2e regression suites all enumerated.
- Relative dates converted to absolute: pass — no relative dates in the prompt; the load-bearing date discipline here is timezone-of-compare, and it is correct: the "as-of-session" lookup pins a UTC-instant compare (`:110`) while ISO-week bucketing stays device-local — Discovery distinguished the two explicitly.

- **Kernel-inventory exhaustiveness: fail (the load-bearing Discovery output for this feature).** Discovery found 2 kernel sites the prompt missed (`computeStripModel` `weekly-volume-strip-math.ts:72-76`, the per-exercise inline reduce `progress.tsx:138-147` — flagged at `:35-36`) — genuinely strong. But it MISSED 2 more that surfaced downstream and drove the D↔V loop: (1) the week-drill-down **headline** `weekVolumeKg` (`history/week/[isoWeek].tsx:82-93`) — an entirely separate inline `w*r` kernel that Discovery's surface table row #2 (`:88`) folded into "uses `groupSessionVolumes`" without noticing the headline is its own reduce → became Validator **MAJ-1** (no-go, `validation-v1.md:31`); (2) the History-list `groupSessionVolumes` **caller** (`history/index.tsx:22-25`) — Discovery's surface table row #1 (`:87`) correctly named `groupSessionVolumes` as the kernel, but its inventory (`:33`) listed `groupSessionVolumes` as ONE site without flagging it has **two** un-wired callers → the second caller became Validator **MAJ-3-NEW** in round 2 (`validation-v2.md:62`). Net: the "13-site inventory" was actually 14 active sites + a two-caller fan-out; the gap cost a no-go round + a carry-in must-fix. Scored against this criterion (a surface-search that looked thorough but was not exhaustive-by-construction).
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks in `transcript.md`; the Discovery routing entry `transcript.md:21` explicitly declares "No peer invocations to log".)
- Write boundaries: pass — only `discovery.md` attributed (baseline `5a2382b` resolved via `git rev-parse --verify`; diff bucketed by the path map).

### Lessons for next run
- **When the feature is "change one predicate everywhere," the kernel inventory must be exhaustive-BY-CONSTRUCTION, not enumeration-from-memory.** The right method is the one the Designer AND Validator both ran in round 2 and confirmed exhaustive: a single `grep -rn '\* r' / 'parseFloat(.*weight' / 'weight ? parseFloat'` across `app/` + `src/`, then map EVERY hit to an inventory row, and explicitly account for the non-volume hits (user-bodyweight, set auto-fill) so the reader can see the set is closed. Discovery built the conceptual surface table but did NOT run the close-the-set grep — so `weekVolumeKg` (a headline reduce that doesn't read like a "kernel") and the second `groupSessionVolumes` caller slipped through. **Add to the Discovery contract: for any "apply X to every site" feature, attach the exhaustiveness grep + a hit→inventory-row table + an explicit "no N+1th site exists" line.** The Validator had to produce exactly this proof in `validation-v2.md:28-53`; Discovery should produce it first and save a round.
- **A surface-table row is NOT the same as a kernel-inventory row.** A single screen can host MULTIPLE independent kernels (the week drill-down has BOTH `groupSessionVolumes` per-session rows AND a separate `weekVolumeKg` headline), and a single kernel function can have MULTIPLE call sites that each need wiring (`groupSessionVolumes` has two). **When a surface "uses kernel K," answer (a) is that the only kernel on this surface? and (b) how many distinct call sites of K exist?** Both questions, answered with grep, would have caught MAJ-1 and MAJ-3 at Discovery time.
- **Sustain the (a)/(b)/(c) Unknown shape and the proactive easy-to-miss flag.** Unknown #9 (`:164`) explicitly told the Designer "`computeStripModel` + per-exercise inline reduce are independent copies the prompt did NOT enumerate — flag as easy-to-miss." That call-out is exactly why the Designer caught those 2. Extend the SAME instinct to the close-the-set grep so NO inline copy is left to memory.
- **The 14-row surface-by-surface data-availability table was high-leverage — sustain it.** It gave the Designer the equipment/measurements gap per surface and drove the "widen `stats.ts` SELECT vs client-join" seam decision. The structure was right; it just needed a kernel-count-per-surface and call-site-count-per-kernel column to be exhaustive.

### Recurring pattern check
- **"Search thorough-looking but not exhaustive-by-construction" is now a 2-run fail variant (severity MEDIUM, rising).** Prior run (bottom-tab-home-link): Discovery grepped the PROJECT for `tabPress`/`popToTop` (greenfield) but missed grepping `node_modules/expo-router` for the framework's own auto-pop → Validator MAJ-1. THIS run: Discovery grepped the project and found most kernels but did NOT run the close-the-set exhaustiveness grep, missing 2 sites the Validator caught as MAJ-1 + MAJ-3-NEW. **Different domain (framework source last time, in-project kernel fan-out this time) but the SAME root: the search was confident-enumeration, not closed-set-proof, and the Validator's grep is what closed the set both times.** The fix is the same shape: when correctness depends on "find ALL sites of X," Discovery owes a closed-set proof (grep + hit→row table + "no further site exists"), not an enumeration. Treat as a priority pre-flight on the next "change everywhere" feature.

## Run 2026-05-26_0307_bottom-tab-home-link — 2026-05-26 04:42 BRT

Pipeline: feature
Final outcome: pass (with documented follow-up)
Score: 4/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope, Affected files (Tabs root, per-tab stacks, hidden tabs, callsites), Conventions, Constraints, Precedents, Unknowns (8), Reusable patterns, Out-of-scope all populated (`discovery.md:8-185`).
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `app/(app)/_layout.tsx:1-74`, `app/(app)/exercises/[id]/progress.tsx:64-86`, `tests/e2e/auth.spec.ts:301-303`, `node_modules/expo-router/build/layouts/TabsClient.d.ts:14-22, 63-71`).
- `fact` vs `assumption` clearly distinguished: pass — "Verified fact"/"verified by grep"/"Verified" markers throughout (e.g. `discovery.md:19, 29, 42, 52, 75`); recommendations explicitly labelled "My recommendation" with confidence (e.g. `:97, :104, :117, :122`).
- `Unknowns` section populated: pass — 8 unknowns each with (a)/(b)/(c) shape (`discovery.md:92-138`); Designer adopted all 8 defaults in v1.
- Searched for precedent / sibling patterns: **fail** — Discovery `:90` claims "No prior 'tap to reset stack' pattern in the codebase — verified by grep returning zero matches across `app/` and `src/`. This feature is greenfield." This is correct as a project-code grep, but Discovery only grepped the **project** code and missed that **Expo Router's forked native-stack at `node_modules/expo-router/build/fork/native-stack/createNativeStackNavigator.js:54-67` ALREADY auto-pops on focused `tabPress`**. Validator caught this as MAJ-1 (`validation-v1.md:18, 48-51`) — the framework already implements the desired behavior, and Designer's v1 unconditional `e.preventDefault()` would have DISABLED the working built-in feature. For a feature explicitly described as "screen state on `<Tabs>` re-press", a search **inside `node_modules/expo-router`** for `tabPress` / `popToTop` was load-bearing and was skipped. Also missed the `PartialState` rehydration nuance (`@react-navigation/routers/src/types.tsx:5-10`) that ultimately produced the deep-link follow-up.
- Relative dates converted to absolute: pass — no relative dates in the prompt.
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — only `discovery.md` attributed.

### Lessons for next run
- **For framework-touching features, grep the framework's source too.** "Greenfield in `app/` + `src/`" is necessary but not sufficient when the feature is "intercept a framework event". Discovery should have run `grep -rn "tabPress\|popToTop" node_modules/expo-router/build` and `node_modules/@react-navigation/*/src` — that one search would have surfaced the fork's auto-pop listener at `createNativeStackNavigator.js:54-67` and re-framed the whole problem (the feature is partially built-in, the user's "it doesn't work" is platform-specific, not greenfield). Make this part of the contract: "when the feature is an event-handler on a framework primitive, grep the framework for the same event before declaring greenfield."
- **Surface state-rehydration shapes when the feature touches navigator state.** The `PartialState<NavigationState>` distinction (where `type`/`key`/`index` may be absent) is documented at `@react-navigation/routers/src/types.tsx:5-10`. Discovery enumerated the listener types but did not call out that `route.state` can be `NavigationState | PartialState<NavigationState>` and the two have different shapes. That gap propagated through Designer → Implementer → Tester before being surfaced as the deep-link follow-up. For any feature that reads `navigation.getState()` or `route.state`, Discovery should include a section on the rehydration shapes (click-through state vs URL-deep-link state vs persisted-state restore).
- **The "(a) what / (b) why / (c) recommended default" Unknown shape continues to pay off.** Designer adopted all 8 defaults wholesale. Sustain.
- **The web-specific `backBehavior="history"` annotation** (`discovery.md:75-78`) was extraordinarily useful — Designer and Validator both used it to scope risks. Sustain the discipline of surfacing load-bearing project-specific invariants up front.

### Recurring pattern check
- **Framework-internals literacy** is the recurring shape worth tracking: in routine-strong-builder (prior run), Discovery surfaced the relevant precedents at `:170-178` with a 7-pattern appendix; in canonical-exercises before that, the precedents block also delivered. This run's `(none found)` framing on the precedents line was technically correct but framework-incomplete. Pattern emerging: when the feature lives on a framework boundary, "no precedent in our code" is the wrong answer — the right answer is "framework already does X, here's the line, here's why our user reports it as broken." Track as a watch-item for the next framework-boundary feature.

## Run 2026-05-26_0101_routine-strong-builder — 2026-05-26 02:58 BRT

Pipeline: feature
Final outcome: pass
Score: 6/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope, Affected files (Schema/migration, API, Hooks, Session-create flow, UI, Tests, Docs), Relevant conventions, Constraints, Existing precedents, Unknowns (7), Reusable patterns, Out-of-scope all populated (`discovery.md:17-378`).
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `src/db/schema.ts:88-115`, `src/api/sets.ts:58-96`, `app/(app)/workout/[sessionId].tsx:118-127`, the 4-policy RLS precedent at `0010_exercise_notes.sql:49-67`, partial-unique precedent triple at `0008:15-17 / 0010:45-47 / 0012:22-24`, the `pickingId` race precedent at `src/components/exercise-picker.tsx`).
- `fact` vs `assumption` clearly distinguished: pass — "Verified fact" / "Verified by grep" markers throughout (e.g. `discovery.md:29-30, 68, 82, 104, 116, 122-123`); recommendations explicitly labelled "My recommendation" vs "Verified" (e.g. `discovery.md:147, 211-213`).
- `Unknowns` section populated: pass — 7 unknowns each with (a)/(b)/(c) shape (`discovery.md:183-241`); Designer adopted all 7 defaults as-is in v1 (`design-v1.md:13-22`), signaling exceptionally well-calibrated defaults.
- Searched for precedent / sibling patterns: pass — 7 precedents enumerated (`discovery.md:170-178`); reusable verbatim copy-paste snippets included as a separate section (`discovery.md:244-364`) — migration boilerplate, RLS 4-policy block, partial-unique idx, touch_updated_at trigger, two-step reorder swap, compute-next set_number, hook template.
- Relative dates converted to absolute: pass — no relative dates in the prompt; references to "recent commit 77029d4" / "Decision 9" anchored to specific commit/file references.
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — only `discovery.md` attributed.

### Lessons for next run
- **The reusable-patterns appendix** (`discovery.md:242-364`, 7 verbatim code/SQL blocks) was extraordinarily high-leverage — Designer's v1 SQL block at `design-v1.md:67-155` reused them verbatim, and Validator's v1 verification table (`validation-v1.md:9-13`) could cross-check by name. Sustain that "verbatim copy-paste material" section whenever the feature spans migration + API + hook layers.
- **The "(a) what / (b) why / (c) recommended default" Unknown shape** continues to pay off — Designer adopted all 7 v1 defaults wholesale (`design-v1.md:13-22`), zero round trips on Unknowns. Keep that as the contract.
- **The Session-create flow trace** (`discovery.md:58-71`, 5 numbered steps from `<RoutineListItem>` press → `startSession` API → live screen mount) handed Designer a zero-grep map for where to thread the seed step. Apply that "trace the existing call path end-to-end with file:line at each hop" discipline whenever a feature adds a step into an existing flow.
- **The "verified by grep" pattern surfaced load-bearing one-line facts** — e.g. `discovery.md:82` "exactly two production sites consume `target_rest_seconds`" determined the entire "keep the column" decision. Sustain.

### Recurring pattern check
- none — only one prior entry exists for Discovery on this project (canonical-exercises run, 6/6). No recurring fail to flag.

## Run 2026-05-25_1921_canonical-exercises — 2026-05-26 00:35 BRT

Pipeline: feature
Final outcome: pass
Score: 6/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope, Affected files (Schema/API/Hook/UI/Test), Conventions, Constraints, Precedents, Unknowns, Out-of-scope, Index analysis all populated (`discovery.md:15-226`).
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `src/db/schema.ts:46-63`, `supabase/migrations/0004_exercise_muscles_array.sql:43-90`, `src/api/exercises.ts:60-78`, the 17 e2e sites enumerated by line `discovery.md:103-121`).
- `fact` vs `assumption` clearly distinguished: pass — "Verified fact" + "verified by grep" markers (e.g. `discovery.md:124-126, 165, 220-225`); recommendations explicitly labelled "My recommendation" vs "verified".
- `Unknowns` section populated: pass — 6 unknowns each with (a) what / (b) why / (c) Conductor check + recommended default (`discovery.md:154-202`); Conductor accepted all 6 defaults wholesale per `transcript.md:27`, signaling well-calibrated defaults.
- Searched for precedent / sibling patterns: pass — 6 precedents enumerated (`discovery.md:144-152`), including `pr-list-row.tsx:48-52` (chip), `0005_measurements.sql:72-96` (RLS replacement), `0004_exercise_muscles_array.sql` (function rewrite), `read-only-exercise-block.tsx` (read-only sibling).
- Relative dates converted to absolute: pass — no relative dates in the prompt; the dated artifacts (Decision 9, recent migration cadence) are all anchored to specific commit/file references.
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — only `discovery.md` attributed.

### Lessons for next run
- Sustain the "recommended default" pattern on every Unknown. Conductor accepted all 6 defaults this run because each had a clear (a)/(b)/(c) shape — keep that as the contract.
- The `schema.ts` drift call-out + Decision-9 cache-buster link inside Discovery's `Relevant conventions` section saved a Designer round trip — keep surfacing project-wide invariants proactively even when the prompt does not mention them.
- The 17-e2e-site enumeration with per-file `:line` references gave Designer/Implementer a zero-grep starting point — apply that "enumerate every blast-radius site at file:line" discipline whenever a migration changes a column read pattern.

### Recurring pattern check
- none — first feedback entry for Discovery on this project.
