# Feedback for Discovery

## Run 2026-06-03_1124_progress-chart-window — 2026-06-03 12:30 BRT

Pipeline: feature
Final outcome: pass (shipped — working tree, uncommitted; D↔V 1 round GO, I↔R 1 round PASS, I↔T 1 round PASS — every gate green round 1)
Score: 6/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope summary (`:6-9`), Affected files split into in-scope-presenters / window-infrastructure / prior-art-selector / tests (`:11-36`), Relevant conventions (6 FACT-marked, `:38-45`), Constraints (Data/UI/Platform/Auth/Performance, `:47-52`), Existing precedents (`:54-59`), the Close-the-set chart inventory table (`:61-73`), 9 Unknowns each with (a)/(b)/(c) + confidence (`:75-121`), Out-of-scope flags (`:123-128`). None missing.
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `weekly-muscle-volume.ts:50-128,59-69`, `e1rm-strength.ts:80-251,214-245`, `window-utils.ts:41-49`, `progress-page-math.ts:74-95,82-84`, `use-progress-page.ts:64-88`, `profile.tsx:147-198,27-34`, `db/types.ts:68,74-76`, `multi-series-chart.tsx:99-102`). The close-the-set table cites file:line per row.
- `fact` vs `assumption` clearly distinguished: pass — "FACT, verified by reading code" markers on the dual-anchor rule (`:40`), the `0`→`undefined` semantics (`:41`), the local-non-persisted-state precedent (`:42`), the no-calendar-dep platform fact (`:50`); recommendations explicitly labelled with confidence inside each Unknown's (c) (HIGH/MEDIUM/LOW), e.g. U4 ephemeral HIGH, U1 discrete-selector MEDIUM, U5 axis MEDIUM.
- `Unknowns` section populated: pass — 9 unknowns ranked most→least design-impacting (`:75-121`), each (a) what / (b) why it matters / (c) recommended default + confidence; U1/U2/U9 correctly flagged for human decision (resolved in `state.md:46-51`); U5 (axis-derivation + LOCF lead-in shift on shrink) is the highest-leverage technical unknown and pre-named the exact unit cases the design/tests later pinned.
- Searched for precedent / sibling patterns: pass — the EXACT wiring template named (`useLifetimeBestWeek` `use-progress-page.ts:64-88` → `useMaxVolumeWindowWeeks()`→`computeWindowStart`→kernel-inside-`useMemo`), the kernel param to copy (`bucketLifetimeWeeklyVolumes`'s `windowStartMs?` filter `progress-page-math.ts:82-84`), the segmented-control idiom (`profile.tsx:147-198`), the bottom-sheet picker precedent (`week-selector.tsx:105-258`) — all with file:line. The close-the-set grep (`:73`) proved no N+1th chart exists.
- Relative dates converted to absolute: pass — no relative dates in the prompt; the date-discipline surface here is the windowing math (device-local ISO weeks via `computeWindowStart`, inclusive `>=` lower bound) which Discovery pinned correctly at `:24,41`.

- **The standout: Discovery correctly re-read "date picker" against the platform and reframed the whole feature before any design.** U1 (`:78-81`) is the load-bearing move: the prompt says "add a date picker" but Discovery verified `package.json` has NO calendar/date-picker dependency (only `date-fns`), traced that a true calendar picker would change the math seam from "weeks→`windowStartMs`" to "arbitrary start/end → bucket boundaries" AND break dual-anchor consistency with the windowed "Max" numbers, and recommended reading "date picker" as "discrete weeks-window selector" reusing `MAX_VOLUME_WINDOW_OPTIONS`. The human confirmed exactly this (`state.md:48`). A literal reading would have triggered a new cross-platform dependency and a materially larger feature. This is the "is the prompt's named mechanism the right one?" check — the same highest-leverage Discovery move the prior 2 entries praised, applied to a UI-vocabulary mismatch this time.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks in `transcript.md` [`grep -c` → 0]; the Discovery routing entry `transcript.md:17-21` describes a self-contained inventory + explicitly "No peer invocations"; a phrasing scan for unlogged invocations across all run `*.md` returned 0 hits.)
- Write boundaries: pass — only `discovery.md` attributed. Baseline `25db98beb5bb219b3405b7d2fa75a13849665d92` resolved via `git rev-parse --verify`; diff vs baseline + `git ls-files --others --exclude-standard` bucketed by the path map; the `design.md`/`validation.md`/`review.md`/`test-report.md` template stubs + `state.md`/`transcript.md`/`final-summary.md` are Conductor scaffolding; the 5 cache-buster files + other-run PNGs + the prior-run `2026-06-01_1301.../state.md` are pre-existing/cross-run noise (verified: the 5 cache-buster files carry ZERO of this run's feature symbols).

### Lessons for next run
- **The "is the prompt's named control/mechanism actually the right one?" check earned its keep again — sustain it as the standing first move.** This run it caught a UI-vocabulary mismatch ("date picker" → discrete weeks selector) that, taken literally, would have added a cross-platform dependency and a much larger math seam. Last run it caught a wrong file + wrong DB mechanism; the run before, a wrong catalog claim. The discipline transfers across surface types (file/mechanism → UI vocabulary). Sustain: when the prompt names a control, library, or mechanism, verify against `package.json`/source that it is the right primitive BEFORE design, and recommend the cheapest correct reframe.
- **The close-the-set chart inventory was exhaustive-by-construction and held end-to-end — sustain the table + "no N+1th" grep.** Discovery built the 6-row chart table (`:61-73`) with each chart's current windowing status + in-scope verdict + the closing grep proving no other progress chart exists. The Designer copied the 2-in-scope verdict, the Validator confirmed (claims 9a/9b), the Reviewer re-verified scope — no missed chart class. The discipline is now a 3-consecutive-run sustained strength on "apply X everywhere" features.
- **U5 (axis-derivation + LOCF lead-in on shrink) was the highest-leverage technical unknown and it pre-specified the design's hardest correctness case.** Discovery flagged at `:98-101` that filtering rows before `earliestMs` auto-shrinks the axis AND that the e1RM LOCF lead-in recomputes over the windowed set — and named the exact unit cases (axis left-edge, all-pre-window exercise drops out, LOCF over windowed first week). The design's W-1/W-2/W-4/W-5 cases map 1:1 to these. Sustain: when windowing/filtering changes a DERIVED axis or a carry-forward, name the recompute behavior AND the unit cases up front.

### Recurring pattern check
- **"Search thorough-looking but not exhaustive-by-construction" — the fail pattern stays BROKEN (RESOLVED 3rd consecutive run).** The arc: bottom-tab (missed framework grep) → bodyweight-volume (missed close-the-set grep) → e1rm-strength-chart (RESOLVED) → favorite-exercises (RESOLVED) → THIS run (RESOLVED again, close-the-set chart inventory + "no N+1th progress chart" grep at `:73`). The discipline has now held across three consecutive runs and transferred across feature types. No recurring fail to flag.
- **"Correct/reframe the prompt's wrong-or-ambiguous premise" — now a 3-run sustained positive pattern.** e1rm (catalog claim) → favorites (wrong file + wrong mechanism) → THIS run (UI-vocabulary "date picker" reframed to discrete weeks selector). A reliable Discovery strength; worth continuing to surface as the cheapest place to catch a wrong premise.

## Run 2026-06-01_1301_favorite-exercises-e1rm — 2026-06-01 15:10 BRT

Pipeline: feature
Final outcome: pass (shipped; D↔V 2 rounds [v1 NO-GO], I↔R 1 round PASS, I↔T 2 rounds — round 1 FAIL test-only; migration 0020 live)
Score: 6/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope summary, Affected files (new + modified, file:line per entry, `:11-28`), §1 the `exercise_notes` precedent studied fully (`:30-52`), §2 the detail-page prompt CORRECTION (`:54-68`), §3 the e1RM close-the-set integration map (12-row table, `:70-102`), §4 favorites→e1RM data join (`:104-111`), §5 migration+RLS (`:113-129`), §6 hooks/cache (`:131-137`), §7 tests (`:139-152`), Relevant conventions, Constraints, Existing precedents, 9 Unknowns each (a)/(b)/(c), Out-of-scope flags — all populated, none missing.
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `0010_exercise_notes.sql:27-67`, `exercise-notes.ts:33-51,52-102`, `use-exercise-note.ts:8-37`, `e1rm-strength.ts:106-148`, `e1rm-strength-section.tsx:29-41,60-70`, `progress.tsx:88-122,224-237`, `rls.test.ts:133-192,308-398`, `canonical-exercise.ts:34-60`, `0011_canonical_exercises.sql:28-30`). The §3 integration table cites file:line per row + per-impact.
- `fact` vs `assumption` clearly distinguished: pass — "FACT (HIGH confidence)" / "verified by reading it" / "verified with `dangerouslyDisableSandbox`" markers throughout; the §1 42P10 analysis is explicitly "FACT-based answer: NO" (`:43`); recommendations labelled "Recommended default" inside each Unknown's (c). The §3 close-the-set verdict is "HIGH confidence … no N+1th site" (`:78`).
- `Unknowns` section populated: pass — 9 unknowns each with (a) what / (b) why / (c) recommended default (`:178-206`); the Designer adopted the defaults wholesale (presenter seam, single list key, header-right star outside `canEdit`, plain INSERT/DELETE, CASCADE, 12-line ceiling). Unknown #2 (palette wrap) was correctly identified as "the headline risk."
- Searched for precedent / sibling patterns: pass — the entire `exercise_notes` triad (migration+API+hook+slot) studied fully as the table template AND its 3 simplifications enumerated (`:52`); the Phase-2a e1RM triad as the integration target; the Pencil header-right pattern; the `measurements.ts:121-159` INSERT+SQLSTATE-discriminator style; the multi-arm RLS script; `pickCanonicalExercise` live-catalog helper — all with file:line.
- Relative dates converted to absolute: pass — no relative dates in the prompt; the date-discipline surface here is the live "today 2026-06-01" used to scope the migration number (0020, verified latest is 0019, `:115`).

- **Two load-bearing prompt corrections, both verified and both upheld by the Conductor.** (1) The detail page is `progress.tsx` (the `useAllExercise`+Pencil+`ExerciseNoteSlot` screen), NOT `index.tsx` (the edit form) — the prompt conflated the two, and Discovery read BOTH files to prove `index.tsx` = `EditExerciseScreen` (react-hook-form, `useExercise`, no header-right slot) vs `progress.tsx` = the actual detail page (`discovery.md:54-68`). A wrong mount point would have wasted a full design+implement cycle. (2) The notes 42P10 partial-unique-upsert trap does NOT apply to favorites — a favorite is presence/absence against a NON-partial composite PK, so plain INSERT (swallow 23505) + DELETE is correct, no read-then-write loop (`:40-43`). The Conductor independently re-verified both (`transcript.md:21`). This "is the prompt itself describing the right file / the right mechanism?" check is the highest-leverage Discovery move this run.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks in `transcript.md` [`grep -c` → 0]; the Discovery routing entry `transcript.md:17-21` describes a self-contained inventory with no peer reference; a phrasing scan for unlogged invocations across all run `*.md` returned 0 hits.)
- Write boundaries: pass — only `discovery.md` attributed. Baseline `5a0b86e6993f39f0721ab6efee2d36bad0c15537` resolved via `git rev-parse --verify`; diff vs baseline + `git ls-files --others --exclude-standard` bucketed by the path map; the `design.md`/`validation.md`/`review.md`/`test-report.md` template stubs + `state.md`/`transcript.md`/`final-summary.md` are Conductor scaffolding; the other-run screenshot PNGs are pre-existing noise.

### Lessons for next run
- **The "read the prompt's named file/mechanism against source and correct it BEFORE design" move is now the standout Discovery value-add — sustain it explicitly.** This run it caught two prompt errors (wrong file + wrong DB mechanism) that each would have cost a full cycle. The discipline: when the prompt names a specific file, mechanism, or precedent, READ it and verify the claim holds (the right screen has the right hook/slot; the cited trap actually applies). Discovery is the cheapest place to catch a wrong premise. Sustain.
- **The close-the-set discipline transferred cleanly from the Phase-2a run.** Last run proved `epley1RM` is centralized in 2 sites; this run proved the e1RM PLOTTED-SET decision is centralized in 2 files via 4 orthogonal greps (symbol / cap+palette constants / model-type consumers / importers) + a 12-row integration table + an explicit "no N+1th site" verdict (`:70-102`). The Designer copied the verdict, the Validator confirmed the closed set, the Reviewer re-verified byte-for-byte — and the "missed site" class did not recur. Sustain the grep-A/B/C/D + hit→row-table + "no N+1th site" shape for any integration/"apply X everywhere" feature.
- **Naming the ONE downstream break (the palette `% length` wrap >8) up front made it the design's headline decision.** Discovery flagged that `colorForRank(i)=PALETTE[i % 8]` with a comment assuming N≤8 is the only hard assumption a union >8 breaks (`:89,98`). The Designer turned that into the 12-color-palette + 12-line-ceiling decision, the Validator confirmed the first-8 byte-for-byte, the Reviewer verified it. **Sustain: when integrating into an existing presenter, isolate the ONE assumption the new path violates and name it as the headline risk — it focuses the whole pipeline.**

### Recurring pattern check
- **"Search thorough-looking but not exhaustive-by-construction" — the 2-run fail pattern stays BROKEN (RESOLVED 2nd consecutive run).** The prior arc was bottom-tab (missed framework-source grep) → bodyweight-volume (missed close-the-set grep) → e1rm-strength-chart (RESOLVED, close-the-set ran at Discovery) → THIS run (RESOLVED again, close-the-set ran on the e1RM selection sites + the integration table). The discipline has now held across two consecutive runs and transferred across feature types (kernel inventory → integration surface). The feedback loop closed this fail class; watch only that it persists on the next "find all sites" feature. No recurring fail to flag this run.
- **A NEW positive pattern is emerging: "correct the prompt's wrong premise."** Last run Discovery corrected the prompt's catalog claim (with a residual about the seed source); this run it corrected TWO premises (wrong file + wrong mechanism) cleanly. 2-run positive arc. Worth watching as a sustained Discovery strength.


## Run 2026-05-30_2006_e1rm-strength-chart — 2026-05-30 21:30 BRT

Pipeline: feature
Final outcome: pass (shipped; D↔V 1 round GO, I↔R 1 round PASS, I↔T 2 rounds — round 1 FAIL test-only)
Score: 6/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope summary, Affected files (verified, file:line per entry), §1 kernel inventory (close-the-set), §2 `<MultiSeriesChart>` reusability, §3 most-performed derivation, §3b per-session vs per-week shape, §4 mount point, §5 test conventions, §6 migration check, Relevant conventions, Constraints, Existing precedents, 9 Unknowns each (a)/(b)/(c), Out-of-scope — all populated, none missing.
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `formulas.ts:1-5`, `progress.tsx:155-159,169-173`, `multi-series-chart.tsx:134-160,81`, `weekly-muscle-volume.ts:112`, `weekly-muscle-volume-section.tsx:60,67-71`, `dates.ts:40-126`, `stats.ts:18-30`, the seed `0001_rls_and_seed.sql:60-91`). The kernel-inventory hit→row table (`:48-52`) cites file:line per row.
- `fact` vs `assumption` clearly distinguished: pass — "verified by reading the current file" / "Verified by grep" / "Verified against source" markers throughout; recommendations explicitly labelled "Recommended" inside each Unknown's (c); HIGH-confidence verdicts stated (e.g. `:97` reuse, `:188` no-migration).
- `Unknowns` section populated: pass — 9 unknowns each with (a) what / (b) why / (c) recommended default (`:216-232`); Unknown #6 proactively pre-empted over-refactoring `progress.tsx` (the carry-in "wire all call sites" lesson, correctly scoped to "only when you CHANGE a kernel"). The Designer adopted the defaults wholesale.
- Searched for precedent / sibling patterns: pass — the entire Phase-1 muscle chart triad (presenter + section + test pair) named as the 1:1 template; the per-exercise e1RM reduce as the semantic reference; `TOP_N=5` in `<ProgressHero>` as the cap precedent; the dangling-skip join idiom; `measurements-chart.ts` null-skip — all enumerated with file:line.
- Relative dates converted to absolute: pass — no relative dates in the prompt; the date discipline here is timezone-of-bucketing, handled correctly (device-local ISO weeks per `dates.ts:12-19`, distinguished from the e2e's UTC seeding).

- **Kernel-inventory exhaustiveness — the load-bearing carry-in lesson APPLIED and PAID OFF.** Last run's fail (search thorough-looking but not exhaustive-by-construction) was the explicit carry-in. This run Discovery ran the close-the-set discipline BY CONSTRUCTION: three orthogonal greps (A = `epley1RM` symbol; B = inline Epley arithmetic `* (1 + reps/30)` and variants; C = alternate 1RM-formula names brzycki/lombardi/wathan/oconner/mayhew), a hit→inventory-row table (`:48-52`), and an explicit "no N+1th site exists" verdict (`:53-54`). Grep B's false positives (NativeWind `/30` opacity tokens, a schema comment) were each named and excluded — the closed-set proof the prior run's Validator had to produce in round 2 was produced HERE, at Discovery, first. The result was correct and held: e1RM is centralized into `epley1RM` from day one (exactly 2 sites — 1 helper + 1 caller), so the "missed inline copy" class that drove last run's no-go round did NOT recur. The Conductor independently re-verified the 2-site claim by grep (`transcript.md:21`) and agreed. **This is the cleanest demonstration so far of a carry-in lesson being read, applied, and verified to close the prior failure class.**
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks in `transcript.md`; the Discovery routing entry `transcript.md:17-21` describes a self-contained inventory with no peer reference.)
- Write boundaries: pass — only `discovery.md` attributed (baseline `3c00d8e` resolved via `git rev-parse --verify`; diff bucketed by the path map; the `design.md`/`validation.md`/`review.md`/`test-report.md` template stubs are Conductor scaffolding).

### Lessons for next run
- **The close-the-set grep + hit→row table + "no N+1th site" verdict is now the proven exhaustiveness pattern — make it the standing first move for any "apply/find X everywhere" feature.** This run it took ~6 lines (grep A/B/C + table + verdict) and pre-empted an entire D↔V no-go round. The crucial sub-move was running greps for the NEGATIVE space too (alternate formula names brzycki/lombardi/… — zero hits = "Epley is the only model"), which is what turns "I found 2 sites" into "there are exactly 2 sites." Sustain.
- **The "is the prompt itself wrong?" check earned its keep — with one residual to tighten.** The Conductor's prompt asserted the catalog "has Chin-up/Dip/Push-up but NOT Pull-up"; Discovery verified against `0001_rls_and_seed.sql:70` and CORRECTED it (Pull-up IS seeded; the load-bearing fact is that abundant WEIGHTED exercises exist for a visible line, and bodyweight ones are the negative case). NOTE the residual: Discovery's correction was anchored on the `0001` per-user TRIGGER seed, but `pickCanonicalExercise` queries the `user_id IS NULL` CANONICAL catalog (migrations `0011`/`0014`) — a distinction the Implementer had to surface as Deviation #1 and the Tester confirmed live (Pull-up MISSING from the live catalog; "Pull Up" no-hyphen in `0014`). Discovery correctly named the WEIGHTED-vs-bodyweight axis (which is what mattered for the design), but the seed-source nuance (`0001` trigger seed ≠ live canonical catalog) is the one thing a future Discovery should pin when citing a migration as "the catalog `pickCanonicalExercise` queries." Minor — it did not cost a round (the Implementer caught it), but Discovery is the right place to name the canonical-catalog source-of-truth so the Designer's e2e plan cites the right migration.
- **The reuse-vs-extend analysis (§2) was decision-grade — sustain it.** Discovery read `<MultiSeriesChart>` in full, extracted the props contract + every behavior (y-min pinned to 0, no gap support, single-dot first week), and concluded "reusable AS-IS, no extension" with HIGH confidence — then flagged the ONE caller-side decision (dynamic palette-by-index for a top-N set vs the muscle chart's fixed keyed record). That single flag is exactly what the Designer turned into Decision #4. Sustain the "read the reuse target in full, extract its contract, name the one decision the caller must make" discipline.

### Recurring pattern check
- **"Search thorough-looking but not exhaustive-by-construction" — the 2-run fail pattern is now BROKEN (RESOLVED this run).** Prior two runs: bottom-tab (missed grepping `node_modules/expo-router` for the framework's auto-pop → Validator MAJ-1) and bodyweight-volume (missed the close-the-set grep → Validator MAJ-1 + MAJ-3-NEW). This run, the carry-in lesson was applied verbatim: the close-the-set grep ran AT Discovery, the closed-set proof was produced first, and the Validator/Conductor independently confirmed it rather than discovering a missed site. The fix transferred across domains (framework-source last time, in-project kernel this time). **This is the second resolution arc in the project (after the Implementer's probe-vs-spec arc) where a named multi-run fail was closed because the carry-in lesson was read and applied — the feedback loop is working.** Watch that the discipline persists on the next "find all sites" feature and does not regress once the lesson scrolls down the file.

## Run 2026-05-30_0126_bodyweight-volume-per-muscle — 2026-05-30 BRT

Pipeline: feature
Final outcome: pass (shipped; 2 D↔V rounds, 1 I↔R, 2 I↔T)
Score: 5/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope summary, Affected files (kernel + every call site), Conventions, Constraints, the 14-row surface-by-surface data-availability table (`discovery.md:85-100`), measurement-query story, exercise→muscle join story, `<ProgressChart>` gap analysis, Migrations (NO migration — verified), Precedents, 9 Unknowns each (a)/(b)/(c), Out-of-scope — all populated.
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `volume-target.ts:74-85,100-114,135-201`, `stats.ts:18-29`, `measurements.ts:100-108`, `measurements-chart.ts:29-31`, `use-progress-page.ts:270-283`, `schema.ts:197-200`, FK restrict `0000_schema.sql:88`). The surface table cites hook + pipeline + kernel + equipment/measurements availability per row.
- `fact` vs `assumption` clearly distinguished: pass — "verified fact" / "verified by grep" / "Verified" markers throughout (`:59, 64-66, 109-110, 132-135`); recommendations explicitly labelled "Recommended default" inside each Unknown.
- `Unknowns` section populated: pass — 9 unknowns each with (a) what / (b) why / (c) recommended default (`:146-164`); Designer adopted the defaults wholesale (plumbing seam, fallback prior→later→null, no window pref, zero-fill, new component, "Other" line). Unknown #9 specifically pre-warned the Designer about the easy-to-miss `computeStripModel` + per-exercise inline reduce.
- Searched for precedent / sibling patterns: pass — windowed-kernel optional-param plumbing (`window-utils.ts`), the `useExercisesThisWeek` client-side join template (`use-progress-page.ts:270-283`), null-weight skip (`measurements-chart.ts:29-31`), ISO-week zero-fill (`computeStripModel`), the removed chart's selector idiom, and the cross-surface volume e2e regression suites all enumerated.
- Relative dates converted to absolute: pass — no relative dates in the prompt; the load-bearing date discipline here is timezone-of-compare, and it is correct: the "as-of-session" lookup pins a UTC-instant compare (`:110`) while ISO-week bucketing stays device-local — Discovery distinguished the two explicitly.

- **Kernel-inventory exhaustiveness: fail (the load-bearing Discovery output for this feature).** Discovery found 2 kernel sites the prompt missed (`computeStripModel` `weekly-volume-strip-math.ts:72-76`, the per-exercise inline reduce `progress.tsx:138-147` — flagged at `:35-36`) — genuinely strong. But it MISSED 2 more that surfaced downstream and drove the D↔V loop: (1) the week-drill-down **headline** `weekVolumeKg` (`history/week/[isoWeek].tsx:82-93`) — an entirely separate inline `w*r` kernel that Discovery's surface table row #2 (`:88`) folded into "uses `groupSessionVolumes`" without noticing the headline is its own reduce → became Validator **MAJ-1** (no-go, `validation-v1.md:31`); (2) the History-list `groupSessionVolumes` **caller** (`history/index.tsx:22-25`) — Discovery's surface table row #1 (`:87`) correctly named `groupSessionVolumes` as the kernel, but its inventory (`:33`) listed `groupSessionVolumes` as ONE site without flagging it has **two** un-wired callers → the second caller became Validator **MAJ-3-NEW** in round 2 (`validation-v2.md:62`). Net: the "13-site inventory" was actually 14 active sites + a two-caller fan-out; the gap cost a no-go round + a carry-in must-fix. Scored against this criterion (a surface-search that looked thorough but was not exhaustive-by-construction).
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks in `transcript.md`; the Discovery routing entry `transcript.md:21` explicitly declares "No peer invocations to log".)
- Write boundaries: pass — only `discovery.md` attributed (baseline `5a2382b` resolved via `git rev-parse --verify`; diff bucketed by the path map).

### Lessons for next run
- **When the feature is "change one predicate everywhere," the kernel inventory must be exhaustive-BY-CONSTRUCTION, not enumeration-from-memory.** The right method is the one the Designer AND Validator both ran in round 2 and confirmed exhaustive: a single `grep -rn '\* r' / 'parseFloat(.*weight' / 'weight ? parseFloat'` across `app/` + `src/`, then map EVERY hit to an inventory row, and explicitly account for the non-volume hits (user-bodyweight, set auto-fill) so the reader can see the set is closed. Discovery built the conceptual surface table but did NOT run the close-the-set grep — so `weekVolumeKg` (a headline reduce that doesn't read like a "kernel") and the second `groupSessionVolumes` caller slipped through. **Add to the Discovery contract: for any "apply X to every site" feature, attach the exhaustiveness grep + a hit→inventory-row table + an explicit "no N+1th site exists" line.** The Validator had to produce exactly this proof in `validation-v2.md:28-53`; Discovery should produce it first and save a round.
- **A surface-table row is NOT the same as a kernel-inventory row.** A single screen can host MULTIPLE independent kernels (the week drill-down has BOTH `groupSessionVolumes` per-session rows AND a separate `weekVolumeKg` headline), and a single kernel function can have MULTIPLE call sites that each need wiring (`groupSessionVolumes` has two). **When a surface "uses kernel K," answer (a) is that the only kernel on this surface? and (b) how many distinct call sites of K exist?** Both questions, answered with grep, would have caught MAJ-1 and MAJ-3 at Discovery time.
- **Sustain the (a)/(b)/(c) Unknown shape and the proactive easy-to-miss flag.** Unknown #9 (`:164`) explicitly told the Designer "`computeStripModel` + per-exercise inline reduce are independent copies the prompt did NOT enumerate — flag as easy-to-miss." That call-out is exactly why the Designer caught those 2. Extend the SAME instinct to the close-the-set grep so NO inline copy is left to memory.
- **The 14-row surface-by-surface data-availability table was high-leverage — sustain it.** It gave the Designer the equipment/measurements gap per surface and drove the "widen `stats.ts` SELECT vs client-join" seam decision. The structure was right; it just needed a kernel-count-per-surface and call-site-count-per-kernel column to be exhaustive.

### Recurring pattern check
- **"Search thorough-looking but not exhaustive-by-construction" is now a 2-run fail variant (severity MEDIUM, rising).** Prior run (bottom-tab-home-link): Discovery grepped the PROJECT for `tabPress`/`popToTop` (greenfield) but missed grepping `node_modules/expo-router` for the framework's own auto-pop → Validator MAJ-1. THIS run: Discovery grepped the project and found most kernels but did NOT run the close-the-set exhaustiveness grep, missing 2 sites the Validator caught as MAJ-1 + MAJ-3-NEW. **Different domain (framework source last time, in-project kernel fan-out this time) but the SAME root: the search was confident-enumeration, not closed-set-proof, and the Validator's grep is what closed the set both times.** The fix is the same shape: when correctness depends on "find ALL sites of X," Discovery owes a closed-set proof (grep + hit→row table + "no further site exists"), not an enumeration. Treat as a priority pre-flight on the next "change everywhere" feature.

## Run 2026-05-26_0307_bottom-tab-home-link — 2026-05-26 04:42 BRT

Pipeline: feature
Final outcome: pass (with documented follow-up)
Score: 4/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope, Affected files (Tabs root, per-tab stacks, hidden tabs, callsites), Conventions, Constraints, Precedents, Unknowns (8), Reusable patterns, Out-of-scope all populated (`discovery.md:8-185`).
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `app/(app)/_layout.tsx:1-74`, `app/(app)/exercises/[id]/progress.tsx:64-86`, `tests/e2e/auth.spec.ts:301-303`, `node_modules/expo-router/build/layouts/TabsClient.d.ts:14-22, 63-71`).
- `fact` vs `assumption` clearly distinguished: pass — "Verified fact"/"verified by grep"/"Verified" markers throughout (e.g. `discovery.md:19, 29, 42, 52, 75`); recommendations explicitly labelled "My recommendation" with confidence (e.g. `:97, :104, :117, :122`).
- `Unknowns` section populated: pass — 8 unknowns each with (a)/(b)/(c) shape (`discovery.md:92-138`); Designer adopted all 8 defaults in v1.
- Searched for precedent / sibling patterns: **fail** — Discovery `:90` claims "No prior 'tap to reset stack' pattern in the codebase — verified by grep returning zero matches across `app/` and `src/`. This feature is greenfield." This is correct as a project-code grep, but Discovery only grepped the **project** code and missed that **Expo Router's forked native-stack at `node_modules/expo-router/build/fork/native-stack/createNativeStackNavigator.js:54-67` ALREADY auto-pops on focused `tabPress`**. Validator caught this as MAJ-1 (`validation-v1.md:18, 48-51`) — the framework already implements the desired behavior, and Designer's v1 unconditional `e.preventDefault()` would have DISABLED the working built-in feature. For a feature explicitly described as "screen state on `<Tabs>` re-press", a search **inside `node_modules/expo-router`** for `tabPress` / `popToTop` was load-bearing and was skipped. Also missed the `PartialState` rehydration nuance (`@react-navigation/routers/src/types.tsx:5-10`) that ultimately produced the deep-link follow-up.
- Relative dates converted to absolute: pass — no relative dates in the prompt.
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — only `discovery.md` attributed.

### Lessons for next run
- **For framework-touching features, grep the framework's source too.** "Greenfield in `app/` + `src/`" is necessary but not sufficient when the feature is "intercept a framework event". Discovery should have run `grep -rn "tabPress\|popToTop" node_modules/expo-router/build` and `node_modules/@react-navigation/*/src` — that one search would have surfaced the fork's auto-pop listener at `createNativeStackNavigator.js:54-67` and re-framed the whole problem (the feature is partially built-in, the user's "it doesn't work" is platform-specific, not greenfield). Make this part of the contract: "when the feature is an event-handler on a framework primitive, grep the framework for the same event before declaring greenfield."
- **Surface state-rehydration shapes when the feature touches navigator state.** The `PartialState<NavigationState>` distinction (where `type`/`key`/`index` may be absent) is documented at `@react-navigation/routers/src/types.tsx:5-10`. Discovery enumerated the listener types but did not call out that `route.state` can be `NavigationState | PartialState<NavigationState>` and the two have different shapes. That gap propagated through Designer → Implementer → Tester before being surfaced as the deep-link follow-up. For any feature that reads `navigation.getState()` or `route.state`, Discovery should include a section on the rehydration shapes (click-through state vs URL-deep-link state vs persisted-state restore).
- **The "(a) what / (b) why / (c) recommended default" Unknown shape continues to pay off.** Designer adopted all 8 defaults wholesale. Sustain.
- **The web-specific `backBehavior="history"` annotation** (`discovery.md:75-78`) was extraordinarily useful — Designer and Validator both used it to scope risks. Sustain the discipline of surfacing load-bearing project-specific invariants up front.

### Recurring pattern check
- **Framework-internals literacy** is the recurring shape worth tracking: in routine-strong-builder (prior run), Discovery surfaced the relevant precedents at `:170-178` with a 7-pattern appendix; in canonical-exercises before that, the precedents block also delivered. This run's `(none found)` framing on the precedents line was technically correct but framework-incomplete. Pattern emerging: when the feature lives on a framework boundary, "no precedent in our code" is the wrong answer — the right answer is "framework already does X, here's the line, here's why our user reports it as broken." Track as a watch-item for the next framework-boundary feature.

## Run 2026-05-26_0101_routine-strong-builder — 2026-05-26 02:58 BRT

Pipeline: feature
Final outcome: pass
Score: 6/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope, Affected files (Schema/migration, API, Hooks, Session-create flow, UI, Tests, Docs), Relevant conventions, Constraints, Existing precedents, Unknowns (7), Reusable patterns, Out-of-scope all populated (`discovery.md:17-378`).
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `src/db/schema.ts:88-115`, `src/api/sets.ts:58-96`, `app/(app)/workout/[sessionId].tsx:118-127`, the 4-policy RLS precedent at `0010_exercise_notes.sql:49-67`, partial-unique precedent triple at `0008:15-17 / 0010:45-47 / 0012:22-24`, the `pickingId` race precedent at `src/components/exercise-picker.tsx`).
- `fact` vs `assumption` clearly distinguished: pass — "Verified fact" / "Verified by grep" markers throughout (e.g. `discovery.md:29-30, 68, 82, 104, 116, 122-123`); recommendations explicitly labelled "My recommendation" vs "Verified" (e.g. `discovery.md:147, 211-213`).
- `Unknowns` section populated: pass — 7 unknowns each with (a)/(b)/(c) shape (`discovery.md:183-241`); Designer adopted all 7 defaults as-is in v1 (`design-v1.md:13-22`), signaling exceptionally well-calibrated defaults.
- Searched for precedent / sibling patterns: pass — 7 precedents enumerated (`discovery.md:170-178`); reusable verbatim copy-paste snippets included as a separate section (`discovery.md:244-364`) — migration boilerplate, RLS 4-policy block, partial-unique idx, touch_updated_at trigger, two-step reorder swap, compute-next set_number, hook template.
- Relative dates converted to absolute: pass — no relative dates in the prompt; references to "recent commit 77029d4" / "Decision 9" anchored to specific commit/file references.
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — only `discovery.md` attributed.

### Lessons for next run
- **The reusable-patterns appendix** (`discovery.md:242-364`, 7 verbatim code/SQL blocks) was extraordinarily high-leverage — Designer's v1 SQL block at `design-v1.md:67-155` reused them verbatim, and Validator's v1 verification table (`validation-v1.md:9-13`) could cross-check by name. Sustain that "verbatim copy-paste material" section whenever the feature spans migration + API + hook layers.
- **The "(a) what / (b) why / (c) recommended default" Unknown shape** continues to pay off — Designer adopted all 7 v1 defaults wholesale (`design-v1.md:13-22`), zero round trips on Unknowns. Keep that as the contract.
- **The Session-create flow trace** (`discovery.md:58-71`, 5 numbered steps from `<RoutineListItem>` press → `startSession` API → live screen mount) handed Designer a zero-grep map for where to thread the seed step. Apply that "trace the existing call path end-to-end with file:line at each hop" discipline whenever a feature adds a step into an existing flow.
- **The "verified by grep" pattern surfaced load-bearing one-line facts** — e.g. `discovery.md:82` "exactly two production sites consume `target_rest_seconds`" determined the entire "keep the column" decision. Sustain.

### Recurring pattern check
- none — only one prior entry exists for Discovery on this project (canonical-exercises run, 6/6). No recurring fail to flag.

## Run 2026-05-25_1921_canonical-exercises — 2026-05-26 00:35 BRT

Pipeline: feature
Final outcome: pass
Score: 6/6

### Rubric
- Every section of `discovery.md` filled: pass — Scope, Affected files (Schema/API/Hook/UI/Test), Conventions, Constraints, Precedents, Unknowns, Out-of-scope, Index analysis all populated (`discovery.md:15-226`).
- Concrete code references cite file:line: pass — every claim cites file:line (e.g. `src/db/schema.ts:46-63`, `supabase/migrations/0004_exercise_muscles_array.sql:43-90`, `src/api/exercises.ts:60-78`, the 17 e2e sites enumerated by line `discovery.md:103-121`).
- `fact` vs `assumption` clearly distinguished: pass — "Verified fact" + "verified by grep" markers (e.g. `discovery.md:124-126, 165, 220-225`); recommendations explicitly labelled "My recommendation" vs "verified".
- `Unknowns` section populated: pass — 6 unknowns each with (a) what / (b) why / (c) Conductor check + recommended default (`discovery.md:154-202`); Conductor accepted all 6 defaults wholesale per `transcript.md:27`, signaling well-calibrated defaults.
- Searched for precedent / sibling patterns: pass — 6 precedents enumerated (`discovery.md:144-152`), including `pr-list-row.tsx:48-52` (chip), `0005_measurements.sql:72-96` (RLS replacement), `0004_exercise_muscles_array.sql` (function rewrite), `read-only-exercise-block.tsx` (read-only sibling).
- Relative dates converted to absolute: pass — no relative dates in the prompt; the dated artifacts (Decision 9, recent migration cadence) are all anchored to specific commit/file references.
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — only `discovery.md` attributed.

### Lessons for next run
- Sustain the "recommended default" pattern on every Unknown. Conductor accepted all 6 defaults this run because each had a clear (a)/(b)/(c) shape — keep that as the contract.
- The `schema.ts` drift call-out + Decision-9 cache-buster link inside Discovery's `Relevant conventions` section saved a Designer round trip — keep surfacing project-wide invariants proactively even when the prompt does not mention them.
- The 17-e2e-site enumeration with per-file `:line` references gave Designer/Implementer a zero-grep starting point — apply that "enumerate every blast-radius site at file:line" discipline whenever a migration changes a column read pattern.

### Recurring pattern check
- none — first feedback entry for Discovery on this project.
