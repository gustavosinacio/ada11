# Feedback for Diagnostician

## Run 2026-06-01_0941_session-finish-exercise-order — 2026-06-01 11:45 BRT

Pipeline: fix
Final outcome: pass (shipped — fix applied + migration 0019 live)
Score: 6/6

### Rubric
- Hypothesis stated before searching: pass — `diagnosis.md:3-5` opens with an explicit "## Hypothesis (stated BEFORE searching)" naming the suspected cause ("there is no stable, persisted, session-scoped EXERCISE ordering") *before* the evidence section. The verdict ("CONFIRMED as fact", `:6`) comes after, and the diagnosis is honest that it EXTENDS the Reproducer's mechanism with three new findings (structural-not-just-tie-break; live order's source provably non-persisted; the per-`set_number` band ordering is itself unspecified so a `created_at` tie-break can't recover routine order) rather than merely restating the repro.
- Aggressive grep for sibling locations: pass — did not stop at the History surface the owner noticed. Enumerated ALL exercise-sequence consumers: 2 affected (History detail = blocker; live screen = the divergence baseline) and **5 explicitly ruled out** with reasoning in a scope-guard table (`diagnosis.md:32-41`): verdict PR list (own deterministic sort), week drill-down (session rows only, no per-session exercises), history list (summary rows only), volume kernels + e1RM/per-muscle charts (group by `exercise_id`, order-independent). This "grep for siblings, prove the ones that DON'T share the root cause" discipline is exactly what kept the fix surface from inflating — and it directly fed the Fix Designer's scope and the Regression Tester's adjacent-check list (which came back 0 regressions across precisely those ruled-out surfaces).
- Each candidate: file:line, severity, reasoning: pass — the candidate-locations table (`diagnosis.md:24-30`) gives file:line, token/pattern, context, and a blocker/major/minor severity for all five entries. The two MAJORs are especially load-bearing: `workout/[sessionId].tsx:124-131` (the authoritative order is client-only, never persisted) and `routine-exercise-sets.ts:233-243` (interleaved seed → why the cheap `created_at` tie-break is insufficient). The minor (`volume-target.ts` stale comments) is correctly parked as a doc-only follow-up not tied to the root cause.
- Fact vs opinion distinguished: pass — exemplary, the strongest criterion this run. Every load-bearing claim is verified at file:line and tagged with evidential weight: `sets.ts:43-56` (`.order("set_number")`, no secondary key — verified by reading), `sets.ts:64-73` (`set_number` scoped to `(session_id, exercise_id)` → N-way tie — verified), `schema.ts:142-188` (no session-scoped position column — verified), and the decisive `workout/[sessionId].tsx:124-131, 281-290` (`exerciseOrderOverride` is `useState` set only by `moveExercise`, "Not persisted across reloads" — verified). The product-intent question ("match-what-user-saw vs merely-deterministic") is explicitly flagged as "A product call, not a code fact" (`diagnosis.md:121`). No opinion masquerading as fact.
- If symptom-only fix, said so explicitly: pass — has a dedicated "## Symptom-only fix risk" section (`diagnosis.md:109-111`) that judges direction (a) (secondary `.order` key) to be "a **legitimate symptom-level fix, NOT a root-cause fix**" — it makes the tie deterministic but does NOT make History match the order the user saw for routine-interleaved/ad-hoc/reordered sessions because those were never persisted. The fix-direction matrix (`:73-85`) scores all three directions across the three session shapes (R/A/M), proving only (c) satisfies the repro's "Expected." This is the diagnosis doing its job: telling the Fix Designer which directions are half-fixes and WHY, with the coverage matrix as proof.
- `security_relevant` populated with reason + surfaces honest: pass — `security_relevant: no` stated explicitly (`diagnosis.md:113-115`) with a substantive justification: the root cause and every candidate location are read/ordering of the user's OWN session/set rows, all already RLS-scoped (`auth.uid()`), no auth/credential/untrusted-input surface. Crucially it does NOT stop at "no" — it adds the forward-looking note that a persistence fix (direction c) "would add a column/table that inherits the same `user_id` RLS posture as `sets`... the Fix Designer/Regression Tester should confirm any new table's RLS policy matches." This is a *true* `no` (the bug exposes no access-control gap) PLUS the right hand-off (the new write path's RLS must be confirmed) — which the Regression Tester then executed (cross-user read AND write both blocked). Not a careless `no`.

### Lessons for next run
- **The single highest-value move: proving the "the user's order is persisted NOWHERE" finding, which DETERMINED the fix shape.** The diagnosis decomposed the live order's four inputs (`diagnosis.md:58-67`) and proved that THREE of four (set-first-occurrence, ad-hoc adds, manual reorder via `exerciseOrderOverride`) are never written to the DB — so a query-only fix can recover a *deterministic* order but never *the order the user saw* for ad-hoc/reordered sessions. This single load-bearing finding is what forced the fix to add persistence (the Fix Designer cited it verbatim to reject directions a/b). **Sustain the discipline: when a "make it match what the user saw" bug hinges on whether the authoritative state is persisted, decompose every input to the displayed value and verify each one's persistence at file:line — the answer often dictates whether the fix is a query change or a schema change.**
- **The three-shape coverage matrix (R/A/M × directions a/b/c) is the model for a multi-option fix hand-off.** Rather than recommend a direction and leave the alternatives vague, the diagnosis scored each direction against each session shape (`diagnosis.md:73-85`) showing (a) = "stably wrong" for R/M, (b) = routine-only, (c) = all three. This gave the Fix Designer and the human approval gate a decision table, not a recommendation to take on faith. **Sustain: for a bug with multiple fix directions, build the coverage matrix (direction × scenario) so the trade-offs are legible at the approval gate.**
- **One thing to watch (not a fail) — the diagnosis recommended the "snapshot at Finish" variant of (c) as lower-write-frequency, and the run later GREW a second write path (History reorder) the owner added at the gate.** The diagnosis's variant choice (`diagnosis.md:83`) was sound for the bug as scoped, but the owner's added requirement (persist on History reorder) meant the column needed a second, non-Finish write path the diagnosis did not anticipate. This was a legitimate scope expansion at the human gate, not a diagnosis miss — but it is a reminder that "lowest-write-frequency" is the right default ONLY if the product won't soon want a manual-edit path on the same data. When recommending a persistence variant, a one-line note on "if a manual-edit/recovery UI is ever wanted on this data, the write path generalizes to X" would have anticipated the gate's expansion. Minor; the HIGH-confidence root cause and the RLS-forward-note were both spot-on.

### Recurring pattern check
- **Framework/source-grounding and the honest forward-looking `security_relevant` note are now a 2-run positive pattern** (navbar-tab fix: framework-source grounding + true `no`; this run: every link verified at file:line + the "confirm the new write path's RLS" hand-off that the Regression Tester executed). No recurring FAIL exists (2 runs, 6/6 both). The prior run's lone watch-item ("when two paths share one mechanism, probe BOTH state shapes") did not recur — this run's mechanism was single-rooted and fully verified. Sustain the file:line-verification rigor and the "decompose every input, verify each one's persistence" discipline.

## Run 2026-05-27_2144_navbar-tab-pop-to-root — 2026-05-27 23:10 BRT

Pipeline: fix
Final outcome: pass
Score: 6/6

### Rubric
- Hypothesis stated before searching: pass — `diagnosis.md:3-4` opens with an explicit "## Hypothesis (stated BEFORE searching)" section naming the suspected `childState.type === "stack"` guard and the PartialState mechanism *before* the evidence section. The verdict ("CONFIRMED as fact", `:6`) comes after, with the distinction made between inference and empirical confirmation.
- Aggressive grep for sibling locations: pass — did not stop at the one guard the Reproducer flagged. Established that `tabBarButton: HomeLinkTabBarButton` is set at the `<Tabs>` `screenOptions` level (`_layout.tsx:119`), so the *single* button serves every tab, and concluded the bug affects deep-link arrivals into **history** and **workout** too, not just exercises (`diagnosis.md:13, 30, 49`). This scope expansion (one button → all tabs) directly shaped the fix's tab-agnostic `TAB_ROOTS` map and the Regression Tester's History generalization spot-check. That is the "grep for siblings, don't fix the one the user noticed" discipline executed well.
- Each candidate: file:line, severity, reasoning: pass — the candidate-locations table (`diagnosis.md:25-31`) gives file:line, token, context, and a blocker/major/minor severity for all 5 locations. The `:83`+`:91` entry is especially sharp — it flags that even relaxing the `:80` type check leaves `popToTop` with no `key` target (blocker-level constraint), pre-empting a half-fix.
- Fact vs opinion distinguished: pass — exemplary, the strongest criterion this run. Every claim is tagged with its evidential weight: framework type defs cited with line numbers ("`types.d.ts:47-50` ... `type`, `index`, **and `key` are all optional/absent**", `:16`), the expo-router source that *produces* the PartialState (`getStateFromPath.js:469-499`, `:17`), and — decisively — the **prior run's runtime probe** (`2026-05-26_0307/implementation.md:73-80`, `:18`) which empirically logged `{childType: stack}` (Path A) vs `{childType: undefined}` (deep-link). The verdict cleanly separates "CONFIRMED as fact" (backed by the prior runtime observation) from "inferred." No opinion masquerading as fact.
- If symptom-only fix, said so explicitly: pass — has a dedicated "## Symptom-only fix risk" section (`diagnosis.md:62-69`) that explicitly judges the candidate fix to be a *legitimate root-cause fix at the navigation layer, NOT a symptom patch*, and then hands the Fix Designer two hard constraints (don't reintroduce the `<a>`/`linkTo` race; `router.replace` has a `backBehavior` interaction risk). This is the diagnosis doing its job: telling the Fix Designer what NOT to do, with the prior-run history as justification.
- `security_relevant` populated with reason + surfaces honest: pass — `security_relevant: no` stated explicitly (`diagnosis.md:71-75`) with a substantive one-paragraph justification: purely client-side navigation state, no auth/RLS/credential/untrusted-input surface, `(app)` already auth-gated upstream (`app/_layout.tsx:14-35`), RLS in `progress.tsx` is server-authoritative and orthogonal. This is a *true* `no` — the root cause genuinely does not touch a security surface — not a careless one. The Regression Tester correctly carried this forward as the `n/a` security note.

### Lessons for next run
- **The single highest-value move: grounding the root cause in a PRIOR RUN's runtime probe rather than re-inferring it.** The exact PartialState divergence (`childType: undefined` on deep-link vs `stack` on click-through) was empirically logged in run `2026-05-26_0307` (`implementation.md:73-80`), and this diagnosis cited it as the load-bearing evidence (`diagnosis.md:18`). That single citation is what let the verdict be "CONFIRMED as fact" instead of "high-confidence inference." **Sustain the discipline of mining prior runs' artifacts for empirical evidence before treating a mechanism as merely inferred — the pipeline's own history is a primary source.**
- **Reading the FRAMEWORK source, not just the project, was decisive.** The diagnosis traced the PartialState all the way to its origin in `expo-router`'s `getStateFromPath.js:469-499` and the type contract in `@react-navigation/routers/.../types.d.ts:47-50` (`diagnosis.md:16-17`). The prior feature-pipeline runs flagged a Discovery gap where the *project* was grepped for nav primitives but the framework's source was not. This diagnosis closed that gap proactively for the fix pipeline. **Sustain: for any bug at a framework boundary, the evidence chain must reach into `node_modules` to the line that produces the offending behaviour.**
- **One thing to watch — the diagnosis modeled the failing child state as a "single-route PartialState" (`type === undefined`), which was correct for Path B but INCOMPLETE for Path C.** The Implementer's runtime probe (`implementation.md:18-20`) found that on Path C the child state is `undefined` *entirely* — indistinguishable from a genuine at-root cross-tab arrival — which is *why* the fix-plan's child-Stack-based gates were unsound and the Implementer had to pivot to `useSegments()`. The diagnosis did not catch this Path-C-vs-at-root ambiguity (it asserted at `:41` that Path C produces "the same partial nested state" as Path B). This did not cost a rework round (the Implementer's mandated runtime verification absorbed it), and the diagnosis's `:67` already hedged "if a stable child key can be obtained post-hydration" — but **next time, when two arrival paths are claimed to share one mechanism, probe the child-state shape of BOTH paths, not just the simplest one.** The "single fix covers B and C" conclusion was right; the underlying state-shape model for C was off by one (undefined, not single-route-partial).

### Recurring pattern check
- none — first Diagnostician feedback entry for this project. No prior pattern to flag. Standout strengths to watch for repetition: framework-source grounding and the prior-run-runtime-probe citation. The one watch-item (incomplete child-state model for Path C) is logged above so the next reading agent treats "probe both paths, not just the simplest" as priority.
