# Feedback for Tester

## Run 2026-06-03_2217_hard-sets-per-muscle — 2026-06-03 23:30 BRT

Pipeline: feature
Final outcome: pass (Tester PASS first round — live close-loop observed sets = working COUNT 3; T-3 teeth proven RED->GREEN at unit AND e2e under an include-flip; regression 54/54 across 11 specs, 0 flaky; all 4 Reviewer hand-offs resolved with runtime evidence; owned + fixed 2 test-quality defects)
Score: 5/5

### Rubric
- Golden path tested with command + observed output: pass — e2e test 5 + a live runtime probe: seeded 3 working Chest sets @ 100x5, signed in via UI, navigated to /progress; default (kg) renders header "Weekly volume per muscle", peak caption `Peak 1,500 kg`, SVG y-ticks `["0 kg","375 kg","750 kg","1,125 kg","1,500 kg"]`; tapping "Metric: hard sets" swaps the header to "Weekly hard sets per muscle", peak caption to `Peak 3 sets`, y-ticks to `["0","1","2","2","3"]` (`test-report-v1.md:43-63`). **The observed sets value is the working-set COUNT (3), NOT tonnage (1,500), NOT a naive row count — observed on the ACTUAL rendered surface via a runtime probe** (`:54`), with the before/after probe output preserved (`:57-62`). Command-and-observed-output, the live proof the metric is correct.
- ≥2 edge cases tested with evidence: pass — 5 edges with evidence: Edge 1 / Invariant S (bodyweight `weight:0`, no weigh-in COUNTS as `[1]`, tonnage drops to `[]` on the same row — unit S-2 + live e2e test 4 bodyweight line, `:90-95`); Edge 2 / Invariant D (dropset does NOT count, sets `[1]` vs tonnage `[1140]`, RED under the include-flip — unit S-4 + e2e test 6, `:97-101`); Edge 3 / U4 (`reps:0` + `reps:null` working rows COUNT -> `[2]`, unit S-3, `:103-107`); Edge 4 / uncheck-all (peak reads "Peak 0 kg"/"Peak 0 sets" gracefully, no crash — live probe, `:109-119`); Edge 5 / windowed parity (pre-window excluded, threshold inclusive — unit S-8a/S-8b, `:121-123`). Each with command + observed output.
- 1-2 adjacent regression checks performed: pass — **54/54 across 11 specs, 0 flaky** (`:125-141`): the feature spec `weekly-muscle-volume` 6/6, plus `e1rm-strength` 3/3 (the chart beside it), `progress-page` 8/8 (the host), `progress-window-selector` 3/3 (the threaded window), `weekly-volume-strip` 4/4 + `week-drill-down` 5/5 + `end-of-session-verdict` 2/2 + `volume-target` 7/7 + `session-total-volume-header` 5/5 + `read-only-history` 5/5 (shared `useLifetimeWeeklyVolume`/`WeeklyVolumeRow` tonnage consumers, the Invariant-T-at-e2e proof), `max-volume-window` 6/6. Each batch health-checked `200` first; OOM cascade did NOT recur.
- Cross-platform smoke when relevant: pass — web tested via Playwright Chromium + Expo web (the harness platform); iOS/Android explicitly flagged "not tested" with reasoning (pure-TS presenter scaffold extraction + `<MultiSeriesChart>` reused as-is + a segmented control reusing the `<ProgressWindowSelector>` idiom already shipping on iOS/Android; no native module, no new dependency, no migration; Risk LOW, `:154-156`). Not silently marked pass.
- Untestable items explicitly flagged: pass — the "Untestable / not-covered" section (`:164-166`) flags iOS/Android (web-only harness, Risk LOW with reasoning) and the R-4 reseed edge (a muscle present in tonnage but all-dropset in sets flips `seriesKeysSig` -> reseed all-on — structurally rare, verified-correct by code path, "a coverage residual, not a miss"). None marked pass by omission.

- **The standout: the T-3 teeth proof — RED->GREEN at BOTH unit and e2e level via an include-predicate flip, then reverted and MD5-verified.** Rather than trusting the green divergence tests, the Tester temporarily flipped the sets include from `(row) => row.set_type === "working"` to `() => true` (count all rows) in `weekly-muscle-volume.ts:257`, then proved the divergence assertions go RED: at the unit layer S-4 FAILED with "expected `[2]` to deeply equal `[1]`" (the dropset got counted, `:34`), and at the e2e layer test 6 FAILED with `getByTestId('weekly-muscle-peak')` Expected "Peak 2 sets" Received "Peak 3 sets" on the live rendered caption (`:38`). It then reverted and verified "the suite ends GREEN (verified: marker count 0 in source, line 257 restored ... full unit suite 515/515)" (`:41`). **A test that stayed green under the flip would be a false-green; both go RED under the flip and GREEN after revert -> genuine teeth.** This closes the Validator's MAJ-1 must-fix at both layers and is the gold standard for proving a divergence test actually guards the divergence.
- **The standout (2): owned + fixed 2 test-quality defects at runtime — including a schema-CHECK-violating dropset seed that NO static layer could catch — without touching production code.** Test 6's original seed inserted a standalone `dropset` row with `parent_set_id = NULL`, which violates the `sets_parent_matches_type` CHECK constraint (`0000_schema.sql:67-68`) and was rejected at the DB before the feature code ran (`test-report-v1.md:79`). The Tester diagnosed it (the insert error), refactored `seedFinishedSession` to append the dropset linked to working set #1 in the SAME session (the schema-honoring pattern used by 3 sibling specs), and re-ran 6/6 green. It also fixed test 5's over-broad page-wide locator (a runtime probe proved "1,500 kg" legitimately renders on 5 other Progress surfaces, `:81-86`) by scoping the assertion to the caption handle. It correctly framed both as TEST-QUALITY defects, not feature defects ("the feature code was correct the whole time; no production change was needed or made", `:11`).
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` blocks; the Tester routing entry `transcript.md:51-55` shows a self-contained test round + "No peer invocations".)
- Write boundaries: pass (with a recorded path slip) — `test-report-v1.md` attributed (the `test-report.md` stub is Conductor scaffolding; the other-run PNGs + prior-run `state.md` are cross-run noise). The T-3 include-flip + the live close-loop probe spec were both correctly REVERTED/REMOVED after use (evidence preserved in the report; the flip MD5-verified reverted at `:41`). **Recorded path slip (does NOT fail the criterion): the two THIS-run screenshots `hard-sets-toggle.png` + `hard-sets-dropset-excluded.png` were written into the WRONG run folder — `docs/runs/2026-05-30_0126_bodyweight-volume-per-muscle/screenshots/` instead of this run's `docs/runs/2026-06-03_2217_hard-sets-per-muscle/screenshots/` (verified via `git ls-files --others`: both are untracked/new).** Screenshots are the Tester's allowed evidence extension, so this is not an out-of-ROLE write — but the run-id misfiling is a real housekeeping slip (the evidence is attributed to the wrong run for any future audit). Criterion passes (in-role write); the slip is recorded for not-repeating.

### Lessons for next run
- **The schema-CHECK-violating dropset seed is the sharpest GENERALIZABLE lesson of the run: e2e seed data must satisfy the table's DB CHECK constraints, not just the column types.** A `dropset` row REQUIRES a non-null `parent_set_id` (`sets_parent_matches_type`, `0000_schema.sql:67-68`); a type-valid standalone dropset is rejected by the DB before the feature runs. No static gate (typecheck, lint, the Reviewer's MIN-3 structure-check) catches this — it only surfaces against the real schema at runtime, which is exactly where the Tester caught it. **Sustain + codify: before authoring (or running) an e2e seed for a row with a `set_type`/status/relational column, read `0000_schema.sql` for CHECK constraints on that table and construct the row to satisfy them — for sets, a dropset must link to a working set via `parent_set_id` (the pattern in `auto-fill-placeholder-on-check.spec.ts`, `rest-timer-auto-start.spec.ts`, `routine-strong-builder.spec.ts`). This is the e2e-seed cousin of the long-running 'verify the runtime source of truth before trusting your seed' discipline — here the runtime truth is a DB constraint.** Worth surfacing to the Conductor for the e2e-authoring guidance.
- **Sharpest improvement lever (a housekeeping slip, not a test miss): the two run screenshots were saved into the WRONG run's folder.** `hard-sets-toggle.png` + `hard-sets-dropset-excluded.png` landed in `docs/runs/2026-05-30_0126_bodyweight-volume-per-muscle/screenshots/` (the bodyweight-volume run that shipped the per-muscle chart this feature augments — a plausible-but-wrong target) instead of this run's `2026-06-03_2217_hard-sets-per-muscle/screenshots/`. The testing was flawless; the evidence is just filed under the wrong run-id, which would mislead a future audit of either run. **Lesson: write all run evidence (screenshots, probe artifacts) under `docs/runs/<this-run-id>/`, double-checking the run-id when the feature builds on a sibling run whose folder name is semantically close. A one-line `pwd`/path check before saving evidence prevents misfiling.** Low severity (evidence preserved, just misplaced); flagged so it doesn't recur.

### Recurring pattern check
- **Instrumented-probe / live-close-loop diagnosis is now a 9-run strength.** canonical -> routine (PostgREST) -> bottom-tab (childState pair) -> bodyweight-volume (substitute-fixture) -> e1rm (SVG-polyline geometry) -> favorites (live union + nav-race) -> progress-window (SVG-polyline 53->9) -> bodyweight-leverage (live 512/640-kg close-loop) -> THIS run (live observed-sets-COUNT-3 close-loop + the T-3 include-flip RED->GREEN teeth proof at both layers + the page-wide-locator probe). The methodology reliably carries the load-bearing proof. Sustain.
- **The hard-reload-nav-race systemic finding did NOT recur — correctly (dormant, not resolved).** This feature is a view-only chart toggle with no optimistic-mutation-on-another-screen e2e; the live close-loop seeds via service-role and reads on a single screen, so the `page.goto`-races-optimistic-write class had no surface. Still worth codifying for the next optimistic-mutation e2e; not exercised here.
- **Dev-server OOM cascade did NOT recur** (`test-report-v1.md:126` "No OOM cascade"; the partition-and-health-check workaround held). Quiet for 6 consecutive runs now. Keep watching.
- **NEW positive: the e2e-teeth relay completed at the Tester end — the Tester PROVED the divergence test red->green that the Validator caught (MAJ-1) and the Implementer fixed (Deviation-1 stable handle).** This is the third consecutive chart-feature run where the e2e-teeth concern was caught-fixed-PROVEN inside a single round; the Tester's T-3 include-flip is the empirical capstone. Reinforce this relay — it is the system working end-to-end on the e2e-teeth discipline.
- **NEW watch-item (LOW severity): run-evidence misfiled into a sibling run's folder.** First occurrence of a screenshot-path slip. Distinct from the testing-quality patterns (which are all strong); this is a housekeeping/run-id-attribution slip. Watch whether it recurs when a feature builds on a semantically-close sibling run. The score-honesty discipline (10 runs at 5/5) is otherwise fully sustained — clean first-round PASS with honest disclosure of the 2 untestable residuals and the 2 owned test-quality fixes.

## Run 2026-06-03_1402_bodyweight-leverage-factors — 2026-06-03 15:40 BRT

Pipeline: feature
Final outcome: pass (Tester PASS first round — live close-loop 512 kg + 640 kg; regression 52/52 across 12 specs, 0 flaky; all 3 Reviewer hand-offs resolved with runtime evidence; migration 0021 live)
Score: 5/5

### Rubric
- Golden path tested with command + observed output: pass — T-A live close-loop: seeded a fresh user (80 kg weigh-in + a live workout with one CHECKED Push-up working set, weight 0 / reps 10), signed in via the UI, purged the persisted cache, navigated to the live session, asserted the `<SessionHeader>` total-volume a11y label = **512 kg** (= 80 × 0.64 × 10), with the full-BW bug number (800 kg) count = 0 (`test-report-v1.md:67-83`). Screenshot `ta-pushup-leveraged-512.png`. This exercises builder #1 (the live session-header total) through the real PostgREST `numeric` column — command-and-observed-output, the runtime proof the factor seam works live.
- ≥2 edge cases tested with evidence: pass — Edge 1 (Dip at 1.0 == today, full BW unchanged): covered by `bodyweight.test.ts` cases 3/4 + the live `session-total-volume-header.spec.ts` 5/5 (Bench Press 100×5 = 500 kg byte-for-byte, `:122-123`). Edge 2 (coalesce-to-1.0-NEVER-0 / non-finite + NULL ⇒ 1.0, Invariant L safety): `bodyweight.test.ts` cases 6-9 + the runtime probe (`seam WITHOUT factor = 80`, `:125-126`). Edge 3 (addedLoad NEVER scaled): `"10",80,"0.64"⇒61.2` not 57.6 (`:128-129`). Edge 4 (bonus, un-wired-surface desync would be caught): the runtime probe showed a factor-missing MAP returns 800 (`:131-132`). Each with command + observed output.
- 1-2 adjacent regression checks performed: pass — **52/52 across 12 specs in 3 batches, 0 flaky** (`:136-154`): `weekly-muscle-volume` 4/4, `e1rm-strength` 3/3 (Invariant D), `progress-page` 8/8, `max-volume-window` 6/6, `progress-window-selector` 3/3, `end-of-session-verdict` 2/2 (builder #2), `week-drill-down` 5/5, `weekly-volume-strip` 4/4, `volume-target` 7/7 (builder #5), `session-total-volume-header` 5/5 (builder #1 + Invariant L), `read-only-history` 5/5 (builder #3). Each batch health-checked `200` first; OOM cascade did NOT recur. This is the strongest Invariant-L proof (every non-bodyweight surface byte-for-byte) AND live coverage of 4 of the 6 builders.
- Cross-platform smoke when relevant: pass — web tested via Playwright Chromium (the harness platform) + Expo web; iOS/Android explicitly flagged "not tested" with reasoning (pure-arithmetic seam + TanStack hooks + PostgREST `select`/Map builds + SQL migration; no native module, no SVG, no platform API, no new dependency — design R-6, `:162-165`). Risk LOW, not silently marked pass.
- Untestable items explicitly flagged: pass — the "Untestable / not-covered" section (`:179-182`) flags iOS/Android (web-only harness), the e1RM unit-case tautology (Reviewer MIN-1 — Invariant D proven dynamically + statically instead), and the other 2 reclassified movements (Chest Dip, Hanging Knee Raise — T-B proved Pull Up; the other two share the identical reclassify mechanism + confirmed in T-C, but not individually UI-exercised). None marked pass by omission.

- **The standout: the LIVE close-loop (512 kg, not 800) over UNIT TESTS — and surfacing that the design's MAJ-2 string premise is INSTANCE-DEPENDENT.** Rather than trusting the green unit suite, the Tester applied the migration's effect on a real rendered surface fed by real PostgREST data: a Push-up renders 512 kg (leveraged), proving the string→number seam works end-to-end. **And it caught the load-bearing real-world finding (`test-report-v1.md:85`): THIS PostgREST instance returns `bodyweight_factor` as a JS `number`, not the string the design's MAJ-2 premise assumed** — observed directly via two read-only probes. It correctly framed this as "the feature is correct regardless because the seam handles BOTH" (NOT a defect), but the finding is real and instance-dependent: the Conductor's MCP `execute_sql` read the SAME column as a STRING (`transcript.md:66`). So the design's string-aware shape was load-bearing for one client and conservative for another — exactly the kind of "the stated premise is more nuanced than monolithic" check the Tester's runtime probe exists to surface. T-B (reclassified Pull Up renders 640 kg where it was 0) + T-C (7 rows backfilled exact) close the retroactive-shift loop.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` blocks; the Tester routing entry `transcript.md:70-74` shows a self-contained test round + "No peer invocations".)
- Write boundaries: pass — `test-report-v1.md` + the 4 screenshots under `docs/runs/<run-id>/screenshots/` (`ta-pushup-leveraged-512.png`, `tb-pullup-reclassified-640.png`, + the strip/empty-state PNGs noted in the report) attributed (Tester's artifact + screenshot extension). The live close-loop probe spec + the read-only backfill tsx were both correctly REMOVED after use (evidence preserved in the report, `:174-175`; verified absent from the tree). The `test-report.md` stub is Conductor scaffolding; the other-run PNGs are pre-existing noise. All within boundary.

### Lessons for next run
- **Closing the loop LIVE on a real rendered number (512 kg) rather than trusting the unit suite — AND surfacing the instance-dependent string-vs-number reality — is the gold standard. Sustain it.** The unit tests proved the seam with synthetic `"0.64"`; the Tester proved the FEATURE by reading the actual rendered total off a live migrated DB, and in doing so found the design's "numeric reads back as a string" premise is not universally true on this instance (this client returns a number; the MCP client returns a string). Sustain: for any feature whose correctness depends on a runtime data SHAPE, drive the real rendered output on live data and observe the actual JSON type — the assumed shape is a hypothesis until observed, and clients can disagree.
- **The 512-vs-800 gap made the cross-surface assertion empirically load-bearing — and the Tester PROVED it with a contrast probe.** It ran a throwaway probe driving the real kernels with the STRING `"0.64"` AND contrasting against the full-BW (bug) 800 and the factor-missing-from-map desync 800 (`:51-58`), showing both failure modes are loud. Sustain: when verifying a cross-surface "same number everywhere" assertion, prove the WRONG number (the bug shape) would actually be produced — a 288-kg gap that the test can observe.
- **Sharpest improvement lever (a coverage residual, not a miss): the other 2 reclassified movements (Chest Dip, Hanging Knee Raise) were not individually UI-exercised.** T-B proved Pull Up renders 640 kg; the other two were confirmed `equipment='bodyweight'` with correct factors in T-C (read-only query) and share the IDENTICAL reclassify mechanism + code path, so Confidence is HIGH they behave identically — but neither was driven through a rendered surface. On a feature whose R-1 risk is "these 3 newly APPEAR," driving all 3 (or at least asserting the 0.50 factor on Hanging Knee Raise, the only non-1.0 reclassified value, renders correctly) would close the residual cheaply. It is genuinely low-risk (same mechanism), so the residual was correctly disclosed not hidden. Consider a one-more-surface assert for the distinct-factor reclassified movement next time a batch-reclassify ships.

### Recurring pattern check
- **Instrumented-probe / live-close-loop diagnosis is now an 8-run strength.** canonical → routine (PostgREST) → bottom-tab (childState pair) → bodyweight-volume (substitute-fixture + stash-to-baseline) → e1rm (SVG-polyline geometry) → favorites (live union + nav-race pair) → progress-window (SVG-polyline 53→9 + live-catalog probe) → THIS run (live 512/640-kg close-loop + the string-vs-number two-client finding + the 800-contrast desync probe). The methodology reliably carries the load-bearing proof. Sustain.
- **The hard-reload-nav-race systemic finding did NOT recur this run — and correctly so (dormant, not resolved).** This feature is backend-only with no optimistic-mutation-on-another-screen e2e; the live close-loop seeds via service-role and reads via the signed-in UI on a single screen, so the `page.goto`-races-optimistic-write class had no surface. Still worth codifying for the next optimistic-mutation e2e; not exercised here.
- **Dev-server OOM cascade did NOT recur** (`test-report-v1.md:17` "no OOM cascade this run"; the partition-and-health-check workaround held). Quiet for 5 consecutive runs now. Keep watching.
- **Score honesty sustained across 9 runs (all 5/5 on the Tester rubric).** Clean first-round PASS, but the report still honestly flagged 3 untestable/coverage residuals (iOS/Android, the e1RM tautology, the 2 un-driven reclassified movements) AND framed the string-vs-number finding as a doc-nit-not-defect rather than inflating it. Output shape (live-golden-path + multi-edge + 12-spec regression + cross-platform-flagged + untestable-disclosed + all-hand-offs-closed) is stable. Sustain.

## Run 2026-06-03_1124_progress-chart-window — 2026-06-03 12:30 BRT

Pipeline: feature
Final outcome: pass (Tester PASS first round — new e2e 3/3, 0-flake over 9; regression 21/21; all 3 Reviewer hand-offs resolved with runtime evidence)
Score: 5/5

### Rubric
- Golden path tested with command + observed output: pass — the page-level selector windows BOTH charts in lockstep, proven via the shipped spec test 1 (chip `Toggle Bench Press` count `1`@All → `0`@10w → `1`@All, with a settle-gate on the active `bg-black` segment + the in-window `Squat` chip before each negative assertion, `test-report-v1.md:49-55`) AND a direct rendered-SVG probe (T-1): a throwaway spec seeding ONE weighted exercise across W52/W30/W8/W0 (constant series set) read `<polyline>` point-counts at All vs 10w → **53 points → 9 points**, axis left edge moved `02/06/25`→`06/04` (`:57-66`). Command-and-observed-output throughout. **This is empirical proof the chart GEOMETRY redraws, not a trust of the chip count.**
- ≥2 edge cases tested with evidence: pass — Edge 1 (empty window keeps the selector mounted, Unknown 6/R-3): seed all-data-40-weeks-old, tap 10w → both section headers count `0` (collapsed) BUT the `Chart window` button still visible/tappable, "All" re-renders them (`:74-78`, `screenshots/03-empty-window.png`). Edge 2 (same-series shrink preserves a toggled-off line, MIN-4/seriesKeysSig stability): toggle `Squat` OFF (`opacity-40`), shrink to 10w without dropping it → chip still count 1 AND still `opacity-40` (`:80-84`). Plus Edge 3 (bonus, unit-level): W-3 boundary inclusivity (`===threshold` IN / `−1ms` OUT) + W-4/W-5 top-N/LOCF recompute (`:86-88`). Each with command + observed output.
- 1-2 adjacent regression checks performed: pass — ran the 4 specs touching the same charts/page in one batch at the DEFAULT window (`windowStartMs=undefined`): `weekly-muscle-volume.spec.ts` 4/4, `e1rm-strength.spec.ts` 3/3, `progress-page.spec.ts` 8/8, `max-volume-window.spec.ts` 6/6 → **21/21, 0 flaky** (`stats: expected 21, unexpected 0, flaky 0`, `:92-103`). This is the strongest end-to-end Invariant-W proof: the two charts + the Profile control behave byte-for-byte as before the feature.
- Cross-platform smoke when relevant: pass — web tested via Playwright Chromium (the harness platform); iOS/Android explicitly flagged "not tested" with reasoning (RN-Web-compatible only: two PURE-TS presenters gain an optional row-filter, a stateless selector reusing the Profile segmented-control markup that already ships on iOS/Android, `react-native-svg` `<MultiSeriesChart>` reused as-is, no native module, no new dependency — `:114-118`). Not silently marked pass.
- Untestable items explicitly flagged: pass — the "Untestable / not-covered" section (`:143-146`) flags iOS/Android (web-only harness, Risk LOW with reasoning), MIN-1 (W-4 rank-FLIP two-survivors-swap, not exercised — mechanism verified-correct, coverage residual), and R-6 cold-mount seed flicker (not reproduced in the e2e — fresh users have no pref; accepted-by-design). None marked pass by omission.

- **The standout: empirically proving the chart GEOMETRY redraws (53→9 polyline points) via a throwaway SVG probe rather than trusting the legend-chip proxy the Reviewer flagged (T-1).** The Reviewer's T-1 noted the shipped spec's chip-count is a model-derived proxy, not the rendered line. The Tester wrote a probe holding the series set CONSTANT (so no chip changes) and read the rendered `<polyline>` point-counts + `<text>` x-labels at All vs 10w — the polyline went 53→9 points and the axis left edge moved from a year ago to the first in-window Monday, the direct rendered-SVG proof the Reviewer asked for. It also resolved T-2 (a service-role probe of the live 95-row canonical catalog: `Bench Press` + `Squat (Barbell)` both FOUND, non-bodyweight, no `pickCanonicalExercise` throw) and T-3 (`bg-black` confirmed emitted on the active web `<Pressable>`). All three hand-offs closed with runtime evidence, none handed onward.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks; the Tester routing entry `transcript.md:52-56` shows a self-contained test round + "No peer invocations".)
- Write boundaries: pass — `test-report-v1.md` + the 3 screenshots under `docs/runs/<run-id>/screenshots/` (`01-all-history.png`, `02-window-10w.png`, `03-empty-window.png`) attributed (Tester's artifact + screenshot extension). The throwaway probe spec (`_probe-window-redraw.spec.ts`) was correctly REMOVED after use (evidence preserved in the report, `:57`). The `test-report.md` stub is Conductor scaffolding; the other-run PNGs are pre-existing noise. All within boundary.

### Lessons for next run
- **The "assert the rendered ARTEFACT, not just the model-derived proxy" probe is the gold standard for a chart feature — sustain it.** The shipped e2e asserts a legend chip (a valid proxy); the Tester additionally probed the actual `<polyline>` geometry to prove the chart REDRAWS. This is the second consecutive chart run where the Tester read SVG geometry to close a "does the chart actually change?" gap (e1rm-strength used the same polyline-geometry probe). Sustain: when an e2e proxies a rendered output, write a throwaway probe that reads the rendered DOM/SVG directly, then remove it and preserve the stdout as evidence.
- **Resolving T-2 with a live service-role catalog probe (after the Implementer was sandbox-blocked) is the right close-the-loop — sustain it.** The Implementer fell back to suite-green names when its probe was blocked; the Tester (with working elevated access) confirmed both names resolve in the live 95-row catalog, so the spec ran green with NO substitution. Sustain: when an upstream agent defers a DB-state confirmation because of a sandbox block, the Tester runs the probe the upstream agent couldn't and records the resolved IDs.
- **Sharpest improvement lever (a coverage residual, not a miss): the MIN-1 rank-FLIP case (two surviving exercises swap order on shrink) stayed unexercised end-to-end.** The Tester correctly flagged it as a verified-correct mechanism with a coverage residual (W-4 proves exclusion + rank-0, not a swap). On a feature whose core is "recompute the windowed top-N," a rank-flip is close to core behavior. It is genuinely hard to assert at the e2e layer (rank order is sub-week-granular), but a UNIT case (two exercises, windowing inverts their relative rank without dropping either) would close it cheaply and is the kind of case the presenter tests CAN pin deterministically. Consider proposing that unit case next time a windowed/filtered ranking ships. Minor — the mechanism is structurally verified.

### Recurring pattern check
- **Instrumented-probe diagnosis is now a 7-run strength.** canonical → routine (PostgREST) → bottom-tab (childState pair) → bodyweight-volume (substitute-fixture + stash-to-baseline) → e1rm-strength-chart (SVG-polyline geometry) → favorite-exercises (live union + nav-race pair + POST-counting) → THIS run (SVG-polyline geometry 53→9 + live-catalog T-2 probe). The methodology reliably carries the load-bearing proof. Sustain.
- **The hard-reload-nav-race systemic finding did NOT recur this run — and correctly so.** The prior 2 runs' round-1 FAILs (an authored e2e returning via `page.goto`, racing an optimistic write + stale persisted-cache rehydration) had no surface here: the feature is a VIEW-ONLY ephemeral selector (no optimistic mutation, no cross-screen return-to-assert), and the spec navigates via client-side tab taps. The convention gap remains worth codifying for the NEXT optimistic-mutation-on-another-screen e2e, but it was not exercised this run — the new spec was authored correctly for its (mutation-free) shape. Net: the residual is dormant, not resolved; keep watching.
- **Dev-server OOM cascade did NOT recur this run** (`test-report-v1.md:17` "RSS ~43 MB throughout — no OOM cascade"). Quiet for 4 consecutive runs now. The partition-and-health-check workaround remains the standing mitigation; keep watching.
- **Score honesty sustained across 8 runs (all 5/5 on the Tester rubric).** This run was a clean first-round PASS, but the report still honestly flagged 3 untestable/coverage residuals (iOS/Android, MIN-1 rank-flip, R-6 seed-flicker) rather than marking them pass by omission. Output shape (golden-path-2-ways + multi-edge + multi-spec regression + cross-platform-flagged + untestable-disclosed + all-hand-offs-closed) is stable. Sustain.

## Run 2026-06-01_1301_favorite-exercises-e1rm — 2026-06-01 15:10 BRT

Pipeline: feature
Final outcome: pass (round 1 FAIL — test-harness nav defect, feature PROVEN working; round 2 PASS — ran the corrected spec itself, 4/4 0-flake)
Score: 5/5

### Rubric
- Golden path tested with command + observed output: pass — round 1 proved the feature 3 independent ways before failing the spec: (4a) an instrumented union probe on the LIVE DB seeding the exact e2e shape, running `presentTopExerciseE1rm` WITHOUT vs WITH the favorite → `[A]` no-fav top-5, `[C]` TARGET pinned at rank 5 values `[76,76]`, count 5→6, chip label `"Toggle Lat Pulldown"` (`test-report-v1.md:59-70`); (4b) drove the real header star → POST 201 + DB row (`:74-80`); (4c) golden path via REAL in-app Progress-tab nav, reliable 3/3, 6 e1RM chips rendered (`:88-94`). Round 2 ran the corrected spec ITSELF (warm-up + `--repeat-each=3` → 4/4 green, 0 flake, parsed from `.last-run.json` + the JSON reporter) — did NOT trust the Implementer's claim (`test-report-v2.md:21-41`).
- ≥2 edge cases tested with evidence: pass — Edge 1 (favorite already INSIDE top-N is a no-op, the MAJ-1 fix): unit case 2 + the union probe `[A]`/`[C]` ranks 0..4 identical (`test-report-v1.md:116-120`); Edge 2 (non-plottable favorite excluded, Invariant D survives the union): unit cases 3/4 + re-ran `e1rm-strength.spec.ts` case 4 (bodyweight-only → NO line) green (`:122-126`); Edge 3 (Invariant F — no favorites = byte-for-byte today): probe `[A]` + unit case 8 (`:128-132`). Each with command + observed output.
- 1-2 adjacent regression checks performed: pass — ran the 3 specs the Reviewer + Validator named as the regression surface in one batch: `canonical-exercise-gating.spec.ts` 5/5 (the header-right slot the diff rewrites — Pencil gate/label intact), `exercise-progress-ia.spec.ts` 4/4 (same slot, IA flows), `e1rm-strength.spec.ts` 3/3 (the Phase-2a chart favorites wire into — Invariant F unchanged). **12/12, 0 failures**, `.last-run.json` per batch (`test-report-v1.md:136-142`).
- Cross-platform smoke when relevant: pass — web tested via Playwright (Chromium); iOS/Android explicitly flagged "not tested" with reasoning (RN-Web-compatible only: pure-TS presenter, TanStack hooks, PostgREST INSERT/DELETE/SELECT, a `lucide-react-native` Star in the existing slot, `<MultiSeriesChart>` reused; no native modules; the hard-reload-cache-staleness defect is web-`page.goto`-specific, native nav is always client-side — `test-report-v1.md:144-147`, re-pinned `test-report-v2.md:73-76`). Not silently marked pass.
- Untestable items explicitly flagged: pass — the round-1 FAIL itself is the model of honest flagging — split-attributed `fail (shipped spec, a real RED test that can't ship) / pass (feature, proven 3 ways)`, with HIGH-confidence root cause (0 INSERT POSTs in the failing trace + the POST-wait fix alone STILL flaked from stale persisted-cache rehydration, 3 independent probes converging). iOS/Android not-tested with risk reasoning; the RLS arm's conclusiveness (the script exits 1 on any arm failure) stated explicitly.

- **The standout: proving the feature works end-to-end via THREE converging probes (live union, in-app persistence, in-app golden path 3/3) BEFORE root-causing the spec's `page.goto` hard-reload as the defect.** The Tester did not let a RED spec masquerade as a feature bug: it isolated the failure to the hard-reload path (racing the optimistic INSERT — only ~107 ms click-to-goto, 0 INSERT POSTs in the failing trace — AND rehydrating stale persisted favorites from `PersistQueryClientProvider` AsyncStorage even after the POST was forced to land), showed the real in-app path is reliable 3/3, and delivered a verified-3/3 test-only fix recipe (client-side tab tap + `waitForResponse`). This is the textbook "feature is correct, the test models navigation wrongly; here's the proof and the surgical fix" diagnosis.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks; the Tester routing entries `transcript.md:53-56,58-61` show self-contained test rounds with no peer reference.)
- Write boundaries: pass — `test-report-v1.md`, `test-report-v2.md`, and the screenshots under `docs/runs/<run-id>/screenshots/` (`favorite-line-in-chart.png`, `favorite-pinned.png`) attributed (Tester's artifact + screenshot extension). The `test-report.md` stub is Conductor scaffolding; the other-run screenshot PNGs are pre-existing noise. All within boundary.

### Lessons for next run
- **The "prove the feature 3 independent ways before failing the spec" discipline is the gold standard for a test-harness FAIL — sustain it.** Live union probe (count 5→6 with the exact e2e seed) + in-app persistence (POST 201 + DB row) + in-app golden path (3/3 via the real nav path) together make "the feature works, the SPEC is wrong" a HIGH-confidence fact, not a hopeful attribution. Sustain: when a spec fails, instrument the feature via the real-user path and the kernel directly, and only then attribute the failure.
- **Root-causing through the SECOND layer (after the obvious one) is what made the attribution airtight.** The first fix attempt (`waitForResponse` so the POST lands) STILL flaked — and rather than declare victory, the Tester probed again and found the SECOND cause (stale persisted-cache rehydration on hard reload), proving BOTH require client-side nav. **Sustain: when your first fix doesn't fully close a flake, keep probing — a partial fix that still flakes means there's a second mechanism.**
- **Running the corrected spec itself in round 2 (`--repeat-each=3`, 4/4) instead of trusting the Implementer's claim is the right close-the-loop rigor.** Sustain "re-run the previously-RED spec yourself, with repeat-each to prove the flake is closed, and read the authoritative `.last-run.json`."

### Recurring pattern check
- **Instrumented-probe diagnosis is now a 6-run strength.** canonical → routine (PostgREST probe) → bottom-tab (childState probe pair) → bodyweight-volume (substitute-fixture + stash-to-baseline) → e1rm-strength-chart (SVG-polyline geometry probe + stash-to-baseline) → THIS run (live union probe + in-app-vs-hard-reload nav probe pair + network-trace POST-counting). The methodology reliably carries the load-bearing diagnosis. Sustain.
- **A NEW systemic finding worth elevating: the "authored e2e returns via `page.goto` (hard reload) and races the optimistic-write + persisted-cache rehydration" pattern is now at least the 2nd run.** This run's round-1 FAIL and the prior fix run's (`2026-06-01_0941_session-finish-exercise-order`) round-2 de-flake share the IDENTICAL root cause and the IDENTICAL fix (client-side nav + `waitForResponse`). **The Tester correctly diagnosed and recipe-fixed it both times — but the recurrence means the e2e CONVENTION should codify it (see `retro.md`): an e2e asserting an optimistic-mutation result on another screen must use client-side nav + `waitForResponse`, never `page.goto`.** Recommend surfacing to the playbook so future specs are authored correctly and this round is pre-empted, not re-diagnosed.
- **Dev-server OOM cascade did NOT recur this run** (`test-report-v1.md:8` "RSS stable, no OOM cascade"; `test-report-v2.md:8` health re-checked 200). Quiet for 3 consecutive runs now. The partition-and-health-check workaround remains the standing mitigation; keep watching.
- **Score honesty sustained across 7 runs (all 5/5 on the Tester rubric).** The round-1 FAIL was a genuine RED spec honestly surfaced — not inflated, not suppressed — and the feature was proven shippable on its own merits in the same report. Output shape (multi-probe feature proof + multi-spec regression + cross-platform-flagged + untestable-disclosed + surgical recipe + ran-the-corrected-spec-in-round-2) is stable. Sustain.

## Run 2026-05-30_2006_e1rm-strength-chart — 2026-05-30 21:30 BRT

Pipeline: feature
Final outcome: pass (round 1 FAIL — narrow adjacent test-locator regression, proven + pre-fixed; round 2 PASS — ran the corrected spec itself)
Score: 5/5

### Rubric
- Golden path tested with command + observed output: pass — round 1 ran the shipped `e1rm-strength.spec.ts` (3/3) AND closed the Reviewer's T-2 gap with a runtime polyline probe (`test-report-v1.md:71-78`): seeded Bench Press 90 kg→100 kg over 2 weeks, parsed the SVG `<polyline>` points `64,34 1232,20`, and asserted `lastY=20 < firstY=34` ⇒ the e1RM line TRENDS UP (105→116.67) — not just the legend chip. Full-page screenshot saved (`screenshots/e1rm-line-fullpage.png`). Round 2 ran the previously-RED `progress-page.spec.ts` itself with a JSON reporter (`{"expected":8,"unexpected":0}`) — did not trust the Implementer's claim (`test-report-v2.md:33-50`). The polyline-trends-up arithmetic is the load-bearing "feature works" evidence.
- ≥2 edge cases tested with evidence: pass — Edge 1 (NEGATIVE / Invariant-D, T-3): bodyweight Chin-up `weight=0` → no e1RM line, with the StreakCard "Streak" hydration anchor (`test-report-v1.md:87-94`); Edge 2 (MAX / Invariant E1 + LOCF, T-4): confirmed the 13-case unit suite ran green and correctly declined to mark E1/LOCF "e2e-verified" — they are unit-verified, the design's intended home (`:96-100`); Edge 3 (seed-name catalog drift): the live-catalog probe (`:26-41`). MAJ-1 teeth proven empirically by the weighted-vs-bodyweight contrast (the assertions test #4 demands be 0 are demonstrably ≥1 when a line should render — `:94`).
- 1-2 adjacent regression checks performed: pass — ran `weekly-muscle-volume.spec.ts` (4) + `progress-page.spec.ts` (8) in one batch, server health-checked (`test-report-v1.md:105-122`). Result 11/12: the muscle-volume sibling (the section the e1RM chart mounts next to) is UNAFFECTED (4/4); only `progress-page.spec.ts :: 4` regressed — isolated and proven feature-caused.
- Cross-platform smoke when relevant: pass — web tested via Playwright (Chromium); iOS/Android explicitly flagged "not tested" with reasoning ("RN-Web-compatible only: pure-TS presenter, TanStack hooks, `react-native-svg` `<MultiSeriesChart>` reused as-is on the same path as the already-shipped muscle chart; no native modules. Risk LOW per design R-5/R-6" — `test-report-v1.md:167-172`, re-pinned `test-report-v2.md:84-89`). Not silently marked pass.
- Untestable items explicitly flagged: pass — E1/LOCF explicitly flagged as unit-verified NOT e2e-verified ("I did NOT mark these as 'e2e-verified' — they are unit-verified, which is the design intent", `test-report-v1.md:100`); iOS/Android not-tested with risk reasoning; the round-1 FAIL itself is the model of honest flagging — split-attributed `fail (test) / pass (feature)` with the feature proven working via the polyline probe.

- **The standout: proving the round-1 regression is a REAL feature-interaction (not pre-existing) via stash-to-baseline replay, then root-causing it to an EXISTING fragile test locator and pre-verifying the one-line fix.** The new e1RM legend chip renders the exercise name earlier in the DOM, so `progress-page.spec.ts:278`'s fragile `getByText(name).first()` bound to the non-navigable chip instead of the navigable row. The Tester proved causation with a before/after toggle (baseline PASS / feature FAIL, reproduced twice, then stash-pop re-reproduced — `test-report-v1.md:135-142`), traced the root cause to DOM order + the chip's "Toggle <name>" label vs the row's "<name>, view progress" a11y name (the asymmetry that explains why sibling test #8 didn't break), and APPLIED the one-line fix → PASS → reverted, leaving a turnkey recipe with the sibling precedent at `:448`. **This is the textbook "feature is correct, the adjacent test's locator was too loose; here's the proof and the surgical fix" diagnosis.**
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks; the Tester routing entries `transcript.md:47-51,53-57` show self-contained test rounds with no peer reference.)
- Write boundaries: pass — `test-report-v1.md`, `test-report-v2.md`, and the screenshots under `docs/runs/<run-id>/screenshots/` (`e1rm-line-fullpage.png`, `e1rm-section.png`, `e1rm-bodyweight-absent.png`) attributed (Tester's artifact + screenshot extension). The throwaway probe spec was removed before completion (`test-report-v1.md:59` "since removed"). The other-run screenshot PNGs in the baseline diff are pre-existing noise (the round-2 report flags this at `test-report-v2.md:79`). All within boundary.

### Lessons for next run
- **The stash-to-baseline causation proof is the gold standard for "is this failure feature-caused or pre-existing?" — sustain it.** Rather than assert "the e1RM feature broke this test," the Tester ran the failing spec on the feature tree (FAIL), stashed the 5 feature files + confirmed `grep -c E1rmStrengthSection = 0` (baseline, PASS), then stash-popped (FAIL again, re-reproduced). A before/after toggle that reproduces twice converts a hypothesis into HIGH-confidence fact. Sustain this on any failure you attribute to (or exonerate from) the feature.
- **Closing the Reviewer's T-2 gap with a runtime SVG polyline probe was the highest-value verification this run.** The shipped e2e asserts the legend chip but not the data-bearing `<polyline>`; the Reviewer flagged this and the Tester closed it by parsing the actual SVG points and proving the line draws AND trends up. **This is the generalizable lesson for chart/visualization features: assert the rendered output element (polyline/path/bars), not just the legend/controls — and prove the data-shape (trends up as weight increases), not just presence.** Sustain the "probe the SVG, parse the geometry, assert the trend" discipline.
- **The "new section surfaces an entity name in a legend → breaks a sibling's loose `getByText().first()` locator" is a generalizable feature-interaction class worth pre-auditing.** The break was deterministic and DOM-order-driven: any new Progress-page section that renders an exercise/entity name EARLIER than an existing navigable row will rebind that row's bare-text `.first()` locator to the new element. **Recommendation for the playbook/Discovery: when adding a section that surfaces entity names already used as e2e locators elsewhere on the same screen, pre-audit adjacent specs for bare `getByText(name).first()` locators and flag them as at-risk.** This is the one systemic finding from this run (carried into the retro).

### Recurring pattern check
- **Instrumented-probe / substitute-fixture diagnosis is now a 5-run strength.** Every Tester-fail-with-recipe scenario has been carried by a precise runtime probe: canonical (helper-hardening pin) → routine-strong-builder (PostgREST `weight_type` probe + trace-screenshot) → bottom-tab (probe pair capturing `childState` divergence) → bodyweight-volume (substitute-exercise golden-path probe + stash-to-baseline causation proof) → **this run (runtime SVG-polyline probe proving the line trends up + stash-to-baseline causation proof for the adjacent regression + live-catalog seed-name probe)**. The methodology — instrument/substitute/probe, observe precise output, name the exact divergence — is reliably the load-bearing diagnostic step. Sustain.
- **Surgical one-line fix-recipe quality is sustained (5 runs).** This run: `progress-page.spec.ts:278` bare-text `.first()` → role+name locator per the sibling test #8 at `:448`, with the a11y name verified against `exercises-this-week-list.tsx:121` and the chip-collision explained. The Conductor/Implementer applied it with zero ambiguity. Sustain the "1-line diff + sibling/source precedent + confidence" recipe contract.
- **Dev-server OOM cascade did NOT recur this run** — `test-report-v1.md:17` notes "RSS stable, no OOM cascade this run"; `test-report-v2.md:18` notes "~417 MB, no OOM cascade." The cascade broke for a SECOND consecutive run (bodyweight-volume held too). The 3-run OOM pattern (canonical → routine → bottom-tab) has now been quiet for 2 runs. Worth noting the environment held; the partition-and-health-check workaround remains the standing mitigation. Keep watching; do not assume it is permanently resolved.
- **Score honesty sustained across 6 runs (all 5/5 on the Tester rubric).** Output shape (live golden-path-with-geometry-evidence + multi-spec regression audit + cross-platform-flagged + untestable-items-disclosed + causation-proof-for-claimed-regressions + surgical-recipe-on-fail + ran-the-corrected-spec-in-round-2) is stable and reliable. The round-1 FAIL was a genuine, narrow regression honestly surfaced — not inflated, not suppressed — and the feature was proven shippable on its own merits in the same report.

## Run 2026-05-30_0126_bodyweight-volume-per-muscle — 2026-05-30 BRT

Pipeline: feature
Final outcome: pass (round 1 FAIL test-only with surgical recipe + live golden-path proof; round 2 PASS, ran the corrected spec itself)
Score: 5/5

### Rubric
- Golden path tested with command + observed output: pass — round 1 drove the EXACT golden path (a weight=0 bodyweight set producing real per-muscle volume) end-to-end through the live UI when the shipped test couldn't (it seeded a non-existent "Pull-up"): authored a temporary probe spec using "Chin-up" (a real canonical bodyweight row), seeded an 80 kg prior weigh-in + a 4×8 unweighted session, and observed **2,560 kg = 80 × 8 × 4** on the chart + hero + strip simultaneously (`test-report-v1.md:109-131`, screenshot evidence). Round 2 ran the CORRECTED shipped spec itself — `4/4 PASS`, `{"status":"passed","failedTests":[]}` — and did not merely trust the Implementer's claim (`test-report-v2.md:41-59`). The 2,560 kg arithmetic is the load-bearing "feature works" evidence.
- ≥2 edge cases tested with evidence: pass — the F-4 audit walked all 8 volume e2e specs for the two Invariant-A conditions (seeds a measurement? all positive weights?) with a per-spec table (`test-report-v1.md:91-100`), PLUS probed the live DB to prove the 4 no-preferred-name specs land on "Arnold Press (Dumbbell)" (equipment=null, not a bodyweight row) so numbers don't move (`:104`), PLUS the `volume-target.spec.ts:353` deliberate `weight: null` draft-set edge (`:105`). Invariants A/B/C/D each spot-checked (`:135-148`).
- 1-2 adjacent regression checks performed: pass — 36/38 across the 8 volume specs in two batches (`test-report-v1.md:64-71`), with JSON-reporter-to-file to defeat terminal truncation, dev-server health-checked between batches. The 2 failures isolated to `chart-scroll-week-selector` and proven NOT a regression.
- Cross-platform smoke when relevant: pass — web tested via Playwright (Chromium); iOS/Android explicitly flagged "not tested" with reasoning ("RN-Web-compatible only: pure TS kernel helpers, TanStack hooks, PostgREST SELECT widening, `react-native-svg` chart on the same path as `<ProgressChart>`; no native modules. Risk LOW per design R-6" — `test-report-v1.md:152-155`, re-pinned `test-report-v2.md:95-98`). Not silently marked pass.
- Untestable items explicitly flagged: pass — Invariant D (per-exercise e1RM-vs-volume divergence) is explicitly flagged as "PASS on the unit/structural evidence; NOT independently UI-exercised — flagged as a residual coverage gap" (`test-report-v1.md:148`, `:179-180`), NOT silently marked pass. The round-1 FAIL itself is the model of honest flagging: split-attributed as `fail (test) / pass (feature)` with the feature proven working via the substitute-exercise probe.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks; transcript routing entries declare no peer invocations.)
- Write boundaries: pass — `test-report-v1.md`, `test-report-v2.md`, and the screenshots under `docs/runs/<run-id>/screenshots/` attributed (Tester's artifact + screenshot extension). The temporary probe spec was removed before completion (`test-report-v1.md:115` "since removed"). All within boundary.

### Lessons for next run
- **The "prove the feature works with a substitute when the shipped test is broken" move was the single highest-value act of the run — sustain it.** Test 4 errored at seed setup on a non-existent "Pull-up". Rather than report a bare FAIL, the Tester drove the identical golden path with "Chin-up" (a real canonical bodyweight row, also primary "Upper back"), produced the exact 2,560 kg = 80×8×4 surface, and split-attributed the failure as test-data-only. This is what let the Conductor route a NARROW one-token I↔T round instead of a feature-rework round. **Sustain: when a shipped test fails at setup, determine whether the FEATURE is broken or just the TEST DATA — and prove the feature path with a substitute fixture before reporting.**
- **The git-stash-to-baseline causation proof for the 2 chart-scroll failures was rigorous, not assumed — sustain it.** The Tester stashed the entire working-tree change, confirmed `progress/index.tsx` re-imported the OLD `SessionVolumeChartSection`, re-ran the spec on baseline `5a2382b`, reproduced the IDENTICAL 2/4 failure with the identical `5/25` date-label locator, then stash-popped cleanly (`test-report-v1.md:73-83`). That converts "I think these are pre-existing" into "proven pre-existing." Round 2 correctly noted the proof still holds because no source changed (mtime audit). **Sustain the "stash to baseline + reproduce + restore" proof for any failure you claim is pre-existing.**
- **Running the corrected spec yourself in round 2 (not trusting the Implementer's claim) is the right discipline.** `test-report-v2.md:41-59` shows the Tester started a fresh server, ran the spec, and captured `4/4 PASS` + the `.last-run.json` status directly. The mtime audit (`:72`) proving no source file changed since round 1 is a sharp way to bound the regression surface of a test-data-only edit. Sustain "re-run, don't trust" + "bound the change surface."
- **Confidence calibration was crisp — separate labels per finding.** Round 1: HIGH on golden-path works, HIGH on defect-is-test-only, HIGH on the 2 fails being pre-existing, HIGH on Invariant A, MEDIUM on the risk of shipping a red test. Round 2: HIGH/HIGH/HIGH/LOW. Future Testers should mirror the per-finding labelling (`test-report-v1.md:184-189`).

### Recurring pattern check
- **Instrumented-probe / substitute-fixture diagnosis is now a 4-run strength.** Every Tester-fail-with-recipe scenario has been carried by a precise runtime probe: canonical (helper-hardening pin) → routine-strong-builder (PostgREST `weight_type:'number'` probe + trace-screenshot) → bottom-tab (probe pair capturing `childState` divergence) → **this run (substitute-exercise golden-path probe proving 2,560 kg + stash-to-baseline causation proof)**. The methodology — instrument/substitute, observe precise output, name the exact divergence — is reliably the load-bearing diagnostic step. Sustain.
- **Surgical one-line fix-recipe quality is sustained (4 runs).** This run: `weekly-muscle-volume.spec.ts:307` "Pull-up"→"Chin-up" with the sibling-precedent note (every other call in the file uses an existing name) + the DB-verified `Chin-up | muscles[0]="Upper back"` so the assertion holds unchanged (`test-report-v1.md:169-172`). The Conductor/Implementer applied it with zero ambiguity. Sustain the "1-line diff + 1-line sibling/DB precedent + confidence" recipe contract.
- **Dev-server OOM cascade did NOT recur this run** — `test-report-v1.md:15` notes "RSS ~386 MB, no OOM cascade this run." The 3-run OOM pattern (canonical → routine → bottom-tab) broke this run. Worth noting the environment held; if it was the smaller e2e batch sizes or a machine state change, the partition-and-health-check workaround remains the standing mitigation. Keep watching; do not assume it is permanently resolved.
- **Score honesty sustained across 5 runs (all 5/5 on the Tester rubric).** Output shape (live golden-path-with-arithmetic-evidence + multi-spec regression audit + cross-platform-flagged + untestable-items-disclosed + causation-proof-for-pre-existing-fails + surgical-recipe-on-fail) is stable and reliable.

## Run 2026-05-26_0307_bottom-tab-home-link — 2026-05-26 04:45 BRT

Pipeline: feature
Final outcome: pass (round 1 fail w/ instrumented-probe diagnosis; round 2 budget-exhausted w/ surgical Conductor-handed recipe; final 3/3 after Conductor Option-A patch)
Score: 5/5

### Rubric
- Golden path tested with command + observed output: pass — `test-report-v1.md:21-63` documents the spec command, observed URL transitions extracted from the trace zip, and the precise failure line (`page.waitForURL: Timeout 10000ms exceeded.`). Round-2 re-run at `test-report-v2.md:18-37` documents the same deterministic 2/3-pass pattern with the same case 1 failing. Two consecutive runs in each round for confirmation.
- ≥2 edge cases tested with evidence: pass — round 1 ran Edge 1 (cross-tab + browser-back for `backBehavior="history"` invariant), Edge 2 (leaf-tab re-tap no-op), and Edge 3 (click-through nav reinforcing the case-1 failure via instrumented probe). Each with steps / expected / actual / result / evidence. Round 2 re-verified all three plus an added instrumented probe pair (Probe A click-through vs Probe B `page.goto` deep-link) that surfaced the state-shape divergence (`test-report-v2.md:43-69`).
- 1-2 adjacent regression checks performed: pass — round 1 `crud.spec.ts` (6/6) + `auth.spec.ts` (6/7 with 1 pre-existing unrelated supabase-side fail attributed accurately). Round 2 `crud.spec.ts` (6/6 post-restart) + `routines-add-exercise-race.spec.ts` (1/1). The Tester correctly handled the dev-server OOM cascade recurrence (`test-report-v2.md:6`) by restarting and re-running rather than reporting tainted data.
- Cross-platform smoke when relevant: pass — web tested via Playwright; iOS/Android explicitly flagged "not tested" with reasoning (`test-report-v1.md:119-122`, re-pinned `test-report-v2.md:99-102`). "The artifact does not ship in a working state regardless of native correctness" is the right framing for round-1 web-broken — does not pretend native is exercised.
- Untestable items explicitly flagged: pass — round 1's Investigation §2 synchronous-emit assumption was explicitly invalidated by the instrumented probe (`test-report-v1.md:78-90`) with a precise "the framework does not fire `tabPress` for that gesture on web at all" framing. Round 2 self-flagged the click-through-vs-`page.goto` divergence with two equally-defensible interpretations (Read 1 test problem vs Read 2 feature gap) and an honest lean toward Read 2 (`test-report-v2.md:71-79`). Confidence calibrated MEDIUM, not pumped to HIGH. No silent untested-but-marked-pass items.
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — `test-report-v1.md` and `test-report-v2.md` attributed. (`test-report-v2-conductor-patch.md` is Conductor-attributed, not Tester.)

### Lessons for next run
- **Both instrumented-probe diagnoses were exemplary.** Round 1's probe (`test-report-v1.md:65-90`) instrumented the listener with `console.log`, captured "BOTTOM-TAB-PROBE events: 1; logs=[...focused:false, focusedTabName:workout, childPresent:false]", and proved the listener fired ONLY on the cross-tab click (workout→exercises), NOT on focused re-tap. The diagnosis "the bottom-tab bar does NOT emit `tabPress` at all on web for re-tap on a focused tab" was precise, file-line-cited, and reproducible. Round 2's probe pair (Probe A click-through vs Probe B `page.goto` deep-link) at `test-report-v2.md:43-69` is even sharper — it captured the SAME button receiving the SAME re-tap with DIFFERENT `childState` shapes (`childType: "stack", childIndex: 1` vs `childType: undefined, childIndex: undefined`). This is the canonical "two runs of the same code with different inputs produce different observable behaviors → name the divergence point" diagnostic methodology. **Sustain — this is the third consecutive run where instrumented-probe-with-precise-log-output has been the load-bearing diagnostic step.**
- **The "two reads, both defensible" framing** at `test-report-v2.md:71-79` is exactly the right shape when the data supports two equally-correct interpretations. Tester named them, listed evidence, picked one (Read 2 — feature gap), and labeled confidence MEDIUM. Then provided **Option A and Option B with code blocks for each** at `:122-154` — Conductor could pick either without re-deriving. Sustain this "two-option recipe + lean + confidence-per-option" contract for budget-exhausted hand-offs.
- **The "Catch on Option B" disclaimer** at `:148-152` ("`PartialState` has `key` as optional ... probe showed `childKey=no` for the deep-link case. So this option may require also expanding what 'the right child Stack key' means in the dispatch target.") was load-bearing for the Conductor's decision — it prevented a "ship Option B without realizing the dispatch target is undefined" failure. **Sustain that "alternative B has these caveats" disclosure even when recommending alternative A.**
- **Round 2 correctly identified the regression test as catching a NEW divergence, not a regression of round 1.** Round 1's diagnosis: `tabPress` doesn't fire. Round 2's diagnosis: button-level mechanism fires correctly, but the GUARD's `childState.type === "stack"` requirement is unmet for deep-link rehydration. These are distinct failure modes; the Tester correctly traced both with separate probes rather than conflating them. Sustain.
- **Dev-server OOM cascade** flagged again at `test-report-v2.md:6` ("a recurrence of the OOM cascade pattern flagged at `docs/feedback/tester.md:11-13,20-22`"). Third consecutive run where the OOM has appeared. Tester correctly restarted and re-ran rather than reporting tainted data. **Escalation: this is now a 3-run pattern — recommend Conductor/infra retro to investigate root cause (memory leak, RN bundle size, Playwright worker count). Tester's partition-and-restart workaround is reliable but not a fix.**
- **Confidence calibration was crisp on both rounds.** Round 1: "HIGH on the observable failure (two deterministic test runs + an instrumented probe with one tabPress event captured vs. the four expected). MEDIUM on the precise framework-side root cause" (`test-report-v1.md:90`). Round 2: "HIGH on the diagnosis (two probes, three deterministic test runs ... the divergence is exact and reproducible). MEDIUM on this interpretation (the design doc doesn't explicitly include 'deep-link rehydration' as a scenario)" (`test-report-v2.md:69, 79`). Future Testers should mirror this — separate confidence labels for observable failure vs root-cause attribution vs interpretation.
- **The trace-extraction discipline** ("Trace inspection ... → unique URL transitions" at `test-report-v1.md:53-61`) walked the call-by-call URL state from the trace zip — turning an abstract timeout into a precise "after call@31 page.goto deep-link, call@37 re-tap had NO effect" narrative. Sustain that "open the trace and walk the call sequence" reflex.

### Recurring pattern check
- **Instrumented-probe diagnosis methodology** is now a 2-consecutive-run strength. Routine-strong-builder run: round-1 probe captured the line-246 `weight_type: 'number'` runtime shape from PostgREST + round-2 trace-screenshot extraction at `test-report-v2.md:59-64`. This run: round-1 probe captured `tabPress` event count + round-2 probe pair captured `childState` shape divergence between click-through and `page.goto` flows. **Pattern: instrumented runtime probes are the load-bearing diagnostic step in every Tester-fail-with-precise-recipe scenario.** Sustain explicitly — this is now the Tester's most valuable methodology.
- **Surgical fix-recipe quality** is now a 2-consecutive-run strength. Routine-strong-builder run: 1-line diff + 1-line sibling precedent cite + 1-line confidence per recipe. This run: Option A and Option B both with code blocks + sibling precedent + confidence per option + explicit "Catch on Option B" caveats. **Pattern: Tester's hand-off to Conductor when budget-exhausted is now turnkey** — Conductor applied this run's Option A out-of-band with zero ambiguity (same as the routine-strong-builder run). Sustain.
- **Dev-server OOM cascade** — third consecutive run hitting this. Severity now medium-high for Tester productivity. **Strong recommendation: post-pipeline retro to investigate root cause** (memory leak in dev server, RN web bundle size, Playwright worker count, or RN/web devserver lifetime). Tester's "restart-between-chunks" workaround is reliable but the cost is 2-3× the wall-clock for adjacent regression runs.
- **Score honesty across consecutive runs.** Both prior runs scored 5/5 on the Tester rubric; this run also 5/5. The Tester rubric is now stable across 4 runs. Output shape (golden-path-with-trace-extraction + ≥2 edges + adjacent-regression + cross-platform-flagged + untestable-items-disclosed + 2-option-recipe-on-budget-exhaust) is reliable.

## Run 2026-05-26_0101_routine-strong-builder — 2026-05-26 02:58 BRT

Pipeline: feature
Final outcome: pass (round 1 fail w/ surgical fix-recipe; round 2 budget-exhausted w/ surgical Conductor-handed recipe; final 7/7 after Conductor `.first()` patch)
Score: 5/5

### Rubric
- Golden path tested with command + observed output: pass — `test-report-v1.md:63-80` documents the 5-step UI flow with 3 screenshots cited as evidence (`01-routine-builder.png`, `02-live-workout-drafts.png`, `03-live-workout-checking.png`). Volume math verified explicitly: "960 kg = 60×8 + 80×6" — the same screenshot is load-bearing evidence that the feature works (`test-report-v1.md:78`).
- ≥2 edge cases tested with evidence: pass — 7 e2e cases in the spec file, all enumerated with file:line and result in the table at `test-report-v1.md:86-94`: golden, dropset, idempotency (double-tap), soft-delete-re-add, edit-then-restart, hard-fail seed (route fulfill 500), 23505 duplicate-exercise. Round-2 re-verified each case in isolation (`test-report-v2.md:26-36`).
- 1-2 adjacent regression checks performed: pass — 8 adjacent specs at `test-report-v1.md:172-184` (race, auto-fill, rest-timer, verdict, soft-delete leak, remove-exercise, crud, set-row-menu) plus a visual-confirmation cross-reference to the May-26 set-ordering fix. Round-2 partitioned the sweep into 2 chunks of 4 to avoid the dev-server OOM cascade (`test-report-v2.md:118-141`); all 8 passed.
- Cross-platform smoke when relevant: pass — web tested via Playwright; iOS/Android explicitly flagged "not tested. Reason: design touched only RN-Web-compatible code (TanStack hooks, PostgREST mutations, NativeWind layout); no native modules. Risk LOW" (`test-report-v1.md:186-189`, re-pinned in `test-report-v2.md:143-146`). Not silently marked pass.
- Untestable items explicitly flagged: pass — round-1 flagged 1-line test-side bug at line 246 with `pass (feature) / fail (test)` split-attribution (`test-report-v1.md:121`). Round-2 self-flagged that round-1 had mis-marked spec #4 as `pass` (`test-report-v2.md:71-75`) with two candidate explanations ("1-in-5 lucky pass" vs "JSON-aggregate parser misattribution"). Round-2 also disclosed the dev-server OOM cascade as a known environmental issue not a regression (`:107`). No silent untested-but-marked-pass items.
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — `test-report-v1.md`, `test-report-v2.md`, and the 3 screenshots under `docs/runs/<run-id>/screenshots/` — all within the Tester's artifact + screenshot boundary. (`test-report-v2-conductor-patch.md` is Conductor-attributed, not Tester.)

### Lessons for next run
- **Both fix recipes were exceptional in shape**: round 1 cited the exact sibling precedent (`auto-fill-placeholder-on-check.spec.ts:340` `parseFloat(row.weight as string)`) and offered two equivalent fix variants (`test-report-v1.md:136-144`); round 2 cited 8 of 9 sibling specs that use `.first()` with file:line for each (`test-report-v2.md:86-94`). **Sustain that "Recipe = 1-line diff + 1-line sibling precedent cite + 1-line confidence statement" contract — it's what enabled the Conductor to apply the round-2 fix out-of-band with zero ambiguity.**
- **Round-1 PostgREST runtime probe was load-bearing evidence.** `test-report-v1.md:124-130` shows a one-off probe against the live DB confirming `weight: 100, weight_type: 'number'` — this turned "I think it's a string-vs-number issue" into a verified diagnosis with HIGH confidence. **Sustain the discipline of running a probe to verify the runtime data shape before authoring the fix recipe.**
- **Round-2 self-correction on spec #4** (`test-report-v2.md:71-75`) was honest and useful: rather than hide the round-1 misread, Tester surfaced both candidate explanations (lucky pass vs JSON aggregator misattribution) and ran 5 sequential reproductions to compute the failure rate (4/5 = 85%). **That's the right shape of "I previously marked this pass; here's what actually happens" disclosure. Sustain.**
- **Partitioning the sweep around the dev-server OOM cascade** (`test-report-v2.md:118-141`) was exemplary work-around behavior. The 100+ "failed" tests from the cascade would have buried the real signal; running in 2 chunks of 4 with a `curl http://localhost:8081` health check between chunks gives clean per-chunk attribution. **Sustain that "cascade => partition + health-check" reflex; it's the second time it's appeared (first was canonical-exercises run).**
- **Confidence calibration was crisp on both rounds**: round-1 "HIGH on diagnosis, HIGH on fix-recipe, LOW risk on the fix itself (test-only one-liner)" (`test-report-v1.md:226`); round-2 "HIGH on diagnosis (5 reproducible runs + 8-of-9 sibling-spec precedent + trace screenshot), HIGH on fix recipe, LOW risk (test-only)" (`test-report-v2.md:180`). Future Testers should mirror this — separate confidence levels for separate findings.
- **Tester deserves credit for surfacing the diagnosis of round-2 as a NEW finding rather than a regression of round-1.** The line-376 strict-mode locator bug was pre-existing in the round-1 spec (it just happened to lucky-pass in that run); the round-2 report explicitly traces "not introduced by the round-2 fix at line 246" (`test-report-v2.md:55`). That kind of "this isn't what you patched, but it's still broken" disclosure protects the pipeline from blame-shifting.
- **Trace-screenshot evidence at `test-report-v2.md:59-64`** (extracting the trace zip and verbalizing what the trace shows — "Routine page header, Name/Notes form, Exercises section header AND the bottom Exercises tab") turned an abstract strict-mode error into a load-bearing explanation. Sustain that "open the trace and describe the visual state at the failure moment" discipline.

### Recurring pattern check
- **Dev-server OOM cascade on full-suite runs** — flagged at `test-report-v2.md:100-107` ("the same OOM cascade pattern flagged in the canonical-exercises retro" — `docs/feedback/tester.md:11-12,20-21`). Second consecutive run where the full-suite path crashed the dev server and the partitioned-chunk approach was needed. **Severity: medium for tester productivity; recommend escalating to a Conductor/infra retro item** to investigate root cause (memory leak in dev server, RN web bundle size, Playwright worker count) — repeated partitioning is a workaround, not a fix. Sustain the partitioning recipe in the meantime.
- **Exceptional test-recipe quality across consecutive runs** — both canonical-exercises (`docs/feedback/tester.md:22-23` cleanup-debt self-disclosure) and this run (two 1-line surgical fixes with sibling precedents) show consistent strength: round-1 fail saved the ship; round-2 budget-exhausted saved the Conductor's time with a turnkey patch recipe. Pattern is positive; no remediation needed.

## Run 2026-05-25_1921_canonical-exercises — 2026-05-26 00:35 BRT

Pipeline: feature
Final outcome: pass (round 2; round 1 returned fail for a real defect)
Score: 5/5

### Rubric
- Golden path tested with command + observed output: pass — `test-report-v1.md:85-122` documents the 9-step UI flow, observed counts table at `:101-110`, 6 screenshots cited as evidence at `:115-121`. Re-pinned in `test-report-v2.md:156-184` via canonical-exercise-gating dynamic spec.
- ≥2 edge cases tested with evidence: pass — round 1 ran 3 edges (RLS rejection, signup trigger no-op, anon-read pin); round 2 added the helper-hardening pin as edge 3 (`test-report-v2.md:217-225`). Each with steps / expected / actual / result format.
- 1-2 adjacent regression checks performed: pass — `test-report-v1.md:155-161` + `test-report-v2.md:235-244` cover live workout / history / routines / progress / notes / measurements. Round 1 correctly flagged "fail in adjacent specs" with traceability (auto-fill + rest-timer cascade to soft-delete leak); round 2 re-verified each in isolation post-restart.
- Cross-platform smoke when relevant: pass — web tested via Playwright; iOS/Android explicitly flagged as "not tested, reason: design has no native-specific code, chip uses live precedent classes" (`test-report-v1.md:163-167`, `test-report-v2.md:246-250`). Not silently marked pass.
- Untestable items explicitly flagged: pass — round 1 self-flagged "I cannot distinguish here between specs that would have failed on their own and specs that are collateral damage from the crash" (`test-report-v1.md:80`); round 2 flagged the cleanup-debt orphan row (`test-report-v2.md:327`). No silent untested-but-marked-pass items.
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — `test-report-v1.md`, `test-report-v2.md`, and the 6 screenshots under `docs/runs/<run-id>/screenshots/` — all within the Tester's artifact + screenshot boundary.

### Lessons for next run
- **Round 1 `fail` decision saved the ship.** Designer + Validator + Reviewer all missed the 30-hidden-canonical-rows defect because it's not visible from static diff — only from running against a DB with pre-existing user soft-delete history. Tester executed and caught it. That's the pipeline gate working as designed; sustain the discipline of running against actual DB state, not mocked.
- **Confidence calibration was crisp**: "HIGH confidence on diagnosis, HIGH on fix-recipe (1)+(2), MEDIUM on the dev-server-crash causation" (`test-report-v1.md:219`). Future Testers should mirror this — separate confidence levels for separate findings inside the same report.
- **Round 2 partitioning of pre-existing failures vs new failures** (`test-report-v2.md:281-296`, table at `:282-292`) was exemplary — every "unexpected" failure traced to either the round-1-known dev-server OOM cascade or 1 pre-existing minor, with isolation re-run evidence per spec. Without this partitioning, the `pass` decision would not be defensible.
- **Helper-hardening pin contract test** (`test-report-v2.md:39-57`) was a sharp move — pinning the load-bearing contract change in a standalone script before reporting pass. That's the kind of "regression guard for the regression guard" thinking that catches future leaks. Sustain.
- **Cleanup-debt self-disclosure** (`test-report-v2.md:327` — orphan `My Custom Lift 1779754255748` row, auto-mode classifier blocked the delete) was courteous and surfaced a real candidate bug (`ON DELETE cascade` on `exercises.user_id → auth.users.id` may not have fired). That kind of honest "I left this dangling, here's why, here's the hypothesis" trailer is exactly what the post-run retro needs.

### Recurring pattern check
- none — first feedback entry for Tester on this project.
