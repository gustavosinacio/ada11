# Feedback for Fix Designer

## Run 2026-06-01_0941_session-finish-exercise-order — 2026-06-01 11:45 BRT

Pipeline: fix
Final outcome: pass (shipped — fix applied + migration 0019 live)
Score: 7/7

### Rubric
- Scope respected (blockers + majors fixed; minors parked unless tied to root cause): pass — the "## Scope" sections of both v1 (`fix-plan.md:8-16`) and v2 (`fix-plan-v2.md:15-25`) map each in-scope item to its diagnosis severity (2 BLOCKERs + 2 MAJORs), with the MAJOR "interleaved seed" addressed-by-avoidance (the snapshot makes insertion order irrelevant) rather than touched. The owner's gate-added requirement (History edit-mode reorder) is folded in as a tagged `[v2-new]` item on the SAME column, explicitly. The minor (`volume-target.ts` stale comments) is correctly parked under "Out of scope" in both versions. Out-of-scope items (live mid-session reorder persistence, drag-and-drop, read-only chevrons, remove-in-History, auto-backfill, never-logged-id pruning) are each listed with a one-line deferral reason (`fix-plan-v2.md:240-248`).
- I/O contracts exact, or explicit `None`: pass — the "## Contratos de I/O" block (`fix-plan-v2.md:87-185`) is precise to the signature: `finishSession(id, exerciseOrder?: string[]): Promise<SessionRow>` (optional second param, back-compatible, with the explicit "OMIT the key — do not write `undefined`" note so a zero-exercise Finish doesn't clobber the column), `updateSessionExerciseOrder(id, order): Promise<SessionRow>`, the `useFinishSession`/`useUpdateSessionExerciseOrder` mutation input shapes, the `SessionRow.session_exercise_order: string[] | null` row shape, the Drizzle `uuid("...").array()` mapping, and the migration DDL verbatim. The optimistic `onMutate`/`onError`/`onSettled` cache logic is spelled out line-by-line, including the narrow-invalidation rationale (do NOT invalidate stats/progress — order is not a stat input). No hand-waving.
- Risks cover regressions, data integrity, platform divergence, performance: pass — "## Riscos" (`fix-plan-v2.md:215-228`) covers all four: regressions (per `listSetsForSession`-consumer analysis; the shared `<ExerciseBlock>` getting new reorder props in History; the added-but-unlogged-id-persisted edge), data integrity (the migration on the shared live Supabase project named as the principal risk — additive/nullable/no-backfill/reversible `DROP COLUMN`; the no-FK-on-array stale-id tolerance; the no-read-modify-write-race full-array-overwrite note), platform divergence (backend-query-derived, identical web/native via `supabase-js`; the `uuid[]`↔`string[]` serialization flagged as the one platform-adjacent unknown), and performance (Finish write folded into the existing UPDATE; one single-row PK UPDATE per chevron tap, optimistic UI hides latency; O(n log n) sort over a handful of exercises).
- ≥1 alternative considered: pass — "## Alternativas descartadas" (`fix-plan-v2.md:230-238`) discards SEVEN with reasoning: (a) secondary `.order` key (stably-wrong), (b) routine-position-in-history (routine-only), junction table (4 RLS policies + trigger + join read — heavier than warranted), `sets.exercise_order int` column (denormalized N copies → update anomalies), persist-live-override-each-reorder (higher write-frequency, out of scope), the v2 persist-timing alternatives (on-Done / debounce — both need a dirty-flag + dual flush), and drag-and-drop (new gesture library = scope creep). Genuinely reasoned trade-offs, each tied to a code constraint, not a token list.
- Out-of-scope items listed explicitly: pass — `fix-plan-v2.md:240-248`, seven items, each with a one-line reason for deferral. The remove-in-History note is especially sharp: the plan VERIFIED against code that History has no remove control today (correcting the state.md prompt that referenced one), and explicitly does not add it (`fix-plan-v2.md:206-211`).
- Explicit Confidence and Risk labels at the end: pass — "## Confidence / Risk" (`fix-plan-v2.md:281-284`): "Confiança: **ALTA**" with the residual gaps named (`uuid[]` round-trip with a `jsonb` fallback; legacy fallback run-to-run stability with a fallback-only secondary-key option) and "Risco: **MÉDIO**" driven entirely by the shared-Supabase migration, with the code-side risk explicitly called LOW. Correctly calibrated — the migration is what holds it at MEDIUM, and the plan says so.
- `security_relevant` carried forward (or upgraded): pass — `security_relevant: no` is carried forward in the front-matter (`fix-plan-v2.md:3`) AND re-assessed in a dedicated "### Security relevance carry-forward" block (`:286-287`) THAT EXPLICITLY RE-CHECKS THE v2 NEW WRITE PATH against the upgrade triggers (no new endpoint — PostgREST table UPDATE through the existing client; no auth-path change; no credential handling; no untrusted-input — the array is the user's own owned `exercise_id`s, RLS-scoped to `auth.uid() = user_id` for UPDATE as well as SELECT). This is the model behavior: the plan added a new WRITE path and re-ran the security assessment rather than blindly carrying the diagnosis's `no` forward — and designated the Regression Tester's cross-user read+write RLS check as the gate (which then ran and passed).

### Lessons for next run
- **The single highest-value move — and a RESOLUTION of the prior run's watch-item: the plan chose the only fix direction whose data source CAN represent the must-match case, and it cited the prior feedback to do so.** The prior Fix Designer entry flagged the "propose a mechanism whose source can't represent the must-match case" miss (the navbar fix proposed primary AND secondary gates both built on undiscriminating child-Stack state). THIS run's plan explicitly references that lesson — `fix-plan-v2.md:33`: "the exact failure class flagged in the prior Fix Designer feedback (a mechanism whose source can't represent the must-match case)" — and uses it to REJECT directions (a) and (b), because the diagnosis proved their data sources cannot recover the order the user saw for ad-hoc/reordered sessions. Only (c) "persist" has a source that can. **This is the first documented instance of the Fix Designer reading its own prior feedback and applying it as a design pre-flight — the recurring-pattern loop driving cross-run learning on this agent.** Sustain: before committing a fix mechanism, ask "can the data source this mechanism reads actually represent the correct answer for EVERY scenario the diagnosis enumerated?" — and if a prior run flagged a source-can't-represent miss, treat it as a priority pre-flight.
- **The v1→v2 revision turned a tradeoff into a feature, cleanly, without scope sprawl.** v1's honest tradeoff was "legacy sessions can't recover their order (never recorded)." When the owner added the History reorder requirement at the gate, the Fix Designer recognized it reuses the SAME column and mechanism and folds the tradeoff into a manual-recovery path: the first reorder of a legacy NULL session writes its full displayed order into the column (`fix-plan-v2.md:198-204`). It reused the EXISTING `<ExerciseBlock>` chevron props (verified present at `exercise-block.tsx:24-25, 204-227`) and the EXISTING optimistic-mutation pattern (`use-routine-exercises.ts:50-80`) — zero novel UI or cache mechanism. **Sustain: when a gate adds a requirement, check whether it reuses the in-flight change's primitives before designing anything new; the cleanest scope-growth is the one that consumes the existing surface and closes an existing tradeoff.**
- **Strong positive: the persist-timing decision was a first-class design artifact, not an afterthought.** v2 has a dedicated "## Persist-timing decision" section (`fix-plan-v2.md:187-196`) choosing per-tap optimistic persist over debounce/on-Done, with the rationale (simplest correct model, no flush-on-exit edge case, write frequency acceptable, matches the existing auto-commit idiom of `useUpdateSessionName`) AND a concurrency note (each tap sends the full intended array → no read-modify-write race). This is exactly the level of design rigor that lets the Implementer ship without a soft-callback (it did: 0 soft-callbacks). Sustain treating non-obvious behavioral decisions (when to persist, what to invalidate) as named sections with explicit rejected alternatives.

### Recurring pattern check
- **The prior run's watch-item ("propose a mechanism whose source can't represent the must-match case") is RESOLVED this run** — and resolved because the Fix Designer cited the prior feedback verbatim (`fix-plan-v2.md:33`) and used it to reject the two directions that shared that flaw. This is the second fix-pipeline run, the first with prior Fix Designer feedback to read, and the agent demonstrably treated it as a design pre-flight. Score improved 6/7 → 7/7. No recurring FAIL. The standout strength to watch for repetition (positive): rejecting fix directions on the "can this source represent the correct answer for every diagnosed scenario" test, and re-running the security assessment when a revision adds a write path. Sustain.

## Run 2026-05-27_2144_navbar-tab-pop-to-root — 2026-05-27 23:10 BRT

Pipeline: fix
Final outcome: pass
Score: 6/7

### Rubric
- Scope respected (blockers + majors fixed; minors parked unless tied to root cause): pass — the "## Scope" section (`fix-plan.md:6-26`) maps each in-scope item to its diagnosis severity: BLOCKER (`_layout.tsx:78-97` PartialState fallback), MAJOR-scope (`:119` single-button-all-tabs → tab-agnostic `TAB_ROOTS`), MAJOR (`:97` no-op fall-through). The minor (test coverage) is correctly pulled in as "bundled, not a separate defect" because it is tied to the masking gap that let the bug ship. Out-of-scope items (scroll-to-top, header-tap-to-pop, hidden-tab parity, breadcrumb, testability refactor) are parked explicitly (`:135-145`).
- I/O contracts exact, or explicit `None`: pass — "## Contratos de I/O" (`fix-plan.md:56-86`) is precise: `HomeLinkTabBarButton(props: BottomTabBarButtonProps)` signature unchanged, one new module-scope `const TAB_ROOTS: Record<string, Href>` (not exported), no DB/query/UI-prop changes ("None" stated for each). The API-surface verification block (`:63-76`) cites the exact type-def line for every primitive (`router.dismissAll` at `imperative-api.d.ts:60`, `canDismiss` at `:66`, `navigate` at `:37`, `Href` import, `typedRoutes: true` at `app.json:49`). That `typedRoutes` note is load-bearing: it explains *why* the map uses string literals not template interpolation.
- Risks cover regressions, data integrity, platform divergence, performance: pass — "## Riscos" (`fix-plan.md:87-112`) covers all four: regressions (per-path analysis of A/cross-tab/leaf, with the `backBehavior="history"` browser-back invariant named as "the load-bearing invariant"), data integrity (none — no auth/RLS/migration), platform divergence (web-SPA-specific, native not runtime-verified, flagged for manual spot-check), and performance (negligible — module-scope const, one `canDismiss()`+`dismissAll()` per re-tap).
- ≥1 alternative considered: pass — "## Alternativas descartadas" (`fix-plan.md:114-133`) discards four with reasoning: relax `:80` + reuse keyed `popToTop` (no `key` on PartialState → half-fix), `router.replace` (the `historyDelta`/`backBehavior` risk), force-synchronous-hydration (fragile timing), and `router.navigate` as *primary* (demoted to secondary because it routes through the `linkTo`/`routingQueue` the round-1 race fought). Genuinely reasoned trade-offs, not a token list.
- Out-of-scope items listed explicitly: pass — `fix-plan.md:135-145`, five items, each with a one-line reason for deferral.
- Explicit Confidence and Risk labels at the end: pass — "## Confidence / Risk" (`fix-plan.md:173-184`): "Confiança: **MÉDIA**" with the specific gap named (the `dismissAll`-on-PartialState behavioural claim is documented-but-not-statically-provable), and "Risco: **BAIXO**" with justification (single additive `else` branch, no DB/auth surface, `canDismiss()`-gated, `replace` avoided). The MEDIUM was the *correct* calibration — see lessons.
- `security_relevant` carried forward (or upgraded): pass — `security_relevant: no` is carried forward in the front-matter (`fix-plan.md:3`) AND re-justified in the "### Security relevance carry-forward" block (`:186-190`), confirming the fix adds no new endpoint/auth/credential/untrusted-input surface and no upgrade is warranted. Correct — the fix is pure client-side navigation.

### Lessons for next run
- **The single highest-value lesson — and the one criterion miss is implicit here, so read it as the headline:** the plan's MEDIUM confidence and its explicit "Implementer MUST runtime-verify... deviate to the secondary and record it" TODO (`fix-plan.md:77-85`) were *correctly calibrated* — both the primary (`dismissAll`/`canDismiss`) and the literal secondary (`navigate` gated on child-Stack `routes`) failed at runtime (`implementation.md:11-22`). The hedging was honest and it gave the Implementer explicit license to pivot, which is why a runtime surprise did NOT cost a rework round. **BUT the deeper miss: BOTH of the plan's proposed mechanisms were built on inspecting the child Stack's state, and the diagnosis the plan was handed already contained the clue that the child state is `undefined`/partial on the failing paths (`diagnosis.md:18, 41`).** The Implementer's probe found the decisive fact the plan could have reasoned to: on Path C the child state is `undefined` — *indistinguishable from a genuine at-root cross-tab arrival* — so ANY child-Stack-based gate (both the plan's primary and secondary) is fundamentally unsound, and the reliable discriminator is the URL (`useSegments()`), not the navigator state. The plan committed two mechanisms to the same unsound foundation. **Next time, when the diagnosis says the failing-path state is `undefined`/partial, ask first: "can this state even DISCRIMINATE the must-act case from the no-op case?" If the answer is no, the gate must come from a different source (here: the URL) — don't propose a primary AND a secondary that share the same blind spot.** That is the difference between MEDIUM-with-a-named-gap (what was delivered, correctly) and HIGH-with-the-right-mechanism (what a deeper read of the diagnosis enabled).
- **Strong positive: the API-surface verification block is exemplary fix-design discipline.** Every primitive cited to its type-def line, plus the `typedRoutes: true` reasoning that pre-explained why `TAB_ROOTS` needs literals not interpolation (`fix-plan.md:73-76`). This is precisely what stops an Implementer from discovering at typecheck time that a route literal is invalid. Sustain.
- **Strong positive: carrying the prior run's hard-won constraints forward verbatim.** The plan explicitly rejected `router.replace` on the documented `backBehavior="history"` risk (`fix-plan.md:41-45, 121-125`) and preserved the plain-button handler to avoid re-introducing the round-1 `<a>`/`linkTo` race (`:37, 66`). Both constraints came from the diagnosis, which sourced them from the prior run. The Implementer's pivot honored both (`navigate` not `replace`, no `onPress`/`href` reroute), and case 2 stayed green. **Sustain: treat the diagnosis's "constraints the fix must respect" as hard requirements that survive any mechanism pivot.**

### Recurring pattern check
- none — first Fix Designer feedback entry for this project. No prior pattern to flag. The watch-item for future runs: the "propose primary + secondary that share the same unsound foundation" miss (both child-Stack-based when the diagnosis hinted the child state is undiscriminating). Log it as a priority read for the next Fix Designer run so a single-source-of-truth check is applied before committing two mechanisms.
