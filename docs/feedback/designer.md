# Feedback for Designer

## Run 2026-06-03_2217_hard-sets-per-muscle — 2026-06-03 23:30 BRT

Pipeline: feature
Final outcome: pass (shipped — code pending commit+deploy; no migration; D↔V 1 round GO [0 blockers / 1 major / 4 minors], I↔R 1 PASS, I↔T 1 PASS; 0 re-loops)
Score: 6/6

### Rubric
- One responsibility per file change: pass — the 4-row `Mudanças por arquivo` table (`design-v1.md:20-25`) groups by responsibility: the presenter file (extract scaffold + thin tonnage wrapper + new sibling — one responsibility, "the bucketing-by-(muscle×week) presenter layer, now with two metrics"), the section (the toggle + display swap, line-selection unchanged), the unit suite (new `describe` block, existing tonnage block untouched), the e2e (parameterize the seed + two new tests). The "Confirmed NO change" line (`:27`) explicitly fences `stats.ts`/`bodyweight.ts`/`multi-series-chart.tsx`/kernels/migrations out of scope. No unrelated edits merged.
- I/O contracts exact: pass — the full `RowMetric` type with `include`/`contribute`/`needsLoad` (`:36-47`), the `bucketByMuscleWeek` scaffold signature (`:49-57`), the verbatim per-row-seam substitution showing the lazy `needsLoad`-gated load + `metric.include`/`metric.contribute` indirection (`:61-73`), the unchanged tonnage wrapper reproducing the old `:123` guard + `:130` value verbatim (`:80-96`), and the new `presentWeeklyHardSetsByMuscle` sibling that OMITS `measurements` (`:104-119`). The section's state/memo/segmented-control/formatter contracts are exact (`:131-182`). The Validator verified all 23 sub-claims against source with zero false claims (`validation-v1.md:35`).
- Risks cover data integrity, UX regressions, platform divergence, performance: pass — 6 numbered risks (`:209-215`) each with explicit Confidence/Risk: Data integrity (LOW/LOW — no write, `set_type` confirmed in SELECT), Refactor-without-regressing-tonnage / Invariant T (MEDIUM/MEDIUM — the one real risk, named as the top Validator-scrutiny item), UX regressions (LOW/LOW — kg path byte-for-byte + the R-4 reseed edge), Intentional divergence / Invariant D (HIGH-conf/MEDIUM-if-mis-implemented — the naive-accumulator-only trap), Platform (LOW/LOW — integer series render unchanged + web `aria-checked` caveat), Performance (LOW/LOW — sets cheaper than tonnage). Decision-grade.
- ≥1 alternative considered: pass — 7 explicit `Alternativas descartadas` (`:217-224`): the `metric` flag (cannot express the divergent include-predicate + carries dead `measurements`), the fully-independent sibling (90% duplication -> U6 two-site edit), the separate stacked section (loses shared line-selection), compute-both-models, fold-dropsets-into-parent (superseded by the human's working-only lock), persist-the-choice, `toFixed(0)`-vs-`Math.round`. Each with a concrete rejection.
- Stayed in scope; adjacent cleanup parked: pass — Out of scope (`:226-233`) lists removing tonnage, widening the SELECT, feature #2, any migration, kg/lbs conversion on the sets axis, persisting the toggle, and the fold-dropset algorithm. Matches the human-locked scope (`state.md:42-46`) exactly.
- (If round > 1) Responded to every Validator blocker/major: n/a — round 1 only (D↔V closed at GO after one round). MAJ-1 (e2e teeth on an un-precedented SVG-tick surface) was a TEST-spec defect on a `go` design, correctly carried forward to the Implementer/Tester as a must-fix, not back to the Designer.

- **The standout: the Designer parameterized the shared scaffold by INCLUDE-predicate + CONTRIBUTION (not just accumulator), making the intentional tonnage-vs-sets divergence structural rather than emergent — AND made the no-bodyweight-in-sets guarantee structural by OMITTING `measurements` from the sibling signature.** The key design insight (`:7`): "the ONLY thing that differs between tonnage and hard-sets is (a) which rows are included and (b) what each included row contributes" -> a scaffold parameterized by exactly those two closures. This is precisely the right shape because a naive accumulator-only `metric` flag "could not express the divergent include-predicate cleanly" and would have re-introduced the U1 trap by carrying the dead bodyweight machinery into the sets path. The sibling presenter OMITS `measurements` entirely (`:99,:104-109`), so the section CANNOT wire `useMeasurements` into the sets path — the "no bodyweight in sets" guarantee is enforced by the type signature, not by discipline. The Validator confirmed this closes the U1 trap "by signature" (`validation-v1.md:53`). Strong single-round GO with two named load-bearing invariants (T byte-for-byte, S/D divergence) proved by construction.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` blocks; the Designer routing entry `transcript.md:27-31` shows a self-contained design + "No peer invocations".)
- Write boundaries: pass — only `design-v1.md` attributed (baseline `80621ba` resolved; the `design.md` template stub is Conductor scaffolding). No stray `.tmp` this run (contrast the bodyweight-leverage run's `design-v1.md.tmp` slip — that did NOT recur). No source/test files attributed to the Designer.

### Lessons for next run
- **The v1-needs-revision pattern stayed BROKEN (clean first-round GO) AND the e2e-teeth sub-pattern improved markedly: the Designer AUTHORED the e2e with teeth this time, asserting a SPECIFIC value, not a tick count.** The design's e2e test 5 (`:206`) explicitly asserts "a SPECIFIC value that changes with the metric ... not the auto-thinned tick COUNT" and test 6 asserts the divergence "still has teeth" (`:207`). This is a direct application of the prior two GO runs' carry-in lesson (the e1rm settle-gate + the progress-window auto-thinned-label-count traps). The Designer's e2e-plan teeth instinct has clearly internalized the lesson. The residual the Validator caught (MAJ-1) is a level DEEPER than the prior class: the design DID assert a specific value, but it targeted a `react-native-svg` `<SvgText>` y-tick — a surface NO existing spec queries and whose values are fractional-rounded/ambiguous (`Math.round((range/4)*i)` produces "2" twice for 3 sets). So the assertion had the right SHAPE (specific value) but the wrong TARGET (un-precedented + ambiguous DOM surface).
- **Sharpest improvement lever: when an e2e asserts a specific value on a chart, check that the TARGET DOM node is one an existing spec already queries — a value-bearing assertion on a never-queried surface (SVG `<text>`) is the new teeth-trap.** The prior lesson was "assert a specific value, not a thinned count." The Designer applied that. The next-level lesson the MAJ-1 exposes: the SURFACE matters as much as the value. The design should have either (a) reused the regular-`<Text>` card/strip assertion shape that every other numeric e2e in the repo uses, or (b) specified the testID handle the Implementer ultimately added (`weekly-muscle-peak`). The Implementer's Deviation-1 peak caption (`implementation.md:15`) is exactly the right antidote — a stable non-SVG `<Text>` handle with repo precedent (`weekly-strip-scroller`). **Rule for the next chart e2e plan: when you assert a specific numeric value, name the stable DOM handle (an existing-precedent `<Text>` testID), never an SVG `<SvgText>` y-tick — and if no such handle exists, specify that the Implementer adds one rather than leaving the Tester to query SVG text.**

### Recurring pattern check
- **v1-needs-revision — BROKEN this run (first-round GO); the production seam was clean and the single major was test-side, same shape as the two prior GO runs (e1rm, progress-window).** The arc: canonical (1 maj) -> routine (3 maj) -> bottom-tab (1 BLK+2 MAJ) -> bodyweight-volume (2 MAJ) -> e1rm (BROKEN, GO, test-side MAJ-1) -> favorites (REGRESSED, NO-GO) -> bodyweight-leverage (REGRESSED, NO-GO on 2 production-contract majors) -> THIS run (BROKEN, GO, test-side MAJ-1). The pattern remains volatile (GO/regress oscillation), but this run lands on the GO side with a CLEAN production design — the major was the e2e-target depth issue, not a contract defect. Production-design discipline reliably strong; the residual is now the e2e-target (not just e2e-teeth) layer.
- **The "toothless negative/shrink e2e assertion" sub-pattern EVOLVED, not recurred: the Designer fixed the teeth (specific value) but the surface-target (SVG tick) is a NEW deeper variant.** Across e1rm (settle-gate) + progress-window (thinned-count) the fail was "the assertion can't observe the change." This run the assertion CAN observe the change in principle (a specific value), but the TARGET surface is unqueryable/ambiguous on web. This is genuine progress (the teeth-shape lesson landed) with a new adjacent gap (the target-surface choice). Name it: e2e value assertions need both a real value AND a stable, precedented DOM handle.
- **Risk/Confidence labelling + dense rejected-alternatives remain consistent strengths across all 9 runs** (6 risks with per-risk Confidence/Risk, 7 rejected alternatives, each cross-checked by the Validator). Sustain.

## Run 2026-06-03_1402_bodyweight-leverage-factors — 2026-06-03 15:40 BRT

Pipeline: feature
Final outcome: pass (shipped; D↔V 2 rounds — v1 NO-GO [0 blockers / 2 majors / 4 minors], v2 GO [0 / 0 / 2]; I↔R 1 PASS; I↔T 1 PASS; migration 0021 live)
Score: 4/6

### Rubric
- One responsibility per file change: pass — both v1 (`:18-60`) and v2 (`:40-96`) group `Mudanças por arquivo` by responsibility: PURE kernels (the seam + threading reads), ROW-fed consumers, full-`ExerciseRow`-fed + prop-fed consumers, the 4 MAJ-1 screen builders (v2), schema/types/query (the one read-path touch), migration, tests. No unrelated edits merged; the "What is NOT in this change" forbidding clause (`:425-434`) explicitly fences drift.
- I/O contracts exact: pass (v2) — the full `effectiveWeightKg` STRING-aware signature with the parse block (`:101-124`), `WeeklyVolumeRow` type + SELECT (`:131-149`), `SetBodyweightInput`/`ComputeVolumeTargetInput.bodyweight` with REQUIRED `factorByExerciseId` (`:164-179`), the 6-builder BUILD SITES table with per-site factor source (`:191-214`), the prop-chain `presentSetVolumeLines`/`presentExerciseSessionRow`/`makeBwInput` (`:217-260`), `ExerciseRow` (`:262-277`), and the migration 0021 DDL (`:285-353`). The Validator verified every contract line against source with zero false claims (`validation-v2.md:42-49`). **v1's contract was NOT exact** — see the score docking below.
- Risks cover data integrity, UX regressions, platform divergence, performance: pass — v2 has 7 numbered risks (`:392-399`) with explicit Confidence/Risk: R-1 retroactive shift (data integrity, +MIN-4 "newly APPEAR" nuance), R-2 numeric-string-shape (RESOLVED in v2), R-3 NEVER-0 coalesce (catastrophe), R-4 shared-kernel blast radius across all 6 builders, R-5 name-match miss, R-6 platform divergence, R-7 performance. Decision-grade.
- ≥1 alternative considered: pass — v2 has 11 explicit `Alternativas descartadas` (`:401-412`): code map, `default 1.0`, strict-`number` typing (now FORBIDDEN), optional `factorByExerciseId`, `Map<string,number|string>`, `(bw+addedLoad)*factor`, UI write path, auto-apply migration, separate fetch hook, CHECK constraint, separate reclassify migration. Each with a concrete rejection.
- Stayed in scope; adjacent cleanup parked: pass — Out of scope (`:414-423`) lists the UI field, zod/`ExerciseInput`/write path, e1RM, addedLoad scaling, non-bodyweight multiplier, per-exercise editor, CHECK constraint, value research, screenshot noise. Matches the human-locked scope (`state.md:43-49`) exactly.
- (If round > 1) Responded to every Validator blocker/major: pass — the "Response to Validator issues" section (`:436-448`) addresses MAJ-1 (6 builders), MAJ-2 (string contract), MIN-1 (cross-surface STRING test), MIN-2 (fixture list), MIN-3/4 (kept sound), R-5 (kept verbatim). The "Changes from v1" diff-intent table (`:6-19`) + the "Top 1-2 things I most want the Validator to re-check" (`:17-19`) make the v2 re-verification a diff, not a from-scratch trace. v2 cleanly resolved both majors.

- **The standout (v2): the REQUIRED-`factorByExerciseId` decision is the model "make the desync structurally impossible" call (`:185-189`).** Faced with the MAJ-1 6-builder wiring, the Designer chose REQUIRED (not optional-defaulting-to-1.0) for the new map field — reasoning explicitly that an optional field "lets a builder dodge the compile error and silently ship un-leveraged numbers on that surface (the MAJ-1 desync)," whereas REQUIRED "makes any missed builder a compile error — the desync is structurally impossible to ship." The Validator confirmed this is "the correct, stronger choice" (`validation-v2.md:37-38`), and the Implementer leaned on exactly this (`tsc` 0 errors = all 6 wired, `implementation.md:167-170`). This is the right antidote to the v1 miss: turn a wiring-completeness invariant into a compiler-enforced one.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` blocks; both Designer routing entries `transcript.md:28-32` (v1) + `:40-44` (v2) show self-contained design work; `design-v2.md:452` explicitly "No peer invocations were required for this re-design".)
- Write boundaries: pass (with a recorded slip) — `design-v1.md` + `design-v2.md` attributed (baseline `3c85c23` resolved; the `design.md` template stub is Conductor scaffolding). **Recorded slip: the Designer left a stray empty `design-v1.md.tmp` at v1 (a write-boundary slip, flagged `transcript.md:31` + `final-summary.md:31`).** The Conductor removed it (`transcript.md:32`), so no out-of-boundary artifact survives in the evaluation diff — boundaries pass at evaluation time, but the slip is real and worth not repeating (a `.tmp` scratch file written into the run folder is outside the declared `design-v*.md` artifact).

### Lessons for next run
- **The v1 NO-GO is a genuine Designer fail on TWO fronts, both docked — score 4/6, not a clean sweep, on a shipped run.** The PASS outcome did not inflate the rubric. (1) **A FALSE closed-set proof:** v1's "There is no third `SetBodyweightInput` builder" (`design-v1.md:140`) was wrong — there are 6; the Validator's grep proved 4 un-wired builders feed the live workout header, verdict+PR detection, and history-detail (`validation-v1.md:91-107`). The Designer asserted a closed set it had not actually closed (it traced kernel call-sites, conflating them with input-construction sites — the same root as Discovery's miss). (2) **Named the risk, then shipped the risky option:** v1 FLAGGED R-2 ("`numeric` may read back as a string ⇒ silent no-op", `design-v1.md:108-109,303`) and even authored Alternative #3 (the string-aware shape) — then adopted the UNSAFE `number | null` typing as its primary contract anyway and routed the decision to the Implementer as a runtime probe (`validation-v1.md:110`). The Validator's verdict is exact: "the design *names* the risk but *adopts the unsafe option*." Both docked the "I/O contracts exact" (v1 was not) and the closed-set-completeness axes.
- **Sharpest improvement lever, generalizable: if you FLAG a risk (R-N), you must ADOPT the safe option IN THE CONTRACT, not defer it to the Implementer as a guess.** The R-2 anti-pattern is the cleanest teachable failure this run: the Designer did the hard analytical work (identified the string-shape risk, wrote the safe alternative) but then shipped the unsafe option and labelled the safe one "optional / FLAGGED / Recommended if the Implementer verifies." A flagged data-integrity risk with a known-safe alternative and decisive static evidence (5 sibling `numeric` columns all `string|null`, which the Validator surfaced in seconds) is NOT a runtime-probe-deferral — it is a contract decision the Designer must make. **The rule: when you flag R-N AND you have authored the safe alternative AND the codebase can settle it statically, commit the contract to the safe option. Deferring a known-safe choice to a downstream "verify at runtime" is how a feature ships silently-no-op.** (Note: the Tester later found THIS PostgREST client returned a number, not a string — `test-report-v1.md:85` — but that does NOT vindicate v1: the Conductor's own MCP read returned a STRING, `transcript.md:66`, so the string-drop bug WAS live on the read path the design's premise actually covered. The string-aware shape was load-bearing, not merely conservative.)
- **v2 cleanly fixed both majors AND made one structurally un-repeatable (REQUIRED field) — credit the recovery while docking the v1 cause.** The recovery was exemplary (self-contained, diff-intent table, REQUIRED-field anti-desync, string contract on every path, fixture list). The dock is for the v1 round the Designer caused, per the "a shipped feature is the sum of what the gates caught, and a v1 NO-GO on a false proof + a named-then-ignored risk is a real Designer fail" calibration from prior runs.

### Recurring pattern check
- **"Named the risk then shipped the risky option" is a NEW Designer anti-pattern this run, and it is the sharpest one to pre-flight.** Distinct from the prior volatile "v1-needs-revision" pattern: here the Designer's analysis was COMPLETE (it found R-2 and authored the fix) but its DECISION was wrong (shipped the unsafe primary). This is a decision-discipline fail, not an analysis-depth fail — and it is more insidious because the design LOOKS thorough (it names the risk). Flag it explicitly: the next time a Designer flags a data-integrity risk and authors the safe alternative, the Validator/Conductor should check that the CONTRACT adopts the safe option, not the flagged-but-shipped unsafe one.
- **"v1-needs-revision" — the most volatile agent pattern on record — REGRESSED again (v1 NO-GO).** The arc: canonical(1maj)→routine(3maj)→bottom-tab(1blk+2maj)→bodyweight-volume(2maj)→e1rm(BROKEN, GO)→favorites(REGRESSED, NO-GO)→progress-window(BROKEN, GO)→THIS run(REGRESSED, NO-GO on 2 majors). The oscillation continues (GO→regress→GO→regress). Unlike the test-side-only majors of the two BROKEN runs, this run's 2 majors were PRODUCTION-contract defects (false closed set + unsafe typing). The carry-in lesson to make explicit in the top entry: "trace your OWN closed-set claim against a grep before asserting it, AND adopt the safe option for any risk you flag."
- **Risk/Confidence labelling + dense rejected-alternatives remain consistent strengths across all 8 runs** (7 risks, 11 rejected alternatives in v2, each cross-checked). Sustain.

## Run 2026-06-03_1124_progress-chart-window — 2026-06-03 12:30 BRT

Pipeline: feature
Final outcome: pass (shipped; D↔V 1 round GO [0 blockers / 1 major / 4 minors], I↔R 1 PASS, I↔T 1 PASS — every gate green round 1)
Score: 6/6

### Rubric
- One responsibility per file change: pass — the F1–F11 table (`design-v1.md:48-62`) groups by responsibility: F1/F2 PURE presenter row-filter, F3 shared-constant relocation, F4 dedup-only import, F5 new presentational selector (no state), F6 the ONLY state holder + `windowStartMs` compute, F7/F8 thin prop pass-throughs, F9–F11 tests. The "Distinguishing pure vs UI changes" note (`:62`) makes the separation explicit; no unrelated edits merged.
- I/O contracts exact: pass — the full `presentWeeklyVolumeByMuscle` + `presentTopExerciseE1rm` signatures with the new `windowStartMs?: number` param (`:68-98`), the VERBATIM 3-line guard block (`:79-84`), the exported `MAX_VOLUME_WINDOW_LABELS` shape (`:101-105`), the `<ProgressWindowSelector>` prop contract (`:108-115`), the page state+memo wiring (`:117-135`), and the section prop contracts + memo deps (`:139-158`). No hand-waving — the Validator verified all 24 sub-claims against source with zero false claims (`validation-v1.md:40`).
- Risks cover data integrity, UX regressions, platform divergence, performance: pass — 8 numbered risks (`:209-217`): R-1 cross-surface consistency (dual-anchor data integrity), R-2 both-loops axis bug, R-3 empty-window trap (UX), R-4 visibility re-seed (UX regression), R-5 platform divergence (iOS/Android/web), R-6 default-seed-before-prefs-load, R-7 performance, R-8 working-tree-noise (process). Each with explicit Confidence/Risk labels.
- ≥1 alternative considered: pass — 8 explicit rejected alternatives (`:219-227`): section-level pre-filter, `weeks`+`now` contract B, per-chart control, write-back, calendar picker, chart-tuned option set, `useEffect` re-sync bind, empty-window-renders-chart-frame. Each with a concrete rejection reason; the Validator cross-checked and agreed none was dismissed incorrectly (`validation-v1.md:89`).
- Stayed in scope; adjacent cleanup parked: pass — Out of scope (`:231-238`) lists the per-exercise screen, the 8-bar strip, Profile write-back, migrations/schema/API, new dependency, Profile label/copy (U8), and the pre-existing cache-buster noise. Matches the human-locked scope (`state.md:46-51`) exactly.
- (If round > 1) Responded to every Validator blocker/major: n/a — round 1 only (D↔V closed at GO after one round). MAJ-1 (e2e shrink-assertion teeth) was a TEST-spec defect on a `go` design, correctly carried forward to the Implementer/Tester as a must-fix, not back to the Designer.

- **The standout: the Designer caught the "both-loops" subtlety Discovery under-specified, and named it as a first-class section.** Discovery said "filter at the top of the row loop" (`discovery.md:101`); the Designer recognized each presenter loops `rows` TWICE — once for `earliestMs` (axis left edge) and once to bucket/aggregate — and that guarding only the bucket loop would leave the axis starting at a pre-window week with a dead flat lead-in (`design-v1.md:28-33`, R-2). It pinned all four loop heads with file:line. The Validator confirmed this was a correct catch the Discovery phrasing under-specified (`validation-v1.md:40`), and the Implementer/Reviewer/Tester all verified the guard landed in all four heads. This is the design's sharpest insight and it pre-empted R-2's MEDIUM-risk axis bug by construction.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks; the Designer routing entry `transcript.md:28-32` shows a self-contained design + "No peer invocations".)
- Write boundaries: pass — only `design-v1.md` attributed (baseline `25db98b` resolved; the `design.md` template stub is Conductor scaffolding; the 5 cache-buster files are pre-existing noise the design explicitly does not touch, R-8 `:217`).

### Lessons for next run
- **"Name the load-bearing invariant + prove it by construction" (Invariant W) is the proven antidote to the v1-needs-revision pattern, applied proactively in v1 again — sustain it.** The Designer made Invariant W ("`windowStartMs===undefined` ⇒ byte-for-byte today's output") a first-class section (`:25-26`), proved it by construction ("the only new code is the skipped guard"), and pinned it with a W-0 deepEqual unit case. This is the same "name the invariant + forbid the drift" shape that broke the v1-needs-revision pattern on the e1rm run, applied here on the FIRST submission. The Validator's verification became a diff against a stated invariant rather than a from-scratch trace, which is why D↔V closed in one round. Sustain naming the load-bearing no-change invariant + its deepEqual anchor for any "add an optional param that must reproduce old output when absent" feature.
- **Choosing contract A (one pre-computed `windowStartMs` threaded from the page) over contract B (each section computes its own) on a stated desync risk is the model "pick the contract that removes a class of bug" call.** The Designer reasoned that two `new Date()` captures (contract B) could desync the two charts across a Monday rollover (`:18-23`) and chose the single-source contract A, matching the existing `bestWeekKg` page-threading idiom. Sustain: when two contracts differ only in where a clock/derived value is captured, prefer the one that captures it ONCE and name the desync bug the other admits.
- **Sharpest improvement lever (the one gap on a clean run): MAJ-1 was a Designer-authored test-spec defect — the e2e shrink assertion had no teeth.** The design's F11 step-3 ("assert x-axis label COUNT drops") would have shipped a false-green because `<MultiSeriesChart>` thins labels to ~5 ticks regardless of axis span (`validation-v1.md:51-58`). This is the SECOND consecutive run where the Designer's PRODUCTION seam was clean but the e2e PLAN carried a teeth-less negative assertion (e1rm-strength MAJ-1 was the cold `toHaveCount(0)` with no settle-gate). **The discipline to carry into the next chart/shrink feature: when the e2e plan asserts a NEGATIVE/shrink signal, verify the signal actually CHANGES with the data — count assertions on auto-thinned/auto-laid-out UI are the recurring trap. Assert a specific old-only label TEXT or legend chip that provably disappears, with a settle-gate.** The Designer knows this for production algorithms (Invariant W + W-0); the gap is applying the same "does the assertion have teeth?" rigor to the e2e plan it authors.

### Recurring pattern check
- **v1-needs-revision — BROKEN this run (first-round GO), but with the SAME residual sub-pattern as the e1rm run: a Designer-authored toothless e2e shrink assertion (MAJ-1).** The arc is now: canonical (1 maj) → routine (3 maj) → bottom-tab (1 BLK+2 MAJ) → bodyweight-volume (2 MAJ+incomplete v2) → e1rm-strength-chart (BROKEN, only a test-side MAJ-1 settle-gate) → favorite-exercises (REGRESSED, v1 NO-GO on a central-algorithm self-contradiction) → THIS run (BROKEN, GO, only a test-side MAJ-1 assertion-teeth). The PRODUCTION design was clean both BROKEN runs; the single major both times was the e2e plan's negative assertion. The pattern is the most VOLATILE on record (GO→regress→GO), and the e2e-assertion-teeth sub-pattern has now recurred across the two GO runs — this is the specific thing to pre-flight next (see the sharpest-lever lesson above). Net: production-design discipline is reliably strong; e2e-plan assertion rigor is the residual.
- **Risk/Confidence labelling + dense rejected-alternatives remain consistent strengths across all 7 runs.** 8 risks with per-risk labels, 8 rejected alternatives, each cross-checked by the Validator. Sustain.

## Run 2026-06-01_1301_favorite-exercises-e1rm — 2026-06-01 15:10 BRT

Pipeline: feature
Final outcome: pass (shipped; D↔V 2 rounds — v1 NO-GO on MAJ-1, v2 GO; I↔R 1 PASS; I↔T 2 rounds, round 1 FAIL test-only)
Score: 5/6

### Rubric
- One responsibility per file change: pass — the `Mudanças por arquivo` table (both v1 `:22-35` and v2 `:43-56`) groups changes by responsibility: migration (table+RLS), schema (Drizzle mirror), types, API (INSERT/DELETE), hook (optimistic toggle), `progress.tsx` (the favorite affordance), `e1rm-strength.ts` (the plotted-set decision), section (wire + palette), + the 3 test files. No unrelated edits merged; the `NOT touched` regression-surface list (`:58`) is explicit.
- I/O contracts exact: pass — the full `presentTopExerciseE1rm` signature with the new `favoriteExerciseIds?: ReadonlySet<string>` arg (`design-v2.md:162-168`), the union-then-cap pseudo-code (`:66-113`), the API contract with exact SQLSTATE handling (`:175-195`), the hook's `onMutate`/`onError`/`onSettled` shape (`:202-225`), the section wiring + the 12-hex palette with per-rank comments (`:246-264`), the `progress.tsx` headerRight JSX (`:282-307`), the migration DDL (`:328-370`), the Drizzle mirror (`:377-395`), and the `UserExerciseFavoriteRow` type (`:318-324`). No hand-waving — the Validator verified every contract line against source.
- Risks cover data integrity, UX regressions, platform divergence, performance: pass — 9 numbered risks in v2 (`:411-422`): RLS data-integrity, FK CASCADE divergence, the no-favorites byte-for-byte regression (Invariant F), the favoriting-a-top-N-no-op UX (the v1 no-go now fixed), palette-color regression, header-right layout, platform web-e2e a11y, performance (no extra scan), locator collision, cache staleness. Each with explicit Confidence/Risk labels.
- ≥1 alternative considered: pass — 14 explicit `Alternativas descartadas` in v2 (`:424-439`): union-in-section, the v1 non-favorite-pool slice (the rejected MAJ-1 algorithm), favorites-pinned-first, wrapping-8-palette, unbounded-palette, smaller-ceiling, soft-delete, 23505-loop, `.upsert`, inline mount, FK RESTRICT, per-exercise cache key, refresh-fanout, separate component file. Dense and decision-grade.
- Stayed in scope; adjacent cleanup parked: pass — `Out of scope` (`:441-451`) lists the separate favorites screen, favoriting from other surfaces, volume/muscle-chart impact, reordering, the refresh-fanout (Alt 13, parked), the note-slot machinery, Phase-2a deferred items, the component-file (Alt 14), and the unbounded palette (Alt 5). Matches the prompt's OUT OF SCOPE exactly.
- (If round > 1) Responded to every Validator blocker/major: pass (for v2) — the `Diff vs v1` lead table (`design-v2.md:5-14`) maps MAJ-1 + all 5 minors to a resolution pointer, and the `Resposta a issues do Validator` section (`:511-518`) closes each one; v2 re-derived MAJ-1 from scratch to top-N-overall ∪ favorites, aligned §A case 2, and added the explicit "do NOT slice from a non-favorite pool" prohibition to the "What is NOT" clause. The Validator confirmed every resolution against real source (`validation-v2.md:28-69`).

- **The v2 recovery was a model fix — but the v1 self-contradiction is the scored fail (see below).** v2's `autoTopOverall = sorted.slice(0, topN)` ∪ `extraFavorites (excluding autoIds)` is byte-for-byte today's selection for the auto pick, makes favoriting a top-N exercise a verified no-op, and Invariant F becomes the literal `sorted.slice(0, topN)` — strictly more obvious than v1. The Designer also pre-empted the Pencil-label residual by re-reading `progress.tsx:112` (it IS `"Edit exercise"`) and the worked-traces section (`:118-121`) pinned each cap branch to a unit case. The v2 ordering decision (top-N-overall first to keep existing lines' ranks/colors stable, `:131-140`) shows real UX judgment.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks in `transcript.md`; the Designer routing entries `transcript.md:23-26,33-36` show self-contained designs with no peer reference.)
- Write boundaries: pass — only `design-v1.md` + `design-v2.md` attributed (baseline `5a0b86e` resolved; diff bucketed by the path map; the `design.md` template stub is Conductor scaffolding).

### Lessons for next run
- **Trace the algorithm against its OWN pinned unit case before submitting v1.** MAJ-1 was not subtle: design-v1's pseudo-code (`autoTop = nonFavorites.slice(0, topN)`, `:55-57`) produces N+1 series when a top-N exercise is favorited, while the SAME design's §A unit case 2 (`design-v1.md:376`) asserts the count stays N. A single mental trace of "favorite C (a top-3) under my own algorithm → what count?" against "what does case 2 assert?" would have caught the contradiction at authoring time, before the no-go round. **Add to the design discipline: for every algorithm with a pinned unit case, hand-trace the algorithm through that exact case and confirm the assertion holds — the pinned test is a spec you must satisfy, not decoration.**
- **The "deduped by id / no overlap possible" comment (MIN-1) was a tell.** The v1 comment claimed the union was "deduped by id (a favorite already in autoTop is NOT duplicated)" — but under the v1 algorithm a favorite can NEVER be in autoTop (it's drawn from the non-favorite pool), so the dedup wording described an impossible case and MASKED the real effect (the favorite was REMOVED from the auto pool, promoting a non-favorite). **When a comment describes a guard against a case the code structurally can't reach, that is a signal the mental model and the code have diverged — stop and re-derive.** v2's rewrite ("the overlap was never built") is the honest description.
- **The v2 disciplines were all correct — carry them into v1 next time.** The `Diff vs v1` resolution table, the worked-traces tied to unit cases, the "What is NOT" prohibition naming the rejected v1 algorithm, the ordering-decision justification, and the standalone-document framing are exactly what a strong v1 should already contain for a central-algorithm feature. The Designer knows how to do this (v2 proves it); the gap is doing it in v1.

### Recurring pattern check
- **v1-needs-revision — REGRESSED to a fail this run after breaking last run.** The arc: canonical (1 major) → routine (3 majors) → bottom-tab (1 BLK+2 MAJ) → bodyweight-volume (2 MAJ + incomplete v2) → e1rm-strength-chart (BROKEN — first-round GO, only a test-side major) → THIS run (REGRESSED — v1 NO-GO on a central-algorithm self-contradiction). The prior entry explicitly warned "watch whether the discipline holds on the next non-sibling feature" — this WAS a mirror-then-diverge sibling feature (the strongest case for the discipline), and v1 still shipped a self-contradiction. **The specific recurrence is the SAME root the prior two entries named: a pinned contract and a description of the same algorithm that disagree — last run it was self-contradictory LOCF prose (MIN-1, cost nothing); this run it was algorithm-vs-pinned-unit-case (MAJ-1, cost a no-go round). The drift moved from prose into the algorithm itself, which is worse.** Treat "trace the algorithm against its pinned test" as a priority pre-flight on the next algorithm feature.
- **Risk/Confidence labelling + dense rejected-alternatives remain consistent strengths across all 6 runs.** 9 risks with per-risk labels, 14 rejected alternatives in v2. Sustain — Validators cross-check the rejection reasoning every run.

## Run 2026-05-30_2006_e1rm-strength-chart — 2026-05-30 21:30 BRT

Pipeline: feature
Final outcome: pass (shipped; D↔V GO round 1, I↔R PASS round 1, I↔T 2 rounds — round 1 FAIL test-only)
Score: 6/6

### Rubric
- One responsibility per file change: pass — the §3 file-by-file table (`design-v1.md:117-123`) groups changes by responsibility: F1 presenter (the data model), F2 section (the UX), F3 mount (1 import + 1 JSX line + a stale-docstring refresh, explicitly justified as "part of registering the section honestly"), F4 unit tests, F5 e2e. No unrelated edits merged; the docstring fix is scoped to the mount responsibility.
- I/O contracts exact: pass — the full `presentTopExerciseE1rm` signature + `E1rmSeries`/`E1rmStrengthModel` types + 7-step canonical semantics (`design-v1.md:148-208`), the `<E1rmStrengthSection>` component contract with exact hook set and `chartSeries` mapping (`:217-246`), the `E1RM_PALETTE` array + `colorForRank` (`:53-66`), and the exact `<MultiSeriesChart>` call (`:236-245`). No hand-waving — the Validator could verify every contract line against source.
- Risks cover data integrity, UX regressions, platform divergence, performance: pass — 9 numbered risks (`design-v1.md:289-299`): R-1 Invariant-D data integrity (bodyweight resurrecting as e1RM), R-2 metric-semantics (`+=` vs MAX), R-3 UX (zero-fill crashing lines), R-4 UX slot starvation, R-5 platform divergence (RN/RN-Web SVG parity + `aria-checked` drop), R-6 performance (extra pass over lifetime rows), R-7 UX duplicate names, R-8 week-bucketing correctness, R-9 live-update. Each with explicit Confidence/Risk labels.
- ≥1 alternative considered: pass — every Decision (#1–#9) carries an explicit "Rejected" subsection: per-session axis, raw-set-count ranking, equipment-based exclusion, hashed colors, extending `<MultiSeriesChart>` for gaps/non-zero-y-min, combined wrapper component, zero-fill, drop-untrained-weeks, "Not enough data" placeholder, header copy alternatives, and the `bestE1rmFromSets` shared-helper extraction (the scope-creep guard). Dense and decision-grade.
- Stayed in scope; adjacent cleanup parked: pass — §7 Out of scope (`design-v1.md:303-311`) lists favorites (Phase 2b), leverage factors, secondary-muscle attribution, hard-sets/week, `<MultiSeriesChart>` extension (gaps + non-zero y-min), refactoring `progress.tsx`'s combined reduce, per-session axis, persisting selection, `max_volume_window_weeks`, strength-decay modeling. The decision NOT to refactor `progress.tsx` (§3, `:136-140`) is the explicit scope-creep guard, stated so the Implementer doesn't over-extract.
- (If round > 1) Responded to every Validator blocker/major: n/a — round 1 only (D↔V closed at GO after one round). The Validator's MAJ-1 (e2e false-green) was carried forward to the Implementer as a must-fix, not back to the Designer — correct routing for a test-robustness defect on a `go` design.

- **Standout call: LOCF (carry-forward) for untrained weeks instead of zero-fill (Decision #7a, `design-v1.md:81-94`).** This is the design's sharpest insight and the Validator confirmed it sound against source. The Designer recognized that the muscle chart's zero-fill (honest for VOLUME — a rest week IS zero volume) would be SEMANTICALLY FALSE for e1RM (a peak metric — you don't get weaker to zero on a rest week) AND would crash every line to the x-axis on `<MultiSeriesChart>` (which renders every value as a polyline vertex + dot, y-min pinned to 0, no gap support — `multi-series-chart.tsx:134-160,81`). The fix (carry the last observed value forward; backfill leading weeks with the first real value) keeps the array fully numeric without touching the shared component. Two equally strong supporting calls: eligibility-before-ranking (Decision #2 — a bodyweight-only high-frequency exercise can't consume a top-N slot for an invisible line, mitigating R-4 by construction) and the explicit "What is NOT in this algorithm" guard (`:210-215`) that forbids `effectiveWeightKg`/`measurements`/`+=`/zero-fill — directly pre-empting the prior run's pseudo-code-contradiction failure class.
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks; the Designer routing entry `transcript.md:23-26` shows a self-contained design with no peer reference.)
- Write boundaries: pass — only `design-v1.md` attributed (baseline `3c00d8e` resolved; diff bucketed by path map; the `design.md` template stub is Conductor scaffolding).

### Lessons for next run
- **"State the divergence + add a What-is-NOT block" is now the proven antidote to the v1-needs-revision pattern.** Two correctness-critical invariants (D = logged weight; E1 = MAX not sum) each got an explicit named invariant AND a forbidding clause in the `What is NOT in this algorithm` footer (`:210-215`). The Validator found this "structurally precludes a copy-paste of the volume presenter" (`validation-v1.md:43`) — exactly the property that turned a potential no-go into a GO. This is the v2-recovery shape from the prior two runs (the `design-v2.md:711` "What is NOT" footer) APPLIED PROACTIVELY in v1. Sustain it as a default footer for any multi-step algorithm that diverges from a sibling it's mirroring.
- **The two-phase "mirror the sibling 1:1, then name the single material divergence" structure is the right way to design a sibling feature.** The design states up front (`:12-25`) that it mirrors the Phase-1 muscle chart 1:1 and then enumerates exactly where it diverges (MAX-not-sum, logged-weight-only, LOCF-not-zero-fill, palette-by-index-not-keyed-record). That made the Validator's job a diff against a known-good sibling rather than a from-scratch verification, and is why D↔V closed in one round. Sustain.
- **One residual prose smell the Validator caught (MIN-1, `design-v1.md:86-89`): the leading-edge LOCF prose negated itself mid-clause.** "carrying the *first* observed value backward is NOT done — instead, leading untrained weeks … carry the **first** observed value forward only from the first real week onward" is hard to parse and self-contradictory as written. The §4-step-6 contract (`:204-206`) and unit case #6 were unambiguous, so it cost nothing (MIN-1, fold-in), but it is the SAME shape as the prior run's MAJ-1 dead-map contradiction in miniature. **When you have BOTH a prose description AND a pinned contract for the same algorithm, make the prose defer to the contract (or delete it) — do not write a second, looser natural-language version that can drift.** This is the one place the otherwise-exemplary design slipped.

### Recurring pattern check
- **v1-needs-revision — the 4-run fail pattern is BROKEN this run (v1 got a clean GO, 0 blockers, 1 major that was a TEST-robustness issue carried to the Implementer, not a design-correctness no-go).** Prior arc: canonical (1 major) → routine (3 majors) → bottom-tab (1 BLK + 2 MAJ) → bodyweight-volume (2 MAJ + an incomplete v2 recovery). This run the Designer pre-applied the v2-recovery disciplines (named invariants + "What is NOT" guard + mirror-then-diverge structure) IN v1, and the Validator's only major was MAJ-1 (a cold `toHaveCount(0)` with no settle-gate in the e2e plan) — a test-robustness gap on a design whose production algorithm was confirmed sound against source. Per the decision rule (0 blockers + 1 major → go), this is a first-round GO. **The v1→v2 goal the prior entries kept naming ("submit v1, expect 0 design-correctness majors") was hit this run.** Note the nuance for honesty: MAJ-1 IS a real major the Designer authored (the e2e plan needed a settle-gate), so the round was not literally "0 majors" — but it was a GO, the major was test-side and recoverable downstream, and the production design needed no revision. Watch whether the discipline holds on the next non-sibling (greenfield) feature, where there's no known-good sibling to mirror and the "name the divergence" crutch isn't available.
- **Risk/Confidence labelling + dense rejected-alternatives remain consistent strengths across all 5 runs.** R-1..R-9 with per-risk Confidence/Risk labels; every Decision carries a Rejected subsection. Sustain — it has caught hidden trade-offs every run.

## Run 2026-05-30_0126_bodyweight-volume-per-muscle — 2026-05-30 BRT

Pipeline: feature
Final outcome: pass (shipped after 2 D↔V rounds; round 1 NO-GO with 2 majors)
Score: 5/6

### Rubric
- One responsibility per file change: pass — both v1 and v2 group changes by responsibility in `Mudanças por arquivo` tables (new `bodyweight.ts` helper; `stats.ts` SELECT widening; per-file kernel-routing edits; Phase-1 chart files; tests). No unrelated edits merged. v2's MAJ-1 + MAJ-2 fixes are scoped to the exact two screens, not bundled with cosmetic churn.
- I/O contracts exact: pass — `effectiveWeightKg(equipment, weight, bodyweightKg)` + `bodyweightKgAsOf(measurements, instantMs)` signatures, the `SetBodyweightInput` / `WeeklyBodyweightInput` types, the widened `WeeklyVolumeRow` shape (`exercises: { equipment }`), and the per-exercise two-variable split (`const w` for e1RM, `const effW` for volume) are all spelled out at the code level in `Contratos de I/O` (design-v2 `:111-262`). No hand-waving.
- Risks cover data integrity, UX regressions, platform divergence, performance: pass — 8 numbered risks (R-1..R-8) in v1, carried into v2: data-integrity (`!inner` row-preservation R-1; retroactive PR/max shift R-2), UX (`currentWeightKg`/`repsToBeat` for bodyweight R-3; per-session chart removal R-4; v2's R-4 covers the week-headline shift), platform (`<MultiSeriesChart>` RN-Web parity R-5), performance (`bodyweightKgAsOf` O(M) memoised per session R-6), correctness (admin non-bodyweight R-7; UTC-instant timezone R-8). Each carries explicit Confidence/Risk labels (e.g. R-2 HIGH/MEDIUM).
- ≥1 alternative considered: pass — 8 explicit alternatives rejected in `Alternativas descartadas` (server-side bodyweight resolution; single flat `bodyweightKg`; extend `<ProgressChart>`; exclude "Other"; prior-only-else-0 fallback; full kernel extraction; chart honors window pref; bodyweight-aware admin totals). v2 adds the rejected v1-single-`const w` (MAJ-2) and rejected leave-`weekVolumeKg`-inline (MAJ-1).
- Stayed in scope; adjacent cleanup parked: pass — `Out of scope` (design-v2 `:496-503`) lists leverage factors, secondary-muscle attribution, e1RM strength chart, dose metric, chart-local window selector, bodyweight-history backfill, bodyweight-aware admin totals, and extracting inline reduces into named functions — all explicitly punted to `features.md "## Open"`. The decision NOT to extract the inline kernels into named functions (just adopt the helper) is the right scope-creep guard, stated explicitly (Decision #9).
- (If round > 1) Responded to every Validator blocker/major: **fail (in spirit, with the twin-miss).** v2 fully resolved BOTH v1 majors (MAJ-1 `weekVolumeKg` 14th site + re-grep exhaustiveness proof; MAJ-2 two-variable e1RM/volume split) and folded all 5 v1 minors — the Validator confirmed each resolved against real code (`validation-v2.md:9-27, 73-79`). BUT v2 introduced MAJ-3-NEW by completing MAJ-1 INCOMPLETELY: the design wired `{ measurements }` into `groupSessionVolumes` for the week-drill-down (`history/week/[isoWeek].tsx`) but missed the History-LIST `groupSessionVolumes` caller (`history/index.tsx:22-25`) — the OTHER call site of the very same function the design was already modifying (`validation-v2.md:62`). The Validator's grep showed `groupSessionVolumes` has exactly two callers; the design touched one. This is the exact-twin of MAJ-1 (a `WeeklyVolumeRow`-driven `groupSessionVolumes` consumer left un-wired), so it is fairly scored as an incomplete response to the MAJ-1 class — not a brand-new unrelated finding. Docking 1 point for "fixed the kernel function but did not wire all its call sites."
- Audit trail: n/a — no peer invocations attributable to this agent. (0 `peer_invocation:` YAML blocks; transcript routing entries declare no peer invocations.)
- Write boundaries: pass — only `design-v1.md` and `design-v2.md` attributed (baseline diff bucketed by path map; the design.md template stub is Conductor scaffolding).

### Lessons for next run
- **When you fix a kernel function, wire ALL its call sites in the same pass — grep for them.** MAJ-3-NEW happened because the design made `groupSessionVolumes` bodyweight-aware and wired the week-drill-down caller, but `groupSessionVolumes` had TWO callers and the History-list one (`history/index.tsx:23`) was left raw. A single `grep -rn "groupSessionVolumes(" app/ src/` while writing the MAJ-1 fix would have surfaced both callers. **Add to the design discipline: for every kernel/helper you change the signature or behavior of, grep its call sites and put EVERY caller in the wiring table — a function-level fix is not done until all call sites are accounted for.** This is the same "close the set" lesson Discovery owes; the Designer is the second line of defense and should run the grep even if Discovery's inventory looks complete.
- **The v2 "Diff vs v1" lead table + the re-grep exhaustiveness proof were exemplary — sustain both.** The `Diff vs v1` table (design-v2 `:5-14`) listed every Validator issue with a resolution pointer, and the re-grep proof (design-v2 `:443-451`, hit→inventory-row mapping + "no 15th site") let the Validator and Reviewer cross-check the kernel set independently. This is exactly the closed-set discipline that should be applied to call sites too, not just kernel definitions.
- **The two-variable split (MAJ-2 fix) is a model of "state the divergence explicitly."** Invariant D ("a 0-weight bodyweight set produces a VOLUME point but NO e1RM point; for non-bodyweight `effW === w` so both guards fire identically") told the Implementer, Reviewer, and Tester precisely where the e1RM and volume guards intentionally diverge. Sustain that "name the invariant + name where it diverges" discipline whenever one variable was doing two jobs.
- **The optional-param "absent = byte-for-byte" seam was the right architectural call** — threading the bodyweight input as an optional param (mirroring the existing `windowStartMs?` precedent) made Invariant A provable by construction. The Validator verified the byte-identity across `null`/`""`/`"0"`/negative weight edge cases (`validation-v1.md:11`). Sustain this "new optional dependency that reproduces old numbers when absent" pattern for any retroactive-math change.

### Recurring pattern check
- **v1-needs-revision is now a 4-run pattern (canonical 1 major → routine 3 majors → bottom-tab 1 BLK+2 MAJ → this run 2 MAJ).** Designer's v1 has needed a Validator catch on a non-trivial issue in all 4 consecutive runs. The shape differs (test-contract pinning; pseudo-code contradiction + DB invariant; framework-internals + API misuse; **kernel-inventory completeness + e1RM single-variable contradiction**), but the common root holds: v1 trusts the surface read and skips one layer of verification that v2 surfaces. THIS run adds a sharper sub-pattern: **even v2's recovery was incomplete on the SAME completeness axis** — v2 fixed the missed 14th kernel but left a missed 2nd call site (MAJ-3-NEW). **Recommendation: the v1→v2 goal is "0 majors in v2," and the way to hit it for a fan-out feature is the closed-set grep applied to BOTH kernel definitions AND their call sites, before submitting.** The Validator's round-2 grep is the proof shape to internalize and run pre-emptively.
- **Risk/Confidence labelling + dense alternatives remain consistent strengths across all 4 runs.** R-1..R-8 with per-risk Confidence/Risk labels, 8+ rejected alternatives every run. Sustain — Validators and Reviewers cross-check rejection reasoning, and it has caught hidden trade-offs every run.

## Run 2026-05-26_0307_bottom-tab-home-link — 2026-05-26 04:42 BRT

Pipeline: feature
Final outcome: pass (after 2 D↔V rounds + 2 I↔T rounds + Conductor out-of-band patch)
Score: 3/6

### Rubric
- One responsibility per file change: pass — `design-v2.md:115-122` table groups changes by responsibility (1 layout edit + 1 new e2e spec); no unrelated edits merged.
- I/O contracts exact: pass — exact JSX for the `screenListeners` block (`design-v2.md:128-185`), exact type contracts cited at file:line for every API surface used (`screenListeners`, `EventArg`, `TabNavigationState.routes`, `StackActions.popToTop`), exact e2e step list per case.
- Risks cover data integrity, UX regressions, platform divergence, performance: pass — 8 numbered risks in v2 (`design-v2.md:216-231`) including action-shape correctness, fork interaction, `backBehavior="history"` substrate, scroll-position UX, header-arrow co-existence, platform divergence, performance, and first-party-import convention. Calibrated MEDIUM-HIGH on the web root cause.
- ≥1 alternative considered: pass — **8 explicit alternatives rejected** in `Alternativas descartadas` (`design-v2.md:233-249`) covering per-screen vs navigator-level, platform-gate, `router.replace`, dispatch-without-target, per-tab `useEffect`, header-tap, scroll-reset, hidden-tabs. Per `node_modules/expo-router/build/fork/native-stack/createNativeStackNavigator.js:62-65` cited as the framework's own dispatch shape — exemplary cross-check against the framework's internals.
- Stayed in scope; adjacent cleanup parked: pass — `Out of scope` (`design-v2.md:251-259`) lists 6 items + the upstream-bug filing as a follow-up. Header-tap, scroll-reset, breadcrumb, long-press, hidden tabs, in-screen state reset all explicitly punted.
- (If round > 1) Responded to every Validator blocker/major: **fail** — v2 responded to all v1 BLK + 2 MAJ + 4 MIN with explicit citations and resolved BLK-1 cleanly (dispatch with target). BUT the v2 `Investigation §2` (`design-v2.md:37-88`) reasoned through the web race correctly at the **registration/wiring** level — it identified `<Link>` + `tabPress` competing, the routingQueue/RAF ordering — and concluded "our explicit dispatch runs synchronously inside `emit('tabPress')` ... pre-empts web race." This synchronous-emit assumption was **wrong on web**: Tester's instrumented probe (`test-report-v1.md:78-86`) captured `tabPress` firing EXACTLY ONCE (on the cross-tab click) and ZERO times on focused re-tap. The listener never fires for the very gesture the feature targets on web. Validator approved v2 (`validation-v2.md:55-66`) because the API surface and dispatch shape are correct, but the runtime semantics weren't verified before declaring `go`. **The v1→v2 recovery moved the design from "API misuse" to "API-correct-but-runtime-wrong"**, which is a different kind of failure but still a v2-shipped-with-defect outcome.
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — only `design-v1.md` and `design-v2.md` attributed.

### Lessons for next run
- **When v2 reasons about runtime semantics on a new framework path, include a runtime probe — not just a source-walk.** Designer v2's Investigation §2 is exceptional source analysis (it correctly identifies the `<Link>` + `linkTo` race, the routingQueue, the RAF deferral) and arrives at a wiring-level conclusion that's technically true. What was missing was a 5-line probe: instrument the listener with `console.log`, run the dev server, click the focused tab, observe whether `tabPress` fires. Tester did this in round 1 (`test-report-v1.md:69-86`) and the answer was "no, it doesn't fire at all." A Designer who runs the same probe before declaring `go` would have caught the divergence one round earlier. **Add to the Designer contract for round 2 when the v1 issue is "runtime semantics on a framework boundary": include a runtime smoke probe (1-2 lines of instrumentation + a description of what was observed) in the Investigation section, not just a source-walk.**
- **"Synchronous inside emit" is not a verifiable property of an event listener registered through React Navigation's `screenListeners`.** The dispatch chain goes through `useNavigationBuilder`'s `setState` (subject to React 18 batching) and through expo-router's `routingQueue` (asynchronous batching via `useEffect`). Validator's MIN-NEW-1 (`validation-v2.md:41`) flagged the synchronous-state-claim overstatement; v2 carried it forward anyway because "the net behavior holds." It didn't on web. **For any design that claims event ordering across framework boundaries, treat the claim as MEDIUM confidence at best until runtime-verified, and reflect that in the labels.** v2's "MEDIUM-HIGH on web root cause" was the right label; what was missing was the empirical close-loop before shipping.
- **The "v2 Diff vs v1" lead table** (`design-v2.md:5-14`) is the right shape — explicit list of every Validator issue and how it was resolved, with a pointer to the resolving section. Sustain that contract.
- **The 8 alternatives rejected** (`design-v2.md:233-249`) including platform-gating (Alt 2 with §3 justification) and dispatch-without-target (Alt 4 — exactly the silent-drop trap that would otherwise have bitten the Implementer) is exceptional defensive design. Sustain.
- **The Investigation section** (`design-v2.md:17-103`) is structurally the right kind of artifact for round 2 — it walks the framework source line-by-line, names the load-bearing files, arrives at a fix shape. The only gap was the empirical close-loop. Pair it with a probe in the next round.

### Recurring pattern check
- **v1-needs-revision** is now a 3-run pattern. Routine-strong-builder (prior): v1 had 3 majors; canonical-exercises before: v1 had 1 major; this run: v1 had 1 BLK + 2 MAJ. **Designer's v1 has needed Validator catches on a non-trivial issue in all 3 consecutive runs.** The shape differs each time (canonical = test-contract pinning; routine-strong-builder = pseudo-code internal contradiction + UX policy + DB invariant; bottom-tab = framework-internals literacy + API misuse). The common root: Designer's v1 trusts the surface-level read of the problem and skips one layer of investigation that v2 surfaces. **Recommendation for the next run: before submitting v1, ask "what's the framework-internals layer I haven't read yet?" and either read it or call it out as an Unknown the Validator should verify.**
- **Runtime-semantic claims** is a new pattern emerging in this run specifically. v2 made a synchronous-emit claim that Tester refuted with a probe. **For framework-event-handler features, treat all "the event fires here" / "this runs before that" claims as needing runtime verification before `go`.** This is the second time a Designer-claimed runtime ordering has needed Tester-probe to be settled (the routine-strong-builder MAJ-2 hard-fail policy also relied on a runtime claim — that one was verified at design time by reading `workout/index.tsx:62-64`, which is the right shape).

## Run 2026-05-26_0101_routine-strong-builder — 2026-05-26 02:58 BRT

Pipeline: feature
Final outcome: pass (after 2 D↔V rounds)
Score: 5/6

### Rubric
- One responsibility per file change: pass — `design-v1.md:25-44` (and v2's expanded table) group changes by responsibility (migration / schema / types / API / hook / UI / live-flow / tests / docs); no unrelated edits merged. Filename rename `routine-exercise-row.tsx → routine-exercise-card.tsx` (delete + new) explicitly justified as a responsibility shift, not a cosmetic rename (`design-v1.md:666`).
- I/O contracts exact: pass — exact migration SQL (`design-v1.md:50-155`), exact TypeScript types for `RoutineExerciseSetRow` + `RoutineExerciseTargets` shrink (`:159-190`), exact API signatures for the 7 new functions (`:198-261`), exact hook signatures (`:288-301`), exact `<RoutineExerciseCard>` props with TypeScript types (`:352-365`), exact `useStartSessionFromRoutine` mutationFn body (`:310-347`). No hand-waving.
- Risks cover data integrity, UX regressions, platform divergence, performance: pass — 13 numbered risks in v1 (`design-v1.md:603-652`): dropset FK invariant on bulk seed, RLS bulk insert, migration atomicity, persisted-cache mismatch, live-screen rest-timer interplay, rest-timer auto-start on seed, checked-set bubble interaction, platform divergence (RN/RN-Web), builder fetch fan-out, seed fetch cost, RLS test regression, e2e fixture migration, mid-session routine-edit isolation. Each rated load-bearing/residual with confidence and risk labels.
- ≥1 alternative considered: pass — **13 explicit alternatives rejected** in `Alternativas descartadas` (`design-v1.md:654-680`): JSON column, defer-seed-to-mount, Postgres trigger, single-pass UUID v5 derivation, per-routine_exercise hook key, rename-in-place vs delete+new, Save button at card level, drag-handle reorder, per-set RPE in v1, cascade backfill into active sessions, hard-drop dropsets during backfill, atomic rollback on seed failure, embedded routine_exercise_sets in `listRoutineExercises`. Exceptionally thorough.
- Stayed in scope; adjacent cleanup parked: pass — `Out of scope` section (`design-v1.md:744-755`) lists 9 items explicitly parked, including drizzle-kit snapshot regen, per-set RPE, drag-handle reorder, atomic rollback, mid-session routine sync.
- (If round > 1) Responded to every Validator blocker/major: **fail (recovered in v2)** — v1 shipped with 3 majors that Validator caught (`validation-v1.md:37-43`). MAJ-1 was an internal pseudo-code contradiction across two code blocks at `design-v1.md:509-553` (Designer wrote both `parentNaturalKeyByRoutineSetId` and a "Refinement (cleaner)" subsection that introduced a different `routineSetIdToNaturalKey` map — pass 2 only used the second, so the first was dead code an Implementer would have retained). MAJ-2 was a silent-failure UX policy choice (log+proceed) without weighing the UX cost. MAJ-3 was an undeclared natural-key invariant ("a routine has at most one routine_exercise per exercise") not enforced at the DB level. **v2 resolved all three with the strongest version of each fix** (`design-v2.md:3-17, 152-164, 425-447, 557-711`): canonical 6-step algorithm with explicit "What is NOT in this algorithm" footnote (line 711) forbidding the dead patterns, hard-fail seed policy with verified `console.warn` catch-site, and a same-migration partial-unique `(routine_id, exercise_id) WHERE deleted_at IS NULL`. Net: round 2 closed with `go` and a strong design, but the v1 mistakes were avoidable.
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — `design-v1.md` and `design-v2.md` attributed only.

### Lessons for next run
- **NEVER ship a design with two code blocks that contradict each other.** MAJ-1 (`design-v1.md:509-553`) was a "Refinement (cleaner)" subsection that introduced a SECOND map name (`routineSetIdToNaturalKey`) while step 4 still showed the first one (`parentNaturalKeyByRoutineSetId`) being populated in a half-written loop with a comment `"actually we capture this in pass 0..."`. Before submitting, re-read every pseudo-code block end to end and confirm: single canonical algorithm, single map name, no dead variables, no "see refinement below" pointers. The v2 fix (`design-v2.md:711` "What is NOT in this algorithm") is the right shape — adopt it as a default footer for any multi-step algorithm.
- **UX policy choices need explicit weighing of the failure surface.** MAJ-2 (silent seed-failure policy = log+proceed) was a Designer call with no explicit weighing of "what does the user see when this fails?" The v2 hard-fail choice (`design-v2.md:425-447`) is correct precisely because it inherits the existing `console.warn("Start failed", err)` catch-site at `workout/index.tsx:62-64` — zero new error surface. **For any error-recovery policy decision, list (a) the failure rate the policy assumes, (b) what the user sees, (c) what existing code path handles the surface.** If (c) is "nothing", the policy probably needs revisiting.
- **Natural-key claims must be enforced at the DB level.** MAJ-3 (`validation-v1.md:29, 43`) — the seed's `(exercise_id, set_number)` natural key assumed "one routine_exercise per (routine, exercise)" was an invariant, but the only enforcement was a picker-level `excludeIds` UI filter that admin-seed and soft-delete-re-add paths could circumvent. **Before relying on a natural key, search for the DB-level constraint that enforces it; if none exists, either add one in the same migration (v2's choice — partial-unique on `(routine_id, exercise_id)`) OR document that the design tolerates duplicates and tighten the algorithm accordingly.** Hope-based invariants are not invariants.
- **Validator's verification table in v1 (`validation-v1.md:7-29`, 22 claims) is worth pre-reading.** Designer can self-audit by walking the same shape against their own draft before submitting — "for every load-bearing claim I just made, can I cite the file:line that proves it?" Designer's v2 implicitly did this (`design-v2.md` claims align with `validation-v2.md` verification table 1-for-1).
- **Sustain the "13 alternatives rejected" discipline** (`design-v1.md:654-680`) — this is the densest section of the design and it carried forward verbatim into v2. Validators and Reviewers cross-check rejection reasoning against the design's other commitments; the more alternatives surface explicitly, the harder it is for a hidden trade-off to slip through.
- **v2 "Diff vs v1" lead section** (`design-v2.md:3-17`) is the right shape for round 2 — explicit list of every Validator issue and how it was resolved, with a pointer to the section that contains the fix. Sustain that as the v2 contract.

### Recurring pattern check
- none — only one prior entry exists for Designer (canonical-exercises run, 5/6). The v1-needed-revision pattern is the second-time recurrence of a pattern variant: in canonical-exercises run, the recovery was via Implementer rounds (MAJ-1 spec-contract clarity); in this run, the v1 needed an explicit D↔V round 2. Both runs converged successfully on `go`, but Designer should treat "submit v1 → expect 0 majors" as the goal, not "submit v1 → recover in v2". Track this as an emerging pattern to monitor: 2 consecutive runs where Designer's v1 needed Validator catch on a non-trivial issue.

## Run 2026-05-25_1921_canonical-exercises — 2026-05-26 00:35 BRT

Pipeline: feature
Final outcome: pass
Score: 5/6

### Rubric
- One responsibility per file change: pass — `design-v1.md:13-66` table groups changes by responsibility (migration/schema, API, UI, tests, cosmetic); no unrelated edits merged into the chip work or the gating work.
- I/O contracts exact: pass — exact SQL bodies for the 4 RLS policies (`design-v1.md:80-92`), exact `ExerciseRow` type (`:96-110`), exact JSX shape for chip + picker + library row + edit-screen read-only branch + progress-screen pencil gate (`:128-244`), exact helper signature (`:248-283`). No hand-waving.
- Risks cover data integrity, UX regressions, platform divergence, performance: pass — 7 numbered risks (`design-v1.md:413-463`): single-txn safety, FK integrity, stale persisted cache, deep-link UX, in-session block scope, RN vs RN-Web platform, RLS predicate perf. Each rated load-bearing/residual.
- ≥1 alternative considered: pass — **13 explicit alternatives rejected** in `Alternativas descartadas` (`design-v1.md:394-411`), covering each Discovery Unknown plus chip-component-vs-inline, chip-text, chip-color, edit-screen-read-only-shape, headerRight-undefined-vs-null, cache-buster-bump-vs-skip, create-user.ts scope. Exceptionally thorough alternative-mapping.
- Stayed in scope; adjacent cleanup parked: pass — `Out of scope` section (`design-v1.md:465-477`) lists 11 items explicitly parked (admin UI, override table, name-uniqueness, sets/routine FK backfill, chip-on-ExerciseBlock, recommended subset, anon-tightening, banner, persisted-cache backfill, iOS smoke, scripts cosmetic — note: scripts cosmetic was pulled into scope with explicit justification, not silently grown).
- (If round > 1) Responded to every Validator blocker/major: n/a — round 1 only; Validator returned `go` with 1 major, addressed by Implementer in round 1 (MAJ-1 closure was Designer-could-have-pinned but recoverable in Implementer brief).
- Audit trail: n/a — no peer invocations attributable to this agent.
- Write boundaries: pass — only `design-v1.md` attributed.

### Lessons for next run
- **Pin the test contract when AC explicitly requires it.** Validator's MAJ-1 (`validation-v1.md:37`) flagged that AC4/AC5/AC7 named "tests added for ... badge rendering, edit/delete gating" but the design deferred "spec body to Tester discretion". When `state.md` AC says "Tests added for X", the Designer must pin the spec file path + named test cases + locator strategy in `design-v1.md`. This is recoverable in Implementer rounds but it cost a round-trip clarification.
- **The persisted-cache-buster bump justification under "Riscos > 3"** (`design-v1.md:431-435`) was load-bearing and could have been an Out-of-scope assumption surfaced by Discovery. Keep the discipline of explaining "why bump now" in the risk section, not just doing it.
- The 13 rejected alternatives format is high-value — keep it. Future Validators and Reviewers can verify the rejection reasoning against the design's other commitments.

### Recurring pattern check
- none — first feedback entry for Designer on this project.
