# Features

## Open

[ ] Secondary-muscle volume attribution — fractional credit to secondary muscles (bench → partial Arms/Shoulders) instead of primary-only `muscles[0]`. (refines Phase 1)

## Done

[x] Volume dose-metric revisit → DECIDED "augment" + shipped "hard sets per muscle/week". Reviewed the hypertrophy literature (Schoenfeld/Ogborn/Krieger 2017 dose-response ~+0.37%/set, 10+ sets/wk; Baz-Valle et al. sets-as-volume-metric; Schoenfeld/Grgic 2017 load-interchangeable-to-failure) → weekly hard sets per muscle is the dose-accurate metric, tonnage is a biased hypertrophy proxy (load conflation) but a useful overload signal → **augment, not switch**. Decision memo: `SecondBrainground/personal/ada11/2026-06-03_2205_hard-sets-vs-tonnage-per-muscle-dose-metric.md`. Implemented a kg↔sets toggle on `<WeeklyMuscleVolumeSection>`: a hard set = one `set_type==='working'` row per (muscle, week), DROPSETS EXCLUDED (a dropset extends one effort, not several), counted regardless of load/reps (must NOT inherit the tonnage `w>0&&r>0` guard — else bodyweight-only sets undercounted). Refactored `presentWeeklyVolumeByMuscle` into a thin wrapper over a shared `bucketByMuscleWeek` scaffold parameterized by a per-row INCLUDE-predicate + CONTRIBUTION + `needsLoad`; new sibling `presentWeeklyHardSetsByMuscle` omits `measurements` (sets path never touches bodyweight). Same `muscles[0]` attribution for both → feature #2 lands in one place. NO migration / stats.ts / kernel change (`set_type` already on `WeeklyVolumeRow`). Ephemeral toggle, default kg, shares per-muscle line selection; added a `testID="weekly-muscle-peak"` "Peak N sets/kg" caption. Invariant T (tonnage byte-for-byte) proved by source-diff; 13 existing tonnage tests unedited. +10 unit (505→515), 54/54 e2e across 11 specs. Verified live: toggle shows working COUNT (3), not tonnage (1,500) nor naive row count; teeth proven red→green by flipping the include-predicate. Shipped via `docs/runs/2026-06-03_2217_hard-sets-per-muscle/`. Out of scope: persisting the toggle, RPE-gated "near-failure" refinement, reference dose bands (~10–20 sets/wk overlay).

[x] Bodyweight leverage factors (refines Phase 0). A bodyweight exercise now contributes `(bodyweightKg × leverageFactor) + addedLoad` per rep instead of full bodyweight. The factor enters at the single seam `effectiveWeightKg` (bodyweight branch `bw*factor + addedLoad`; addedLoad/belt-vest NEVER scaled; NULL/non-finite ⇒ coalesce to 1.0 NEVER 0 ⇒ byte-for-byte prior numbers, Invariant L) and threads to ALL 6 `SetBodyweightInput` builders (volume-target-slot, makeBwInput, live workout header, end-of-session verdict + PR detection, History detail, session-verdict-math) + the ROW/full-`ExerciseRow` kernel sites. New nullable `exercises.bodyweight_factor numeric` column (migration `0021`), backfilled on 7 canonical rows: Push-up 0.64; Dip/Chin-up/Pull Up/Chest Dip 1.0; Hanging Leg Raise/Hanging Knee Raise 0.50. The SAME migration reclassifies `Pull Up`, `Chest Dip`, `Hanging Knee Raise` from `equipment=null` → `bodyweight` so they count bodyweight volume at all (RETROACTIVE shift — owner-locked U3; they now appear on per-muscle/strip/PR surfaces where they were absent). Backend-only — no UI/zod/write-path change; e1RM stays logged-weight (Invariant D). The factor is string- AND number-safe at the seam (`factor?: number | string | null` + internal parseFloat) because `numeric` can read back either way. +20 unit tests (485→505) incl. STRING-input teeth + a cross-surface case; 52/52 e2e across 12 specs. Verified live: Push-up 80kg×10 = 512 kg (0.64-leveraged, not 800); reclassified Pull Up 640 kg (was 0). Migration `0021` applied to the live Supabase project. 2 D↔V rounds (v1 NO-GO: closed-set proof miscounted INPUT BUILDERS as call sites → 4 un-wired; `numeric`-as-string would've silently no-op'd a `number`-typed factor — both fixed in v2). Shipped via `docs/runs/2026-06-03_1402_bodyweight-leverage-factors/`. Out of scope: editable per-exercise factor UI (no user-owned bodyweight exercises exist yet), factor-range CHECK constraint.

[x] Favorite exercises, pinned into the e1RM chart (Phase 2b). A star toggle on the exercise detail page (`app/(app)/exercises/[id]/progress.tsx`, header-right, OUTSIDE the `canEdit` gate so canonical exercises are favoritable too) marks an exercise as a favorite; favorited + plottable exercises are pinned INTO the Phase-2a "Estimated 1RM per exercise" chart in addition to the auto-selected most-performed top-5. New per-user join table `user_exercise_favorites(user_id, exercise_id, created_at)` (migration `0020`, composite PK, FK CASCADE both, RLS + 3 policies SELECT/INSERT/DELETE gated `auth.uid() = user_id` — NOT a column on the shared canonical `exercises` rows). New `src/api/exercise-favorites.ts` (plain INSERT swallow-23505 / DELETE — no 42P10 upsert trap) + optimistic `src/hooks/use-exercise-favorites.ts`. The e1RM presenter `presentTopExerciseE1rm` gains `favoriteExerciseIds?: ReadonlySet<string>`; the selection became **top-N-overall ∪ favorites** (deduped, dense ranks), capped at `E1RM_MAX_LINES=12` (palette extended 8→12, first 8 hexes byte-for-byte unchanged) dropping lowest NON-favorites first so favorites stay visible. **Invariant F**: with no favorites the chart is byte-for-byte the prior top-5 (unit-pinned). A favorited bodyweight-only exercise can't plot (Invariant D) → excluded from the chart, favorite row persists. +22 unit tests (455→477) + an RLS arm (`tests/rls.test.ts`: A insert → B blocked on SELECT/DELETE/spoof-INSERT) + a new e2e. 2 D↔V rounds (round 1 NO-GO: v1 union took top-N of the non-favorite pool, promoting a hidden line + contradicting its own test → fixed to top-N-overall ∪ favorites). I↔R single-pass PASS. 2 I↔T rounds (round 1 FAIL was test-only: the e2e returned via a hard `page.goto` reload racing the optimistic write + a stale-cache rehydration; fixed with client-side nav + `waitForResponse`). Migration `0020` applied to the live Supabase DB. Verified live: favoriting a non-top-5 exercise ("Lat Pulldown") makes it appear in the chart (5→6), unfavorite removes it. Shipped via `docs/runs/2026-06-01_1301_favorite-exercises-e1rm/`. Out of scope: separate favorites screen, favoriting from other surfaces, favorites on the volume/muscle chart, reordering favorites.

[x] TEST FIX: `chart-scroll-week-selector.spec.ts` green again (was 2/4 failing across several recent runs). The app-wide date-format swap (`5a2382b`, dd/mm zero-padded via `formatShortDate`) updated some e2e bar-label assertions but missed this spec, whose `currentLabel` still computed the old `M/d` ("5/25") while the "View week of" a11y label renders "25/05". Fixed the spec's two `currentLabel` computations to `dd/mm` zero-padded. Test-only (app behavior was correct); direct fix, no pipeline. 4/4 pass.

[x] BUG FIX: finishing a session no longer "messes up" the exercise order in History — plus exercise reorder is now editable on past sessions. Root cause (bug-fix pipeline): the exercise order you saw during a live workout was never persisted; History rebuilt order from `listSetsForSession`, which sorts by `set_number` only — every exercise's first set is `set_number=1`, an N-way tie with no secondary key, so Postgres fell back to unspecified physical insertion order (and routine sessions seed sets interleaved across exercises, so insertion order ≠ routine order). Fix: a new nullable `session_exercise_order uuid[]` column on `sessions` (migration `0019_session_exercise_order.sql`, additive/no-backfill/no new RLS) that **snapshots the displayed exercise order at Finish**; History reads it to order its exercise blocks (within-exercise set order untouched — `set_number` stays primary). Legacy sessions read NULL → deterministic first-occurrence fallback (they stop reshuffling but can't recover an order that was never recorded). Owner-added scope: the up/down reorder **chevrons now appear in History EDIT mode** (reusing the live screen's existing `<ExerciseBlock>` controls) and persist per tap via a new optimistic `useUpdateSessionExerciseOrder` — so reordering a legacy session writes its order for the first time (manual recovery path). New pure helper `src/utils/session-exercise-order.ts` (+11 unit tests); new e2e `tests/e2e/session-exercise-order.spec.ts` verifies routine/ad-hoc/manually-reordered all match the live order + legacy recovery + `uuid[]` round-trip against the live DB. 0 adjacent regressions (read-only-history / verdict / progress / e1rm / weekly-muscle / remove-exercise all green); 455/455 unit. Migration applied to the live Supabase project. Shipped via `docs/runs/2026-06-01_0941_session-finish-exercise-order/`. Out of scope (follow-ups): remove-exercise control in History edit mode (only reorder + add today), drag-and-drop, live mid-session reorder persistence, stale `volume-target.ts` comments about completion-timestamp ordering.

[x] Estimated-1RM (e1RM) strength-progress chart on the Progress page (Phase 2a). New "Estimated 1RM per exercise" section below the weekly per-muscle volume chart: a multi-line SVG chart (reusing `<MultiSeriesChart>`) plotting each top-N exercise's best e1RM per ISO week over time, auto-populated with the owner's most-performed (weighted) exercises, with selectable lines + check-all/uncheck-all. New pure presenter `presentTopExerciseE1rm` (`src/utils/e1rm-strength.ts`) reuses the existing `epley1RM` (Epley) — no formula duplication (e1RM was already centralized; the chart is just `epley1RM`'s 2nd caller). Key decisions: (1) **per-WEEK x-axis** — the only shape that aligns multiple exercises on the shared chart index without extending the component; (2) **untrained weeks carry the last observed e1RM forward (LOCF), NOT zero-fill** — a 0 for a peak metric is false (you don't get weaker to zero on a rest week) and would crash the line to the axis; leading weeks take the first real value (flat lead-in); (3) **MAX per (exercise, week)**, not a sum (Invariant E1); (4) **e1RM uses LOGGED weight only (Invariant D)** — a 0-weight bodyweight set produces no e1RM point, and eligibility-before-ranking keeps bodyweight-only movements from claiming an invisible top-N slot; (5) top-5 by distinct sessions, deterministic 4-key tie-break, palette-by-rank. Section mirrors `<WeeklyMuscleVolumeSection>`; NO `useMeasurements` (bodyweight is irrelevant to logged-weight e1RM). `app/(app)/exercises/[id]/progress.tsx` deliberately NOT refactored. +13 unit tests (431→444); new e2e 3/3 (seeds a weighted exercise for a visible line + a bodyweight negative case). One adjacent regression caught + fixed in-run: the new legend chip surfaced the exercise name earlier in the DOM, breaking `progress-page.spec.ts`'s fragile `getByText(name).first()` locator — fixed test-only by targeting the navigable row's role+accessible-name (the pattern the file's own test #8 already used). Shipped via `docs/runs/2026-05-30_2006_e1rm-strength-chart/`. Open follow-up in "## Open": favorites (Phase 2b — pin exercises into this chart).

[x] Weekly volume per muscle group chart (Progress page) + per-session chart removed. New "Weekly volume per muscle" section between the weekly strip and exercises-this-week list: a multi-line SVG chart (`<MultiSeriesChart>`) with one selectable line per muscle group (the 7 `MUSCLE_GROUPS` + a gray "Other" when it has data) and a check-all / uncheck-all control. Buckets are WEEKLY (ISO week — matches how hypertrophy volume is dosed in the literature), zero-filled across a shared contiguous week axis (first-trained week → now) so a drop to zero reads honestly; primary-muscle attribution via `muscles[0]` (`groupExercisesByPrimaryMuscle`), dangling exercise_ids skipped. New pure presenter `presentWeeklyVolumeByMuscle` (`src/utils/weekly-muscle-volume.ts`) consumes the bodyweight-aware kernel. It is a full-history TREND viz — deliberately does NOT honor the `max_volume_window_weeks` preference. Selection is component-local (not persisted). The old per-session chart (`SessionVolumeChartSection` + `presentSessionVolumeChart`) was removed — the per-muscle chart supersedes it. Owner chose selectable lines over stacked area / small-multiples, and per-muscle grouping over per-workout-type (muscle groups stay stable when the split changes; today PPL, future maybe full-body). Shipped via `docs/runs/2026-05-30_0126_bodyweight-volume-per-muscle/`. Open follow-ups in "## Open": secondary-muscle fractional attribution (currently primary-only), a chart-local window selector, "hard sets per muscle/week" dose metric.

[x] Volume kernel is now bodyweight-aware on EVERY surface. Bodyweight-equipment exercises (Pull-up/Chin-up/Dip/Push-up/etc., logged with `weight = 0`) previously contributed ZERO volume everywhere; they now contribute `(bodyweight-as-of-session + added load) × reps`. New pure helpers in `src/utils/bodyweight.ts`: `effectiveWeightKg(equipment, weight, bodyweightKg)` (the single arithmetic seam — the addend fires ONLY on the exact canonical token `equipment === "bodyweight"`, NaN-safe, returns `addedLoad` for everything else so non-bodyweight numbers are byte-for-byte unchanged) and `bodyweightKgAsOf(measurements, instantMs)` (resolves the user's weigh-in as of the session: nearest PRIOR non-null `weight_kg` → else nearest LATER → else null = contribution 0, never NaN; UTC-instant compare, input-order-independent, mirrors the `measurements-chart.ts` null-skip). Applied CANONICALLY to all 14 inline kernel sites (`sumLiveVolume`/`sumPastVolume`/`computeVolumeTarget` in `volume-target.ts`; `bucketLifetimeWeeklyVolumes`/`computeCurrentWeekVolume`/`computeLifetimeMaxPerExercise`/`groupSessionVolumes`/`computePrsThisWeek` in `progress-page-math.ts`; `computeStripModel`; the per-exercise progress reduce; `presentSetVolumeLines`/`presentExerciseSessionRow`; `useExercisesThisWeek`'s nowKg reduce; the History-list + week-drill-down `weekVolumeKg`) — threaded as OPTIONAL params (the `windowStartMs?` precedent), so absent input reproduces the pre-feature numbers exactly. `src/api/stats.ts` SELECT widened with `exercises!inner(equipment)` (provably row-preserving: FK `ON DELETE restrict` + deleted_at-agnostic RLS); the `SetRow`/`SessionSets` single-session callers get equipment from a `useAllExercises` map. e1RM stays logged-weight (a 0-weight bodyweight set yields a VOLUME point but NO e1RM point — the two guards intentionally diverge). Admin session-detail total stays non-bodyweight (it views other users' sessions). No migration (all columns pre-existed; canonical exercises were already backfilled by 0014). This retroactively shifts historical volume for bodyweight movements and can create/erase past bodyweight PRs + shift max-volume — intended, per owner decision to preserve the "same number everywhere" invariant. +47 unit tests (384→431); regression invariants verified (non-bodyweight byte-for-byte unchanged; bodyweight PR create/erase; per-set-sum holds; e1RM divergence). Shipped via `docs/runs/2026-05-30_0126_bodyweight-volume-per-muscle/`. Open follow-up in "## Open": per-exercise leverage factors (push-up ≈ 0.64 BW) — currently full-bodyweight approximation.

[x] Date format swept to `dd/mm` (current year) / `dd/mm/yy` (prior year) everywhere — replaces the previous "May 24" / "5/24" / "Nov 8, 2025" / "11/8/25" mix. Implemented at the single source of truth (`src/utils/format-display-date.ts`) which 12+ surfaces flow through, so the change cascades to the History session list, history detail header, exercise progress chart axes, sessions list on the progress page, measurements list/detail/chart, weekly-volume strip pill + per-bar a11y, week drill-down header, and the verdict screen by construction. `formatDisplayDate` now builds the numeric date manually (locale-independent order) and locks weekday + 24h time to `en-GB` so output is deterministic across devices (English short weekday names + 24h clock — matches BR/EU convention). `formatShortDate` mirrors the same `dd/mm` / `dd/mm/yy` / `dd/mm/yyyy` rule. Updated `formatShortDate` 2-digit values are now zero-padded ("24/05" not "5/24") so chart axes have consistent width. JSDoc examples in `format-session-times.ts` + `weekly-volume-strip.tsx` updated; tests with literal date assertions (`format-display-date.test.ts`, `measurements-chart.test.ts`, `progress-page-math.test.ts`, e2e `week-drill-down.spec.ts` bar-label construction) updated. Typecheck / lint / 384 unit clean; exercise-session-row-list + read-only-history + week-drill-down + measurements + end-of-session-verdict + weekly-volume-strip e2e all pass.

[x] Set rows now make their type visually distinct, with chained dropsets explicit. **Warmup** rows: thin yellow left accent strip + subtle yellow background tint (light + dark). **Dropset** rows: thin purple left accent strip + subtle purple bg tint + a left indent + an inline `↳ N` parent reference (N = the working set's `set_number` that the dropset chains onto, resolved via the surrounding block's `set_id → set_number` map). **Working** sets stay the baseline. The checked-state green still wins on background (saved = primary signal), but the type accent + indent + parent label remain visible on top of the green so type identity is never lost. Applied consistently to `<SetInput>` (live workout + history-edit) AND `<ReadOnlySetRow>` (history-read) — toggling Edit on/off doesn't reshape the row visually. Typecheck/lint/384 unit clean; read-only-history + set-row-menu e2e pass.

[x] BUG: focused bottom-tab re-tap didn't pop to the tab root on deep-link / page-refresh or cross-tab arrival. `HomeLinkTabBarButton` gated its pop on `childState.type === "stack"`, which fails when expo-router rehydrates a tab's child stack from the URL as a PartialState (`type`/`key` undefined) — the press fell through to a no-op. Pipeline diagnosis: a child-state gate is fundamentally undiscriminating because the cross-tab `childState` is `undefined`, identical to a genuine at-root arrival; only the URL can tell them apart. Fix gates the fallback on `useSegments()` and pops via `router.navigate(TAB_ROOTS[route.name])`. Keyed fast-path (same-tab click-through) untouched; `router.replace` rejected to protect the `backBehavior="history"` browser-back invariant; press stays a plain-button imperative call (no `onPress`/`href` reroute). Two new e2e cases (deep-link + cross-tab from a live workout) close a masking gap — `bottom-tab-home-link.spec.ts` previously covered only the working path. Bug pipeline (Reproducer → Diagnostician → Fix Designer → Implementer → Regression Tester → Evaluator) shipped via `docs/runs/2026-05-27_2144_navbar-tab-pop-to-root/`. Notable: the approved `dismissAll`-based mechanism failed at runtime; the Implementer pivoted to the URL-gate (documented 3-deviation, runtime-verified Paths A/B/C, History generalization spot-check).

[x] Back from an exercise's progress screen now returns to where you came from, not always the exercises list. The progress route lives in the `exercises` tab, so opening it from the live workout (tapping an exercise name) and hitting the header back popped that tab's stack → the exercises list. The workout now passes a `backHref` param and the progress screen renders a custom `headerLeft` that `router.navigate`s to that origin when present; openers within the exercises tab (the list) pass nothing and keep the default back (which already lands on the list). New e2e `exercise-progress-back-nav.spec.ts` (session → progress → back → session); exercise-progress-ia + rest-timer regression pass. Known follow-up: other cross-tab entries (Progress-tab PR rows, verdict screen) still back to the exercises list — they can opt in by passing `backHref` too.

[x] All max-volume figures now respect the max-volume-window preference. The exercise progress page's "Max volume session" callout computed the best single-session volume over ALL history while the live `<VolumeTargetSlot>` "Max" was windowed — so the same exercise showed two different max numbers. The callout now applies the same `computeWindowStart(weeks)` session-level `started_at` filter the live strip uses (header gains a "· last Nw" hint when a window is set). The e1RM summary + both trend charts deliberately stay all-history (a chart truncated to the window would defeat its purpose). Typecheck/lint/384 unit clean; exercise-progress-ia + max-volume-window e2e pass.

[x] Exercise progress "Sessions" rows now show each set (weight × reps) with its per-set volume, not just "N × total volume". New pure presenter `presentSetVolumeLines(sets, unit)` in `exercise-session-row-format.ts` (the deferred per-set line the file already anticipated) + shared `<SetVolumeBreakdown>` component. Per-set volumes sum to the row total by construction — same scope as `sumPastVolume` (warmups excluded, `w>0 && r>0`). 8 new unit tests; no e2e assertion change needed (the aggregate `^\d+ × [\d,]+ (kg|lbs)$` regex still uniquely matches the total line — per-set labels like "100 × 8" have no unit suffix).

[x] Volume per set on exercise max volume (both surfaces). `computeVolumeTarget` now also returns `previousMaxSets` — the sets of the best single-session volume. The live `<VolumeTargetSlot>` renders that session's per-set breakdown beneath the "Max …" line (chasing + surpassed states); the exercise progress page gains a "Max volume session" callout (date + total + per-set breakdown). Both reuse `<SetVolumeBreakdown>` + `presentSetVolumeLines`. 2 new kernel unit tests assert the winning session's set array is returned by reference.

[x] Set checkbox loading state. While a check/uncheck mutation is in flight the checkbox swaps to an `ActivityIndicator` and disables re-taps (per-set `pendingCheckIds` set tracked on the workout screen, threaded `<ExerciseBlock>` → `<SetInput>` via `pendingCheckSetIds`/`checkPending`). Built on top of the optimistic flip from the prior change — the row still greens instantly, the spinner is a "saving" affordance, and the disable closes the rapid re-toggle race the rest-timer observer was guarding against. Existing auto-fill + rest-timer e2e pass (the re-check test's 5s/1.5s waits exceed the brief pending window).

[x] Create-routine lands on the builder, not the routines list. `app/(app)/routines/new.tsx` now `router.replace`s into `/routines/{newId}` on save (instead of `router.back()`), so the user can add exercises immediately after picking the name + notes — no round-trip via the workout list + Edit pencil. `replace` (not `push`) so the browser/native back button skips the now-empty create form. Minimal 1-line behaviour change; existing routes/screens untouched.

[x] Reorder exercises / sets — delay eliminated via optimistic UI updates. `useReorderRoutineExercises` and `useReorderRoutineExerciseSets` now rewrite the TanStack cache synchronously in `onMutate` so the chevron tap is instant; the actual server-side two-phase swap (2N sequential PATCHes for N items) runs in the background, with `onError` restoring the previous order on failure and `onSettled` invalidating to reconcile. The user's spec said "if we can't remove the delay, show a loading state" — optimistic removes the delay outright, no loading state needed. No new tests (UI-only behavioural change; existing reorder e2e specs still pass because the final state matches).

[x] Body parts backfilled for 54 canonical exercises. Migration `0014_backfill_exercise_muscles.sql` populates `muscles` on every canonical (`user_id IS NULL`) exercise that was previously empty, using the existing 7-group vocabulary (Arms / Chest / Core / Legs / Lower back / Shoulders / Upper back). Idempotent — each UPDATE includes a `muscles IS NULL OR muscles = '{}'` guard so re-runs are no-ops. Also normalises `equipment` to lowercase (8 distinct case-mixed values → 5 canonical: barbell / bodyweight / cable / dumbbell / machine). Zero user-owned exercises were affected — investigation script confirmed all empty-muscle rows were canonical. Verified: `0/54 missing` post-migration.

[x] Per-session volume progression chart. New section "Volume per session" on the Progress tab (between WeeklyVolumeStrip and ExercisesThisWeekList). One point per finished session, oldest left → newest right; uses the existing `<ProgressChart>` component for layout consistency. New presenter `presentSessionVolumeChart(rows)` in `src/utils/progress-page-math.ts` reuses `groupSessionVolumes` so cross-surface volume numbers match by construction. Cost concern flagged in the original spec was unfounded — the dataset is already cached server-side by `useLifetimeWeeklyVolume()` (TanStack staleTime 60s) and the per-session grouping is O(N) in-memory; no SQL window-function needed at current data volumes. New component `<SessionVolumeChartSection>` hides automatically when there are zero finished sessions.

[x] Equipment selector mirror of MuscleGroupPicker. New `<EquipmentPicker>` component (single-select chip layout matching the existing `<MuscleGroupPicker>`) replaces the free-text Equipment input on both `app/(app)/exercises/new.tsx` and `app/(app)/exercises/[id]/index.tsx`. Canonical equipment vocabulary: barbell / bodyweight / cable / dumbbell / machine (matches the 5 distinct values surviving the 0014 case normalisation). Stored as lowercase token; rendered with title-case label via `equipmentLabel()`. New helper `formatEquipment(value)` gracefully passes through legacy non-canonical strings on user-owned rows so existing data isn't lost. Display sites updated to use it: `<ExerciseListItem>`, `<ExerciseBlock>`, `<RoutineExerciseCard>`, and the read-only header on the exercise detail screen. Zod schema tightened from `z.string().max(40)` to `z.enum(EQUIPMENT_OPTIONS)`.

[x] Max-volume-window — 40 and 50 weeks added + all-time PR shown alongside windowed Max on the Progress hero. Migration `0015_max_volume_window_40_50.sql` extends the `user_preferences_max_volume_window_weeks_check` CHECK constraint from `IN (0,10,20,30)` to `IN (0,10,20,30,40,50)`. `MaxVolumeWindowWeeks` union + `MAX_VOLUME_WINDOW_OPTIONS` array updated; the Profile segmented control auto-renders the new chips because it iterates the constant. `<ProgressHero>` now also computes the all-time best week independently (via `findBestWeek(bucketLifetimeWeeklyVolumes(data, undefined))` — windowStartMs omitted) and renders a secondary line "All-time best: X kg/lbs" below the `Max·Now·To PR` row when the user's window is non-zero AND differs from the all-time max. When window=0 (All) the lines would duplicate, so the secondary line is suppressed. Other PR surfaces (per-muscle exercises-this-week list, weekly volume strip dotted overlay, end-of-session verdict, live workout `<VolumeTargetSlot>`) remain windowed-only — Progress hero is the canonical "where to glance for momentum" surface; expanding dual-PR display to the other 4 surfaces is a known follow-up if user finds the asymmetry confusing. Pushed to remote (`0014 | 0014 | 0015 | 0015`).

[x] Bottom-tab home link. Tapping the focused bottom-tab icon now pops the child Stack back to that tab's root (Strong / iOS-standard "tap-to-pop-to-root" behavior). New `HomeLinkTabBarButton` wired via `<Tabs>` `screenOptions.tabBarButton` at `app/(app)/_layout.tsx`. On focused re-tap dispatches `StackActions.popToTop({ target: childState.key })`; otherwise delegates to the framework's default `onPress`. Strips `href` from `BottomTabBarButtonProps` so PlatformPressable renders as `<div role="button">` instead of `<a>` (load-bearing on web — eliminates browser-native click interference + the routingQueue `linkTo` race that round-1's screenListeners approach lost to). The "bottomsheet icon button" in the user's original report was a malapropism for the bottom tab bar (Discovery HIGH confidence). 3 new e2e cases (re-tap pops nested→root, cross-tab + browser-back preserves `backBehavior="history"`, leaf-tab re-tap is harmless no-op). Shipped via `docs/runs/2026-05-26_0307_bottom-tab-home-link/` after 2 D↔V (round 1: 1 BLK `navigation.popToTop()` doesn't exist on Tabs prop + 2 MAJ: design unaware Expo Router's forked native-stack already auto-pops on focused tabPress; round 2 found the real web-only root cause = race between expo-router's `<Link>` interception of the tab-button click and the fork's RAF-deferred popToTop) + 1 I↔R (pass first try) + 2 I↔T (round 1 fail: screenListeners doesn't fire on focused re-tap on web; round 2 fail: design works on click-through but fails on `page.goto` deep-link due to PartialState shape with `type === undefined`) + 1 Conductor out-of-band Option-A test-side patch (changed Case 1 from `page.goto` to click-through navigation). 3/3 pass. **Known follow-up**: deep-link rehydration → tab tap doesn't pop — needs `router.replace(tabRoot)` fallback (~10-15 LOC + 1 e2e case). Defensible deferral per user's verbatim prompt scope.

[x] Strong-like routine builder with per-set targets. The routine builder no longer takes a global "sets/reps/weight" trio per exercise — each set is now explicit (Set 1: 60 kg × 8, Set 2: 70 kg × 8, ...). New table `routine_exercise_sets` (migration `0013_routine_exercise_sets.sql`) carries `set_number`, `set_type` ('working'|'warmup'|'dropset'), `target_reps`, `target_weight`, `parent_set_id` (self-FK for dropsets). RLS gated on owning user via `routine_exercises.user_id`. Partial UNIQUE `(routine_exercise_id, set_number) WHERE deleted_at IS NULL`. ON DELETE CASCADE from routine_exercises; self-FK `ON DELETE set null`. Migration deletes the now-unused `target_sets / target_reps / target_weight` from `routine_exercises` (target_rest_seconds + notes stay) and includes a `generate_series` backfill for any pre-feature routine with `target_sets > 0`. New partial-unique `(routine_id, exercise_id) WHERE deleted_at IS NULL` enforces "one routine_exercise per exercise per routine" — was a runtime assumption, is now a DB invariant. Defense-in-depth on the add-exercise picker via `DuplicateRoutineExerciseError` 23505 discriminator. New `<RoutineExerciseCard>` (replaces `<RoutineExerciseRow>`): expandable header + per-set rows + "+ Working / + Warm-up / + Drop set" footer + per-card Rest field; soft-delete via trash icon; chevron reorder. Live workout: starting a session from a routine pre-seeds the `sets` table with one unchecked draft per `routine_exercise_set`, carrying the target weight/reps as initial values; dropset `parent_set_id` is remapped two-pass via a `(exercise_id, set_number)` natural key. Hard-fail seed policy: if the bulk-INSERT fails, the entire mutation rejects; the user stays on the routines list; the orphan session row remains salvageable in History (intentional). Idempotency guard at `routine_exercise_id` granularity prevents double-seeding. `pendingRoutineId` in-flight guard on the Start button. `queryCacheBuster` bumped to clear persisted client cache after the schema break. 12 unit + 7 e2e (golden / dropset / idempotency / soft-delete-re-add / edit-then-restart / hard-fail / 23505) + RLS arm + `tests/migration-backfill.ts` script wired via `npm run test:migration`. Shipped via `docs/runs/2026-05-26_0101_routine-strong-builder/` after 2 D↔V (round 1 caught 3 majors: pseudo-code self-contradiction, silent seed-failure UX, undeclared natural-key invariant — all resolved in v2) + 1 I↔R (pass first try) + 2 I↔T + 1 Conductor out-of-band `.first()` locator patch. Migration pushed to remote (`0013 | 0013` per `supabase migration list --linked`).

[x] BUG: routine edit — adding an exercise failed with `409 / 23505 / routine_exercises_routine_position_uq` whenever the routine had any soft-deleted entry. Root cause: the unique index in `0000_schema.sql` covered all rows including tombstones, while `addExerciseToRoutine` at `src/api/routine-exercises.ts:37-45` computed `nextPosition = max(position) + 1 WHERE deleted_at IS NULL` — so on a routine where the trailing position belonged to a tombstone, the new INSERT reused that position and collided. The Supabase client's built-in `executeWithRetry` made it look like a double-tap race in the logs (two POSTs from one click). Same conceptual shape as 0008's `sets_session_exercise_set_number_unique` and 0010's `exercise_notes`-style partial uniques — index just hadn't been converted yet. Fix: new migration `0012_routine_exercises_unique_partial.sql` drops and recreates the index as `WHERE deleted_at IS NULL`; the position-computation logic in the API stays unchanged. App-side change limited to removing the now-inaccurate `uniqueIndex` declaration from `src/db/schema.ts` (Drizzle 0.38 has no `.where()` on `uniqueIndex`, matching the convention already used for 0008/0010 partial uniques). No new tests — fix is a pure DB constraint loosening, strictly looser than the prior full unique. Pushed to remote (`0012 | 0012`). Aria-hidden warning on the same flow noted but out of scope — separate cosmetic issue, the May 22 picker fix already addresses the focus-blur path; remaining warning likely fires from the "Edit" button on a parent screen before navigation.

[x] Exercise note — personal per-(user, exercise) note visible on 4 surfaces (progress screen + live `<ExerciseBlock>` + history-edit `<ExerciseBlock>` + history-read `<ReadOnlyExerciseBlock>`). New `exercise_notes` table via migration `0010_exercise_notes.sql` with 4 RLS policies (SELECT/INSERT/UPDATE/DELETE gated on `auth.uid() = user_id`), UNIQUE partial index `(user_id, exercise_id) WHERE deleted_at IS NULL`, named CHECK `exercise_notes_body_length_check (char_length(body) <= 2000)`, `exercise_id ON DELETE RESTRICT` (matches `sets`/`routine_exercises` sibling precedent), `touch_updated_at` trigger. New API `src/api/exercise-notes.ts` with `getMyExerciseNote(exerciseId)` + `upsertMyExerciseNote(exerciseId, body)` (iterative 23505-retry read-then-write; NOT `.upsert()` — PostgREST cannot forward `WHERE` predicate of `ON CONFLICT` so partial-UNIQUE upsert is a guaranteed `42P10` failure, an architectural insight surfaced by Validator round 1). New hook `src/hooks/use-exercise-note.ts` with `["exercise_note", exerciseId, "me"]` cache key. New self-wired component `<ExerciseNoteSlot>` with `{exerciseId, editable, alwaysExpanded?}` props — collapsed `+ Add note` Pressable for empty editable on dense surfaces (live ExerciseBlock), full Textarea via `alwaysExpanded={true}` on progress screen. Draft-divergence resync guard preserves user typing through background refetch (strictly stronger than spec to cover `alwaysExpanded` surfaces). 2000-char body cap at 3 layers (zod + maxLength + DB CHECK). Empty-bail in `commit()` skips mutate for never-existed empty rows. 10 unit tests + 6 e2e specs + RLS test arm (User A insert → User B cannot SELECT/UPDATE/DELETE + INSERT-spoof rejection). Shipped via `docs/runs/2026-05-24_2327_exercise-note/` after 2 D↔V + 1 I↔R + 2 I↔T rounds — user mandated "special care" and authorized ship-with-known-debt at close. **Known debt**: golden e2e test #1 is ~33-50% flaky due to React Query in-memory cache priming empty `sets` before admin INSERT (~30 LOC test-only fix well-understood, not applied this run — follow-up ticket warranted). 5/6 specs deterministic; RLS arm pass; 364/364 unit; network trace + screenshots confirm feature works. User decision U1: chose new table over reusing existing `exercises.notes` column (per spec) for clean separation between exercise metadata and personal notes (future-proof for shared exercise library).

[x] Sessions list on per-exercise progress screen. The `/exercises/{id}/progress` screen now appends a "SESSIONS" section below the Total volume chart. Each row shows the session's date (with weekday, year-aware) and a `N × VOLUME` aggregate (working-set count × per-exercise-per-session volume via canonical `sumPastVolume` — cross-screen consistent with chart points). DESC (newest first). Tap routes to `/history/{sessionId}`. Pure UI consumer of `useExerciseProgress(exerciseId)` — zero new queries. New pure presenter `presentExerciseSessionRow({sets, unit}): {count, volumeKg, volumeLabel}` extracted to `src/utils/exercise-session-row-format.ts` (JSDoc explains why parts are returned). `<ExerciseSessionRow>` component with `accessibilityLabel` including time-of-day (disambiguates same-day sessions). One non-additive code change: `sumPastVolume` made `export` for kernel reuse. Section gated inside the existing `e1rmData.length > 0` truthy branch — preserves the 3 pinned empty-state e2e assertions. 7 new unit + 3 new e2e (354/354 unit). Shipped via `docs/runs/2026-05-24_2233_sessions-list-on-progress-chart/` after 2 D↔V rounds. Validator round 1 caught: aggregate regex hardcoded `kg` (failed lbs mode), same-day a11y label collision, section styling diverged from existing `SECTION_HEADER` precedent at `history/week/[isoWeek].tsx:20-21` + horizontal alignment issue (`px-4` row vs ambient `px-6`). v2: unit-agnostic `(kg|lbs)`, `includeTime: true` on a11y label, inlined `SECTION_HEADER` literal classes, row drops `px-4`.

[x] Auto-fill checked sets from placeholder. When a working set is checked done with empty or zero weight/reps inputs, the placeholder values (from `useLastWorkingSet` + in-session walk-backward) auto-commit as the saved values. Per-field: only the empty/zero field is filled; if the user typed `reps=5` with weight blank, only weight gets the placeholder. Warmups + dropsets explicitly skipped (only `set_type === "working"`). Unchecking is a no-op. Bulk "Check all and finish" bypasses the toggle handler so naturally skipped. Side-effect order: `computeAutoFillPayload({currentInput, previous})` → `await updateSet({weight?, reps?})` (only if non-null patch) → `restTimer.start(rest)` → `await checkSet(id)`. The hard architectural insight (caught by Validator over 3 D↔V rounds): the typed-but-uncommitted value MUST flow synchronously through the toggle callback from `<SetInput>`'s local state — reading the cache after `Keyboard.dismiss()` is architecturally impossible because `setsByExercise` is closed over via `useMemo` and `Keyboard.dismiss()` is async on native. v3 widened `<SetInput>.onToggleChecked(next, {weight, reps})` → `<ExerciseBlock>.onToggleSetChecked(setId, next, {previousSet, currentInput})` → screen handler. No cache reads in the predicate, no `setQueryData` shim, no `sessionId` prop on `<ExerciseBlock>` (preserves history-edit caller). 15 new unit tests + 10 new e2e (347/347 unit, 10/10 e2e). Shipped via `docs/runs/2026-05-24_2020_auto-fill-placeholder-on-check/` after 3 D↔V rounds + 2 I↔T rounds. Known follow-up: app-level race between blur-driven `commit()` PATCH and auto-fill `updateSet` PATCH (no PostgREST ordering guarantee); masked by test stabilization (explicit `blur()` + wait); source fix warranted for follow-up.

[x] Total volume on History session rows. Each row now renders `Sat, May 24 · 1h 23m · 12,345 kg` inline on line 2 (pulls from `useWeightUnit()` so kg/lbs follows the user's preference). Detail-screen header also routes through canonical `sumLiveVolume` for cross-surface consistency. Zero new queries — derive from `useLifetimeWeeklyVolume()` via new `groupSessionVolumes(rows): Map<sessionId, kg>` helper that groups by `session_id` and delegates to `sumLiveVolume` per group. Three folded fixes: `formatWeight → formatVolume` (renders thousands separator), ad-hoc reduction at `history/[id].tsx:124-136` replaced with `sumLiveVolume` (proper warmup + `completed_at` + `w>0`/`r>0` predicates — warmup-heavy historical sessions now show correct lower volume), `sumLiveVolume` parameter widened from `SetRow[]` to `Pick<SetRow, "completed_at" | "set_type" | "weight" | "reps">[]` (removes `as unknown as` cast at the new callsite). New pure presenter `presentSessionVolumeSlot(totalVolumeKg, unit): string | null` extracted to `src/utils/session-row-format.ts` for `.test.ts` coverage (no RNTL in repo). 25 new unit tests (332/332). Shipped via `docs/runs/2026-05-24_1925_history-row-total-volume/` after 2 D↔V rounds. Validator round 1 caught: design's `.tsx` render test would have been silently skipped (vitest config restricts to `*.test.ts`), detail-screen zero-volume branch was self-contradicting. Round 2 caught: JSX `{" "}` whitespace nuance (must remove BOTH `{" "}` and the literal word `volume` to avoid trailing space), `useLifetimeWeeklyVolume()` destructure extension. I↔R + I↔T single-pass; 6 screenshots pinned.

[x] Progress graph starts on the current week (regression fix). The weekly volume strip on both History and Progress tabs was opening scrolled to the OLDEST week on narrow viewports because `rightAnchorX = Math.max(0, contentWidth)` at `weekly-volume-strip.tsx:238` was missing the viewport subtraction, declaratively passed via `contentOffset`. The bug was introduced by `2026-05-22_1130_chart-scroll-week-selector` but masked from CI because Playwright's default ~1280pt viewport fits all 16 seeded bars without scrolling. Fix: replaced the declarative path with `scrollRef.current?.scrollToEnd({animated: false})` inside an `onContentSizeChange` handler gated by a one-shot `didInitialPinRef`. `scrollToEnd` delegates the math to the underlying platform's `scrollWidth - clientWidth`, sidestepping both the viewport-read race AND the outer-wrapper `px-4` padding bug. Same primitive as the existing rollover effect at line 167, in production since the original chart-scroll run. New e2e narrow-viewport case at 390pt iPhone 12 with deterministic `scrollLeft + clientWidth >= scrollWidth - 4` assertion. Tester verified at 390pt + 320pt iPhone SE + 1280pt wide; scroll-left preserved across refetch (172→172). 4 screenshots pinned. Implementer deviation `testID="weekly-strip-scroller"` over Designer's `dataSet` — strictly superior (typed prop, identical `data-testid` on web). Shipped via `docs/runs/2026-05-23_2357_progress-graph-current-week/` after 2 D↔V rounds. Validator round 1 caught two majors that would have left the bug only half-fixed: (a) adding `- viewportWidth` would still under-scroll by 32pt because `onLayout` measures the outer wrapper with padding, not the ScrollView's inner viewport; (b) `onContentSizeChange` vs `onLayout` race on iOS — both collapsed into the single `scrollToEnd` swap.

[x] Read-only history view. History detail (`app/(app)/history/[id].tsx`) is now read-only by default — no inputs, no trash icons, no add-set/add-exercise/delete-workout/session-name affordances. Header Pencil (`Edit workout`) swaps to "Done" (`Exit edit mode`) to unlock the editable surface; per-screen scope (Edit unlocks all blocks at once). Time-edit pencil (`Edit start and end times`) stays independent — works in both modes. Two new components: `<ReadOnlySetRow>` (static `<Text>` cells for weight/reps/RPE/notes/non-interactive check icon) and `<ReadOnlyExerciseBlock>` (preserves `(deleted)` suffix, italic empty-state for zero-set blocks, NO `useLastWorkingSet`, NO `<VolumeTargetSlot>`). Pure-helper module `src/utils/set-display.ts` consumed by both for vitest-testable contract. Structural mutation gating: 6 mutation hooks (`useLogSet`, `useUpdateSet`, `useUpdateSetMeta`, `useDeleteSet`, `useUpdateSessionName`, `useSoftDeleteSession`) stay declared at the top of the screen but JSX callsites mount only when `isEditing===true`. `<ExercisePicker>` mount-gated behind `{isEditing ? ... : null}`. Done handler runs `Keyboard.dismiss()` → `setPickerOpen(false)` → `setIsEditing(false)` so any unblurred `<SetInput>` commits via existing `onBlur` before unmount (MAJ-2 mitigation). Single `screenOptions` const reused across loading/error/happy-path branches for header consistency. 5 new e2e + 23 new unit tests (307/307 pass). Shipped via `docs/runs/2026-05-23_1855_read-only-history-view/` after 2 D↔V rounds. Validator round 1 caught: MAJ-1 wrong a11y label cited in design (`"Edit workout times"` doesn't exist; actual `"Edit start and end times"`), MAJ-2 NEW data-loss failure mode (tapping Done while TextInput focused unmounts before `onBlur`) — both fixed in v2. Round 2 caught NEW-MIN-1 (`||` short-circuit dead-coded `"Session"` loading title). I↔R + I↔T single-pass; 3 screenshots pinned (read-only desktop, edit-mode desktop, read-only 320pt iPhone SE).

[x] Session total volume in the workout header. New `Volume · X kg/lbs` metric block beside `Elapsed` in `<SessionHeader>`. Updates live as sets are checked/unchecked/edited/added/removed via the shared `["sets", sessionId]` TanStack cache. Pure UI consumer of `sumLiveVolume(sets)` from `src/utils/volume-target.ts:88-100` — same kernel as the verdict screen and per-exercise live strip's `Now`, so cross-screen numbers match by construction. No DB / API / hook / cache change. Empty + loading = `0 kg`. Both metric numerals demoted to `text-xl` (from `text-2xl`) to guarantee no overflow at 320pt iPhone SE worst case (`1:00:08` elapsed + `22,046 lbs` volume + Finish button — 13.55px clearance, no wrap, no horizontal scroll, screenshot pinned in run folder). A11y label `Session total volume: ${formatVolume(volumeKg, unit)}` on the inner `<Text>`. 5 new e2e + 16 new unit tests. Smallest single-pass run yet (~47 min): 1 D↔V (go with 1 known-debt major + 4 minors — all resolved during Implement) + 1 I↔R + 1 I↔T, all first try. Validator catches that saved time: MAJ-1 (320pt overflow risk), MIN-1 (duplicate-import/identifier — `useMemo`+`useWeightUnit`+`unit` already in scope), MIN-2 (Playwright strict-mode collision with per-exercise strip on single-exercise sessions — forced `getByLabelText(/^Session total volume:/)` from the start), MIN-3 (a11y placement on inner `<Text>` per `volume-target-slot.tsx` precedent). Shipped via `docs/runs/2026-05-23_1805_session-total-volume-header/`.

[x] Configurable max-volume window. User picks `All / 10w / 20w / 30w` in Profile (new section under Length unit, with caption "Max-volume window — how many recent weeks to compare against."). Default `All` (lifetime) preserves the existing behaviour — no current user sees any change until they opt in. The choice flows uniformly into 5 surfaces: Progress hero `Max` + legend (`"Max = best of last N weeks"` when windowed), weekly volume strip dotted overlay, exercises-this-week per-row `Best session`, end-of-session verdict PR detection, and the live-workout `<VolumeTargetSlot>`. New `user_preferences.max_volume_window_weeks integer NOT NULL DEFAULT 0 CHECK (… IN (0,10,20,30))` via migration `0009_max_volume_window.sql`. Single threshold helper `computeWindowStart(weeks, now): number | undefined` in `src/utils/window-utils.ts`; all four kernels (`bucketLifetimeWeeklyVolumes`, `computeLifetimeMaxPerExercise`, `computePrsThisWeek`, `computeVolumeTarget` + `computePrsForSession`) now accept `windowStartMs?` and use `parseISO(...).getTime() >= windowStartMs` numeric compare. Consistency rule pinned: `session.started_at` is the universal windowing anchor, with per-session aggregation BEFORE the filter so a cross-week session is never split (`bucketLifetimeWeeklyVolumes` dual-anchors: `started_at` decides inclusion, `completed_at` decides bucket placement). 6 new e2e + 39 new unit tests (268/268 pass). Shipped via `docs/runs/2026-05-23_0211_configurable-max-volume-window/` after 2 D↔V rounds. Validator round 1 caught the design-load-bearing bugs: lexicographic ISO string compare (`+00:00` PostgREST vs `Z` JS — same trap the `import-strong-csv` retro documented) and cross-anchor session-split — both fixed in v2 before any code was written. Out of scope: per-exercise `bestE1rm` chart (intentionally lifetime; JSDoc note added).

[x] BUG: routines detail — quick double-tap on an exercise row in the picker fired multiple `POST /routine_exercises` requests that collided on `routine_exercises_routine_position_uq` (4× 409s + console error toast). Same shape as the F6 add-set race: no UI in-flight guard, DB unique index correctly caught it. Fix: `<ExercisePicker>` now tracks `pickingId: string | null` and disables the row Pressable while the mutation is in flight (mirrors the F6 add-set pattern). Bundled the adjacent aria-hidden warning in the same run: Expo Router web keeps prior screens with `display:none` but DOM-mounted, so the Edit button from `<RoutineListItem>` retained focus across the navigation → modal opens → RN-Web puts `aria-hidden="true"` on backdrop ancestors → focused button now inside an aria-hidden subtree → React/RN-Web warning. Fix: `Modal.onShow` calls `(document.activeElement as HTMLElement | null)?.blur()` on web (`typeof document` guard preserves native paths). New e2e `tests/e2e/routines-add-exercise-race.spec.ts` intercepts `POST /rest/v1/routine_exercises`, drives `Promise.all([target.click(), target.click()])` on the same row, asserts 1 POST/201/1 DB row. Shipped via `docs/runs/2026-05-22_1640_routines-409-and-aria/`.

[x] Date format unification app-wide. New central helpers `formatDisplayDate(date, opts?)` + `formatShortDate(date, opts?)` in `src/utils/format-display-date.ts`. Year-suffix now consistent everywhere — current-year dates show `"May 24"` (or `"5/24"` on chart axes); prior-year dates show `"Nov 8, 2019"` (or `"11/8/25"` on chart axes). Weekday kept on session-list rows + history detail (`{includeWeekday: true}`); dropped on chart axes for axis-width stability. 12 sites migrated: session-summary-row, format-session-times, history week drill-down (+ new `accessibilityLabel="Week range:"`), exercise progress chart, measurements list+detail+chart, weekly-volume strip pill + per-bar a11y, IsoWeek.label, best-week label. Per-bar a11y label keeps its existing 4-digit year via `{yearFormat: "numeric"}` opt — byte-for-byte preserved. Three existing test files pinned with `vi.setSystemTime(2026-05-22)` so literal `"5/20"`/`"5/18"` assertions stay year-stable. Shipped via `docs/runs/2026-05-22_1530_date-format-unification/` after 2 D↔V + 2 I↔R rounds.

[x] Rest timer now auto-starts when the user checks a working set during a live workout. Uses the set's exercise `target_rest_seconds`. Re-checking another set resets the timer (`useRestTimer.start()` overwrites idempotently). Unchecking is a no-op. Bulk-check-all (Finish flow) bypasses naturally — it doesn't route through `onToggleSetChecked`. Warmup + dropset checks don't fire (no inter-drop rest in real lifting). The pipeline caught a real pre-existing architectural bug: `useRestTimer` was a per-component `useState` hook, so the workout screen and overlay had separate instances — the pre-existing add-set auto-start at `[sessionId].tsx:373-376` was silently broken too (no e2e coverage). Fixed by lifting to React Context (`<RestTimerProvider>` wraps the screen + overlay). Also added an observer-based `useEffect([setsQ.data])` backstop to handle a RN-Web `Pressable` race (responder's `onPress` config lags one render behind `accessibilityLabel` update). 7 new e2e cases. Shipped via `docs/runs/2026-05-22_1415_rest-timer-auto-start/` after 1 D↔V + 2 I↔T rounds.

[x] PR context on Progress page. The hero `PRs this week: N` count is now tappable — taps expand an accordion of celebratory PR rows (top 5 + "Show all (N)" for power weeks). Each row reads `{exercise} PR! +X kg (was Y kg)`; tap routes to that exercise's progress chart. Per-muscle list PR'd rows now render the same celebratory `<PrListRow>` instead of `Max · Now · To PR: 0 kg`. Per-row triplet's "Max" relabeled to "Best session" to avoid colliding with the hero's `Max = best week ever`. New legend under the hero clarifies the three terms (only when `maxKg > 0`). Verdict screen now consumes the same `<PrListRow>` (zero behavior change). Multi-PR semantic: `currentMaxKg = max(in-week sessions)` so 800→900→1000 displays `+200 (was 800)`. New helper `computePrsThisWeek({rows, currentWeekStartIso, currentWeekEndIso})` returns ordered array; existing `computePrExerciseIdsThisWeek` is now a thin wrapper. `usePrsThisWeek` returns `prsByExerciseId: Map` consumed by `useExercisesThisWeek` (single lifetime walk per render). Shipped via `docs/runs/2026-05-22_1300_pr-context/` after 3 D↔V + 2 I↔T rounds.

[x] Weekly volume strip now scrolls horizontally through full ISO-week history. New `<VisibleRangePill>` (tappable header showing current visible range, year-aware) opens a bottom-sheet `<WeekSelector>` to jump to a year/month. Default position pinned to right edge (most recent week). Lifetime-best dotted overlay stays anchored inside the scroller. Data consolidation: `useWeeklyVolume` (8-week hook) deleted; all consumers use `useLifetimeWeeklyVolume`. New helpers `isoWeeksBetween` + `isoWeekContaining` in `src/utils/dates.ts`. Scroll re-render avoidance: pill owns its own state via `forwardRef` + `useImperativeHandle` so the 260-bar strip parent never `setState`s on scroll. Week-rollover auto-scrolls the right edge while page mounted. Shipped via `docs/runs/2026-05-22_1130_chart-scroll-week-selector/` after 2 D↔V + 2 I↔T rounds (caught: signature drop would break test #43, missing `isoWeeksBetween` helper, modal mockup contradicting bottom-sheet cite, scroll-rerender perf, week-drill-down regex collision + seed gap under dynamic-bucket model).

[x] Set row declutter — RPE + notes moved behind a per-row bottom-sheet menu (`MoreHorizontal` trigger right of the check button, tints blue when data exists). RPE is now a chip selector (12 chips: `—, 5.0, 5.5, …, 10.0`), not a free-form input. Notes is a 4-line textarea inside the same menu. Default row visual: weight + reps + previous placeholder + check + menu trigger. Caught a real data-loss BLK-1 in v2 review: the unchanged `updateSet` was clobbering all 4 columns on every reps/weight blur once `<SetInput>.onCommit` got narrowed to `{reps, weight}`. Fixed at the root by applying the same partial-spread pattern (`if (patch.X !== undefined) payload.X = patch.X`) to `updateSet`. Shipped via `docs/runs/2026-05-22_1000_set-row-declutter/` after 3 D↔V + 2 I↔T rounds.

[x] End-of-session verdict screen. After Finish (and the existing unchecked-sets dialog, if any), `router.replace` lands on `/(app)/workout/verdict/{sessionId}` with `+N PRs · Y kg · Zh Wm` headline + PR list (each row `{exercise} PR! +X kg (was Y kg)`, tap → exercise progress chart). Done button returns to `/workout`. Cancel flow bypasses. PR detection filters lifetime rows by `session_id !== currentSessionId` before computing per-exercise max (first-ever session NOT a PR; strict `>` tie not a PR). Empty-state copy splits on `totalVolumeKg === 0` vs normal no-PR. Caught a real cache race in MAJ-2: `useBulkCheckAllInSession.onSuccess` was fire-and-forget invalidation; "Check all and finish" branch raced the verdict mount → stale `completed_at = null` rows filtered out → verdict would have rendered `0 kg`. Fixed by awaiting `refetchQueries`. E2E Case A's `600 kg` headline assertion is the load-bearing regression guard. Shipped via `docs/runs/2026-05-22_0152_end-of-session-verdict/`.

[x] New Progress tab + page. `TrendingUp` icon between History and Profile. Hero: `PRs this week: N` + weekly volume `Max · Now · To PR` (lifetime-best anchor). Bars: extended `<WeeklyVolumeStrip>` with dotted lifetime-best overlay (max-aware denominator so the line stays inside the plot when best exceeds the 8-week window). List: exercises trained this week, grouped by `muscles[0]` (empty → "Other"), each row shows per-exercise `Max · Now · To PR`. Streak card: current consecutive weeks + best-ever. All Progress queries under `["stats", "progress-page", …]` so the existing `["stats"]` invalidation cascade catches them. Pure helpers in `src/utils/progress-page-math.ts` (59 unit tests). Shipped via `docs/runs/2026-05-22_0030_progress-page/` after 3 D↔V rounds that caught a hard crash (null `completed_at` in unchecked-set rows would have `parseISO(null) → Invalid Date → format() RangeError` in render) before Implement.

[x] Checked-set rows now have a light green background (`bg-green-50 dark:bg-green-950/30`) matching Strong's reference. Check icon also tinted green.

[x] Time-field on history detail now auto-inserts the `:` between hours and minutes as the user types. New `maskTimeInput` helper in `src/utils/session-times-form.ts` with 7 unit tests covering type-forward, paste, stray-colon, and delete-bypass paths.

[x] Live workout now has a red "Cancel workout" button below the exercise list. Confirm dialog → soft-deletes the session (cascades nothing; session disappears from history). Uses the existing `useSoftDeleteSession` hook.

[x] Volume-target strip's surpassed/matched state now shows a "Prev. Max X · Now Y" line below the celebration text so the reference numbers stay visible after the PR is beaten.

[x] History session-list dates now include the year when the session is not in the current calendar year (e.g. "Fri, Nov 8, 2019" vs "Sat, May 24").

[x] "+ Working set" button (and Warm-up / Dropset variants) are now disabled while the insert mutation is in flight, preventing the quick-double-tap race that produced duplicate `set_number` rows. The DB unique index from migration 0008 backs this up — if the UI debounce ever slips, the server rejects the duplicate cleanly.

[x] Strong-import `set_number = 1` bug fixed in DB. Full-history scan found 356 collision groups / 1,118 corrupted rows (24× the originally-estimated 46). Backfilled 7,933 rows by re-numbering each (session, exercise) group in CSV chronological order (matched 11 user-renamed exercises automatically via session+weight+reps fingerprint). Added migration `0008_sets_unique_set_number.sql` — partial unique index on `(session_id, exercise_id, set_number) WHERE deleted_at IS NULL` — to catch any future regression at the DB layer. Found and fixed one bonus native double-tap dup on the Leggiday Leg-Extension. Importer (`scripts/import-strong.ts`) deleted since the user won't re-import. Shipped via `docs/runs/2026-05-21_2330_strong-import-setnumber/`.

[x] Per-exercise live-workout strip now shows Max · Now · To PR · ≈ reps @ Wkg. "Now" counts only checked working sets (per F10 semantic). Reps clause auto-hides when no sets are checked yet to prevent a misleading "Now 0 · ≈ 10 reps @ 100" render. Shipped via `docs/runs/2026-05-21_2225_multi-metric-strip/`.

[x] Weekly volume count appeared wrong because the "k" shorthand obscured the real number (e.g. "26.2k kg" felt off). `formatVolume` now renders integer kg with thousands separator (`"26,210 kg"`). Investigated via the bug-fix pipeline (`docs/runs/2026-05-21_2155_volume-math-wrong/`); diagnostic confirmed displayed values matched DB exactly — the bug was display, not math. The original "Previous PR" concern folded into the multi-metric strip feature (still open above).

[x] From the live workout session, tapping an exercise's name should navigate to that exercise's history/progress page.
[x] While training, each exercise should show the amount of total volume left to achieve the previous max volume of that exercise. It should also calculate, using the current used weights, the amount of reps left to surpass that volume. Those reps can be shown with floating points like "7.2 reps".
[x] Add a check button for each set during workout. Helps the user track which set they're currently on. Unchecked sets are NOT saved when the workout is finished — only checked sets persist. If any sets are unchecked when Finish is pressed, show a warning dialog with two options: (a) finish without saving unchecked sets, or (b) auto-check all sets before saving.
[x] Soft-deleted exercises should remain fully visible in past workout history (sessions, sets, totals, weekly volume aggregates) — only the exercise picker and the Exercises library list should exclude them.
[x] Edit a workout's start and end time from the history detail screen — e.g. fix a session that was finished hours late, or one started before the user remembered to tap "Quick start".
[x] Allow user to remove exercise from current session in progress.
[x] Measurements screen does not need to live on the bottom bar. It can be moved to the profile page as a button in the page itself
[x] Tela de iniciar treino pode ser unificada com a tela de rotinas, como no strong
[x] when clicking on the measurements, we should see a screen to view what was measured, and then go to the edit measurements when i press an "Edit" button
[x] measurements screen should show a graph to represent progress
[x] history graph for the week needs a click functionality, where i'll see a more detailed view of my progress history
[x] when clicking on an exercise, i want to see a progress graph showing important info
